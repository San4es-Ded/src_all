package aethereal;

/** Offline replacement for the original proxy geolocation lookup. */
public final class ProxyGeoService {
   private ProxyGeoService() {
   }

   public static void method0907(ProxyEntry proxyEntry) {
      // Country lookup deliberately disabled: no external requests are made.
   }
}
