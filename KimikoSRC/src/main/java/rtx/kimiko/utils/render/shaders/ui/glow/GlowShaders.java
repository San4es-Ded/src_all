/*
 * Decompiled with CFR 0.152.
 * 
 * Could not load the following classes:
 *  kotlin.Metadata
 *  kotlin.collections.CollectionsKt
 *  kotlin.jvm.JvmStatic
 *  kotlin.jvm.internal.Intrinsics
 *  org.jetbrains.annotations.NotNull
 */
package rtx.kimiko.utils.render.shaders.ui.glow;

import java.util.Map;
import kotlin.Metadata;
import kotlin.collections.CollectionsKt;
import kotlin.jvm.JvmStatic;
import kotlin.jvm.internal.Intrinsics;
import org.jetbrains.annotations.NotNull;

@Metadata(mv={2, 4, 0}, k=1, xi=48, d1={"\u0000\"\n\u0002\u0018\u0002\n\u0002\u0010\u0000\n\u0002\b\u0002\n\u0002\u0010%\n\u0002\u0010\u000e\n\u0000\n\u0002\u0010\u0002\n\u0002\u0018\u0002\n\u0002\b\u0017\b\u00c6\u0002\u0018\u00002\u00020\u0001B\t\b\u0002\u00a2\u0006\u0004\b\u0002\u0010\u0003J'\u0010\t\u001a\u00020\u00072\u0012\u0010\u0006\u001a\u000e\u0012\u0004\u0012\u00020\u0005\u0012\u0004\u0012\u00020\u00050\u0004H\u0007b\u0002\b\b\u00a2\u0006\u0004\b\t\u0010\nJ\u001b\u0010\f\u001a\u00020\u00052\u0006\u0010\u000b\u001a\u00020\u0005H\u0007b\u0002\b\b\u00a2\u0006\u0004\b\f\u0010\rJ\u000f\u0010\u000e\u001a\u00020\u0005H\u0002\u00a2\u0006\u0004\b\u000e\u0010\u000fJ\u000f\u0010\u0010\u001a\u00020\u0005H\u0002\u00a2\u0006\u0004\b\u0010\u0010\u000fJ\u000f\u0010\u0011\u001a\u00020\u0005H\u0002\u00a2\u0006\u0004\b\u0011\u0010\u000fJ\u000f\u0010\u0012\u001a\u00020\u0005H\u0002\u00a2\u0006\u0004\b\u0012\u0010\u000fJ\u000f\u0010\u0013\u001a\u00020\u0005H\u0002\u00a2\u0006\u0004\b\u0013\u0010\u000fJ\u000f\u0010\u0014\u001a\u00020\u0005H\u0002\u00a2\u0006\u0004\b\u0014\u0010\u000fJ\u000f\u0010\u0015\u001a\u00020\u0005H\u0002\u00a2\u0006\u0004\b\u0015\u0010\u000fJ\u000f\u0010\u0016\u001a\u00020\u0005H\u0002\u00a2\u0006\u0004\b\u0016\u0010\u000fJ\u000f\u0010\u0017\u001a\u00020\u0005H\u0002\u00a2\u0006\u0004\b\u0017\u0010\u000fJ\u000f\u0010\u0018\u001a\u00020\u0005H\u0002\u00a2\u0006\u0004\b\u0018\u0010\u000fJ\u000f\u0010\u0019\u001a\u00020\u0005H\u0002\u00a2\u0006\u0004\b\u0019\u0010\u000fJ\u000f\u0010\u001a\u001a\u00020\u0005H\u0002\u00a2\u0006\u0004\b\u001a\u0010\u000fJ\u000f\u0010\u001b\u001a\u00020\u0005H\u0002\u00a2\u0006\u0004\b\u001b\u0010\u000fJ\u000f\u0010\u001c\u001a\u00020\u0005H\u0002\u00a2\u0006\u0004\b\u001c\u0010\u000fJ\u000f\u0010\u001d\u001a\u00020\u0005H\u0002\u00a2\u0006\u0004\b\u001d\u0010\u000fJ\u000f\u0010\u001e\u001a\u00020\u0005H\u0002\u00a2\u0006\u0004\b\u001e\u0010\u000f\u00a8\u0006\u001f"}, d2={"Lrtx/kimiko/utils/render/shaders/ui/glow/GlowShaders;", "", "<init>", "()V", "", "", "sources", "", "Lkotlin/jvm/JvmStatic;", "register", "(Ljava/util/Map;)V", "key", "source", "(Ljava/lang/String;)Ljava/lang/String;", "s0", "()Ljava/lang/String;", "s1", "s2", "s3", "s4", "s5", "s6", "s7", "s8", "s9", "s10", "s11", "s12", "s13", "s14", "s15", "rtx.kimiko:kimiko"})
public final class GlowShaders {
    @NotNull
    public static final GlowShaders INSTANCE = new GlowShaders();

    private GlowShaders() {
    }

    @JvmStatic
    public static final void register(@NotNull Map<String, String> sources) {
        Intrinsics.checkNotNullParameter(sources, (String)"sources");
        sources.put("ui/glow/glow_blur.glsl", INSTANCE.s0());
        sources.put("ui/glow/glow_blur.vsh", INSTANCE.s1());
        sources.put("ui/glow/glow_blur_down.glsl", INSTANCE.s2());
        sources.put("ui/glow/glow_blur_down.fsh", INSTANCE.s3());
        sources.put("ui/glow/glow_blur_up.glsl", INSTANCE.s4());
        sources.put("ui/glow/glow_blur_up.fsh", INSTANCE.s5());
        sources.put("ui/glow/glow_composite.glsl", INSTANCE.s6());
        sources.put("ui/glow/glow_composite.vsh", INSTANCE.s7());
        sources.put("ui/glow/glow_composite.fsh", INSTANCE.s8());
        sources.put("ui/glow/glow_cutout.glsl", INSTANCE.s9());
        sources.put("ui/glow/glow_cutout.fsh", INSTANCE.s10());
        sources.put("ui/glow/glow_shape.glsl", INSTANCE.s11());
        sources.put("ui/glow/glow_shape.vsh", INSTANCE.s12());
        sources.put("ui/glow/glow_shape.fsh", INSTANCE.s13());
        sources.put("ui/glow/glow_source.glsl", INSTANCE.s14());
        sources.put("ui/glow/glow_source.vsh", INSTANCE.s15());
    }

    @JvmStatic
    @NotNull
    public static final String source(@NotNull String key) {
        Intrinsics.checkNotNullParameter((Object)key, (String)"key");
        return switch (key) {
            case "ui/glow/glow_blur.glsl" -> INSTANCE.s0();
            case "ui/glow/glow_blur.vsh" -> INSTANCE.s1();
            case "ui/glow/glow_blur_down.glsl" -> INSTANCE.s2();
            case "ui/glow/glow_blur_down.fsh" -> INSTANCE.s3();
            case "ui/glow/glow_blur_up.glsl" -> INSTANCE.s4();
            case "ui/glow/glow_blur_up.fsh" -> INSTANCE.s5();
            case "ui/glow/glow_composite.glsl" -> INSTANCE.s6();
            case "ui/glow/glow_composite.vsh" -> INSTANCE.s7();
            case "ui/glow/glow_composite.fsh" -> INSTANCE.s8();
            case "ui/glow/glow_cutout.glsl" -> INSTANCE.s9();
            case "ui/glow/glow_cutout.fsh" -> INSTANCE.s10();
            case "ui/glow/glow_shape.glsl" -> INSTANCE.s11();
            case "ui/glow/glow_shape.vsh" -> INSTANCE.s12();
            case "ui/glow/glow_shape.fsh" -> INSTANCE.s13();
            case "ui/glow/glow_source.glsl" -> INSTANCE.s14();
            case "ui/glow/glow_source.vsh" -> INSTANCE.s15();
            default -> throw new IllegalArgumentException(key);
        };
    }

    private final String s0() {
        Object[] objectArray = new String[]{"//!vertex", "#version 150", "", "in vec3 Position;", "", "out vec2 TexCoord;", "", "void main() {", "    gl_Position = vec4(Position, 1.0);", "    TexCoord = Position.xy * 0.5 + 0.5;", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }

    private final String s1() {
        Object[] objectArray = new String[]{"#version 150", "", "in vec3 Position;", "", "out vec2 TexCoord;", "", "void main() {", "    gl_Position = vec4(Position, 1.0);", "    TexCoord = Position.xy * 0.5 + 0.5;", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }

    private final String s2() {
        Object[] objectArray = new String[]{"//!fragment", "#version 150", "", "uniform sampler2D Sampler0;", "", "layout(std140) uniform KawaseParams {", "    vec4 SourceRect;", "    vec4 HalfPixel;", "    vec4 FallbackColor;", "};", "", "in vec2 TexCoord;", "", "out vec4 OutColor;", "", "void main() {", "    vec2 c = SourceRect.xy + TexCoord * SourceRect.zw;", "    vec4 sum = texture(Sampler0, c) * 4.0;", "    sum += texture(Sampler0, c - HalfPixel.xy);", "    sum += texture(Sampler0, c + HalfPixel.xy);", "    sum += texture(Sampler0, c + vec2(HalfPixel.x, -HalfPixel.y));", "    sum += texture(Sampler0, c - vec2(HalfPixel.x, -HalfPixel.y));", "    OutColor = sum / 8.0;", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }

    private final String s3() {
        Object[] objectArray = new String[]{"#version 150", "", "uniform sampler2D Sampler0;", "", "layout(std140) uniform KawaseParams {", "    vec4 SourceRect;", "    vec4 HalfPixel;", "    vec4 FallbackColor;", "};", "", "in vec2 TexCoord;", "", "out vec4 OutColor;", "", "void main() {", "    vec2 c = SourceRect.xy + TexCoord * SourceRect.zw;", "    vec4 sum = texture(Sampler0, c) * 4.0;", "    sum += texture(Sampler0, c - HalfPixel.xy);", "    sum += texture(Sampler0, c + HalfPixel.xy);", "    sum += texture(Sampler0, c + vec2(HalfPixel.x, -HalfPixel.y));", "    sum += texture(Sampler0, c - vec2(HalfPixel.x, -HalfPixel.y));", "    OutColor = sum / 8.0;", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }

    private final String s4() {
        Object[] objectArray = new String[]{"//!fragment", "#version 150", "", "uniform sampler2D Sampler0;", "", "layout(std140) uniform KawaseParams {", "    vec4 SourceRect;", "    vec4 HalfPixel;", "    vec4 FallbackColor;", "};", "", "in vec2 TexCoord;", "", "out vec4 OutColor;", "", "void main() {", "    vec2 c = SourceRect.xy + TexCoord * SourceRect.zw;", "    vec4 sum = texture(Sampler0, c + vec2(-HalfPixel.x * 2.0, 0.0));", "    sum += texture(Sampler0, c + vec2(-HalfPixel.x, HalfPixel.y)) * 2.0;", "    sum += texture(Sampler0, c + vec2(0.0, HalfPixel.y * 2.0));", "    sum += texture(Sampler0, c + vec2(HalfPixel.x, HalfPixel.y)) * 2.0;", "    sum += texture(Sampler0, c + vec2(HalfPixel.x * 2.0, 0.0));", "    sum += texture(Sampler0, c + vec2(HalfPixel.x, -HalfPixel.y)) * 2.0;", "    sum += texture(Sampler0, c + vec2(0.0, -HalfPixel.y * 2.0));", "    sum += texture(Sampler0, c + vec2(-HalfPixel.x, -HalfPixel.y)) * 2.0;", "    OutColor = sum / 12.0;", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }

    private final String s5() {
        Object[] objectArray = new String[]{"#version 150", "", "uniform sampler2D Sampler0;", "", "layout(std140) uniform KawaseParams {", "    vec4 SourceRect;", "    vec4 HalfPixel;", "    vec4 FallbackColor;", "};", "", "in vec2 TexCoord;", "", "out vec4 OutColor;", "", "void main() {", "    vec2 c = SourceRect.xy + TexCoord * SourceRect.zw;", "    vec4 sum = texture(Sampler0, c + vec2(-HalfPixel.x * 2.0, 0.0));", "    sum += texture(Sampler0, c + vec2(-HalfPixel.x, HalfPixel.y)) * 2.0;", "    sum += texture(Sampler0, c + vec2(0.0, HalfPixel.y * 2.0));", "    sum += texture(Sampler0, c + vec2(HalfPixel.x, HalfPixel.y)) * 2.0;", "    sum += texture(Sampler0, c + vec2(HalfPixel.x * 2.0, 0.0));", "    sum += texture(Sampler0, c + vec2(HalfPixel.x, -HalfPixel.y)) * 2.0;", "    sum += texture(Sampler0, c + vec2(0.0, -HalfPixel.y * 2.0));", "    sum += texture(Sampler0, c + vec2(-HalfPixel.x, -HalfPixel.y)) * 2.0;", "    OutColor = sum / 12.0;", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }

    private final String s6() {
        Object[] objectArray = new String[]{"//!vertex", "#version 150", "", "#moj_import <kimiko:releon_common.glsl>", "", "#moj_import <minecraft:dynamictransforms.glsl>", "#moj_import <minecraft:projection.glsl>", "", "in vec3 Position;", "in vec4 Color;", "in float LineWidth;", "", "out vec2 FragCoord;", "flat out int QuadIndex;", "", "layout(std140) uniform GlowParamsArray {", "    vec4 params[1120];", "};", "", "void main() {", "    int index = max(int(LineWidth + 0.5) - 1, 0);", "    int base = index * 5;", "    float z = params[base + 2].z;", "", "    gl_Position = ProjMat * ModelViewMat * vec4(Position.xy, z, 1.0);", "    FragCoord = rvertexcoord(gl_VertexID);", "    QuadIndex = index;", "}", "//!fragment", "#version 150", "", "uniform sampler2D Sampler0;", "", "in vec2 FragCoord;", "flat in int QuadIndex;", "", "layout(std140) uniform GlowParamsArray {", "    vec4 params[1120];", "};", "", "out vec4 OutColor;", "", "void main() {", "    int base = QuadIndex * 5;", "", "    vec4 reg       = params[base + 3];", "    vec4 colorVec  = params[base + 2];", "", "    vec2 texCoord = clamp(reg.xy + FragCoord * reg.zw, vec2(0.0), vec2(1.0));", "    vec4 blurred  = texture(Sampler0, texCoord);", "", "    float intensity = colorVec.a;", "", "    float a = blurred.a * intensity;", "    OutColor = vec4(blurred.rgb * a, a);", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }

    private final String s7() {
        Object[] objectArray = new String[]{"#version 150", "", "#moj_import <kimiko:releon_common.glsl>", "", "#moj_import <minecraft:dynamictransforms.glsl>", "#moj_import <minecraft:projection.glsl>", "", "in vec3 Position;", "in vec4 Color;", "in float LineWidth;", "", "out vec2 FragCoord;", "flat out int QuadIndex;", "", "layout(std140) uniform GlowParamsArray {", "    vec4 params[1120];", "};", "", "void main() {", "    int index = max(int(LineWidth + 0.5) - 1, 0);", "    int base = index * 5;", "    float z = params[base + 2].z;", "", "    gl_Position = ProjMat * ModelViewMat * vec4(Position.xy, z, 1.0);", "    FragCoord = rvertexcoord(gl_VertexID);", "    QuadIndex = index;", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }

    private final String s8() {
        Object[] objectArray = new String[]{"#version 150", "", "uniform sampler2D Sampler0;", "", "in vec2 FragCoord;", "flat in int QuadIndex;", "", "layout(std140) uniform GlowParamsArray {", "    vec4 params[1120];", "};", "", "out vec4 OutColor;", "", "void main() {", "    int base = QuadIndex * 5;", "", "    vec4 reg       = params[base + 3];", "    vec4 colorVec  = params[base + 2];", "", "    vec2 texCoord = clamp(reg.xy + FragCoord * reg.zw, vec2(0.0), vec2(1.0));", "    vec4 blurred  = texture(Sampler0, texCoord);", "", "    float intensity = colorVec.a;", "", "    float a = blurred.a * intensity;", "    OutColor = vec4(blurred.rgb * a, a);", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }

    private final String s9() {
        Object[] objectArray = new String[]{"//!fragment", "#version 150", "", "uniform sampler2D Sampler0;", "uniform sampler2D Sampler1;", "", "layout(std140) uniform KawaseParams {", "    vec4 SourceRect;", "    vec4 HalfPixel;", "    vec4 FallbackColor;", "};", "", "in vec2 TexCoord;", "", "out vec4 OutColor;", "", "void main() {", "    vec2 c = SourceRect.xy + TexCoord * SourceRect.zw;", "    vec4 blurred = texture(Sampler0, c);", "    float body   = texture(Sampler1, c).a;", "", "    float keep = 1.0 - smoothstep(0.0, 0.4, body);", "    OutColor = vec4(blurred.rgb, blurred.a * keep);", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }

    private final String s10() {
        Object[] objectArray = new String[]{"#version 150", "", "uniform sampler2D Sampler0;", "uniform sampler2D Sampler1;", "", "layout(std140) uniform KawaseParams {", "    vec4 SourceRect;", "    vec4 HalfPixel;", "    vec4 FallbackColor;", "};", "", "in vec2 TexCoord;", "", "out vec4 OutColor;", "", "void main() {", "    vec2 c = SourceRect.xy + TexCoord * SourceRect.zw;", "    vec4 blurred = texture(Sampler0, c);", "    float body   = texture(Sampler1, c).a;", "", "    float keep = 1.0 - smoothstep(0.0, 0.4, body);", "    OutColor = vec4(blurred.rgb, blurred.a * keep);", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }

    private final String s11() {
        Object[] objectArray = new String[]{"//!vertex", "#version 150", "", "in vec3 Position;", "", "out vec2 FragCoord;", "", "void main() {", "", "    gl_Position = vec4(Position.xy, 0.0, 1.0);", "", "    FragCoord = vec2(Position.x * 0.5 + 0.5, 0.5 - Position.y * 0.5);", "}", "//!fragment", "#version 150", "", "#moj_import <kimiko:theme_wave.glsl>", "#moj_import <kimiko:releon_common.glsl>", "", "in vec2 FragCoord;", "", "layout(std140) uniform GlowParamsArray {", "", "    vec4 params[79];", "};", "", "layout(std140) uniform SplitParams {", "    vec4 splitData[48];", "};", "", "out vec4 OutColor;", "", "vec2 kimikoFrag = vec2(0.0);", "", "vec3 glowPaletteColorAt(int idx) {", "    if (params[78].w > 0.5) {", "        return kimikoClientStop(idx, kimikoFrag);", "    }", "    if (idx <= 0) return params[71].rgb;", "    if (idx == 1) return params[72].rgb;", "    if (idx == 2) return params[73].rgb;", "    if (idx == 3) return params[74].rgb;", "    if (idx == 4) return params[75].rgb;", "    return params[76].rgb;", "}", "", "vec3 glowPaletteRamp(float t) {", "    int count = int(params[70].x + 0.5);", "    if (count <= 1) {", "        return glowPaletteColorAt(0);", "    }", "    float f = clamp(t, 0.0, 1.0) * float(count - 1);", "    int i = clamp(int(floor(f)), 0, count - 1);", "    int j = min(i + 1, count - 1);", "    float frac = clamp(f - float(i), 0.0, 1.0);", "    frac = frac * frac * (3.0 - 2.0 * frac);", "    return mix(glowPaletteColorAt(i), glowPaletteColorAt(j), frac);", "}", "", "vec3 glowPaletteLoop(float t) {", "    int count = int(params[70].x + 0.5);", "    if (count <= 1) {", "        return glowPaletteColorAt(0);", "    }", "    float f = fract(t) * float(count);", "    int i1 = clamp(int(floor(f)), 0, count - 1);", "    float u = clamp(f - float(i1), 0.0, 1.0);", "", "    int i0 = i1 - 1; if (i0 < 0) i0 += count;", "    int i2 = i1 + 1; if (i2 >= count) i2 -= count;", "    int i3 = i1 + 2; if (i3 >= count) i3 -= count;", "", "    vec3 c0 = glowPaletteColorAt(i0);", "    vec3 c1 = glowPaletteColorAt(i1);", "    vec3 c2 = glowPaletteColorAt(i2);", "    vec3 c3 = glowPaletteColorAt(i3);", "", "    float u2 = u * u;", "    float u3 = u2 * u;", "    vec3 cyclicCol = 0.5 * ((2.0 * c1)", "                      + (-c0 + c2) * u", "                      + (2.0 * c0 - 5.0 * c1 + 4.0 * c2 - c3) * u2", "                      + (-c0 + 3.0 * c1 - 3.0 * c2 + c3) * u3);", "", "    float tri = 0.5 - 0.5 * cos(6.2831853 * fract(t));", "    vec3 mirrorCol = glowPaletteRamp(tri);", "", "    float closed = clamp(params[77].y, 0.0, 1.0);", "    return clamp(mix(mirrorCol, cyclicCol, closed), 0.0, 1.0);", "}", "", "vec3 glowBox(vec2 uv, float phase) {", "    vec3 tl = glowPaletteLoop(phase);", "    vec3 tr = glowPaletteLoop(phase + 0.25);", "    vec3 br = glowPaletteLoop(phase + 0.5);", "    vec3 bl = glowPaletteLoop(phase + 0.75);", "    vec3 top = mix(tl, tr, uv.x);", "    vec3 bot = mix(bl, br, uv.x);", "    return mix(top, bot, uv.y);", "}", "", "vec3 glowSelect(float id, vec3 ramp, vec3 mesh, vec3 box) {", "    int s = int(id + 0.5);", "    if (s == 1) return mesh;", "    if (s == 2) return box;", "    return ramp;", "}", "", "vec3 glowMesh(vec2 uv, float phase, float aspect) {", "    float a = 6.2831853 * phase;", "    vec3 sum = vec3(0.0);", "    float wsum = 0.0;", "    for (int i = 0; i < 6; i++) {", "        float fi = float(i);", "        float kx = mod(fi, 2.0) < 0.5 ? 1.0 : 2.0;", "        float ky = mod(fi, 2.0) < 0.5 ? 2.0 : 1.0;", "        vec2 pos = vec2(0.5) + 0.34 * vec2(sin(a * kx + fi * 2.3999), cos(a * ky - fi * 1.618));", "        vec2 d = (uv - pos) * vec2(aspect, 1.0);", "        float w = 1.0 / (dot(d, d) * 5.0 + 0.06);", "        sum += glowPaletteRamp(fi / 5.0) * w;", "        wsum += w;", "    }", "    return sum / wsum;", "}", "", "vec4 getSegment_glow(int i) {", "    vec4 span = params[6 + i];", "    return vec4(span.x, span.z, span.y - span.x, span.w - span.z);", "}", "", "float roundedBoxSDF_glow(vec2 center, vec2 halfSize, vec4 r) {", "    r.xy = (center.x > 0.0) ? r.xy : r.zw;", "    r.x  = (center.y > 0.0) ? r.x  : r.y;", "    vec2 q = abs(center) - halfSize + r.x;", "    return min(max(q.x, q.y), 0.0) + length(max(q, 0.0)) - r.x;", "}", "", "vec4 resolveSegmentRadius_glow(int index, vec4 seg, int count, float r,", "                               float leftAligned, float bottomAnchored) {", "    float rowX      = seg.x;", "    float rowRightX = seg.x + seg.z;", "", "    float topLeft     = (index == 0)         ? r : 0.0;", "    float topRight    = (index == 0)         ? r : 0.0;", "    float bottomLeft  = (index == count - 1) ? r : 0.0;", "    float bottomRight = (index == count - 1) ? r : 0.0;", "", "    if (leftAligned > 0.5) {", "        if (index > 0) {", "            vec4 prev = getSegment_glow(index - 1);", "            float prevRightX = prev.x + prev.z;", "            topRight = (rowRightX > prevRightX + 0.01) ? min(r, rowRightX - prevRightX) : 0.0;", "        }", "        if (index < count - 1) {", "            vec4 next = getSegment_glow(index + 1);", "            float nextRightX = next.x + next.z;", "            bottomRight = (rowRightX > nextRightX + 0.01) ? min(r, rowRightX - nextRightX) : 0.0;", "        }", "    } else {", "        if (index > 0) {", "            vec4 prev = getSegment_glow(index - 1);", "            topLeft = (rowX < prev.x - 0.01) ? min(r, prev.x - rowX) : 0.0;", "        }", "        if (index < count - 1) {", "            vec4 next = getSegment_glow(index + 1);", "            bottomLeft = (rowX < next.x - 0.01) ? min(r, next.x - rowX) : 0.0;", "        }", "    }", "    return vec4(topRight, bottomRight, topLeft, bottomLeft);", "}", "", "float boxesSDF(vec2 p, float spanCountF, float innerRadius, out vec2 boxUV) {", "    float minDist = 1e9;", "    boxUV = vec2(0.5);", "    int count = int(spanCountF + 0.5);", "    for (int i = 0; i < 64; i++) {", "        if (i >= count) break;", "        vec4 seg       = getSegment_glow(i);", "", "        vec2 fullHalf  = max(seg.zw * 0.5, vec2(0.001));", "        vec2 segCenter = seg.xy + fullHalf;", "        vec2 halfSize  = max(fullHalf - 1.0, vec2(0.001));", "        float rad = min(innerRadius, min(halfSize.x, halfSize.y));", "        vec2 center = p - segCenter;", "        center.y = -center.y;", "        float d = roundedBoxSDF_glow(center, halfSize, vec4(rad));", "        if (d < minDist) {", "            minDist = d;", "            boxUV = clamp((p - seg.xy) / max(seg.zw, vec2(1.0)), vec2(0.0), vec2(1.0));", "        }", "    }", "    return minDist;", "}", "", "float staircaseSDF(vec2 p, float spanCountF, float innerRadius,", "                   float leftAligned, float bottomAnchored) {", "    float minDist = 1e9;", "    int count = int(spanCountF + 0.5);", "    for (int i = 0; i < 64; i++) {", "        if (i >= count) break;", "        vec4 seg       = getSegment_glow(i);", "", "        vec2 fullHalf  = max(seg.zw * 0.5, vec2(0.001));", "        vec2 segCenter = seg.xy + fullHalf;", "        vec2 halfSize  = max(fullHalf - 1.0, vec2(0.001));", "        float rad = min(innerRadius, min(halfSize.x, halfSize.y));", "        vec4 r = resolveSegmentRadius_glow(i, seg, count, rad, leftAligned, bottomAnchored);", "", "        vec2 center = p - segCenter;", "        center.y = -center.y;", "        float d = roundedBoxSDF_glow(center, halfSize, r);", "        minDist = min(minDist, d);", "    }", "    return minDist;", "}", "", "void main() {", "", "    vec4 radius   = max(params[0], vec4(0.0));", "    vec4 sizeData = params[1];", "    vec4 colorVec = params[2];", "", "    vec2 rectSize = max(sizeData.xy, vec2(1.0));", "    float pad = max(sizeData.z, 0.0);", "    float smoothness = max(sizeData.w, 0.5);", "", "    vec2 targetSize = rectSize + vec2(pad * 2.0);", "    vec2 pixel = FragCoord * targetSize;", "    kimikoFrag = kimikoFragXYMappedFlipY(pixel, params[78]);", "", "    float spanCount    = params[5].x;", "    float innerRadius  = params[5].y;", "    float leftAligned  = params[5].z;", "    float bottomAnchored = params[5].w;", "", "    float rawSplit = params[3].x;", "    bool glowCut = rawSplit < -0.5;", "    int splitIdx = int(abs(rawSplit) + 0.5) - 1;", "    float parentDist = -1e9;", "", "    float dist;", "    bool boxesMode = false;", "    vec2 boxUV = vec2(0.5);", "    if (splitIdx >= 0) {", "", "        vec2 local = pixel - vec2(pad);", "        local.y = rectSize.y - local.y;", "        vec4 pr = splitData[splitIdx * 3];", "        vec4 cr = splitData[splitIdx * 3 + 1];", "        vec4 sm = splitData[splitIdx * 3 + 2];", "        float dP = rdist(pr.xy - local, max(pr.zw - 1.0, vec2(0.0)), radius);", "        float dC = rdist(cr.xy - local, max(cr.zw - 1.0, vec2(0.0)), vec4(max(sm.x, 0.0)));", "        dist = rsmin(dP, dC, max(sm.y, 0.0));", "        parentDist = dP;", "    } else if (spanCount >= 0.5) {", "", "        if (leftAligned < -0.5) {", "            boxesMode = true;", "            dist = boxesSDF(pixel, spanCount, innerRadius, boxUV);", "        } else {", "            dist = staircaseSDF(pixel, spanCount, innerRadius, leftAligned, bottomAnchored);", "        }", "    } else {", "", "        vec2 pos = pixel - targetSize * 0.5;", "        vec2 halfSize = max(rectSize * 0.5 - 1.0, vec2(0.0));", "        dist = rdist(pos, halfSize, radius);", "    }", "", "    float a = 1.0 - smoothstep(-smoothness, 0.0, dist);", "", "    if (glowCut) {", "        a *= smoothstep(-1.0, 3.0, parentDist);", "    }", "    if (a < 0.001) discard;", "", "    vec4 secondData = params[4];", "", "    vec2 shapeUV = boxesMode", "        ? boxUV", "        : clamp((pixel - vec2(pad)) / max(rectSize, vec2(1.0)), vec2(0.0), vec2(1.0));", "", "    shapeUV.y = 1.0 - shapeUV.y;", "", "    vec3 mixedColor;", "    if (params[3].w > 0.5) {", "", "        float rowPhase = 0.5 + 0.5 * sin(shapeUV.y * rectSize.y * params[3].y - params[3].z);", "        mixedColor = mix(colorVec.rgb, secondData.rgb, rowPhase);", "    } else if (int(params[70].x + 0.5) >= 2) {", "        float tri = abs(fract(shapeUV.x + fract(secondData.w)) * 2.0 - 1.0);", "", "        vec3 rampCol = glowPaletteRamp(smoothstep(0.0, 1.0, tri)) * 0.86;", "        vec3 meshCol = glowMesh(shapeUV, params[70].y, rectSize.x / rectSize.y);", "        vec3 boxCol = glowBox(shapeUV, params[70].y * 20.0);", "        vec3 targetCol = glowSelect(params[70].z, rampCol, meshCol, boxCol);", "", "        float sweep = params[70].w;", "        if (sweep >= 0.0) {", "            vec3 prevCol = glowSelect(params[77].x, rampCol, meshCol, boxCol);", "            float axis = shapeUV.x + (shapeUV.y - 0.5) * 0.30;", "            float head = sweep * 1.5 - 0.25;", "            float wipe = 1.0 - smoothstep(head - 0.04, head + 0.04, axis);", "            mixedColor = mix(prevCol, targetCol, wipe);", "        } else {", "            mixedColor = targetCol;", "        }", "    } else {", "        mixedColor = colorVec.rgb;", "    }", "", "    OutColor = vec4(mixedColor, colorVec.a * a);", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }

    private final String s12() {
        Object[] objectArray = new String[]{"#version 150", "", "in vec3 Position;", "", "out vec2 FragCoord;", "", "void main() {", "", "    gl_Position = vec4(Position.xy, 0.0, 1.0);", "", "    FragCoord = vec2(Position.x * 0.5 + 0.5, 0.5 - Position.y * 0.5);", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }

    private final String s13() {
        Object[] objectArray = new String[]{"#version 150", "", "#moj_import <kimiko:theme_wave.glsl>", "#moj_import <kimiko:releon_common.glsl>", "", "in vec2 FragCoord;", "", "layout(std140) uniform GlowParamsArray {", "", "    vec4 params[79];", "};", "", "layout(std140) uniform SplitParams {", "    vec4 splitData[48];", "};", "", "out vec4 OutColor;", "", "vec2 kimikoFrag = vec2(0.0);", "", "vec3 glowPaletteColorAt(int idx) {", "    if (params[78].w > 0.5) {", "        return kimikoClientStop(idx, kimikoFrag);", "    }", "    if (idx <= 0) return params[71].rgb;", "    if (idx == 1) return params[72].rgb;", "    if (idx == 2) return params[73].rgb;", "    if (idx == 3) return params[74].rgb;", "    if (idx == 4) return params[75].rgb;", "    return params[76].rgb;", "}", "", "vec3 glowPaletteRamp(float t) {", "    int count = int(params[70].x + 0.5);", "    if (count <= 1) {", "        return glowPaletteColorAt(0);", "    }", "    float f = clamp(t, 0.0, 1.0) * float(count - 1);", "    int i = clamp(int(floor(f)), 0, count - 1);", "    int j = min(i + 1, count - 1);", "    float frac = clamp(f - float(i), 0.0, 1.0);", "    frac = frac * frac * (3.0 - 2.0 * frac);", "    return mix(glowPaletteColorAt(i), glowPaletteColorAt(j), frac);", "}", "", "vec3 glowPaletteLoop(float t) {", "    int count = int(params[70].x + 0.5);", "    if (count <= 1) {", "        return glowPaletteColorAt(0);", "    }", "    float f = fract(t) * float(count);", "    int i1 = clamp(int(floor(f)), 0, count - 1);", "    float u = clamp(f - float(i1), 0.0, 1.0);", "", "    int i0 = i1 - 1; if (i0 < 0) i0 += count;", "    int i2 = i1 + 1; if (i2 >= count) i2 -= count;", "    int i3 = i1 + 2; if (i3 >= count) i3 -= count;", "", "    vec3 c0 = glowPaletteColorAt(i0);", "    vec3 c1 = glowPaletteColorAt(i1);", "    vec3 c2 = glowPaletteColorAt(i2);", "    vec3 c3 = glowPaletteColorAt(i3);", "", "    float u2 = u * u;", "    float u3 = u2 * u;", "    vec3 cyclicCol = 0.5 * ((2.0 * c1)", "                      + (-c0 + c2) * u", "                      + (2.0 * c0 - 5.0 * c1 + 4.0 * c2 - c3) * u2", "                      + (-c0 + 3.0 * c1 - 3.0 * c2 + c3) * u3);", "", "    float tri = 0.5 - 0.5 * cos(6.2831853 * fract(t));", "    vec3 mirrorCol = glowPaletteRamp(tri);", "", "    float closed = clamp(params[77].y, 0.0, 1.0);", "    return clamp(mix(mirrorCol, cyclicCol, closed), 0.0, 1.0);", "}", "", "vec3 glowBox(vec2 uv, float phase) {", "    vec3 tl = glowPaletteLoop(phase);", "    vec3 tr = glowPaletteLoop(phase + 0.25);", "    vec3 br = glowPaletteLoop(phase + 0.5);", "    vec3 bl = glowPaletteLoop(phase + 0.75);", "    vec3 top = mix(tl, tr, uv.x);", "    vec3 bot = mix(bl, br, uv.x);", "    return mix(top, bot, uv.y);", "}", "", "vec3 glowSelect(float id, vec3 ramp, vec3 mesh, vec3 box) {", "    int s = int(id + 0.5);", "    if (s == 1) return mesh;", "    if (s == 2) return box;", "    return ramp;", "}", "", "vec3 glowMesh(vec2 uv, float phase, float aspect) {", "    float a = 6.2831853 * phase;", "    vec3 sum = vec3(0.0);", "    float wsum = 0.0;", "    for (int i = 0; i < 6; i++) {", "        float fi = float(i);", "        float kx = mod(fi, 2.0) < 0.5 ? 1.0 : 2.0;", "        float ky = mod(fi, 2.0) < 0.5 ? 2.0 : 1.0;", "        vec2 pos = vec2(0.5) + 0.34 * vec2(sin(a * kx + fi * 2.3999), cos(a * ky - fi * 1.618));", "        vec2 d = (uv - pos) * vec2(aspect, 1.0);", "        float w = 1.0 / (dot(d, d) * 5.0 + 0.06);", "        sum += glowPaletteRamp(fi / 5.0) * w;", "        wsum += w;", "    }", "    return sum / wsum;", "}", "", "vec4 getSegment_glow(int i) {", "    vec4 span = params[6 + i];", "    return vec4(span.x, span.z, span.y - span.x, span.w - span.z);", "}", "", "float roundedBoxSDF_glow(vec2 center, vec2 halfSize, vec4 r) {", "    r.xy = (center.x > 0.0) ? r.xy : r.zw;", "    r.x  = (center.y > 0.0) ? r.x  : r.y;", "    vec2 q = abs(center) - halfSize + r.x;", "    return min(max(q.x, q.y), 0.0) + length(max(q, 0.0)) - r.x;", "}", "", "vec4 resolveSegmentRadius_glow(int index, vec4 seg, int count, float r,", "                               float leftAligned, float bottomAnchored) {", "    float rowX      = seg.x;", "    float rowRightX = seg.x + seg.z;", "", "    float topLeft     = (index == 0)         ? r : 0.0;", "    float topRight    = (index == 0)         ? r : 0.0;", "    float bottomLeft  = (index == count - 1) ? r : 0.0;", "    float bottomRight = (index == count - 1) ? r : 0.0;", "", "    if (leftAligned > 0.5) {", "        if (index > 0) {", "            vec4 prev = getSegment_glow(index - 1);", "            float prevRightX = prev.x + prev.z;", "            topRight = (rowRightX > prevRightX + 0.01) ? min(r, rowRightX - prevRightX) : 0.0;", "        }", "        if (index < count - 1) {", "            vec4 next = getSegment_glow(index + 1);", "            float nextRightX = next.x + next.z;", "            bottomRight = (rowRightX > nextRightX + 0.01) ? min(r, rowRightX - nextRightX) : 0.0;", "        }", "    } else {", "        if (index > 0) {", "            vec4 prev = getSegment_glow(index - 1);", "            topLeft = (rowX < prev.x - 0.01) ? min(r, prev.x - rowX) : 0.0;", "        }", "        if (index < count - 1) {", "            vec4 next = getSegment_glow(index + 1);", "            bottomLeft = (rowX < next.x - 0.01) ? min(r, next.x - rowX) : 0.0;", "        }", "    }", "    return vec4(topRight, bottomRight, topLeft, bottomLeft);", "}", "", "float boxesSDF(vec2 p, float spanCountF, float innerRadius, out vec2 boxUV) {", "    float minDist = 1e9;", "    boxUV = vec2(0.5);", "    int count = int(spanCountF + 0.5);", "    for (int i = 0; i < 64; i++) {", "        if (i >= count) break;", "        vec4 seg       = getSegment_glow(i);", "", "        vec2 fullHalf  = max(seg.zw * 0.5, vec2(0.001));", "        vec2 segCenter = seg.xy + fullHalf;", "        vec2 halfSize  = max(fullHalf - 1.0, vec2(0.001));", "        float rad = min(innerRadius, min(halfSize.x, halfSize.y));", "        vec2 center = p - segCenter;", "        center.y = -center.y;", "        float d = roundedBoxSDF_glow(center, halfSize, vec4(rad));", "        if (d < minDist) {", "            minDist = d;", "            boxUV = clamp((p - seg.xy) / max(seg.zw, vec2(1.0)), vec2(0.0), vec2(1.0));", "        }", "    }", "    return minDist;", "}", "", "float staircaseSDF(vec2 p, float spanCountF, float innerRadius,", "                   float leftAligned, float bottomAnchored) {", "    float minDist = 1e9;", "    int count = int(spanCountF + 0.5);", "    for (int i = 0; i < 64; i++) {", "        if (i >= count) break;", "        vec4 seg       = getSegment_glow(i);", "", "        vec2 fullHalf  = max(seg.zw * 0.5, vec2(0.001));", "        vec2 segCenter = seg.xy + fullHalf;", "        vec2 halfSize  = max(fullHalf - 1.0, vec2(0.001));", "        float rad = min(innerRadius, min(halfSize.x, halfSize.y));", "        vec4 r = resolveSegmentRadius_glow(i, seg, count, rad, leftAligned, bottomAnchored);", "", "        vec2 center = p - segCenter;", "        center.y = -center.y;", "        float d = roundedBoxSDF_glow(center, halfSize, r);", "        minDist = min(minDist, d);", "    }", "    return minDist;", "}", "", "void main() {", "", "    vec4 radius   = max(params[0], vec4(0.0));", "    vec4 sizeData = params[1];", "    vec4 colorVec = params[2];", "", "    vec2 rectSize = max(sizeData.xy, vec2(1.0));", "    float pad = max(sizeData.z, 0.0);", "    float smoothness = max(sizeData.w, 0.5);", "", "    vec2 targetSize = rectSize + vec2(pad * 2.0);", "    vec2 pixel = FragCoord * targetSize;", "    kimikoFrag = kimikoFragXYMappedFlipY(pixel, params[78]);", "", "    float spanCount    = params[5].x;", "    float innerRadius  = params[5].y;", "    float leftAligned  = params[5].z;", "    float bottomAnchored = params[5].w;", "", "    float rawSplit = params[3].x;", "    bool glowCut = rawSplit < -0.5;", "    int splitIdx = int(abs(rawSplit) + 0.5) - 1;", "    float parentDist = -1e9;", "", "    float dist;", "    bool boxesMode = false;", "    vec2 boxUV = vec2(0.5);", "    if (splitIdx >= 0) {", "", "        vec2 local = pixel - vec2(pad);", "        local.y = rectSize.y - local.y;", "        vec4 pr = splitData[splitIdx * 3];", "        vec4 cr = splitData[splitIdx * 3 + 1];", "        vec4 sm = splitData[splitIdx * 3 + 2];", "        float dP = rdist(pr.xy - local, max(pr.zw - 1.0, vec2(0.0)), radius);", "        float dC = rdist(cr.xy - local, max(cr.zw - 1.0, vec2(0.0)), vec4(max(sm.x, 0.0)));", "        dist = rsmin(dP, dC, max(sm.y, 0.0));", "        parentDist = dP;", "    } else if (spanCount >= 0.5) {", "", "        if (leftAligned < -0.5) {", "            boxesMode = true;", "            dist = boxesSDF(pixel, spanCount, innerRadius, boxUV);", "        } else {", "            dist = staircaseSDF(pixel, spanCount, innerRadius, leftAligned, bottomAnchored);", "        }", "    } else {", "", "        vec2 pos = pixel - targetSize * 0.5;", "        vec2 halfSize = max(rectSize * 0.5 - 1.0, vec2(0.0));", "        dist = rdist(pos, halfSize, radius);", "    }", "", "    float a = 1.0 - smoothstep(-smoothness, 0.0, dist);", "", "    if (glowCut) {", "        a *= smoothstep(-1.0, 3.0, parentDist);", "    }", "    if (a < 0.001) discard;", "", "    vec4 secondData = params[4];", "", "    vec2 shapeUV = boxesMode", "        ? boxUV", "        : clamp((pixel - vec2(pad)) / max(rectSize, vec2(1.0)), vec2(0.0), vec2(1.0));", "", "    shapeUV.y = 1.0 - shapeUV.y;", "", "    vec3 mixedColor;", "    if (params[3].w > 0.5) {", "", "        float rowPhase = 0.5 + 0.5 * sin(shapeUV.y * rectSize.y * params[3].y - params[3].z);", "        mixedColor = mix(colorVec.rgb, secondData.rgb, rowPhase);", "    } else if (int(params[70].x + 0.5) >= 2) {", "        float tri = abs(fract(shapeUV.x + fract(secondData.w)) * 2.0 - 1.0);", "", "        vec3 rampCol = glowPaletteRamp(smoothstep(0.0, 1.0, tri)) * 0.86;", "        vec3 meshCol = glowMesh(shapeUV, params[70].y, rectSize.x / rectSize.y);", "        vec3 boxCol = glowBox(shapeUV, params[70].y * 20.0);", "        vec3 targetCol = glowSelect(params[70].z, rampCol, meshCol, boxCol);", "", "        float sweep = params[70].w;", "        if (sweep >= 0.0) {", "            vec3 prevCol = glowSelect(params[77].x, rampCol, meshCol, boxCol);", "            float axis = shapeUV.x + (shapeUV.y - 0.5) * 0.30;", "            float head = sweep * 1.5 - 0.25;", "            float wipe = 1.0 - smoothstep(head - 0.04, head + 0.04, axis);", "            mixedColor = mix(prevCol, targetCol, wipe);", "        } else {", "            mixedColor = targetCol;", "        }", "    } else {", "        mixedColor = colorVec.rgb;", "    }", "", "    OutColor = vec4(mixedColor, colorVec.a * a);", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }

    private final String s14() {
        Object[] objectArray = new String[]{"//!vertex", "#version 150", "", "#moj_import <kimiko:releon_common.glsl>", "", "in vec3 Position;", "", "out vec2 FragCoord;", "", "void main() {", "    gl_Position = vec4(Position.xy, 0.0, 1.0);", "", "    vec2 uv = rvertexcoord(gl_VertexID);", "    FragCoord = vec2(uv.x, 1.0 - uv.y);", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }

    private final String s15() {
        Object[] objectArray = new String[]{"#version 150", "", "#moj_import <kimiko:releon_common.glsl>", "", "in vec3 Position;", "", "out vec2 FragCoord;", "", "void main() {", "    gl_Position = vec4(Position.xy, 0.0, 1.0);", "", "    vec2 uv = rvertexcoord(gl_VertexID);", "    FragCoord = vec2(uv.x, 1.0 - uv.y);", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }
}

