/*
 * Decompiled with CFR 0.152.
 * 
 * Could not load the following classes:
 *  kotlin.Metadata
 *  kotlin.collections.CollectionsKt
 *  kotlin.jvm.JvmStatic
 *  kotlin.jvm.internal.Intrinsics
 *  org.jetbrains.annotations.NotNull
 */
package rtx.kimiko.utils.render.shaders.post.customsky;

import java.util.Map;
import kotlin.Metadata;
import kotlin.collections.CollectionsKt;
import kotlin.jvm.JvmStatic;
import kotlin.jvm.internal.Intrinsics;
import org.jetbrains.annotations.NotNull;

@Metadata(mv={2, 4, 0}, k=1, xi=48, d1={"\u0000\"\n\u0002\u0018\u0002\n\u0002\u0010\u0000\n\u0002\b\u0002\n\u0002\u0010%\n\u0002\u0010\u000e\n\u0000\n\u0002\u0010\u0002\n\u0002\u0018\u0002\n\u0002\b\u0013\b\u00c6\u0002\u0018\u00002\u00020\u0001B\t\b\u0002\u00a2\u0006\u0004\b\u0002\u0010\u0003J'\u0010\t\u001a\u00020\u00072\u0012\u0010\u0006\u001a\u000e\u0012\u0004\u0012\u00020\u0005\u0012\u0004\u0012\u00020\u00050\u0004H\u0007b\u0002\b\b\u00a2\u0006\u0004\b\t\u0010\nJ\u001b\u0010\f\u001a\u00020\u00052\u0006\u0010\u000b\u001a\u00020\u0005H\u0007b\u0002\b\b\u00a2\u0006\u0004\b\f\u0010\rJ\u000f\u0010\u000e\u001a\u00020\u0005H\u0002\u00a2\u0006\u0004\b\u000e\u0010\u000fJ\u000f\u0010\u0010\u001a\u00020\u0005H\u0002\u00a2\u0006\u0004\b\u0010\u0010\u000fJ\u000f\u0010\u0011\u001a\u00020\u0005H\u0002\u00a2\u0006\u0004\b\u0011\u0010\u000fJ\u000f\u0010\u0012\u001a\u00020\u0005H\u0002\u00a2\u0006\u0004\b\u0012\u0010\u000fJ\u000f\u0010\u0013\u001a\u00020\u0005H\u0002\u00a2\u0006\u0004\b\u0013\u0010\u000fJ\u000f\u0010\u0014\u001a\u00020\u0005H\u0002\u00a2\u0006\u0004\b\u0014\u0010\u000fJ\u000f\u0010\u0015\u001a\u00020\u0005H\u0002\u00a2\u0006\u0004\b\u0015\u0010\u000fJ\u000f\u0010\u0016\u001a\u00020\u0005H\u0002\u00a2\u0006\u0004\b\u0016\u0010\u000fJ\u000f\u0010\u0017\u001a\u00020\u0005H\u0002\u00a2\u0006\u0004\b\u0017\u0010\u000fJ\u000f\u0010\u0018\u001a\u00020\u0005H\u0002\u00a2\u0006\u0004\b\u0018\u0010\u000fJ\u000f\u0010\u0019\u001a\u00020\u0005H\u0002\u00a2\u0006\u0004\b\u0019\u0010\u000fJ\u000f\u0010\u001a\u001a\u00020\u0005H\u0002\u00a2\u0006\u0004\b\u001a\u0010\u000f\u00a8\u0006\u001b"}, d2={"Lrtx/kimiko/utils/render/shaders/post/customsky/CustomskyShaders;", "", "<init>", "()V", "", "", "sources", "", "Lkotlin/jvm/JvmStatic;", "register", "(Ljava/util/Map;)V", "key", "source", "(Ljava/lang/String;)Ljava/lang/String;", "s0", "()Ljava/lang/String;", "s1", "s2", "s3", "s4", "s5", "s6", "s7", "s8", "s9", "s10", "s11", "rtx.kimiko:kimiko"})
public final class CustomskyShaders {
    @NotNull
    public static final CustomskyShaders INSTANCE = new CustomskyShaders();

    private CustomskyShaders() {
    }

    @JvmStatic
    public static final void register(@NotNull Map<String, String> sources) {
        Intrinsics.checkNotNullParameter(sources, (String)"sources");
        sources.put("post/customsky/aurora.glsl", INSTANCE.s0());
        sources.put("post/customsky/aurora.fsh", INSTANCE.s1());
        sources.put("post/customsky/blackhole.glsl", INSTANCE.s2());
        sources.put("post/customsky/blackhole.fsh", INSTANCE.s3());
        sources.put("post/customsky/bloom_down.glsl", INSTANCE.s4());
        sources.put("post/customsky/bloom_down.fsh", INSTANCE.s5());
        sources.put("post/customsky/composite.glsl", INSTANCE.s6());
        sources.put("post/customsky/composite.fsh", INSTANCE.s7());
        sources.put("post/customsky/customsky.glsl", INSTANCE.s8());
        sources.put("post/customsky/customsky.vsh", INSTANCE.s9());
        sources.put("post/customsky/starfall.glsl", INSTANCE.s10());
        sources.put("post/customsky/starfall.fsh", INSTANCE.s11());
    }

    @JvmStatic
    @NotNull
    public static final String source(@NotNull String key) {
        Intrinsics.checkNotNullParameter((Object)key, (String)"key");
        return switch (key) {
            case "post/customsky/aurora.glsl" -> INSTANCE.s0();
            case "post/customsky/aurora.fsh" -> INSTANCE.s1();
            case "post/customsky/blackhole.glsl" -> INSTANCE.s2();
            case "post/customsky/blackhole.fsh" -> INSTANCE.s3();
            case "post/customsky/bloom_down.glsl" -> INSTANCE.s4();
            case "post/customsky/bloom_down.fsh" -> INSTANCE.s5();
            case "post/customsky/composite.glsl" -> INSTANCE.s6();
            case "post/customsky/composite.fsh" -> INSTANCE.s7();
            case "post/customsky/customsky.glsl" -> INSTANCE.s8();
            case "post/customsky/customsky.vsh" -> INSTANCE.s9();
            case "post/customsky/starfall.glsl" -> INSTANCE.s10();
            case "post/customsky/starfall.fsh" -> INSTANCE.s11();
            default -> throw new IllegalArgumentException(key);
        };
    }

    private final String s0() {
        Object[] objectArray = new String[]{"//!fragment", "#version 150", "", "#moj_import <kimiko:theme_wave.glsl>", "", "layout(std140) uniform SkyParams {", "    mat4 invViewProj;", "    vec4 misc;", "    vec4 skyColor;", "    vec4 skyColor2;", "    vec4 taa;", "    mat4 prevViewProj;", "    vec4 skyExtra;", "};", "", "in vec2 texCoord;", "out vec4 fragColor;", "", "const mat2 ROT = mat2(0.80, 0.60, -0.60, 0.80);", "", "vec3 hsv2rgb(vec3 c) {", "    vec3 rgb = clamp(abs(mod(c.x * 6.0 + vec3(0.0, 4.0, 2.0), 6.0) - 3.0) - 1.0, 0.0, 1.0);", "    return c.z * mix(vec3(1.0), rgb, c.y);", "}", "", "vec3 nmzHash33(vec3 q) {", "    uvec3 p = uvec3(ivec3(q));", "    p = p * uvec3(374761393U, 1103515245U, 668265263U) + p.zxy + p.yzx;", "    p = p.yzx * (p.zxy ^ (p >> 3U));", "    return vec3(p ^ (p >> 16U)) * (1.0 / vec3(0xffffffffU));", "}", "", "float hash21(vec2 p) {", "    uvec2 q = uvec2(ivec2(p)) * uvec2(1597334673U, 3812015801U);", "    uint n = (q.x ^ q.y) * 1597334673U;", "    return float(n) * (1.0 / 4294967295.0);", "}", "", "float vnoise(vec2 p) {", "    vec2 i = floor(p);", "    vec2 f = fract(p);", "    f = f * f * (3.0 - 2.0 * f);", "    float a = hash21(i);", "    float b = hash21(i + vec2(1.0, 0.0));", "    float c = hash21(i + vec2(0.0, 1.0));", "    float d = hash21(i + vec2(1.0, 1.0));", "    return mix(mix(a, b, f.x), mix(c, d, f.x), f.y);", "}", "", "float fbm(vec2 p) {", "    float amp = 0.5;", "    float sum = 0.0;", "    for (int i = 0; i < 4; i++) {", "        sum += amp * vnoise(p);", "        p = ROT * p * 2.03;", "        amp *= 0.5;", "    }", "    return sum;", "}", "", "vec3 starField(vec3 dir, float time, float density, float twinkle) {", "    vec3 c = vec3(0.0);", "    vec3 p = dir * 42.0;", "    float dens = density;", "    float amp = 1.0;", "    for (int i = 0; i < 4; i++) {", "        vec3 id = floor(p);", "        vec3 q = fract(p) - 0.5;", "        vec3 rn = nmzHash33(id);", "        float core = smoothstep(0.36, 0.0, length(q));", "        float hit = step(rn.x, dens);", "        float tw = 1.0 - twinkle + twinkle * (0.55 + 0.45 * sin(time * (1.2 + rn.z * 2.6) + rn.y * 47.0));", "        vec3 tint = mix(vec3(1.0, 0.72, 0.45), vec3(0.72, 0.86, 1.0), rn.y);", "        c += hit * core * core * tint * (0.35 + 0.65 * rn.z) * tw * amp * 0.75;", "        p = p * 1.63 + 17.0;", "        dens *= 0.62;", "        amp *= 0.78;", "    }", "    return c;", "}", "", "vec3 skyTint(float axis, float t, float time) {", "    float mode = skyColor.w;", "    if (mode > 1.5) {", "        float hue = fract(axis * 0.159155 + time * 0.02 + t * 0.15);", "        return hsv2rgb(vec3(hue, 0.85, 1.0));", "    }", "    vec2 fragXY = kimikoFragXYFromUV(texCoord);", "    vec3 c1 = skyExtra.x > 0.5 ? kimikoClientPrimary(fragXY) : skyColor.rgb;", "    if (mode > 0.5) {", "        vec3 c2 = skyExtra.x > 0.5 ? kimikoClientSecondary(fragXY) : skyColor2.rgb;", "        float k = clamp(0.5 + 0.5 * sin(axis * 2.0 + time * 0.15) + (t - 0.5) * 0.7, 0.0, 1.0);", "        return mix(c1, c2, k);", "    }", "    return c1;", "}", "", "void main() {", "    float time = misc.x;", "    float brightness = misc.w;", "", "    vec2 ndc = texCoord * 2.0 - 1.0;", "    vec4 pFar = invViewProj * vec4(ndc, 1.0, 1.0);", "    vec4 pNear = invViewProj * vec4(ndc, -1.0, 1.0);", "    vec3 rd = normalize(pFar.xyz / pFar.w - pNear.xyz / pNear.w);", "", "    float axis = atan(rd.z, rd.x);", "", "    vec3 col = vec3(0.024, 0.036, 0.076) * (0.55 + 0.45 * smoothstep(-1.0, 1.0, rd.y));", "    col += starField(rd, time, 0.030, 0.35) * 0.9;", "", "    vec3 aur = vec3(0.0);", "    float rawLum = 0.0;", "", "    for (int i = 0; i < 3; i++) {", "        float fi = float(i);", "        float height = 1.0 + fi * 0.75;", "        float seed = fi * 4.7;", "", "        float tp = height / (max(rd.y, 0.0) + 0.13);", "        vec2 p = rd.xz * tp * 0.55;", "        p += vec2(time * 0.045 + seed * 3.3, time * 0.017 - seed * 2.1);", "", "        float d = fbm(p * vec2(0.85, 0.30) + vec2(seed, 0.0));", "        float center = 0.46 + fi * 0.075;", "        float band = exp(-pow((d - center) / 0.052, 2.0));", "", "        float rays = 0.5 + 0.5 * sin(p.x * 6.0 + fbm(p * 0.6) * 11.0 + time * (0.25 + fi * 0.1));", "        band *= 0.30 + 0.70 * rays * rays;", "", "        float fade = smoothstep(0.02 + fi * 0.02, 0.34 + fi * 0.12, rd.y);", "        band *= fade;", "", "        rawLum += band * (1.0 - fi * 0.3);", "", "        vec3 tint = skyTint(axis + fi * 0.6, fi * 0.5, time);", "        aur += tint * band * (1.0 - fi * 0.18);", "    }", "", "    col += aur * (1.25 * brightness);", "", "    vec3 horizonTint = skyTint(axis, 0.0, time);", "    float glow = exp(-abs(rd.y) * 6.5) * (0.08 + 0.42 * clamp(rawLum, 0.0, 1.5));", "    col += horizonTint * glow * 0.55 * brightness;", "", "    fragColor = vec4(col, 1.0);", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }

    private final String s1() {
        Object[] objectArray = new String[]{"#version 150", "", "#moj_import <kimiko:theme_wave.glsl>", "", "layout(std140) uniform SkyParams {", "    mat4 invViewProj;", "    vec4 misc;", "    vec4 skyColor;", "    vec4 skyColor2;", "    vec4 taa;", "    mat4 prevViewProj;", "    vec4 skyExtra;", "};", "", "in vec2 texCoord;", "out vec4 fragColor;", "", "const mat2 ROT = mat2(0.80, 0.60, -0.60, 0.80);", "", "vec3 hsv2rgb(vec3 c) {", "    vec3 rgb = clamp(abs(mod(c.x * 6.0 + vec3(0.0, 4.0, 2.0), 6.0) - 3.0) - 1.0, 0.0, 1.0);", "    return c.z * mix(vec3(1.0), rgb, c.y);", "}", "", "vec3 nmzHash33(vec3 q) {", "    uvec3 p = uvec3(ivec3(q));", "    p = p * uvec3(374761393U, 1103515245U, 668265263U) + p.zxy + p.yzx;", "    p = p.yzx * (p.zxy ^ (p >> 3U));", "    return vec3(p ^ (p >> 16U)) * (1.0 / vec3(0xffffffffU));", "}", "", "float hash21(vec2 p) {", "    uvec2 q = uvec2(ivec2(p)) * uvec2(1597334673U, 3812015801U);", "    uint n = (q.x ^ q.y) * 1597334673U;", "    return float(n) * (1.0 / 4294967295.0);", "}", "", "float vnoise(vec2 p) {", "    vec2 i = floor(p);", "    vec2 f = fract(p);", "    f = f * f * (3.0 - 2.0 * f);", "    float a = hash21(i);", "    float b = hash21(i + vec2(1.0, 0.0));", "    float c = hash21(i + vec2(0.0, 1.0));", "    float d = hash21(i + vec2(1.0, 1.0));", "    return mix(mix(a, b, f.x), mix(c, d, f.x), f.y);", "}", "", "float fbm(vec2 p) {", "    float amp = 0.5;", "    float sum = 0.0;", "    for (int i = 0; i < 4; i++) {", "        sum += amp * vnoise(p);", "        p = ROT * p * 2.03;", "        amp *= 0.5;", "    }", "    return sum;", "}", "", "vec3 starField(vec3 dir, float time, float density, float twinkle) {", "    vec3 c = vec3(0.0);", "    vec3 p = dir * 42.0;", "    float dens = density;", "    float amp = 1.0;", "    for (int i = 0; i < 4; i++) {", "        vec3 id = floor(p);", "        vec3 q = fract(p) - 0.5;", "        vec3 rn = nmzHash33(id);", "        float core = smoothstep(0.36, 0.0, length(q));", "        float hit = step(rn.x, dens);", "        float tw = 1.0 - twinkle + twinkle * (0.55 + 0.45 * sin(time * (1.2 + rn.z * 2.6) + rn.y * 47.0));", "        vec3 tint = mix(vec3(1.0, 0.72, 0.45), vec3(0.72, 0.86, 1.0), rn.y);", "        c += hit * core * core * tint * (0.35 + 0.65 * rn.z) * tw * amp * 0.75;", "        p = p * 1.63 + 17.0;", "        dens *= 0.62;", "        amp *= 0.78;", "    }", "    return c;", "}", "", "vec3 skyTint(float axis, float t, float time) {", "    float mode = skyColor.w;", "    if (mode > 1.5) {", "        float hue = fract(axis * 0.159155 + time * 0.02 + t * 0.15);", "        return hsv2rgb(vec3(hue, 0.85, 1.0));", "    }", "    vec2 fragXY = kimikoFragXYFromUV(texCoord);", "    vec3 c1 = skyExtra.x > 0.5 ? kimikoClientPrimary(fragXY) : skyColor.rgb;", "    if (mode > 0.5) {", "        vec3 c2 = skyExtra.x > 0.5 ? kimikoClientSecondary(fragXY) : skyColor2.rgb;", "        float k = clamp(0.5 + 0.5 * sin(axis * 2.0 + time * 0.15) + (t - 0.5) * 0.7, 0.0, 1.0);", "        return mix(c1, c2, k);", "    }", "    return c1;", "}", "", "void main() {", "    float time = misc.x;", "    float brightness = misc.w;", "", "    vec2 ndc = texCoord * 2.0 - 1.0;", "    vec4 pFar = invViewProj * vec4(ndc, 1.0, 1.0);", "    vec4 pNear = invViewProj * vec4(ndc, -1.0, 1.0);", "    vec3 rd = normalize(pFar.xyz / pFar.w - pNear.xyz / pNear.w);", "", "    float axis = atan(rd.z, rd.x);", "", "    vec3 col = vec3(0.024, 0.036, 0.076) * (0.55 + 0.45 * smoothstep(-1.0, 1.0, rd.y));", "    col += starField(rd, time, 0.030, 0.35) * 0.9;", "", "    vec3 aur = vec3(0.0);", "    float rawLum = 0.0;", "", "    for (int i = 0; i < 3; i++) {", "        float fi = float(i);", "        float height = 1.0 + fi * 0.75;", "        float seed = fi * 4.7;", "", "        float tp = height / (max(rd.y, 0.0) + 0.13);", "        vec2 p = rd.xz * tp * 0.55;", "        p += vec2(time * 0.045 + seed * 3.3, time * 0.017 - seed * 2.1);", "", "        float d = fbm(p * vec2(0.85, 0.30) + vec2(seed, 0.0));", "        float center = 0.46 + fi * 0.075;", "        float band = exp(-pow((d - center) / 0.052, 2.0));", "", "        float rays = 0.5 + 0.5 * sin(p.x * 6.0 + fbm(p * 0.6) * 11.0 + time * (0.25 + fi * 0.1));", "        band *= 0.30 + 0.70 * rays * rays;", "", "        float fade = smoothstep(0.02 + fi * 0.02, 0.34 + fi * 0.12, rd.y);", "        band *= fade;", "", "        rawLum += band * (1.0 - fi * 0.3);", "", "        vec3 tint = skyTint(axis + fi * 0.6, fi * 0.5, time);", "        aur += tint * band * (1.0 - fi * 0.18);", "    }", "", "    col += aur * (1.25 * brightness);", "", "    vec3 horizonTint = skyTint(axis, 0.0, time);", "    float glow = exp(-abs(rd.y) * 6.5) * (0.08 + 0.42 * clamp(rawLum, 0.0, 1.5));", "    col += horizonTint * glow * 0.55 * brightness;", "", "    fragColor = vec4(col, 1.0);", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }

    private final String s2() {
        Object[] objectArray = new String[]{"//!fragment", "#version 150", "", "#moj_import <kimiko:theme_wave.glsl>", "", "uniform sampler2D NoiseTex;", "uniform sampler2D History;", "", "layout(std140) uniform SkyParams {", "    mat4 invViewProj;", "    vec4 misc;", "    vec4 skyColor;", "    vec4 skyColor2;", "    vec4 taa;", "    mat4 prevViewProj;", "    vec4 skyExtra;", "};", "", "in vec2 texCoord;", "out vec4 fragColor;", "", "const int ITERATIONS = 200;", "const float FAR = 16.0;", "const float ENTRY_R = 8.0;", "const float CAM_DIST = 14.0;", "const float INCL = 0.10;", "const float EXPOSURE = 0.024;", "const float THEME_MIX = 0.5;", "const float STAR_GAIN = 0.26;", "const float ZOOM = 2.4;", "const float TAA_BLEND = 0.85;", "const float SPIN = 0.16;", "const float TAU = 6.28318531;", "", "vec3 hsv2rgb(vec3 c) {", "    vec3 rgb = clamp(abs(mod(c.x * 6.0 + vec3(0.0, 4.0, 2.0), 6.0) - 3.0) - 1.0, 0.0, 1.0);", "    return c.z * mix(vec3(1.0), rgb, c.y);", "}", "", "vec3 nmzHash33(vec3 q) {", "    uvec3 p = uvec3(ivec3(q));", "    p = p * uvec3(374761393U, 1103515245U, 668265263U) + p.zxy + p.yzx;", "    p = p.yzx * (p.zxy ^ (p >> 3U));", "    return vec3(p ^ (p >> 16U)) * (1.0 / vec3(0xffffffffU));", "}", "", "float hash21(vec2 p) {", "    vec3 p3 = fract(vec3(p.xyx) * 0.1031);", "    p3 += dot(p3, p3.yzx + 33.33);", "    return fract((p3.x + p3.y) * p3.z);", "}", "", "float noise(vec3 x) {", "    vec3 p = floor(x);", "    vec3 f = fract(x);", "    f = f * f * (3.0 - 2.0 * f);", "    vec2 uv = (p.xy + vec2(37.0, 17.0) * p.z) + f.xy;", "    vec2 rg = textureLod(NoiseTex, (uv + 0.5) / 256.0, 0.0).yx;", "    return -1.0 + 2.0 * mix(rg.x, rg.y, f.z);", "}", "", "vec3 discCoord(float ang, float rad, float freq) {", "    float a = 1.425 * freq;", "    return vec3(cos(ang) * a, sin(ang) * a, rad * freq);", "}", "", "float octave(float ang, float rad, float freq, float aa) {", "    float w = clamp(1.5 - aa * freq * 1.5, 0.0, 1.0);", "    if (w <= 0.01) {", "        return 0.5;", "    }", "    return mix(0.5, noise(discCoord(ang, rad, freq)) * 0.5 + 0.5, w);", "}", "", "float pcurve(float x, float a, float b) {", "    float k = pow(a + b, a + b) / (pow(a, a) * pow(b, b));", "    return k * pow(x, a) * pow(1.0 - x, b);", "}", "", "float sdTorus(vec3 p, vec2 t) {", "    vec2 q = vec2(length(p.xz) - t.x, p.y);", "    return length(q) - t.y;", "}", "", "vec3 encodeHDR(vec3 c) {", "    c = max(c, vec3(0.0));", "    return pow(c / (1.0 + c), vec3(1.0 / 2.2));", "}", "", "vec3 starField(vec3 dir, float time, float pixAngle) {", "    vec3 c = vec3(0.0);", "    vec3 p = dir * 42.0;", "    float scale = 42.0;", "    float dens = 0.05;", "    float amp = 1.0;", "    for (int i = 0; i < 4; i++) {", "        float fade = clamp(2.0 - pixAngle * scale / 0.17, 0.0, 1.0);", "        if (fade > 0.01) {", "            vec3 id = floor(p);", "            vec3 q = fract(p) - 0.5;", "            vec3 rn = nmzHash33(id);", "            float core = smoothstep(0.44, 0.05, length(q));", "            float hit = step(rn.x, dens);", "            float tw = 0.80 + 0.20 * sin(time * (0.6 + rn.z * 1.3) + rn.y * 47.0);", "            vec3 tint = mix(vec3(1.0, 0.72, 0.45), vec3(0.72, 0.86, 1.0), rn.y);", "            c += hit * core * core * tint * (0.35 + 0.65 * rn.z) * tw * amp * fade;", "        }", "        p = p * 1.63 + 17.0;", "        scale *= 1.63;", "        dens *= 0.62;", "        amp *= 0.78;", "    }", "    return c;", "}", "", "vec3 skyTint(float axis, float t, float time) {", "    float mode = skyColor.w;", "    if (mode > 1.5) {", "        float hue = fract(axis * 0.159155 + time * 0.02 + t * 0.15);", "        return hsv2rgb(vec3(hue, 0.85, 1.0));", "    }", "    vec2 fragXY = kimikoFragXYFromUV(texCoord);", "    vec3 c1 = skyExtra.x > 0.5 ? kimikoClientPrimary(fragXY) : skyColor.rgb;", "    if (mode > 0.5) {", "        vec3 c2 = skyExtra.x > 0.5 ? kimikoClientSecondary(fragXY) : skyColor2.rgb;", "        float k = clamp(0.5 + 0.5 * sin(axis * 2.0 + time * 0.15) + (t - 0.5) * 0.7, 0.0, 1.0);", "        return mix(c1, c2, k);", "    }", "    return c1;", "}", "", "void Haze(inout vec3 color, vec3 pos, float alpha, vec3 mainColor) {", "    if (dot(pos, pos) > 36.0) {", "        return;", "    }", "    vec2 t = vec2(1.0, 0.01);", "    float torusDist = length(sdTorus(pos + vec3(0.0, -0.05, 0.0), t));", "    float bloomDisc = 1.0 / (pow(torusDist, 2.0) + 0.001);", "    bloomDisc *= length(pos) < 0.5 ? 0.0 : 1.0;", "    color += mainColor * bloomDisc * (2.9 / float(ITERATIONS)) * (1.0 - alpha);", "}", "", "void GasDisc(inout vec3 color, inout float alpha, vec3 pos, float time, vec3 mainColor, float aa, vec3 eyevec) {", "    float discRadius = 3.2;", "    float discWidth = 5.3;", "    float discInner = discRadius - discWidth * 0.5;", "", "    vec3 discNormal = vec3(0.0, 1.0, 0.0);", "    float discThickness = 0.1;", "", "    float distFromCenter = length(pos);", "    if (distFromCenter > 7.0) {", "        return;", "    }", "    float distFromDisc = dot(discNormal, pos);", "", "    float radialGradient = 1.0 - clamp((distFromCenter - discInner) / discWidth * 0.5, 0.0, 1.0);", "    float coverage = pcurve(radialGradient, 4.0, 0.9);", "", "    discThickness *= radialGradient;", "    coverage *= clamp(1.0 - abs(distFromDisc) / max(discThickness, 1e-5), 0.0, 1.0);", "", "    vec3 tangent = normalize(vec3(-pos.z, 0.0, pos.x));", "    float dop = pow(clamp(1.0 - 0.55 * dot(tangent, eyevec), 0.45, 1.9), -2.5);", "    vec3 dopTint = mix(vec3(1.30, 0.72, 0.45), vec3(0.80, 0.92, 1.45), clamp((dop - 0.7) * 0.9, 0.0, 1.0));", "", "    vec3 dustColorLit = mainColor * dop * dopTint;", "    float dustGlow = 1.0 / (pow(1.0 - radialGradient, 2.0) * 290.0 + 0.002);", "    vec3 dustColor = dustColorLit * dustGlow * 8.2;", "", "    coverage = clamp(coverage * 0.7, 0.0, 1.0);", "", "    float fade = pow((abs(distFromCenter - discInner) + 0.4), 4.0) * 0.04;", "    float bloomFactor = 1.0 / (pow(distFromDisc, 2.0) * 40.0 + fade + 0.00002);", "    vec3 b = dustColorLit * pow(bloomFactor, 1.5);", "", "    b *= mix(vec3(1.7, 1.1, 1.0), vec3(0.5, 0.6, 1.0), vec3(pow(radialGradient, 2.0)));", "    b *= mix(vec3(1.7, 0.5, 0.1), vec3(1.0), vec3(pow(radialGradient, 0.5)));", "", "    dustColor = mix(dustColor, b * 150.0, clamp(1.0 - coverage, 0.0, 1.0));", "    coverage = clamp(coverage + bloomFactor * bloomFactor * 0.1, 0.0, 1.0);", "", "    if (coverage < 0.01) {", "        return;", "    }", "", "    float ang = atan(-pos.x, -pos.z);", "    float rad = (distFromCenter * 1.5 + 0.55 + distFromDisc * 1.5) * 0.95 + time * 0.012;", "", "    float omega = SPIN * pow(3.2 / max(distFromCenter, 0.75), 1.5);", "    float angA = ang + mod(time * omega, TAU);", "    float angB = ang + mod(time * omega * 0.45, TAU);", "", "    float n1 = 1.0;", "    n1 *= octave(angA, rad, 3.0, aa);", "    n1 *= octave(angB, rad, 6.0, aa);", "    n1 *= octave(angA, rad, 12.0, aa);", "    n1 *= octave(angB, rad, 24.0, aa);", "", "    float n2 = 2.0;", "    float rad2 = rad + 30.0;", "    n2 *= octave(angB, rad2, 3.0, aa);", "    n2 *= octave(angA, rad2, 6.0, aa);", "    n2 *= octave(angB, rad2, 12.0, aa);", "    n2 *= octave(angA, rad2, 24.0, aa);", "    n2 *= octave(angB, rad2, 48.0, aa);", "    n2 *= octave(angA, rad2, 92.0, aa);", "", "    dustColor *= n1 * 0.998 + 0.002;", "    coverage *= n2;", "", "    float bandAng = ang + mod(time * omega * 0.5, TAU);", "    float band = noise(discCoord(bandAng, rad, 1.35)) * 0.5 + 0.5;", "    float grain = noise(discCoord(bandAng, rad + 70.0, 3.46)) * 0.5 + 0.5;", "    vec3 texCol = mix(vec3(0.95, 0.55, 0.26), vec3(0.42, 0.60, 1.0), band) * (0.45 + 0.80 * grain);", "    dustColor *= pow(texCol, vec3(2.0)) * 4.0;", "", "    float arm = 0.5 + 0.5 * cos(2.0 * ang + 2.6 * log(max(distFromCenter, 0.3)) + time * SPIN * 4.5);", "    dustColor *= 0.40 + 1.05 * arm * arm;", "    coverage *= 0.78 + 0.22 * arm;", "", "    float dAngA = abs(mod(angA - 1.35, TAU) - 3.14159265);", "    float dRadA = distFromCenter - 1.5;", "    float dAngB = abs(mod(angB - 4.2, TAU) - 3.14159265);", "    float dRadB = distFromCenter - 2.7;", "    float hot = exp(-(dAngA * dAngA * 0.5 + dRadA * dRadA * 7.0)) * 3.4", "              + exp(-(dAngB * dAngB * 0.9 + dRadB * dRadB * 5.0)) * 1.8;", "    dustColor *= 1.0 + hot;", "", "    coverage = clamp(coverage * 1200.0 / float(ITERATIONS), 0.0, 1.0);", "    dustColor = max(vec3(0.0), dustColor);", "", "    coverage *= pcurve(radialGradient, 4.0, 0.9);", "", "    color = (1.0 - alpha) * dustColor * coverage + color;", "    alpha = (1.0 - alpha) * coverage + alpha;", "}", "", "void WarpSpace(inout vec3 eyevec, vec3 raypos) {", "    float singularityDist = length(raypos);", "    float warpFactor = 1.0 / (pow(singularityDist, 2.0) + 0.000001);", "    vec3 singularityVector = -raypos / max(singularityDist, 1e-4);", "    float warpAmount = 5.0;", "    eyevec = normalize(eyevec + singularityVector * warpFactor * warpAmount / float(ITERATIONS));", "}", "", "vec3 rayDir(vec2 uv) {", "    vec2 ndc = uv * 2.0 - 1.0;", "    vec4 pFar = invViewProj * vec4(ndc, 1.0, 1.0);", "    vec4 pNear = invViewProj * vec4(ndc, -1.0, 1.0);", "    return normalize(pFar.xyz / pFar.w - pNear.xyz / pNear.w);", "}", "", "vec3 toLocal(vec3 dir, vec3 eX, vec3 eY, vec3 eZ) {", "    vec3 local = vec3(dot(dir, eX), dot(dir, eY), dot(dir, eZ));", "    local.xy /= ZOOM;", "    return normalize(local);", "}", "", "vec3 sampleHistory(vec2 uv, vec2 res) {", "    vec2 samplePos = uv * res;", "    vec2 texPos1 = floor(samplePos - 0.5) + 0.5;", "    vec2 f = samplePos - texPos1;", "", "    vec2 w0 = f * (-0.5 + f * (1.0 - 0.5 * f));", "    vec2 w1 = 1.0 + f * f * (-2.5 + 1.5 * f);", "    vec2 w2 = f * (0.5 + f * (2.0 - 1.5 * f));", "    vec2 w3 = f * f * (-0.5 + 0.5 * f);", "", "    vec2 w12 = w1 + w2;", "    vec2 offset12 = w2 / w12;", "", "    vec2 p0 = (texPos1 - 1.0) / res;", "    vec2 p3 = (texPos1 + 2.0) / res;", "    vec2 p12 = (texPos1 + offset12) / res;", "", "    vec3 result = vec3(0.0);", "    result += textureLod(History, vec2(p0.x, p0.y), 0.0).rgb * (w0.x * w0.y);", "    result += textureLod(History, vec2(p12.x, p0.y), 0.0).rgb * (w12.x * w0.y);", "    result += textureLod(History, vec2(p3.x, p0.y), 0.0).rgb * (w3.x * w0.y);", "    result += textureLod(History, vec2(p0.x, p12.y), 0.0).rgb * (w0.x * w12.y);", "    result += textureLod(History, vec2(p12.x, p12.y), 0.0).rgb * (w12.x * w12.y);", "    result += textureLod(History, vec2(p3.x, p12.y), 0.0).rgb * (w3.x * w12.y);", "    result += textureLod(History, vec2(p0.x, p3.y), 0.0).rgb * (w0.x * w3.y);", "    result += textureLod(History, vec2(p12.x, p3.y), 0.0).rgb * (w12.x * w3.y);", "    result += textureLod(History, vec2(p3.x, p3.y), 0.0).rgb * (w3.x * w3.y);", "", "    return clamp(result, vec3(0.0), vec3(1.0));", "}", "", "void main() {", "    float time = misc.x;", "    float brightness = misc.w;", "", "    vec2 res = vec2(textureSize(History, 0));", "", "    vec3 rd = rayDir(texCoord);", "    vec3 rdJitter = rayDir(texCoord + taa.xy / res);", "", "    vec3 bhDir = normalize(vec3(0.34, 0.62, 0.71));", "    vec3 upRef = vec3(0.0, 1.0, 0.0);", "    vec3 tang = normalize(upRef - dot(upRef, bhDir) * bhDir);", "    vec3 eY = normalize(tang * cos(INCL) - bhDir * sin(INCL));", "    vec3 eX = normalize(cross(eY, bhDir));", "    vec3 eZ = cross(eX, eY);", "", "    vec3 tint = skyTint(atan(rd.z, rd.x), 0.5, time);", "    vec3 mainColor = mix(vec3(1.0), tint, THEME_MIX);", "", "    vec3 camWorld = -bhDir * CAM_DIST;", "    vec3 pos = vec3(dot(camWorld, eX), dot(camWorld, eY), dot(camWorld, eZ));", "    vec3 eyevec = toLocal(rdJitter, eX, eY, eZ);", "    float pixAngle = length(toLocal(rayDir(texCoord + vec2(1.0, 0.0) / res), eX, eY, eZ)", "            - toLocal(rd, eX, eY, eZ));", "    float aa = pixAngle * CAM_DIST * 1.425;", "", "    vec3 color = vec3(0.0);", "    float alpha = 0.0;", "    float captured = 0.0;", "", "    float impact = length(cross(pos, eyevec));", "    float along = dot(pos, eyevec);", "", "    if (impact < ENTRY_R && along < 0.0) {", "        float tEnter = -along - sqrt(max(0.0, ENTRY_R * ENTRY_R - impact * impact));", "        float stepLen = FAR / float(ITERATIONS);", "        float dither = fract(hash21(gl_FragCoord.xy) + taa.w);", "        vec3 raypos = pos + eyevec * (tEnter + dither * stepLen);", "", "        for (int i = 0; i < ITERATIONS; i++) {", "            WarpSpace(eyevec, raypos);", "            raypos += eyevec * stepLen;", "            GasDisc(color, alpha, raypos, time, mainColor, aa, eyevec);", "            Haze(color, raypos, alpha, mainColor);", "", "            float r2 = dot(raypos, raypos);", "            captured = max(captured, 1.0 - smoothstep(0.32, 0.85, sqrt(r2)));", "            if (alpha > 0.995) {", "                break;", "            }", "            if (r2 > 49.0 && dot(raypos, eyevec) > 0.0) {", "                break;", "            }", "        }", "    }", "", "    color *= EXPOSURE;", "", "    float ring = pow(clamp(captured * (1.0 - captured) * 4.0, 0.0, 1.0), 6.0);", "    color += mix(vec3(1.0), mainColor, 0.35) * ring * 0.6 * (1.0 - alpha);", "", "    color *= brightness;", "", "    vec3 cur = encodeHDR(color);", "", "    if (taa.z > 0.5) {", "        vec4 pc = prevViewProj * vec4(rd, 0.0);", "        if (pc.w > 1e-5) {", "            vec2 prevUV = pc.xy / pc.w * 0.5 + 0.5;", "            vec2 guard = 1.5 / res;", "            if (all(greaterThan(prevUV, guard)) && all(lessThan(prevUV, 1.0 - guard))) {", "                vec3 hist = clamp(sampleHistory(prevUV, res), cur - 0.35, cur + 0.35);", "                cur = mix(cur, hist, TAA_BLEND);", "            }", "        }", "    }", "", "    cur += (fract(hash21(gl_FragCoord.xy + 13.7) + taa.w) - 0.5) / 255.0;", "", "    fragColor = vec4(max(cur, vec3(0.0)), 1.0);", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }

    private final String s3() {
        Object[] objectArray = new String[]{"#version 150", "", "#moj_import <kimiko:theme_wave.glsl>", "", "uniform sampler2D NoiseTex;", "uniform sampler2D History;", "", "layout(std140) uniform SkyParams {", "    mat4 invViewProj;", "    vec4 misc;", "    vec4 skyColor;", "    vec4 skyColor2;", "    vec4 taa;", "    mat4 prevViewProj;", "    vec4 skyExtra;", "};", "", "in vec2 texCoord;", "out vec4 fragColor;", "", "const int ITERATIONS = 200;", "const float FAR = 16.0;", "const float ENTRY_R = 8.0;", "const float CAM_DIST = 14.0;", "const float INCL = 0.10;", "const float EXPOSURE = 0.024;", "const float THEME_MIX = 0.5;", "const float STAR_GAIN = 0.26;", "const float ZOOM = 2.4;", "const float TAA_BLEND = 0.85;", "const float SPIN = 0.16;", "const float TAU = 6.28318531;", "", "vec3 hsv2rgb(vec3 c) {", "    vec3 rgb = clamp(abs(mod(c.x * 6.0 + vec3(0.0, 4.0, 2.0), 6.0) - 3.0) - 1.0, 0.0, 1.0);", "    return c.z * mix(vec3(1.0), rgb, c.y);", "}", "", "vec3 nmzHash33(vec3 q) {", "    uvec3 p = uvec3(ivec3(q));", "    p = p * uvec3(374761393U, 1103515245U, 668265263U) + p.zxy + p.yzx;", "    p = p.yzx * (p.zxy ^ (p >> 3U));", "    return vec3(p ^ (p >> 16U)) * (1.0 / vec3(0xffffffffU));", "}", "", "float hash21(vec2 p) {", "    vec3 p3 = fract(vec3(p.xyx) * 0.1031);", "    p3 += dot(p3, p3.yzx + 33.33);", "    return fract((p3.x + p3.y) * p3.z);", "}", "", "float noise(vec3 x) {", "    vec3 p = floor(x);", "    vec3 f = fract(x);", "    f = f * f * (3.0 - 2.0 * f);", "    vec2 uv = (p.xy + vec2(37.0, 17.0) * p.z) + f.xy;", "    vec2 rg = textureLod(NoiseTex, (uv + 0.5) / 256.0, 0.0).yx;", "    return -1.0 + 2.0 * mix(rg.x, rg.y, f.z);", "}", "", "vec3 discCoord(float ang, float rad, float freq) {", "    float a = 1.425 * freq;", "    return vec3(cos(ang) * a, sin(ang) * a, rad * freq);", "}", "", "float octave(float ang, float rad, float freq, float aa) {", "    float w = clamp(1.5 - aa * freq * 1.5, 0.0, 1.0);", "    if (w <= 0.01) {", "        return 0.5;", "    }", "    return mix(0.5, noise(discCoord(ang, rad, freq)) * 0.5 + 0.5, w);", "}", "", "float pcurve(float x, float a, float b) {", "    float k = pow(a + b, a + b) / (pow(a, a) * pow(b, b));", "    return k * pow(x, a) * pow(1.0 - x, b);", "}", "", "float sdTorus(vec3 p, vec2 t) {", "    vec2 q = vec2(length(p.xz) - t.x, p.y);", "    return length(q) - t.y;", "}", "", "vec3 encodeHDR(vec3 c) {", "    c = max(c, vec3(0.0));", "    return pow(c / (1.0 + c), vec3(1.0 / 2.2));", "}", "", "vec3 starField(vec3 dir, float time, float pixAngle) {", "    vec3 c = vec3(0.0);", "    vec3 p = dir * 42.0;", "    float scale = 42.0;", "    float dens = 0.05;", "    float amp = 1.0;", "    for (int i = 0; i < 4; i++) {", "        float fade = clamp(2.0 - pixAngle * scale / 0.17, 0.0, 1.0);", "        if (fade > 0.01) {", "            vec3 id = floor(p);", "            vec3 q = fract(p) - 0.5;", "            vec3 rn = nmzHash33(id);", "            float core = smoothstep(0.44, 0.05, length(q));", "            float hit = step(rn.x, dens);", "            float tw = 0.80 + 0.20 * sin(time * (0.6 + rn.z * 1.3) + rn.y * 47.0);", "            vec3 tint = mix(vec3(1.0, 0.72, 0.45), vec3(0.72, 0.86, 1.0), rn.y);", "            c += hit * core * core * tint * (0.35 + 0.65 * rn.z) * tw * amp * fade;", "        }", "        p = p * 1.63 + 17.0;", "        scale *= 1.63;", "        dens *= 0.62;", "        amp *= 0.78;", "    }", "    return c;", "}", "", "vec3 skyTint(float axis, float t, float time) {", "    float mode = skyColor.w;", "    if (mode > 1.5) {", "        float hue = fract(axis * 0.159155 + time * 0.02 + t * 0.15);", "        return hsv2rgb(vec3(hue, 0.85, 1.0));", "    }", "    vec2 fragXY = kimikoFragXYFromUV(texCoord);", "    vec3 c1 = skyExtra.x > 0.5 ? kimikoClientPrimary(fragXY) : skyColor.rgb;", "    if (mode > 0.5) {", "        vec3 c2 = skyExtra.x > 0.5 ? kimikoClientSecondary(fragXY) : skyColor2.rgb;", "        float k = clamp(0.5 + 0.5 * sin(axis * 2.0 + time * 0.15) + (t - 0.5) * 0.7, 0.0, 1.0);", "        return mix(c1, c2, k);", "    }", "    return c1;", "}", "", "void Haze(inout vec3 color, vec3 pos, float alpha, vec3 mainColor) {", "    if (dot(pos, pos) > 36.0) {", "        return;", "    }", "    vec2 t = vec2(1.0, 0.01);", "    float torusDist = length(sdTorus(pos + vec3(0.0, -0.05, 0.0), t));", "    float bloomDisc = 1.0 / (pow(torusDist, 2.0) + 0.001);", "    bloomDisc *= length(pos) < 0.5 ? 0.0 : 1.0;", "    color += mainColor * bloomDisc * (2.9 / float(ITERATIONS)) * (1.0 - alpha);", "}", "", "void GasDisc(inout vec3 color, inout float alpha, vec3 pos, float time, vec3 mainColor, float aa, vec3 eyevec) {", "    float discRadius = 3.2;", "    float discWidth = 5.3;", "    float discInner = discRadius - discWidth * 0.5;", "", "    vec3 discNormal = vec3(0.0, 1.0, 0.0);", "    float discThickness = 0.1;", "", "    float distFromCenter = length(pos);", "    if (distFromCenter > 7.0) {", "        return;", "    }", "    float distFromDisc = dot(discNormal, pos);", "", "    float radialGradient = 1.0 - clamp((distFromCenter - discInner) / discWidth * 0.5, 0.0, 1.0);", "    float coverage = pcurve(radialGradient, 4.0, 0.9);", "", "    discThickness *= radialGradient;", "    coverage *= clamp(1.0 - abs(distFromDisc) / max(discThickness, 1e-5), 0.0, 1.0);", "", "    vec3 tangent = normalize(vec3(-pos.z, 0.0, pos.x));", "    float dop = pow(clamp(1.0 - 0.55 * dot(tangent, eyevec), 0.45, 1.9), -2.5);", "    vec3 dopTint = mix(vec3(1.30, 0.72, 0.45), vec3(0.80, 0.92, 1.45), clamp((dop - 0.7) * 0.9, 0.0, 1.0));", "", "    vec3 dustColorLit = mainColor * dop * dopTint;", "    float dustGlow = 1.0 / (pow(1.0 - radialGradient, 2.0) * 290.0 + 0.002);", "    vec3 dustColor = dustColorLit * dustGlow * 8.2;", "", "    coverage = clamp(coverage * 0.7, 0.0, 1.0);", "", "    float fade = pow((abs(distFromCenter - discInner) + 0.4), 4.0) * 0.04;", "    float bloomFactor = 1.0 / (pow(distFromDisc, 2.0) * 40.0 + fade + 0.00002);", "    vec3 b = dustColorLit * pow(bloomFactor, 1.5);", "", "    b *= mix(vec3(1.7, 1.1, 1.0), vec3(0.5, 0.6, 1.0), vec3(pow(radialGradient, 2.0)));", "    b *= mix(vec3(1.7, 0.5, 0.1), vec3(1.0), vec3(pow(radialGradient, 0.5)));", "", "    dustColor = mix(dustColor, b * 150.0, clamp(1.0 - coverage, 0.0, 1.0));", "    coverage = clamp(coverage + bloomFactor * bloomFactor * 0.1, 0.0, 1.0);", "", "    if (coverage < 0.01) {", "        return;", "    }", "", "    float ang = atan(-pos.x, -pos.z);", "    float rad = (distFromCenter * 1.5 + 0.55 + distFromDisc * 1.5) * 0.95 + time * 0.012;", "", "    float omega = SPIN * pow(3.2 / max(distFromCenter, 0.75), 1.5);", "    float angA = ang + mod(time * omega, TAU);", "    float angB = ang + mod(time * omega * 0.45, TAU);", "", "    float n1 = 1.0;", "    n1 *= octave(angA, rad, 3.0, aa);", "    n1 *= octave(angB, rad, 6.0, aa);", "    n1 *= octave(angA, rad, 12.0, aa);", "    n1 *= octave(angB, rad, 24.0, aa);", "", "    float n2 = 2.0;", "    float rad2 = rad + 30.0;", "    n2 *= octave(angB, rad2, 3.0, aa);", "    n2 *= octave(angA, rad2, 6.0, aa);", "    n2 *= octave(angB, rad2, 12.0, aa);", "    n2 *= octave(angA, rad2, 24.0, aa);", "    n2 *= octave(angB, rad2, 48.0, aa);", "    n2 *= octave(angA, rad2, 92.0, aa);", "", "    dustColor *= n1 * 0.998 + 0.002;", "    coverage *= n2;", "", "    float bandAng = ang + mod(time * omega * 0.5, TAU);", "    float band = noise(discCoord(bandAng, rad, 1.35)) * 0.5 + 0.5;", "    float grain = noise(discCoord(bandAng, rad + 70.0, 3.46)) * 0.5 + 0.5;", "    vec3 texCol = mix(vec3(0.95, 0.55, 0.26), vec3(0.42, 0.60, 1.0), band) * (0.45 + 0.80 * grain);", "    dustColor *= pow(texCol, vec3(2.0)) * 4.0;", "", "    float arm = 0.5 + 0.5 * cos(2.0 * ang + 2.6 * log(max(distFromCenter, 0.3)) + time * SPIN * 4.5);", "    dustColor *= 0.40 + 1.05 * arm * arm;", "    coverage *= 0.78 + 0.22 * arm;", "", "    float dAngA = abs(mod(angA - 1.35, TAU) - 3.14159265);", "    float dRadA = distFromCenter - 1.5;", "    float dAngB = abs(mod(angB - 4.2, TAU) - 3.14159265);", "    float dRadB = distFromCenter - 2.7;", "    float hot = exp(-(dAngA * dAngA * 0.5 + dRadA * dRadA * 7.0)) * 3.4", "              + exp(-(dAngB * dAngB * 0.9 + dRadB * dRadB * 5.0)) * 1.8;", "    dustColor *= 1.0 + hot;", "", "    coverage = clamp(coverage * 1200.0 / float(ITERATIONS), 0.0, 1.0);", "    dustColor = max(vec3(0.0), dustColor);", "", "    coverage *= pcurve(radialGradient, 4.0, 0.9);", "", "    color = (1.0 - alpha) * dustColor * coverage + color;", "    alpha = (1.0 - alpha) * coverage + alpha;", "}", "", "void WarpSpace(inout vec3 eyevec, vec3 raypos) {", "    float singularityDist = length(raypos);", "    float warpFactor = 1.0 / (pow(singularityDist, 2.0) + 0.000001);", "    vec3 singularityVector = -raypos / max(singularityDist, 1e-4);", "    float warpAmount = 5.0;", "    eyevec = normalize(eyevec + singularityVector * warpFactor * warpAmount / float(ITERATIONS));", "}", "", "vec3 rayDir(vec2 uv) {", "    vec2 ndc = uv * 2.0 - 1.0;", "    vec4 pFar = invViewProj * vec4(ndc, 1.0, 1.0);", "    vec4 pNear = invViewProj * vec4(ndc, -1.0, 1.0);", "    return normalize(pFar.xyz / pFar.w - pNear.xyz / pNear.w);", "}", "", "vec3 toLocal(vec3 dir, vec3 eX, vec3 eY, vec3 eZ) {", "    vec3 local = vec3(dot(dir, eX), dot(dir, eY), dot(dir, eZ));", "    local.xy /= ZOOM;", "    return normalize(local);", "}", "", "vec3 sampleHistory(vec2 uv, vec2 res) {", "    vec2 samplePos = uv * res;", "    vec2 texPos1 = floor(samplePos - 0.5) + 0.5;", "    vec2 f = samplePos - texPos1;", "", "    vec2 w0 = f * (-0.5 + f * (1.0 - 0.5 * f));", "    vec2 w1 = 1.0 + f * f * (-2.5 + 1.5 * f);", "    vec2 w2 = f * (0.5 + f * (2.0 - 1.5 * f));", "    vec2 w3 = f * f * (-0.5 + 0.5 * f);", "", "    vec2 w12 = w1 + w2;", "    vec2 offset12 = w2 / w12;", "", "    vec2 p0 = (texPos1 - 1.0) / res;", "    vec2 p3 = (texPos1 + 2.0) / res;", "    vec2 p12 = (texPos1 + offset12) / res;", "", "    vec3 result = vec3(0.0);", "    result += textureLod(History, vec2(p0.x, p0.y), 0.0).rgb * (w0.x * w0.y);", "    result += textureLod(History, vec2(p12.x, p0.y), 0.0).rgb * (w12.x * w0.y);", "    result += textureLod(History, vec2(p3.x, p0.y), 0.0).rgb * (w3.x * w0.y);", "    result += textureLod(History, vec2(p0.x, p12.y), 0.0).rgb * (w0.x * w12.y);", "    result += textureLod(History, vec2(p12.x, p12.y), 0.0).rgb * (w12.x * w12.y);", "    result += textureLod(History, vec2(p3.x, p12.y), 0.0).rgb * (w3.x * w12.y);", "    result += textureLod(History, vec2(p0.x, p3.y), 0.0).rgb * (w0.x * w3.y);", "    result += textureLod(History, vec2(p12.x, p3.y), 0.0).rgb * (w12.x * w3.y);", "    result += textureLod(History, vec2(p3.x, p3.y), 0.0).rgb * (w3.x * w3.y);", "", "    return clamp(result, vec3(0.0), vec3(1.0));", "}", "", "void main() {", "    float time = misc.x;", "    float brightness = misc.w;", "", "    vec2 res = vec2(textureSize(History, 0));", "", "    vec3 rd = rayDir(texCoord);", "    vec3 rdJitter = rayDir(texCoord + taa.xy / res);", "", "    vec3 bhDir = normalize(vec3(0.34, 0.62, 0.71));", "    vec3 upRef = vec3(0.0, 1.0, 0.0);", "    vec3 tang = normalize(upRef - dot(upRef, bhDir) * bhDir);", "    vec3 eY = normalize(tang * cos(INCL) - bhDir * sin(INCL));", "    vec3 eX = normalize(cross(eY, bhDir));", "    vec3 eZ = cross(eX, eY);", "", "    vec3 tint = skyTint(atan(rd.z, rd.x), 0.5, time);", "    vec3 mainColor = mix(vec3(1.0), tint, THEME_MIX);", "", "    vec3 camWorld = -bhDir * CAM_DIST;", "    vec3 pos = vec3(dot(camWorld, eX), dot(camWorld, eY), dot(camWorld, eZ));", "    vec3 eyevec = toLocal(rdJitter, eX, eY, eZ);", "    float pixAngle = length(toLocal(rayDir(texCoord + vec2(1.0, 0.0) / res), eX, eY, eZ)", "            - toLocal(rd, eX, eY, eZ));", "    float aa = pixAngle * CAM_DIST * 1.425;", "", "    vec3 color = vec3(0.0);", "    float alpha = 0.0;", "    float captured = 0.0;", "", "    float impact = length(cross(pos, eyevec));", "    float along = dot(pos, eyevec);", "", "    if (impact < ENTRY_R && along < 0.0) {", "        float tEnter = -along - sqrt(max(0.0, ENTRY_R * ENTRY_R - impact * impact));", "        float stepLen = FAR / float(ITERATIONS);", "        float dither = fract(hash21(gl_FragCoord.xy) + taa.w);", "        vec3 raypos = pos + eyevec * (tEnter + dither * stepLen);", "", "        for (int i = 0; i < ITERATIONS; i++) {", "            WarpSpace(eyevec, raypos);", "            raypos += eyevec * stepLen;", "            GasDisc(color, alpha, raypos, time, mainColor, aa, eyevec);", "            Haze(color, raypos, alpha, mainColor);", "", "            float r2 = dot(raypos, raypos);", "            captured = max(captured, 1.0 - smoothstep(0.32, 0.85, sqrt(r2)));", "            if (alpha > 0.995) {", "                break;", "            }", "            if (r2 > 49.0 && dot(raypos, eyevec) > 0.0) {", "                break;", "            }", "        }", "    }", "", "    color *= EXPOSURE;", "", "    float ring = pow(clamp(captured * (1.0 - captured) * 4.0, 0.0, 1.0), 6.0);", "    color += mix(vec3(1.0), mainColor, 0.35) * ring * 0.6 * (1.0 - alpha);", "", "    color *= brightness;", "", "    vec3 cur = encodeHDR(color);", "", "    if (taa.z > 0.5) {", "        vec4 pc = prevViewProj * vec4(rd, 0.0);", "        if (pc.w > 1e-5) {", "            vec2 prevUV = pc.xy / pc.w * 0.5 + 0.5;", "            vec2 guard = 1.5 / res;", "            if (all(greaterThan(prevUV, guard)) && all(lessThan(prevUV, 1.0 - guard))) {", "                vec3 hist = clamp(sampleHistory(prevUV, res), cur - 0.35, cur + 0.35);", "                cur = mix(cur, hist, TAA_BLEND);", "            }", "        }", "    }", "", "    cur += (fract(hash21(gl_FragCoord.xy + 13.7) + taa.w) - 0.5) / 255.0;", "", "    fragColor = vec4(max(cur, vec3(0.0)), 1.0);", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }

    private final String s4() {
        Object[] objectArray = new String[]{"//!fragment", "#version 150", "", "uniform sampler2D Source;", "", "layout(std140) uniform BloomParams {", "    vec4 params;", "};", "", "in vec2 texCoord;", "out vec4 fragColor;", "", "vec3 decodeHDR(vec3 c) {", "    c = pow(min(c, vec3(0.9975)), vec3(2.2));", "    return c / max(1.0 - c, vec3(0.0055));", "}", "", "vec3 encodeHDR(vec3 c) {", "    c = max(c, vec3(0.0));", "    return pow(c / (1.0 + c), vec3(1.0 / 2.2));", "}", "", "vec3 fetch(vec2 uv, float hdr) {", "    vec3 c = texture(Source, uv).rgb;", "    return hdr > 0.5 ? decodeHDR(c) : c;", "}", "", "void main() {", "    vec2 hp = params.xy;", "    float hdr = params.w;", "", "    vec3 s = fetch(texCoord, hdr) * 4.0;", "    s += fetch(texCoord + vec2(hp.x, 0.0), hdr) * 2.0;", "    s += fetch(texCoord - vec2(hp.x, 0.0), hdr) * 2.0;", "    s += fetch(texCoord + vec2(0.0, hp.y), hdr) * 2.0;", "    s += fetch(texCoord - vec2(0.0, hp.y), hdr) * 2.0;", "    s += fetch(texCoord + hp, hdr);", "    s += fetch(texCoord - hp, hdr);", "    s += fetch(texCoord + vec2(hp.x, -hp.y), hdr);", "    s += fetch(texCoord - vec2(hp.x, -hp.y), hdr);", "    s /= 16.0;", "", "    if (hdr > 0.5) {", "        fragColor = vec4(encodeHDR(s), 1.0);", "        return;", "    }", "", "    s = max(s - params.z, vec3(0.0));", "    fragColor = vec4(s, 1.0);", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }

    private final String s5() {
        Object[] objectArray = new String[]{"#version 150", "", "uniform sampler2D Source;", "", "layout(std140) uniform BloomParams {", "    vec4 params;", "};", "", "in vec2 texCoord;", "out vec4 fragColor;", "", "vec3 decodeHDR(vec3 c) {", "    c = pow(min(c, vec3(0.9975)), vec3(2.2));", "    return c / max(1.0 - c, vec3(0.0055));", "}", "", "vec3 encodeHDR(vec3 c) {", "    c = max(c, vec3(0.0));", "    return pow(c / (1.0 + c), vec3(1.0 / 2.2));", "}", "", "vec3 fetch(vec2 uv, float hdr) {", "    vec3 c = texture(Source, uv).rgb;", "    return hdr > 0.5 ? decodeHDR(c) : c;", "}", "", "void main() {", "    vec2 hp = params.xy;", "    float hdr = params.w;", "", "    vec3 s = fetch(texCoord, hdr) * 4.0;", "    s += fetch(texCoord + vec2(hp.x, 0.0), hdr) * 2.0;", "    s += fetch(texCoord - vec2(hp.x, 0.0), hdr) * 2.0;", "    s += fetch(texCoord + vec2(0.0, hp.y), hdr) * 2.0;", "    s += fetch(texCoord - vec2(0.0, hp.y), hdr) * 2.0;", "    s += fetch(texCoord + hp, hdr);", "    s += fetch(texCoord - hp, hdr);", "    s += fetch(texCoord + vec2(hp.x, -hp.y), hdr);", "    s += fetch(texCoord - vec2(hp.x, -hp.y), hdr);", "    s /= 16.0;", "", "    if (hdr > 0.5) {", "        fragColor = vec4(encodeHDR(s), 1.0);", "        return;", "    }", "", "    s = max(s - params.z, vec3(0.0));", "    fragColor = vec4(s, 1.0);", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }

    private final String s6() {
        Object[] objectArray = new String[]{"//!fragment", "#version 150", "", "uniform sampler2D Scene;", "uniform sampler2D DepthTex;", "uniform sampler2D Sky;", "uniform sampler2D Bloom0;", "uniform sampler2D Bloom1;", "uniform sampler2D Bloom2;", "uniform sampler2D Bloom3;", "uniform sampler2D Bloom4;", "uniform sampler2D Bloom5;", "", "layout(std140) uniform SkyParams {", "    mat4 invViewProj;", "    vec4 misc;", "    vec4 skyColor;", "    vec4 skyColor2;", "    vec4 taa;", "    mat4 prevViewProj;", "    vec4 skyExtra;", "};", "", "in vec2 texCoord;", "out vec4 fragColor;", "", "const float BLOOM_STRENGTH = 0.13;", "", "vec3 nmzHash33(vec3 q) {", "    uvec3 p = uvec3(ivec3(q));", "    p = p * uvec3(374761393U, 1103515245U, 668265263U) + p.zxy + p.yzx;", "    p = p.yzx * (p.zxy ^ (p >> 3U));", "    return vec3(p ^ (p >> 16U)) * (1.0 / vec3(0xffffffffU));", "}", "", "vec3 bhStars(vec3 dir, float time) {", "    vec3 c = vec3(0.0);", "    vec3 p = dir * 42.0;", "    float dens = 0.05;", "    float amp = 1.0;", "    for (int i = 0; i < 4; i++) {", "        vec3 id = floor(p);", "        vec3 q = fract(p) - 0.5;", "        vec3 rn = nmzHash33(id);", "        float core = smoothstep(0.30, 0.0, length(q));", "        float hit = step(rn.x, dens);", "        float tw = 0.78 + 0.22 * sin(time * (0.6 + rn.z * 1.3) + rn.y * 47.0);", "        vec3 tint = mix(vec3(1.0, 0.74, 0.50), vec3(0.72, 0.86, 1.0), rn.y);", "        c += hit * core * core * tint * (0.35 + 0.65 * rn.z) * tw * amp;", "        p = p * 1.63 + 17.0;", "        dens *= 0.62;", "        amp *= 0.78;", "    }", "    return c * 0.42;", "}", "", "vec3 decodeHDR(vec3 c) {", "    c = pow(min(c, vec3(0.9975)), vec3(2.2));", "    return c / max(1.0 - c, vec3(0.0055));", "}", "", "vec4 cubic(float x) {", "    float x2 = x * x;", "    float x3 = x2 * x;", "    vec4 w;", "    w.x = -x3 + 3.0 * x2 - 3.0 * x + 1.0;", "    w.y = 3.0 * x3 - 6.0 * x2 + 4.0;", "    w.z = -3.0 * x3 + 3.0 * x2 + 3.0 * x + 1.0;", "    w.w = x3;", "    return w / 6.0;", "}", "", "vec3 bicubic(sampler2D tex, vec2 uv) {", "    vec2 resolution = vec2(textureSize(tex, 0));", "    vec2 coord = uv * resolution;", "", "    float fx = fract(coord.x);", "    float fy = fract(coord.y);", "    coord.x -= fx;", "    coord.y -= fy;", "    fx -= 0.5;", "    fy -= 0.5;", "", "    vec4 xcubic = cubic(fx);", "    vec4 ycubic = cubic(fy);", "", "    vec4 c = vec4(coord.x - 0.5, coord.x + 1.5, coord.y - 0.5, coord.y + 1.5);", "    vec4 s = vec4(xcubic.x + xcubic.y, xcubic.z + xcubic.w, ycubic.x + ycubic.y, ycubic.z + ycubic.w);", "    vec4 offset = c + vec4(xcubic.y, xcubic.w, ycubic.y, ycubic.w) / s;", "", "    vec3 s0 = texture(tex, vec2(offset.x, offset.z) / resolution).rgb;", "    vec3 s1 = texture(tex, vec2(offset.y, offset.z) / resolution).rgb;", "    vec3 s2 = texture(tex, vec2(offset.x, offset.w) / resolution).rgb;", "    vec3 s3 = texture(tex, vec2(offset.y, offset.w) / resolution).rgb;", "", "    float sx = s.x / (s.x + s.y);", "    float sy = s.z / (s.z + s.w);", "", "    return mix(mix(s3, s2, sx), mix(s1, s0, sx), sy);", "}", "", "vec3 blackHole() {", "    vec3 color = decodeHDR(bicubic(Sky, texCoord));", "", "    vec3 bloom = decodeHDR(bicubic(Bloom0, texCoord)) * 1.00;", "    bloom += decodeHDR(bicubic(Bloom1, texCoord)) * 1.00;", "    bloom += decodeHDR(bicubic(Bloom2, texCoord)) * 1.00;", "    bloom += decodeHDR(bicubic(Bloom3, texCoord)) * 0.90;", "    bloom += decodeHDR(bicubic(Bloom4, texCoord)) * 0.80;", "    bloom += decodeHDR(bicubic(Bloom5, texCoord)) * 0.65;", "", "    color += bloom * BLOOM_STRENGTH;", "", "    color = pow(color, vec3(1.5));", "    color = color / (1.0 + color);", "    color = pow(color, vec3(1.0 / 1.5));", "    color = color * color * (3.0 - 2.0 * color);", "    color = pow(color, vec3(1.3, 1.20, 1.0));", "    color = clamp(color * 1.01, 0.0, 1.0);", "    color = pow(color, vec3(0.7 / 2.2));", "", "    vec2 ndc = texCoord * 2.0 - 1.0;", "    vec4 pFar = invViewProj * vec4(ndc, 1.0, 1.0);", "    vec4 pNear = invViewProj * vec4(ndc, -1.0, 1.0);", "    vec3 rd = normalize(pFar.xyz / pFar.w - pNear.xyz / pNear.w);", "", "    vec3 bhDir = normalize(vec3(0.34, 0.62, 0.71));", "    float cosT = clamp(dot(rd, bhDir), -1.0, 1.0);", "    float theta = acos(cosT);", "    vec3 toHole = bhDir - cosT * rd;", "    float tl = length(toHole);", "    vec3 rdL = rd;", "    if (tl > 1e-4) {", "        rdL = normalize(rd + toHole / tl * (0.010 / max(theta, 0.05)));", "    }", "", "    float lum = dot(color, vec3(0.299, 0.587, 0.114));", "    vec3 stars = bhStars(rdL, misc.x) * smoothstep(0.10, 0.17, theta);", "    color += stars * misc.w * clamp(1.0 - lum * 2.4, 0.0, 1.0);", "", "    return color;", "}", "", "void main() {", "    float depth = texture(DepthTex, texCoord).r;", "    vec3 scene = texture(Scene, texCoord).rgb;", "    if (depth < 0.9999) {", "        fragColor = vec4(scene, 1.0);", "        return;", "    }", "", "    vec2 dt = 1.0 / vec2(textureSize(DepthTex, 0));", "    float dmin = min(", "        min(texture(DepthTex, texCoord + vec2(dt.x, 0.0)).r, texture(DepthTex, texCoord - vec2(dt.x, 0.0)).r),", "        min(texture(DepthTex, texCoord + vec2(0.0, dt.y)).r, texture(DepthTex, texCoord - vec2(0.0, dt.y)).r)", "    );", "    float edgeDim = 1.0 - 0.6 * step(dmin, 0.9998);", "", "    vec3 result;", "    if (skyColor2.w > 0.5 && skyColor2.w < 1.5) {", "        result = blackHole();", "    } else {", "        vec3 sky = texture(Sky, texCoord).rgb;", "        vec3 bloom = texture(Bloom0, texCoord).rgb * 1.00", "                   + texture(Bloom1, texCoord).rgb * 0.90", "                   + texture(Bloom2, texCoord).rgb * 0.75;", "        result = sky + bloom * 0.34;", "    }", "", "    fragColor = vec4(result * edgeDim, 1.0);", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }

    private final String s7() {
        Object[] objectArray = new String[]{"#version 150", "", "uniform sampler2D Scene;", "uniform sampler2D DepthTex;", "uniform sampler2D Sky;", "uniform sampler2D Bloom0;", "uniform sampler2D Bloom1;", "uniform sampler2D Bloom2;", "uniform sampler2D Bloom3;", "uniform sampler2D Bloom4;", "uniform sampler2D Bloom5;", "", "layout(std140) uniform SkyParams {", "    mat4 invViewProj;", "    vec4 misc;", "    vec4 skyColor;", "    vec4 skyColor2;", "    vec4 taa;", "    mat4 prevViewProj;", "    vec4 skyExtra;", "};", "", "in vec2 texCoord;", "out vec4 fragColor;", "", "const float BLOOM_STRENGTH = 0.13;", "", "vec3 nmzHash33(vec3 q) {", "    uvec3 p = uvec3(ivec3(q));", "    p = p * uvec3(374761393U, 1103515245U, 668265263U) + p.zxy + p.yzx;", "    p = p.yzx * (p.zxy ^ (p >> 3U));", "    return vec3(p ^ (p >> 16U)) * (1.0 / vec3(0xffffffffU));", "}", "", "vec3 bhStars(vec3 dir, float time) {", "    vec3 c = vec3(0.0);", "    vec3 p = dir * 42.0;", "    float dens = 0.05;", "    float amp = 1.0;", "    for (int i = 0; i < 4; i++) {", "        vec3 id = floor(p);", "        vec3 q = fract(p) - 0.5;", "        vec3 rn = nmzHash33(id);", "        float core = smoothstep(0.30, 0.0, length(q));", "        float hit = step(rn.x, dens);", "        float tw = 0.78 + 0.22 * sin(time * (0.6 + rn.z * 1.3) + rn.y * 47.0);", "        vec3 tint = mix(vec3(1.0, 0.74, 0.50), vec3(0.72, 0.86, 1.0), rn.y);", "        c += hit * core * core * tint * (0.35 + 0.65 * rn.z) * tw * amp;", "        p = p * 1.63 + 17.0;", "        dens *= 0.62;", "        amp *= 0.78;", "    }", "    return c * 0.42;", "}", "", "vec3 decodeHDR(vec3 c) {", "    c = pow(min(c, vec3(0.9975)), vec3(2.2));", "    return c / max(1.0 - c, vec3(0.0055));", "}", "", "vec4 cubic(float x) {", "    float x2 = x * x;", "    float x3 = x2 * x;", "    vec4 w;", "    w.x = -x3 + 3.0 * x2 - 3.0 * x + 1.0;", "    w.y = 3.0 * x3 - 6.0 * x2 + 4.0;", "    w.z = -3.0 * x3 + 3.0 * x2 + 3.0 * x + 1.0;", "    w.w = x3;", "    return w / 6.0;", "}", "", "vec3 bicubic(sampler2D tex, vec2 uv) {", "    vec2 resolution = vec2(textureSize(tex, 0));", "    vec2 coord = uv * resolution;", "", "    float fx = fract(coord.x);", "    float fy = fract(coord.y);", "    coord.x -= fx;", "    coord.y -= fy;", "    fx -= 0.5;", "    fy -= 0.5;", "", "    vec4 xcubic = cubic(fx);", "    vec4 ycubic = cubic(fy);", "", "    vec4 c = vec4(coord.x - 0.5, coord.x + 1.5, coord.y - 0.5, coord.y + 1.5);", "    vec4 s = vec4(xcubic.x + xcubic.y, xcubic.z + xcubic.w, ycubic.x + ycubic.y, ycubic.z + ycubic.w);", "    vec4 offset = c + vec4(xcubic.y, xcubic.w, ycubic.y, ycubic.w) / s;", "", "    vec3 s0 = texture(tex, vec2(offset.x, offset.z) / resolution).rgb;", "    vec3 s1 = texture(tex, vec2(offset.y, offset.z) / resolution).rgb;", "    vec3 s2 = texture(tex, vec2(offset.x, offset.w) / resolution).rgb;", "    vec3 s3 = texture(tex, vec2(offset.y, offset.w) / resolution).rgb;", "", "    float sx = s.x / (s.x + s.y);", "    float sy = s.z / (s.z + s.w);", "", "    return mix(mix(s3, s2, sx), mix(s1, s0, sx), sy);", "}", "", "vec3 blackHole() {", "    vec3 color = decodeHDR(bicubic(Sky, texCoord));", "", "    vec3 bloom = decodeHDR(bicubic(Bloom0, texCoord)) * 1.00;", "    bloom += decodeHDR(bicubic(Bloom1, texCoord)) * 1.00;", "    bloom += decodeHDR(bicubic(Bloom2, texCoord)) * 1.00;", "    bloom += decodeHDR(bicubic(Bloom3, texCoord)) * 0.90;", "    bloom += decodeHDR(bicubic(Bloom4, texCoord)) * 0.80;", "    bloom += decodeHDR(bicubic(Bloom5, texCoord)) * 0.65;", "", "    color += bloom * BLOOM_STRENGTH;", "", "    color = pow(color, vec3(1.5));", "    color = color / (1.0 + color);", "    color = pow(color, vec3(1.0 / 1.5));", "    color = color * color * (3.0 - 2.0 * color);", "    color = pow(color, vec3(1.3, 1.20, 1.0));", "    color = clamp(color * 1.01, 0.0, 1.0);", "    color = pow(color, vec3(0.7 / 2.2));", "", "    vec2 ndc = texCoord * 2.0 - 1.0;", "    vec4 pFar = invViewProj * vec4(ndc, 1.0, 1.0);", "    vec4 pNear = invViewProj * vec4(ndc, -1.0, 1.0);", "    vec3 rd = normalize(pFar.xyz / pFar.w - pNear.xyz / pNear.w);", "", "    vec3 bhDir = normalize(vec3(0.34, 0.62, 0.71));", "    float cosT = clamp(dot(rd, bhDir), -1.0, 1.0);", "    float theta = acos(cosT);", "    vec3 toHole = bhDir - cosT * rd;", "    float tl = length(toHole);", "    vec3 rdL = rd;", "    if (tl > 1e-4) {", "        rdL = normalize(rd + toHole / tl * (0.010 / max(theta, 0.05)));", "    }", "", "    float lum = dot(color, vec3(0.299, 0.587, 0.114));", "    vec3 stars = bhStars(rdL, misc.x) * smoothstep(0.10, 0.17, theta);", "    color += stars * misc.w * clamp(1.0 - lum * 2.4, 0.0, 1.0);", "", "    return color;", "}", "", "void main() {", "    float depth = texture(DepthTex, texCoord).r;", "    vec3 scene = texture(Scene, texCoord).rgb;", "    if (depth < 0.9999) {", "        fragColor = vec4(scene, 1.0);", "        return;", "    }", "", "    vec2 dt = 1.0 / vec2(textureSize(DepthTex, 0));", "    float dmin = min(", "        min(texture(DepthTex, texCoord + vec2(dt.x, 0.0)).r, texture(DepthTex, texCoord - vec2(dt.x, 0.0)).r),", "        min(texture(DepthTex, texCoord + vec2(0.0, dt.y)).r, texture(DepthTex, texCoord - vec2(0.0, dt.y)).r)", "    );", "    float edgeDim = 1.0 - 0.6 * step(dmin, 0.9998);", "", "    vec3 result;", "    if (skyColor2.w > 0.5 && skyColor2.w < 1.5) {", "        result = blackHole();", "    } else {", "        vec3 sky = texture(Sky, texCoord).rgb;", "        vec3 bloom = texture(Bloom0, texCoord).rgb * 1.00", "                   + texture(Bloom1, texCoord).rgb * 0.90", "                   + texture(Bloom2, texCoord).rgb * 0.75;", "        result = sky + bloom * 0.34;", "    }", "", "    fragColor = vec4(result * edgeDim, 1.0);", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }

    private final String s8() {
        Object[] objectArray = new String[]{"//!vertex", "#version 150", "", "out vec2 texCoord;", "", "void main() {", "    vec2 positions[6] = vec2[](", "        vec2(-1.0, -1.0),", "        vec2(1.0, -1.0),", "        vec2(1.0, 1.0),", "        vec2(-1.0, -1.0),", "        vec2(1.0, 1.0),", "        vec2(-1.0, 1.0)", "    );", "", "    vec2 uvs[6] = vec2[](", "        vec2(0.0, 0.0),", "        vec2(1.0, 0.0),", "        vec2(1.0, 1.0),", "        vec2(0.0, 0.0),", "        vec2(1.0, 1.0),", "        vec2(0.0, 1.0)", "    );", "", "    gl_Position = vec4(positions[gl_VertexID], 0.0, 1.0);", "    texCoord = uvs[gl_VertexID];", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }

    private final String s9() {
        Object[] objectArray = new String[]{"#version 150", "", "out vec2 texCoord;", "", "void main() {", "    vec2 positions[6] = vec2[](", "        vec2(-1.0, -1.0),", "        vec2(1.0, -1.0),", "        vec2(1.0, 1.0),", "        vec2(-1.0, -1.0),", "        vec2(1.0, 1.0),", "        vec2(-1.0, 1.0)", "    );", "", "    vec2 uvs[6] = vec2[](", "        vec2(0.0, 0.0),", "        vec2(1.0, 0.0),", "        vec2(1.0, 1.0),", "        vec2(0.0, 0.0),", "        vec2(1.0, 1.0),", "        vec2(0.0, 1.0)", "    );", "", "    gl_Position = vec4(positions[gl_VertexID], 0.0, 1.0);", "    texCoord = uvs[gl_VertexID];", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }

    private final String s10() {
        Object[] objectArray = new String[]{"//!fragment", "#version 150", "", "#moj_import <kimiko:theme_wave.glsl>", "", "layout(std140) uniform SkyParams {", "    mat4 invViewProj;", "    vec4 misc;", "    vec4 skyColor;", "    vec4 skyColor2;", "    vec4 taa;", "    mat4 prevViewProj;", "    vec4 skyExtra;", "};", "", "in vec2 texCoord;", "out vec4 fragColor;", "", "const mat2 ROT = mat2(0.80, 0.60, -0.60, 0.80);", "const float TAU = 6.28318530718;", "const float PI = 3.14159265359;", "const int METEORS = 10;", "", "vec3 hsv2rgb(vec3 c) {", "    vec3 rgb = clamp(abs(mod(c.x * 6.0 + vec3(0.0, 4.0, 2.0), 6.0) - 3.0) - 1.0, 0.0, 1.0);", "    return c.z * mix(vec3(1.0), rgb, c.y);", "}", "", "vec3 nmzHash33(vec3 q) {", "    uvec3 p = uvec3(ivec3(q));", "    p = p * uvec3(374761393U, 1103515245U, 668265263U) + p.zxy + p.yzx;", "    p = p.yzx * (p.zxy ^ (p >> 3U));", "    return vec3(p ^ (p >> 16U)) * (1.0 / vec3(0xffffffffU));", "}", "", "float hash21(vec2 p) {", "    uvec2 q = uvec2(ivec2(p)) * uvec2(1597334673U, 3812015801U);", "    uint n = (q.x ^ q.y) * 1597334673U;", "    return float(n) * (1.0 / 4294967295.0);", "}", "", "float vnoise(vec2 p) {", "    vec2 i = floor(p);", "    vec2 f = fract(p);", "    f = f * f * (3.0 - 2.0 * f);", "    float a = hash21(i);", "    float b = hash21(i + vec2(1.0, 0.0));", "    float c = hash21(i + vec2(0.0, 1.0));", "    float d = hash21(i + vec2(1.0, 1.0));", "    return mix(mix(a, b, f.x), mix(c, d, f.x), f.y);", "}", "", "float fbm(vec2 p) {", "    float amp = 0.5;", "    float sum = 0.0;", "    for (int i = 0; i < 4; i++) {", "        sum += amp * vnoise(p);", "        p = ROT * p * 2.03;", "        amp *= 0.5;", "    }", "    return sum;", "}", "", "vec3 skyTint(float axis, float t, float time) {", "    float mode = skyColor.w;", "    if (mode > 1.5) {", "        float hue = fract(axis * 0.159155 + time * 0.02 + t * 0.15);", "        return hsv2rgb(vec3(hue, 0.85, 1.0));", "    }", "    vec2 fragXY = kimikoFragXYFromUV(texCoord);", "    vec3 c1 = skyExtra.x > 0.5 ? kimikoClientPrimary(fragXY) : skyColor.rgb;", "    if (mode > 0.5) {", "        vec3 c2 = skyExtra.x > 0.5 ? kimikoClientSecondary(fragXY) : skyColor2.rgb;", "        float k = clamp(0.5 + 0.5 * sin(axis * 2.0 + time * 0.15) + (t - 0.5) * 0.7, 0.0, 1.0);", "        return mix(c1, c2, k);", "    }", "    return c1;", "}", "", "vec3 starField(vec3 dir, float time) {", "    vec3 c = vec3(0.0);", "    vec3 p = dir * 42.0;", "    float dens = 0.06;", "    float amp = 1.0;", "    for (int i = 0; i < 4; i++) {", "        vec3 id = floor(p);", "        vec3 q = fract(p) - 0.5;", "        vec3 rn = nmzHash33(id);", "        float core = smoothstep(0.30, 0.0, length(q));", "        float hit = step(rn.x, dens);", "        float tw = 0.75 + 0.25 * sin(time * (0.8 + rn.z * 1.8) + rn.y * 47.0);", "        vec3 tint = mix(vec3(1.0, 0.74, 0.50), vec3(0.72, 0.86, 1.0), rn.y);", "        c += hit * core * core * tint * (0.35 + 0.65 * rn.z) * tw * amp;", "        p = p * 1.63 + 17.0;", "        dens *= 0.62;", "        amp *= 0.78;", "    }", "    return c;", "}", "", "vec3 meteors(vec3 rd, float time, vec3 themeTint) {", "    vec3 sum = vec3(0.0);", "    for (int i = 0; i < METEORS; i++) {", "        float fi = float(i);", "        vec3 gh = nmzHash33(vec3(fi * 13.0 + 7.0, 51.0, 3.0));", "", "        float period = mix(2.2, 6.5, gh.x);", "        float cycle = time / period + gh.y * 19.0;", "        float fly = fract(cycle);", "        if (fly > 0.16) {", "            continue;", "        }", "        float t01 = fly / 0.16;", "", "        float shower = floor(cycle);", "        vec3 rnd = nmzHash33(vec3(fi * 29.0 + 5.0, shower, 11.0));", "        vec3 rnd2 = nmzHash33(vec3(fi * 31.0 + 3.0, shower, 23.0));", "", "        float az = rnd.x * TAU;", "        float el = mix(0.12, 0.95, rnd.y);", "        vec3 headStart = vec3(cos(el) * cos(az), sin(el), cos(el) * sin(az));", "", "        vec3 east = normalize(cross(vec3(0.0, 1.0, 0.0), headStart));", "        vec3 north = cross(headStart, east);", "        vec3 dirT = normalize(-north + east * (rnd.z - 0.5) * 1.6);", "        vec3 n = normalize(cross(headStart, dirT));", "        vec3 ea = headStart;", "        vec3 eb = cross(n, ea);", "", "        float off = dot(rd, n);", "        if (abs(off) > 0.02) {", "            continue;", "        }", "", "        float along = atan(dot(rd, eb), dot(rd, ea));", "", "        float arc = mix(0.35, 0.80, rnd2.x);", "        float head = t01 * arc;", "        float tailLen = mix(0.10, 0.22, rnd2.y) * (0.35 + 0.65 * t01);", "", "        float s = along - head;", "        if (s > 0.02 || s < -(tailLen + 0.05)) {", "            continue;", "        }", "", "        float env = pow(sin(t01 * PI), 0.65);", "", "        float w = mix(0.0016, 0.0026, rnd2.z);", "        float line = exp(-off * off / (w * w));", "", "        float tailT = clamp(-s / tailLen, 0.0, 1.0);", "        float profile = (1.0 - tailT) * (1.0 - tailT);", "        float trail = line * profile * step(s, 0.0);", "", "        float dHead = length(vec2(s, off));", "        float headCore = exp(-dHead * dHead / (0.0035 * 0.0035)) * 2.5;", "        float headGlow = exp(-dHead * dHead / (0.012 * 0.012)) * 0.5;", "", "        float sparkle = 0.85 + 0.15 * sin(along * 240.0 + shower * 12.0);", "", "        vec3 hot = vec3(1.0, 0.97, 0.90);", "        vec3 warm = mix(vec3(1.0, 0.55, 0.22), themeTint, 0.45);", "        vec3 col = mix(hot, warm, tailT);", "", "        sum += (trail * 1.35 * sparkle + headCore + headGlow) * col * env;", "    }", "    return sum;", "}", "", "void main() {", "    float time = misc.x;", "    float brightness = misc.w;", "", "    vec2 ndc = texCoord * 2.0 - 1.0;", "    vec4 pFar = invViewProj * vec4(ndc, 1.0, 1.0);", "    vec4 pNear = invViewProj * vec4(ndc, -1.0, 1.0);", "    vec3 rd = normalize(pFar.xyz / pFar.w - pNear.xyz / pNear.w);", "", "    float axis = atan(rd.z, rd.x);", "    vec3 tint = skyTint(axis, 0.5, time);", "", "    vec3 col = vec3(0.014, 0.021, 0.050) * (0.55 + 0.45 * smoothstep(-1.0, 1.0, rd.y));", "    col += starField(rd, time) * 1.05;", "", "    float band = exp(-pow((abs(rd.y) - 0.18) * 2.4, 2.0));", "    float milky = fbm(rd.xz * 3.2 + rd.y * 1.7 + time * 0.004);", "    col += tint * band * pow(clamp(milky, 0.0, 1.0), 2.4) * 0.10;", "", "    col += meteors(rd, time, tint);", "", "    fragColor = vec4(tanh(min(col * brightness, vec3(16.0))), 1.0);", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }

    private final String s11() {
        Object[] objectArray = new String[]{"#version 150", "", "#moj_import <kimiko:theme_wave.glsl>", "", "layout(std140) uniform SkyParams {", "    mat4 invViewProj;", "    vec4 misc;", "    vec4 skyColor;", "    vec4 skyColor2;", "    vec4 taa;", "    mat4 prevViewProj;", "    vec4 skyExtra;", "};", "", "in vec2 texCoord;", "out vec4 fragColor;", "", "const mat2 ROT = mat2(0.80, 0.60, -0.60, 0.80);", "const float TAU = 6.28318530718;", "const float PI = 3.14159265359;", "const int METEORS = 10;", "", "vec3 hsv2rgb(vec3 c) {", "    vec3 rgb = clamp(abs(mod(c.x * 6.0 + vec3(0.0, 4.0, 2.0), 6.0) - 3.0) - 1.0, 0.0, 1.0);", "    return c.z * mix(vec3(1.0), rgb, c.y);", "}", "", "vec3 nmzHash33(vec3 q) {", "    uvec3 p = uvec3(ivec3(q));", "    p = p * uvec3(374761393U, 1103515245U, 668265263U) + p.zxy + p.yzx;", "    p = p.yzx * (p.zxy ^ (p >> 3U));", "    return vec3(p ^ (p >> 16U)) * (1.0 / vec3(0xffffffffU));", "}", "", "float hash21(vec2 p) {", "    uvec2 q = uvec2(ivec2(p)) * uvec2(1597334673U, 3812015801U);", "    uint n = (q.x ^ q.y) * 1597334673U;", "    return float(n) * (1.0 / 4294967295.0);", "}", "", "float vnoise(vec2 p) {", "    vec2 i = floor(p);", "    vec2 f = fract(p);", "    f = f * f * (3.0 - 2.0 * f);", "    float a = hash21(i);", "    float b = hash21(i + vec2(1.0, 0.0));", "    float c = hash21(i + vec2(0.0, 1.0));", "    float d = hash21(i + vec2(1.0, 1.0));", "    return mix(mix(a, b, f.x), mix(c, d, f.x), f.y);", "}", "", "float fbm(vec2 p) {", "    float amp = 0.5;", "    float sum = 0.0;", "    for (int i = 0; i < 4; i++) {", "        sum += amp * vnoise(p);", "        p = ROT * p * 2.03;", "        amp *= 0.5;", "    }", "    return sum;", "}", "", "vec3 skyTint(float axis, float t, float time) {", "    float mode = skyColor.w;", "    if (mode > 1.5) {", "        float hue = fract(axis * 0.159155 + time * 0.02 + t * 0.15);", "        return hsv2rgb(vec3(hue, 0.85, 1.0));", "    }", "    vec2 fragXY = kimikoFragXYFromUV(texCoord);", "    vec3 c1 = skyExtra.x > 0.5 ? kimikoClientPrimary(fragXY) : skyColor.rgb;", "    if (mode > 0.5) {", "        vec3 c2 = skyExtra.x > 0.5 ? kimikoClientSecondary(fragXY) : skyColor2.rgb;", "        float k = clamp(0.5 + 0.5 * sin(axis * 2.0 + time * 0.15) + (t - 0.5) * 0.7, 0.0, 1.0);", "        return mix(c1, c2, k);", "    }", "    return c1;", "}", "", "vec3 starField(vec3 dir, float time) {", "    vec3 c = vec3(0.0);", "    vec3 p = dir * 42.0;", "    float dens = 0.06;", "    float amp = 1.0;", "    for (int i = 0; i < 4; i++) {", "        vec3 id = floor(p);", "        vec3 q = fract(p) - 0.5;", "        vec3 rn = nmzHash33(id);", "        float core = smoothstep(0.30, 0.0, length(q));", "        float hit = step(rn.x, dens);", "        float tw = 0.75 + 0.25 * sin(time * (0.8 + rn.z * 1.8) + rn.y * 47.0);", "        vec3 tint = mix(vec3(1.0, 0.74, 0.50), vec3(0.72, 0.86, 1.0), rn.y);", "        c += hit * core * core * tint * (0.35 + 0.65 * rn.z) * tw * amp;", "        p = p * 1.63 + 17.0;", "        dens *= 0.62;", "        amp *= 0.78;", "    }", "    return c;", "}", "", "vec3 meteors(vec3 rd, float time, vec3 themeTint) {", "    vec3 sum = vec3(0.0);", "    for (int i = 0; i < METEORS; i++) {", "        float fi = float(i);", "        vec3 gh = nmzHash33(vec3(fi * 13.0 + 7.0, 51.0, 3.0));", "", "        float period = mix(2.2, 6.5, gh.x);", "        float cycle = time / period + gh.y * 19.0;", "        float fly = fract(cycle);", "        if (fly > 0.16) {", "            continue;", "        }", "        float t01 = fly / 0.16;", "", "        float shower = floor(cycle);", "        vec3 rnd = nmzHash33(vec3(fi * 29.0 + 5.0, shower, 11.0));", "        vec3 rnd2 = nmzHash33(vec3(fi * 31.0 + 3.0, shower, 23.0));", "", "        float az = rnd.x * TAU;", "        float el = mix(0.12, 0.95, rnd.y);", "        vec3 headStart = vec3(cos(el) * cos(az), sin(el), cos(el) * sin(az));", "", "        vec3 east = normalize(cross(vec3(0.0, 1.0, 0.0), headStart));", "        vec3 north = cross(headStart, east);", "        vec3 dirT = normalize(-north + east * (rnd.z - 0.5) * 1.6);", "        vec3 n = normalize(cross(headStart, dirT));", "        vec3 ea = headStart;", "        vec3 eb = cross(n, ea);", "", "        float off = dot(rd, n);", "        if (abs(off) > 0.02) {", "            continue;", "        }", "", "        float along = atan(dot(rd, eb), dot(rd, ea));", "", "        float arc = mix(0.35, 0.80, rnd2.x);", "        float head = t01 * arc;", "        float tailLen = mix(0.10, 0.22, rnd2.y) * (0.35 + 0.65 * t01);", "", "        float s = along - head;", "        if (s > 0.02 || s < -(tailLen + 0.05)) {", "            continue;", "        }", "", "        float env = pow(sin(t01 * PI), 0.65);", "", "        float w = mix(0.0016, 0.0026, rnd2.z);", "        float line = exp(-off * off / (w * w));", "", "        float tailT = clamp(-s / tailLen, 0.0, 1.0);", "        float profile = (1.0 - tailT) * (1.0 - tailT);", "        float trail = line * profile * step(s, 0.0);", "", "        float dHead = length(vec2(s, off));", "        float headCore = exp(-dHead * dHead / (0.0035 * 0.0035)) * 2.5;", "        float headGlow = exp(-dHead * dHead / (0.012 * 0.012)) * 0.5;", "", "        float sparkle = 0.85 + 0.15 * sin(along * 240.0 + shower * 12.0);", "", "        vec3 hot = vec3(1.0, 0.97, 0.90);", "        vec3 warm = mix(vec3(1.0, 0.55, 0.22), themeTint, 0.45);", "        vec3 col = mix(hot, warm, tailT);", "", "        sum += (trail * 1.35 * sparkle + headCore + headGlow) * col * env;", "    }", "    return sum;", "}", "", "void main() {", "    float time = misc.x;", "    float brightness = misc.w;", "", "    vec2 ndc = texCoord * 2.0 - 1.0;", "    vec4 pFar = invViewProj * vec4(ndc, 1.0, 1.0);", "    vec4 pNear = invViewProj * vec4(ndc, -1.0, 1.0);", "    vec3 rd = normalize(pFar.xyz / pFar.w - pNear.xyz / pNear.w);", "", "    float axis = atan(rd.z, rd.x);", "    vec3 tint = skyTint(axis, 0.5, time);", "", "    vec3 col = vec3(0.014, 0.021, 0.050) * (0.55 + 0.45 * smoothstep(-1.0, 1.0, rd.y));", "    col += starField(rd, time) * 1.05;", "", "    float band = exp(-pow((abs(rd.y) - 0.18) * 2.4, 2.0));", "    float milky = fbm(rd.xz * 3.2 + rd.y * 1.7 + time * 0.004);", "    col += tint * band * pow(clamp(milky, 0.0, 1.0), 2.4) * 0.10;", "", "    col += meteors(rd, time, tint);", "", "    fragColor = vec4(tanh(min(col * brightness, vec3(16.0))), 1.0);", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }
}

