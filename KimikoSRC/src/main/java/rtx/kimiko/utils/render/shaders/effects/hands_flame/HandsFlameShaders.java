/*
 * Decompiled with CFR 0.152.
 * 
 * Could not load the following classes:
 *  kotlin.Metadata
 *  kotlin.collections.CollectionsKt
 *  kotlin.jvm.JvmStatic
 *  kotlin.jvm.internal.Intrinsics
 *  org.jetbrains.annotations.NotNull
 */
package rtx.kimiko.utils.render.shaders.effects.hands_flame;

import java.util.Map;
import kotlin.Metadata;
import kotlin.collections.CollectionsKt;
import kotlin.jvm.JvmStatic;
import kotlin.jvm.internal.Intrinsics;
import org.jetbrains.annotations.NotNull;

@Metadata(mv={2, 4, 0}, k=1, xi=48, d1={"\u0000\"\n\u0002\u0018\u0002\n\u0002\u0010\u0000\n\u0002\b\u0002\n\u0002\u0010%\n\u0002\u0010\u000e\n\u0000\n\u0002\u0010\u0002\n\u0002\u0018\u0002\n\u0002\b\u000f\b\u00c6\u0002\u0018\u00002\u00020\u0001B\t\b\u0002\u00a2\u0006\u0004\b\u0002\u0010\u0003J'\u0010\t\u001a\u00020\u00072\u0012\u0010\u0006\u001a\u000e\u0012\u0004\u0012\u00020\u0005\u0012\u0004\u0012\u00020\u00050\u0004H\u0007b\u0002\b\b\u00a2\u0006\u0004\b\t\u0010\nJ\u001b\u0010\f\u001a\u00020\u00052\u0006\u0010\u000b\u001a\u00020\u0005H\u0007b\u0002\b\b\u00a2\u0006\u0004\b\f\u0010\rJ\u000f\u0010\u000e\u001a\u00020\u0005H\u0002\u00a2\u0006\u0004\b\u000e\u0010\u000fJ\u000f\u0010\u0010\u001a\u00020\u0005H\u0002\u00a2\u0006\u0004\b\u0010\u0010\u000fJ\u000f\u0010\u0011\u001a\u00020\u0005H\u0002\u00a2\u0006\u0004\b\u0011\u0010\u000fJ\u000f\u0010\u0012\u001a\u00020\u0005H\u0002\u00a2\u0006\u0004\b\u0012\u0010\u000fJ\u000f\u0010\u0013\u001a\u00020\u0005H\u0002\u00a2\u0006\u0004\b\u0013\u0010\u000fJ\u000f\u0010\u0014\u001a\u00020\u0005H\u0002\u00a2\u0006\u0004\b\u0014\u0010\u000fJ\u000f\u0010\u0015\u001a\u00020\u0005H\u0002\u00a2\u0006\u0004\b\u0015\u0010\u000fJ\u000f\u0010\u0016\u001a\u00020\u0005H\u0002\u00a2\u0006\u0004\b\u0016\u0010\u000f\u00a8\u0006\u0017"}, d2={"Lrtx/kimiko/utils/render/shaders/effects/hands_flame/HandsFlameShaders;", "", "<init>", "()V", "", "", "sources", "", "Lkotlin/jvm/JvmStatic;", "register", "(Ljava/util/Map;)V", "key", "source", "(Ljava/lang/String;)Ljava/lang/String;", "s0", "()Ljava/lang/String;", "s1", "s2", "s3", "s4", "s5", "s6", "s7", "rtx.kimiko:kimiko"})
public final class HandsFlameShaders {
    @NotNull
    public static final HandsFlameShaders INSTANCE = new HandsFlameShaders();

    private HandsFlameShaders() {
    }

    @JvmStatic
    public static final void register(@NotNull Map<String, String> sources) {
        Intrinsics.checkNotNullParameter(sources, (String)"sources");
        sources.put("effects/hands_flame/composite.glsl", INSTANCE.s0());
        sources.put("effects/hands_flame/composite.fsh", INSTANCE.s1());
        sources.put("effects/hands_flame/fullscreen.glsl", INSTANCE.s2());
        sources.put("effects/hands_flame/fullscreen.vsh", INSTANCE.s3());
        sources.put("effects/hands_flame/restore.glsl", INSTANCE.s4());
        sources.put("effects/hands_flame/restore.fsh", INSTANCE.s5());
        sources.put("effects/hands_flame/trail.glsl", INSTANCE.s6());
        sources.put("effects/hands_flame/trail.fsh", INSTANCE.s7());
    }

    @JvmStatic
    @NotNull
    public static final String source(@NotNull String key) {
        Intrinsics.checkNotNullParameter((Object)key, (String)"key");
        return switch (key) {
            case "effects/hands_flame/composite.glsl" -> INSTANCE.s0();
            case "effects/hands_flame/composite.fsh" -> INSTANCE.s1();
            case "effects/hands_flame/fullscreen.glsl" -> INSTANCE.s2();
            case "effects/hands_flame/fullscreen.vsh" -> INSTANCE.s3();
            case "effects/hands_flame/restore.glsl" -> INSTANCE.s4();
            case "effects/hands_flame/restore.fsh" -> INSTANCE.s5();
            case "effects/hands_flame/trail.glsl" -> INSTANCE.s6();
            case "effects/hands_flame/trail.fsh" -> INSTANCE.s7();
            default -> throw new IllegalArgumentException(key);
        };
    }

    private final String s0() {
        Object[] objectArray = new String[]{"//!fragment", "#version 150", "", "in vec2 texCoord;", "out vec4 fragColor;", "", "uniform sampler2D SceneSampler;", "uniform sampler2D BeforeSampler;", "uniform sampler2D AfterSampler;", "uniform sampler2D TrailSampler;", "uniform sampler2D DepthSampler;", "uniform sampler2D NoHandDepthSampler;", "", "layout(std140) uniform HandsFlameData {", "    vec4 flameColor;", "    vec4 params0;", "    vec4 params1;", "    vec4 screen;", "    vec4 gradColor1;", "    vec4 gradColor2;", "    vec4 gradColor3;", "    vec4 gradColor4;", "    vec4 flameExtra;", "};", "", "float quickTrailAlpha(vec2 uv, vec2 blurPx) {", "    return texture(TrailSampler, uv).a;", "}", "", "vec4 blurredTrailColor(vec2 uv, vec2 blurPx) {", "    vec4 trail = texture(TrailSampler, uv) * 0.36;", "    trail += texture(TrailSampler, clamp(uv + vec2(blurPx.x, 0.0), vec2(0.0), vec2(1.0))) * 0.11;", "    trail += texture(TrailSampler, clamp(uv - vec2(blurPx.x, 0.0), vec2(0.0), vec2(1.0))) * 0.11;", "    trail += texture(TrailSampler, clamp(uv + vec2(0.0, blurPx.y), vec2(0.0), vec2(1.0))) * 0.11;", "    trail += texture(TrailSampler, clamp(uv - vec2(0.0, blurPx.y), vec2(0.0), vec2(1.0))) * 0.11;", "    trail += texture(TrailSampler, clamp(uv + blurPx, vec2(0.0), vec2(1.0))) * 0.05;", "    trail += texture(TrailSampler, clamp(uv - blurPx, vec2(0.0), vec2(1.0))) * 0.05;", "    trail += texture(TrailSampler, clamp(uv + vec2(blurPx.x, -blurPx.y), vec2(0.0), vec2(1.0))) * 0.05;", "    trail += texture(TrailSampler, clamp(uv + vec2(-blurPx.x, blurPx.y), vec2(0.0), vec2(1.0))) * 0.05;", "    return trail;", "}", "", "float handDepthMaskAt(vec2 uv, float irisDepthMode) {", "    float depth = texture(DepthSampler, uv).r;", "    if (irisDepthMode > 0.5) {", "        float noHandDepth = texture(NoHandDepthSampler, uv).r;", "        if (depth < 0.9999 && (noHandDepth >= 0.9999 || depth < noHandDepth - 0.00002)) {", "            return 1.0;", "        }", "    } else {", "        if (depth < 0.9999) {", "            return 1.0;", "        }", "    }", "    vec4 beforeColor = texture(BeforeSampler, uv);", "    vec4 afterColor = texture(AfterSampler, uv);", "    vec3 delta = abs(afterColor.rgb - beforeColor.rgb);", "    float peak = max(max(delta.r, delta.g), delta.b);", "    float luma = dot(delta, vec3(0.299, 0.587, 0.114));", "    float value = peak * 0.78 + luma * 0.88 + abs(afterColor.a - beforeColor.a);", "    return smoothstep(0.004, 0.060, value);", "}", "", "void main() {", "    vec2 px = screen.zw;", "    float brightness = params1.x;", "    float packedColorMode = params1.z;", "    float irisDepthMode = step(20.0, packedColorMode);", "", "    vec2 blurPx = px * 2.65;", "    vec2 widePx = px * 5.85;", "", "    float trailHint = quickTrailAlpha(texCoord, blurPx);", "    if (trailHint < 0.0010) {", "        fragColor = texture(SceneSampler, texCoord);", "        return;", "    }", "", "    vec4 scene = texture(SceneSampler, texCoord);", "    vec4 sceneBefore = texture(BeforeSampler, texCoord);", "    vec4 afterColor = texture(AfterSampler, texCoord);", "", "    float mask = 0.0;", "    if (irisDepthMode > 0.5) {", "        float depthMask = handDepthMaskAt(texCoord, irisDepthMode);", "        if (depthMask > 0.5) {", "            mask = 1.0;", "        } else {", "            float depth = texture(DepthSampler, texCoord).r;", "            float noHandDepth = texture(NoHandDepthSampler, texCoord).r;", "            if (depth >= 0.9999 || noHandDepth < 0.0001 || depth >= noHandDepth - 0.00002) {", "                mask = 0.0;", "            } else {", "                vec3 delta = abs(afterColor.rgb - sceneBefore.rgb);", "                float peak = max(max(delta.r, delta.g), delta.b);", "                float luma = dot(delta, vec3(0.299, 0.587, 0.114));", "                float maskValue = peak * 0.78 + luma * 0.88 + abs(afterColor.a - sceneBefore.a);", "                mask = smoothstep(0.004, 0.060, maskValue);", "            }", "        }", "    } else {", "        float depth = texture(DepthSampler, texCoord).r;", "        if (depth < 0.9999) {", "            mask = 1.0;", "        } else {", "            vec3 delta = abs(afterColor.rgb - sceneBefore.rgb);", "            float peak = max(max(delta.r, delta.g), delta.b);", "            float luma = dot(delta, vec3(0.299, 0.587, 0.114));", "            float maskValue = peak * 0.78 + luma * 0.88 + abs(afterColor.a - sceneBefore.a);", "            mask = smoothstep(0.004, 0.060, maskValue);", "        }", "    }", "    float itemCover = smoothstep(0.04, 0.40, mask);", "    vec4 trail = blurredTrailColor(texCoord, blurPx);", "", "    float behindItem = 1.0 - itemCover * 0.98;", "", "    float field = smoothstep(0.006, 0.40, trail.a) * behindItem;", "", "    float sceneLuma = dot(scene.rgb, vec3(0.299, 0.587, 0.114));", "    float darkCover = 1.0 - smoothstep(0.16, 0.72, sceneLuma);", "    float brightnessCurve = 0.42 + (1.0 - exp(-clamp(brightness, 0.0, 2.0) * 0.8)) * 0.64;", "", "    float glowIntensity = pow(trail.a, 0.48) * field;", "    float veil = clamp(glowIntensity * (0.20 + brightnessCurve * 0.22) * (1.0 + darkCover * 0.38), 0.0, 0.66);", "", "    veil += itemCover * smoothstep(0.05, 0.32, trail.a) * 0.10;", "", "    vec3 flameFill = min(trail.rgb * (0.78 + brightnessCurve * 0.42) + vec3(0.025), vec3(1.0));", "", "    float blurAmount = field * smoothstep(0.10, 0.52, trail.a);", "    vec3 sceneBase;", "    if (blurAmount > 0.01) {", "        vec2 bgPx = px * (4.0 + blurAmount * 4.5);", "        vec3 blurred = scene.rgb * 0.28;", "        blurred += texture(SceneSampler, clamp(texCoord + vec2(bgPx.x, 0.0), vec2(0.0), vec2(1.0))).rgb * 0.12;", "        blurred += texture(SceneSampler, clamp(texCoord - vec2(bgPx.x, 0.0), vec2(0.0), vec2(1.0))).rgb * 0.12;", "        blurred += texture(SceneSampler, clamp(texCoord + vec2(0.0, bgPx.y), vec2(0.0), vec2(1.0))).rgb * 0.12;", "        blurred += texture(SceneSampler, clamp(texCoord - vec2(0.0, bgPx.y), vec2(0.0), vec2(1.0))).rgb * 0.12;", "        blurred += texture(SceneSampler, clamp(texCoord + bgPx, vec2(0.0), vec2(1.0))).rgb * 0.07;", "        blurred += texture(SceneSampler, clamp(texCoord - bgPx, vec2(0.0), vec2(1.0))).rgb * 0.07;", "        blurred += texture(SceneSampler, clamp(texCoord + vec2(bgPx.x, -bgPx.y), vec2(0.0), vec2(1.0))).rgb * 0.05;", "        blurred += texture(SceneSampler, clamp(texCoord + vec2(-bgPx.x, bgPx.y), vec2(0.0), vec2(1.0))).rgb * 0.05;", "", "        sceneBase = mix(scene.rgb, blurred, blurAmount);", "    } else {", "        sceneBase = scene.rgb;", "    }", "", "    sceneBase *= (1.0 - veil * darkCover * 0.18);", "", "    vec3 covered = mix(sceneBase, flameFill, veil);", "", "    float glowPulse = 0.94 + sin((texCoord.y + texCoord.x * 0.35) * 16.0 + params1.y * 3.6) * 0.035;", "    vec3 glow = trail.rgb * pow(trail.a, 0.48) * field * glowPulse;", "", "    float coreIntensity = pow(trail.a * field, 2.15);", "    vec3 core = min(trail.rgb * 1.22 + vec3(0.04), vec3(1.0)) * coreIntensity;", "", "    vec3 color = covered", "        + glow * (0.78 + brightnessCurve * 0.54)", "        + core * (0.10 + brightnessCurve * 0.10);", "", "    fragColor = vec4(clamp(color, 0.0, 1.0), 1.0);", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }

    private final String s1() {
        Object[] objectArray = new String[]{"#version 150", "", "in vec2 texCoord;", "out vec4 fragColor;", "", "uniform sampler2D SceneSampler;", "uniform sampler2D BeforeSampler;", "uniform sampler2D AfterSampler;", "uniform sampler2D TrailSampler;", "uniform sampler2D DepthSampler;", "uniform sampler2D NoHandDepthSampler;", "", "layout(std140) uniform HandsFlameData {", "    vec4 flameColor;", "    vec4 params0;", "    vec4 params1;", "    vec4 screen;", "    vec4 gradColor1;", "    vec4 gradColor2;", "    vec4 gradColor3;", "    vec4 gradColor4;", "    vec4 flameExtra;", "};", "", "float quickTrailAlpha(vec2 uv, vec2 blurPx) {", "    return texture(TrailSampler, uv).a;", "}", "", "vec4 blurredTrailColor(vec2 uv, vec2 blurPx) {", "    vec4 trail = texture(TrailSampler, uv) * 0.36;", "    trail += texture(TrailSampler, clamp(uv + vec2(blurPx.x, 0.0), vec2(0.0), vec2(1.0))) * 0.11;", "    trail += texture(TrailSampler, clamp(uv - vec2(blurPx.x, 0.0), vec2(0.0), vec2(1.0))) * 0.11;", "    trail += texture(TrailSampler, clamp(uv + vec2(0.0, blurPx.y), vec2(0.0), vec2(1.0))) * 0.11;", "    trail += texture(TrailSampler, clamp(uv - vec2(0.0, blurPx.y), vec2(0.0), vec2(1.0))) * 0.11;", "    trail += texture(TrailSampler, clamp(uv + blurPx, vec2(0.0), vec2(1.0))) * 0.05;", "    trail += texture(TrailSampler, clamp(uv - blurPx, vec2(0.0), vec2(1.0))) * 0.05;", "    trail += texture(TrailSampler, clamp(uv + vec2(blurPx.x, -blurPx.y), vec2(0.0), vec2(1.0))) * 0.05;", "    trail += texture(TrailSampler, clamp(uv + vec2(-blurPx.x, blurPx.y), vec2(0.0), vec2(1.0))) * 0.05;", "    return trail;", "}", "", "float handDepthMaskAt(vec2 uv, float irisDepthMode) {", "    float depth = texture(DepthSampler, uv).r;", "    if (irisDepthMode > 0.5) {", "        float noHandDepth = texture(NoHandDepthSampler, uv).r;", "        if (depth < 0.9999 && (noHandDepth >= 0.9999 || depth < noHandDepth - 0.00002)) {", "            return 1.0;", "        }", "    } else {", "        if (depth < 0.9999) {", "            return 1.0;", "        }", "    }", "    vec4 beforeColor = texture(BeforeSampler, uv);", "    vec4 afterColor = texture(AfterSampler, uv);", "    vec3 delta = abs(afterColor.rgb - beforeColor.rgb);", "    float peak = max(max(delta.r, delta.g), delta.b);", "    float luma = dot(delta, vec3(0.299, 0.587, 0.114));", "    float value = peak * 0.78 + luma * 0.88 + abs(afterColor.a - beforeColor.a);", "    return smoothstep(0.004, 0.060, value);", "}", "", "void main() {", "    vec2 px = screen.zw;", "    float brightness = params1.x;", "    float packedColorMode = params1.z;", "    float irisDepthMode = step(20.0, packedColorMode);", "", "    vec2 blurPx = px * 2.65;", "    vec2 widePx = px * 5.85;", "", "    float trailHint = quickTrailAlpha(texCoord, blurPx);", "    if (trailHint < 0.0010) {", "        fragColor = texture(SceneSampler, texCoord);", "        return;", "    }", "", "    vec4 scene = texture(SceneSampler, texCoord);", "    vec4 sceneBefore = texture(BeforeSampler, texCoord);", "    vec4 afterColor = texture(AfterSampler, texCoord);", "", "    float mask = 0.0;", "    if (irisDepthMode > 0.5) {", "        float depthMask = handDepthMaskAt(texCoord, irisDepthMode);", "        if (depthMask > 0.5) {", "            mask = 1.0;", "        } else {", "            float depth = texture(DepthSampler, texCoord).r;", "            float noHandDepth = texture(NoHandDepthSampler, texCoord).r;", "            if (depth >= 0.9999 || noHandDepth < 0.0001 || depth >= noHandDepth - 0.00002) {", "                mask = 0.0;", "            } else {", "                vec3 delta = abs(afterColor.rgb - sceneBefore.rgb);", "                float peak = max(max(delta.r, delta.g), delta.b);", "                float luma = dot(delta, vec3(0.299, 0.587, 0.114));", "                float maskValue = peak * 0.78 + luma * 0.88 + abs(afterColor.a - sceneBefore.a);", "                mask = smoothstep(0.004, 0.060, maskValue);", "            }", "        }", "    } else {", "        float depth = texture(DepthSampler, texCoord).r;", "        if (depth < 0.9999) {", "            mask = 1.0;", "        } else {", "            vec3 delta = abs(afterColor.rgb - sceneBefore.rgb);", "            float peak = max(max(delta.r, delta.g), delta.b);", "            float luma = dot(delta, vec3(0.299, 0.587, 0.114));", "            float maskValue = peak * 0.78 + luma * 0.88 + abs(afterColor.a - sceneBefore.a);", "            mask = smoothstep(0.004, 0.060, maskValue);", "        }", "    }", "    float itemCover = smoothstep(0.04, 0.40, mask);", "    vec4 trail = blurredTrailColor(texCoord, blurPx);", "", "    float behindItem = 1.0 - itemCover * 0.98;", "", "    float field = smoothstep(0.006, 0.40, trail.a) * behindItem;", "", "    float sceneLuma = dot(scene.rgb, vec3(0.299, 0.587, 0.114));", "    float darkCover = 1.0 - smoothstep(0.16, 0.72, sceneLuma);", "    float brightnessCurve = 0.42 + (1.0 - exp(-clamp(brightness, 0.0, 2.0) * 0.8)) * 0.64;", "", "    float glowIntensity = pow(trail.a, 0.48) * field;", "    float veil = clamp(glowIntensity * (0.20 + brightnessCurve * 0.22) * (1.0 + darkCover * 0.38), 0.0, 0.66);", "", "    veil += itemCover * smoothstep(0.05, 0.32, trail.a) * 0.10;", "", "    vec3 flameFill = min(trail.rgb * (0.78 + brightnessCurve * 0.42) + vec3(0.025), vec3(1.0));", "", "    float blurAmount = field * smoothstep(0.10, 0.52, trail.a);", "    vec3 sceneBase;", "    if (blurAmount > 0.01) {", "        vec2 bgPx = px * (4.0 + blurAmount * 4.5);", "        vec3 blurred = scene.rgb * 0.28;", "        blurred += texture(SceneSampler, clamp(texCoord + vec2(bgPx.x, 0.0), vec2(0.0), vec2(1.0))).rgb * 0.12;", "        blurred += texture(SceneSampler, clamp(texCoord - vec2(bgPx.x, 0.0), vec2(0.0), vec2(1.0))).rgb * 0.12;", "        blurred += texture(SceneSampler, clamp(texCoord + vec2(0.0, bgPx.y), vec2(0.0), vec2(1.0))).rgb * 0.12;", "        blurred += texture(SceneSampler, clamp(texCoord - vec2(0.0, bgPx.y), vec2(0.0), vec2(1.0))).rgb * 0.12;", "        blurred += texture(SceneSampler, clamp(texCoord + bgPx, vec2(0.0), vec2(1.0))).rgb * 0.07;", "        blurred += texture(SceneSampler, clamp(texCoord - bgPx, vec2(0.0), vec2(1.0))).rgb * 0.07;", "        blurred += texture(SceneSampler, clamp(texCoord + vec2(bgPx.x, -bgPx.y), vec2(0.0), vec2(1.0))).rgb * 0.05;", "        blurred += texture(SceneSampler, clamp(texCoord + vec2(-bgPx.x, bgPx.y), vec2(0.0), vec2(1.0))).rgb * 0.05;", "", "        sceneBase = mix(scene.rgb, blurred, blurAmount);", "    } else {", "        sceneBase = scene.rgb;", "    }", "", "    sceneBase *= (1.0 - veil * darkCover * 0.18);", "", "    vec3 covered = mix(sceneBase, flameFill, veil);", "", "    float glowPulse = 0.94 + sin((texCoord.y + texCoord.x * 0.35) * 16.0 + params1.y * 3.6) * 0.035;", "    vec3 glow = trail.rgb * pow(trail.a, 0.48) * field * glowPulse;", "", "    float coreIntensity = pow(trail.a * field, 2.15);", "    vec3 core = min(trail.rgb * 1.22 + vec3(0.04), vec3(1.0)) * coreIntensity;", "", "    vec3 color = covered", "        + glow * (0.78 + brightnessCurve * 0.54)", "        + core * (0.10 + brightnessCurve * 0.10);", "", "    fragColor = vec4(clamp(color, 0.0, 1.0), 1.0);", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }

    private final String s2() {
        Object[] objectArray = new String[]{"//!vertex", "#version 150", "", "out vec2 texCoord;", "", "void main() {", "    vec2 positions[6] = vec2[](", "        vec2(-1.0, -1.0),", "        vec2(1.0, -1.0),", "        vec2(1.0, 1.0),", "        vec2(-1.0, -1.0),", "        vec2(1.0, 1.0),", "        vec2(-1.0, 1.0)", "    );", "", "    vec2 uvs[6] = vec2[](", "        vec2(0.0, 0.0),", "        vec2(1.0, 0.0),", "        vec2(1.0, 1.0),", "        vec2(0.0, 0.0),", "        vec2(1.0, 1.0),", "        vec2(0.0, 1.0)", "    );", "", "    gl_Position = vec4(positions[gl_VertexID], 0.0, 1.0);", "    texCoord = uvs[gl_VertexID];", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }

    private final String s3() {
        Object[] objectArray = new String[]{"#version 150", "", "out vec2 texCoord;", "", "void main() {", "    vec2 positions[6] = vec2[](", "        vec2(-1.0, -1.0),", "        vec2(1.0, -1.0),", "        vec2(1.0, 1.0),", "        vec2(-1.0, -1.0),", "        vec2(1.0, 1.0),", "        vec2(-1.0, 1.0)", "    );", "", "    vec2 uvs[6] = vec2[](", "        vec2(0.0, 0.0),", "        vec2(1.0, 0.0),", "        vec2(1.0, 1.0),", "        vec2(0.0, 0.0),", "        vec2(1.0, 1.0),", "        vec2(0.0, 1.0)", "    );", "", "    gl_Position = vec4(positions[gl_VertexID], 0.0, 1.0);", "    texCoord = uvs[gl_VertexID];", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }

    private final String s4() {
        Object[] objectArray = new String[]{"//!fragment", "#version 150", "", "in vec2 texCoord;", "out vec4 fragColor;", "", "uniform sampler2D SceneSampler;", "", "void main() {", "    fragColor = texture(SceneSampler, texCoord);", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }

    private final String s5() {
        Object[] objectArray = new String[]{"#version 150", "", "in vec2 texCoord;", "out vec4 fragColor;", "", "uniform sampler2D SceneSampler;", "", "void main() {", "    fragColor = texture(SceneSampler, texCoord);", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }

    private final String s6() {
        Object[] objectArray = new String[]{"//!fragment", "#version 150", "", "#moj_import <kimiko:theme_wave.glsl>", "", "in vec2 texCoord;", "out vec4 fragColor;", "", "uniform sampler2D BeforeSampler;", "uniform sampler2D AfterSampler;", "uniform sampler2D PrevTrailSampler;", "uniform sampler2D DepthSampler;", "uniform sampler2D NoHandDepthSampler;", "uniform sampler2D BoundsTexL;", "uniform sampler2D BoundsTexR;", "", "layout(std140) uniform HandsFlameData {", "    vec4 flameColor;", "    vec4 params0;", "    vec4 params1;", "    vec4 screen;", "    vec4 gradColor1;", "    vec4 gradColor2;", "    vec4 gradColor3;", "    vec4 gradColor4;", "    vec4 flameExtra;", "};", "", "vec2 handGradUV(vec2 p) {", "    vec4 l = texture(BoundsTexL, vec2(0.5));", "    vec4 r = texture(BoundsTexR, vec2(0.5));", "    bool lok = l.z > l.x + 0.001 && l.w > l.y + 0.001;", "    bool rok = r.z > r.x + 0.001 && r.w > r.y + 0.001;", "    vec4 b;", "    if (lok && rok) {", "        if (r.x - l.z > 0.04) {", "            float split = 0.5 * (l.z + r.x);", "            b = (p.x < split) ? l : r;", "        } else {", "            b = vec4(min(l.xy, r.xy), max(l.zw, r.zw));", "        }", "    } else if (lok) {", "        b = l;", "    } else if (rok) {", "        b = r;", "    } else {", "        return p;", "    }", "    return clamp((p - b.xy) / (b.zw - b.xy), vec2(0.0), vec2(1.0));", "}", "", "vec4 gradientColor(vec2 pos) {", "    float x = smoothstep(0.0, 1.0, pos.x);", "    float y = smoothstep(0.0, 1.0, pos.y);", "    vec4 c1 = gradColor1;", "    vec4 c2 = gradColor2;", "    vec4 c3 = gradColor3;", "    vec4 c4 = gradColor4;", "    if (flameExtra.y > 0.5) {", "        vec2 fragXY = kimikoFragXYFromUV(texCoord);", "        float base = flameExtra.x;", "        c1 = vec4(kimikoClientPaletteLoop(base + 0.75, fragXY), 1.0);", "        c2 = vec4(kimikoClientPaletteLoop(base + 0.5, fragXY), 1.0);", "        c3 = vec4(kimikoClientPaletteLoop(base + 0.25, fragXY), 1.0);", "        c4 = vec4(kimikoClientPaletteLoop(base, fragXY), 1.0);", "    }", "    vec4 top = mix(c1, c2, x);", "    vec4 bottom = mix(c4, c3, x);", "    return mix(top, bottom, y);", "}", "", "const vec2 dirs[4] = vec2[](", "    vec2( 1.000,  0.000),", "    vec2( 0.000,  1.000),", "    vec2(-1.000,  0.000),", "    vec2( 0.000, -1.000)", ");", "", "float hash12(vec2 p) {", "    return fract(sin(dot(p, vec2(127.1, 311.7))) * 43758.5453123);", "}", "", "float noise(vec2 p) {", "    vec2 i = floor(p);", "    vec2 f = fract(p);", "    vec2 u = f * f * (3.0 - 2.0 * f);", "    return mix(", "        mix(hash12(i), hash12(i + vec2(1.0, 0.0)), u.x),", "        mix(hash12(i + vec2(0.0, 1.0)), hash12(i + vec2(1.0, 1.0)), u.x),", "        u.y", "    );", "}", "", "float fbm3(vec2 p) {", "    float value = 0.0;", "    float amplitude = 0.5;", "    for (int i = 0; i < 3; i++) {", "        value += noise(p) * amplitude;", "        p = p * 2.03 + vec2(17.13, 9.27);", "        amplitude *= 0.5;", "    }", "    return value;", "}", "", "float fbm2(vec2 p) {", "    float value = 0.0;", "    float amplitude = 0.5;", "    for (int i = 0; i < 2; i++) {", "        value += noise(p) * amplitude;", "        p = p * 2.03 + vec2(17.13, 9.27);", "        amplitude *= 0.5;", "    }", "    return value;", "}", "float handDepthMaskAt(vec2 uv, float irisDepthMode) {", "    float depth = texture(DepthSampler, uv).r;", "    if (irisDepthMode > 0.5) {", "        float noHandDepth = texture(NoHandDepthSampler, uv).r;", "        if (depth < 0.9999 && (noHandDepth >= 0.9999 || depth < noHandDepth - 0.00002)) {", "            return 1.0;", "        }", "    } else {", "        if (depth < 0.9999) {", "            return 1.0;", "        }", "    }", "    vec4 beforeColor = texture(BeforeSampler, uv);", "    vec4 afterColor = texture(AfterSampler, uv);", "    vec3 delta = abs(afterColor.rgb - beforeColor.rgb);", "    float peak = max(max(delta.r, delta.g), delta.b);", "    float luma = dot(delta, vec3(0.299, 0.587, 0.114));", "    float value = peak * 0.78 + luma * 0.88 + abs(afterColor.a - beforeColor.a);", "    return smoothstep(0.004, 0.060, value);", "}", "", "float rawHandMaskColor(vec2 uv, float itemOnly, float irisDepthMode, out vec3 outColor) {", "    outColor = texture(AfterSampler, uv).rgb;", "    float depth = texture(DepthSampler, uv).r;", "", "    if (irisDepthMode > 0.5) {", "        float depthMask = handDepthMaskAt(uv, irisDepthMode);", "        if (depthMask > 0.5) {", "            return 1.0;", "        }", "        float noHandDepth = texture(NoHandDepthSampler, uv).r;", "        if (depth >= 0.9999 || noHandDepth < 0.0001 || depth >= noHandDepth - 0.00002) {", "            return 0.0;", "        }", "    } else {", "        if (depth < 0.9999) {", "            return 1.0;", "        }", "    }", "", "    vec4 beforeColor = texture(BeforeSampler, uv);", "    vec4 afterColor = texture(AfterSampler, uv);", "    vec3 delta = abs(afterColor.rgb - beforeColor.rgb);", "    float peak = max(max(delta.r, delta.g), delta.b);", "    float luma = dot(delta, vec3(0.299, 0.587, 0.114));", "    float value = peak * 0.78 + luma * 0.88 + abs(afterColor.a - beforeColor.a);", "    float colorMask = smoothstep(0.004, 0.060, value);", "    float itemMask = smoothstep(0.035, 0.135, value);", "", "    if (irisDepthMode > 0.5) {", "        float depthMask = handDepthMaskAt(uv, irisDepthMode);", "        return mix(max(depthMask, colorMask), max(itemMask, depthMask * irisDepthMode), itemOnly);", "    } else {", "        return mix(colorMask, itemMask, itemOnly);", "    }", "}", "", "float nearbyDepthMaskFast(vec2 uv, vec2 px, float radiusPx, float irisDepthMode) {", "    float probe = radiusPx * 0.72;", "    float mask = handDepthMaskAt(uv, irisDepthMode);", "    mask = max(mask, handDepthMaskAt(clamp(uv + vec2(probe * px.x, 0.0), vec2(0.0), vec2(1.0)), irisDepthMode));", "    mask = max(mask, handDepthMaskAt(clamp(uv - vec2(probe * px.x, 0.0), vec2(0.0), vec2(1.0)), irisDepthMode));", "    mask = max(mask, handDepthMaskAt(clamp(uv + vec2(0.0, probe * px.y), vec2(0.0), vec2(1.0)), irisDepthMode));", "    mask = max(mask, handDepthMaskAt(clamp(uv - vec2(0.0, probe * px.y), vec2(0.0), vec2(1.0)), irisDepthMode));", "    return mask;", "}", "", "float frameDt() {", "    return clamp(flameColor.a, 0.0005, 0.05);", "}", "", "float hintFade() {", "", "    return exp(-28.68 * frameDt());", "}", "", "float previousTrailHint(vec2 uv, vec2 histUv, vec2 px, float wobble) {", "    float probe = px.x * (6.0 + wobble * 6.0);", "    float alpha = texture(PrevTrailSampler, uv).a;", "    alpha = max(alpha, texture(PrevTrailSampler, histUv).a);", "    alpha = max(alpha, texture(PrevTrailSampler, clamp(histUv + vec2(probe, 0.0), vec2(0.0), vec2(1.0))).a);", "    alpha = max(alpha, texture(PrevTrailSampler, clamp(histUv - vec2(probe, 0.0), vec2(0.0), vec2(1.0))).a);", "    return alpha;", "}", "", "vec4 previousTrailColor(vec2 histUv, vec2 softPx) {", "    vec4 previous = texture(PrevTrailSampler, histUv) * 0.34;", "    previous += texture(PrevTrailSampler, clamp(histUv + vec2(softPx.x, 0.0), vec2(0.0), vec2(1.0))) * 0.105;", "    previous += texture(PrevTrailSampler, clamp(histUv - vec2(softPx.x, 0.0), vec2(0.0), vec2(1.0))) * 0.105;", "    previous += texture(PrevTrailSampler, clamp(histUv + vec2(0.0, softPx.y), vec2(0.0), vec2(1.0))) * 0.105;", "    previous += texture(PrevTrailSampler, clamp(histUv - vec2(0.0, softPx.y), vec2(0.0), vec2(1.0))) * 0.105;", "    previous += texture(PrevTrailSampler, clamp(histUv + softPx, vec2(0.0), vec2(1.0))) * 0.045;", "    previous += texture(PrevTrailSampler, clamp(histUv - softPx, vec2(0.0), vec2(1.0))) * 0.045;", "    previous += texture(PrevTrailSampler, clamp(histUv + vec2(softPx.x, -softPx.y), vec2(0.0), vec2(1.0))) * 0.045;", "    previous += texture(PrevTrailSampler, clamp(histUv + vec2(-softPx.x, softPx.y), vec2(0.0), vec2(1.0))) * 0.045;", "    return previous;", "}", "", "void computeFlameField(vec2 uv, vec2 px, float radiusPx, float prevAlpha, vec3 prevColor, float itemOnly, float irisDepthMode,", "                       float centerMask, vec3 centerColor,", "                       out float envelope, out float currentMask, out vec3 itemColor) {", "    float maskSum = 0.0;", "    float maskWSum = 0.0;", "    vec3 colorSum = vec3(0.0);", "    float colorWSum = 0.0;", "", "    vec3 cc = centerColor;", "    float cm = centerMask;", "    maskSum += cm * 1.70;", "    maskWSum += 1.70;", "    float sat = max(max(cc.r, cc.g), cc.b) - min(min(cc.r, cc.g), cc.b);", "    float lum = dot(cc, vec3(0.299, 0.587, 0.114));", "    float cw = cm * (0.3 + sat * 1.5 + lum * 0.3) * 1.70;", "    colorSum += cc * cw;", "    colorWSum += cw;", "", "    for (int i = 0; i < 4; i++) {", "        vec2 dir = dirs[i];", "", "        vec2 nearUv = clamp(uv + dir * radiusPx * 0.45 * px, vec2(0.0), vec2(1.0));", "        vec3 nearColor;", "        float nearMask = rawHandMaskColor(nearUv, itemOnly, irisDepthMode, nearColor);", "        maskSum += nearMask * 0.70;", "        maskWSum += 0.70;", "        float nearSat = max(max(nearColor.r, nearColor.g), nearColor.b) - min(min(nearColor.r, nearColor.g), nearColor.b);", "        float nearLum = dot(nearColor, vec3(0.299, 0.587, 0.114));", "        float nearWeight = nearMask * 0.70 * (0.3 + nearSat * 1.5 + nearLum * 0.3);", "        colorSum += nearColor * nearWeight;", "        colorWSum += nearWeight;", "", "        vec2 farUv = clamp(uv + dir * radiusPx * px, vec2(0.0), vec2(1.0));", "        vec3 farColor;", "        float farMask = rawHandMaskColor(farUv, itemOnly, irisDepthMode, farColor);", "        maskSum += farMask * 0.30;", "        maskWSum += 0.30;", "        float farSat = max(max(farColor.r, farColor.g), farColor.b) - min(min(farColor.r, farColor.g), farColor.b);", "        float farLum = dot(farColor, vec3(0.299, 0.587, 0.114));", "        float farWeight = farMask * 0.30 * (0.3 + farSat * 1.5 + farLum * 0.3);", "        colorSum += farColor * farWeight;", "        colorWSum += farWeight;", "    }", "", "    float blurredMask = smoothstep(0.020, 0.68, maskSum / max(maskWSum, 0.001));", "    currentMask = blurredMask;", "    envelope = max(blurredMask, prevAlpha * hintFade());", "    itemColor = colorWSum > 0.001 ? colorSum / colorWSum : (prevAlpha > 0.01 ? prevColor : flameColor.rgb);", "}", "", "void main() {", "    float strength = params0.x;", "    float riseSpeed = params0.y;", "    float wobble = params0.z;", "    float flameLength = params0.w;", "    float brightness = params1.x;", "    float time = params1.y;", "    float packedColorMode = params1.z;", "    float irisDepthMode = step(20.0, packedColorMode);", "    packedColorMode -= irisDepthMode * 20.0;", "    float itemOnly = step(10.0, packedColorMode);", "    float colorMode = packedColorMode - itemOnly * 10.0;", "    float colorAlpha = params1.w;", "    vec2 px = screen.zw;", "    float lengthCurve = clamp(flameLength / 2.5, 0.0, 1.0);", "    float radiusPx = mix(22.0, 62.0, lengthCurve);", "", "    float rise = 0.05 + smoothstep(0.0, 2.0, riseSpeed) * 0.80;", "", "    float stepScale = clamp(frameDt() * 60.0, 0.25, 3.0);", "    float nearCurrentHand = handDepthMaskAt(texCoord, irisDepthMode);", "", "    if (nearCurrentHand < 0.5) {", "        vec2 quickHistUv = clamp(texCoord + vec2(0.0, -px.y * rise * stepScale), vec2(0.0), vec2(1.0));", "        if (previousTrailHint(texCoord, quickHistUv, px, wobble) < 0.0015) {", "            fragColor = vec4(0.0);", "            return;", "        }", "    }", "", "    vec3 quickColor;", "    float quickMask = rawHandMaskColor(texCoord, itemOnly, irisDepthMode, quickColor);", "", "    float curl = noise(texCoord * vec2(8.0, 6.0) + vec2(time * 0.20, time * 0.11)) - 0.5;", "    vec2 drift = vec2(", "        sin(texCoord.y * 18.0 + time * 2.35) * px.x * wobble * 2.6 + curl * px.x * (2.0 + wobble * 3.2),", "        -px.y * rise", "    ) * stepScale;", "    vec2 histUv = clamp(texCoord + drift, vec2(0.0), vec2(1.0));", "", "    vec2 softPx = px * (2.1 + wobble * 0.65);", "    vec4 previous = previousTrailColor(histUv, softPx);", "", "    float envelope;", "    float currentMask;", "    vec3 itemColor;", "    if (quickMask < 0.01 && nearCurrentHand < 0.5) {", "        currentMask = 0.0;", "        envelope = previous.a * hintFade();", "        itemColor = previous.a > 0.01 ? previous.rgb : flameColor.rgb;", "    } else {", "        computeFlameField(texCoord, px, radiusPx, previous.a, previous.rgb, itemOnly, irisDepthMode, quickMask, quickColor, envelope, currentMask, itemColor);", "    }", "", "    float flow = time * (0.08 + riseSpeed * 0.50);", "    float lateralWobble = sin(texCoord.y * 28.0 + time * (1.5 + wobble * 2.5)) * wobble * 0.22;", "    vec2 flameUv = vec2(texCoord.x + lateralWobble, texCoord.y - flow);", "", "    float n1 = fbm3(vec2(flameUv.x * 4.4, flameUv.y * (5.2 + flameLength * 2.7)));", "    float n2 = fbm2(vec2(flameUv.x * 8.2 + 3.7, flameUv.y * 9.5 - time * 0.35));", "    float fireNoise = n1 * 0.72 + n2 * 0.28;", "", "    float solidZone = smoothstep(0.15, 0.55, envelope);", "    float flameShape = mix(fireNoise, 1.0, solidZone * 0.78);", "    flameShape = smoothstep(0.18, 0.78, flameShape);", "", "    float source = currentMask * mix(0.64, 1.35, flameShape);", "    source = clamp(source, 0.0, 1.0);", "", "    float strengthCurve = 1.0 - exp(-clamp(strength, 0.0, 2.0) * 0.95);", "    float brightnessCurve = 0.45 + (1.0 - exp(-clamp(brightness, 0.0, 2.0) * 0.85)) * 0.70;", "    source *= strengthCurve * brightnessCurve * 1.22 * colorAlpha;", "    source = clamp(source, 0.0, 0.92);", "", "    vec3 baseColor;", "    if (colorMode < 0.5) {", "        baseColor = itemColor;", "        float maxVal = max(baseColor.r, max(baseColor.g, baseColor.b));", "        if (maxVal > 0.001) {", "            baseColor *= 0.65 / maxVal;", "        }", "    } else {", "        baseColor = gradColor1.a > 0.5", "                ? gradientColor(handGradUV(texCoord)).rgb", "                : flameColor.rgb;", "", "        float luma = dot(baseColor, vec3(0.299, 0.587, 0.114));", "        if (luma > 0.45) {", "            baseColor *= 0.45 / luma;", "        }", "    }", "    float distFromItem = 1.0 - clamp(envelope * 1.5, 0.0, 1.0);", "    vec3 coreColor = min(baseColor * 1.4 + vec3(0.06), vec3(1.0));", "    vec3 midColor = baseColor * 0.85;", "    vec3 tipColor = baseColor * 0.35;", "    vec3 fireColor = mix(coreColor, midColor, smoothstep(0.0, 0.35, distFromItem));", "    fireColor = mix(fireColor, tipColor, smoothstep(0.25, 0.9, distFromItem));", "    fireColor *= 0.92 + 0.08 * sin(time * 7.3 + texCoord.x * 20.0 + texCoord.y * 15.0);", "", "    float dt = frameDt();", "    float historyFade = exp(dt * mix(-4.353, -3.078, lengthCurve));", "    float bfBase = clamp(source * 0.56 + 0.10, 0.08, 0.66);", "    float blendFactor = 1.0 - exp(60.0 * log(1.0 - bfBase) * dt);", "    float alpha = clamp(mix(previous.a * historyFade, source, blendFactor), 0.0, 0.95);", "    alpha = max(alpha, previous.a * historyFade);", "", "    float colorBlend = blendFactor * smoothstep(0.015, 0.22, currentMask);", "    vec3 color = alpha > 0.001 ? mix(previous.rgb, fireColor, colorBlend) : vec3(0.0);", "    fragColor = vec4(color, alpha);", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }

    private final String s7() {
        Object[] objectArray = new String[]{"#version 150", "", "#moj_import <kimiko:theme_wave.glsl>", "", "in vec2 texCoord;", "out vec4 fragColor;", "", "uniform sampler2D BeforeSampler;", "uniform sampler2D AfterSampler;", "uniform sampler2D PrevTrailSampler;", "uniform sampler2D DepthSampler;", "uniform sampler2D NoHandDepthSampler;", "uniform sampler2D BoundsTexL;", "uniform sampler2D BoundsTexR;", "", "layout(std140) uniform HandsFlameData {", "    vec4 flameColor;", "    vec4 params0;", "    vec4 params1;", "    vec4 screen;", "    vec4 gradColor1;", "    vec4 gradColor2;", "    vec4 gradColor3;", "    vec4 gradColor4;", "    vec4 flameExtra;", "};", "", "vec2 handGradUV(vec2 p) {", "    vec4 l = texture(BoundsTexL, vec2(0.5));", "    vec4 r = texture(BoundsTexR, vec2(0.5));", "    bool lok = l.z > l.x + 0.001 && l.w > l.y + 0.001;", "    bool rok = r.z > r.x + 0.001 && r.w > r.y + 0.001;", "    vec4 b;", "    if (lok && rok) {", "        if (r.x - l.z > 0.04) {", "            float split = 0.5 * (l.z + r.x);", "            b = (p.x < split) ? l : r;", "        } else {", "            b = vec4(min(l.xy, r.xy), max(l.zw, r.zw));", "        }", "    } else if (lok) {", "        b = l;", "    } else if (rok) {", "        b = r;", "    } else {", "        return p;", "    }", "    return clamp((p - b.xy) / (b.zw - b.xy), vec2(0.0), vec2(1.0));", "}", "", "vec4 gradientColor(vec2 pos) {", "    float x = smoothstep(0.0, 1.0, pos.x);", "    float y = smoothstep(0.0, 1.0, pos.y);", "    vec4 c1 = gradColor1;", "    vec4 c2 = gradColor2;", "    vec4 c3 = gradColor3;", "    vec4 c4 = gradColor4;", "    if (flameExtra.y > 0.5) {", "        vec2 fragXY = kimikoFragXYFromUV(texCoord);", "        float base = flameExtra.x;", "        c1 = vec4(kimikoClientPaletteLoop(base + 0.75, fragXY), 1.0);", "        c2 = vec4(kimikoClientPaletteLoop(base + 0.5, fragXY), 1.0);", "        c3 = vec4(kimikoClientPaletteLoop(base + 0.25, fragXY), 1.0);", "        c4 = vec4(kimikoClientPaletteLoop(base, fragXY), 1.0);", "    }", "    vec4 top = mix(c1, c2, x);", "    vec4 bottom = mix(c4, c3, x);", "    return mix(top, bottom, y);", "}", "", "const vec2 dirs[4] = vec2[](", "    vec2( 1.000,  0.000),", "    vec2( 0.000,  1.000),", "    vec2(-1.000,  0.000),", "    vec2( 0.000, -1.000)", ");", "", "float hash12(vec2 p) {", "    return fract(sin(dot(p, vec2(127.1, 311.7))) * 43758.5453123);", "}", "", "float noise(vec2 p) {", "    vec2 i = floor(p);", "    vec2 f = fract(p);", "    vec2 u = f * f * (3.0 - 2.0 * f);", "    return mix(", "        mix(hash12(i), hash12(i + vec2(1.0, 0.0)), u.x),", "        mix(hash12(i + vec2(0.0, 1.0)), hash12(i + vec2(1.0, 1.0)), u.x),", "        u.y", "    );", "}", "", "float fbm3(vec2 p) {", "    float value = 0.0;", "    float amplitude = 0.5;", "    for (int i = 0; i < 3; i++) {", "        value += noise(p) * amplitude;", "        p = p * 2.03 + vec2(17.13, 9.27);", "        amplitude *= 0.5;", "    }", "    return value;", "}", "", "float fbm2(vec2 p) {", "    float value = 0.0;", "    float amplitude = 0.5;", "    for (int i = 0; i < 2; i++) {", "        value += noise(p) * amplitude;", "        p = p * 2.03 + vec2(17.13, 9.27);", "        amplitude *= 0.5;", "    }", "    return value;", "}", "float handDepthMaskAt(vec2 uv, float irisDepthMode) {", "    float depth = texture(DepthSampler, uv).r;", "    if (irisDepthMode > 0.5) {", "        float noHandDepth = texture(NoHandDepthSampler, uv).r;", "        if (depth < 0.9999 && (noHandDepth >= 0.9999 || depth < noHandDepth - 0.00002)) {", "            return 1.0;", "        }", "    } else {", "        if (depth < 0.9999) {", "            return 1.0;", "        }", "    }", "    vec4 beforeColor = texture(BeforeSampler, uv);", "    vec4 afterColor = texture(AfterSampler, uv);", "    vec3 delta = abs(afterColor.rgb - beforeColor.rgb);", "    float peak = max(max(delta.r, delta.g), delta.b);", "    float luma = dot(delta, vec3(0.299, 0.587, 0.114));", "    float value = peak * 0.78 + luma * 0.88 + abs(afterColor.a - beforeColor.a);", "    return smoothstep(0.004, 0.060, value);", "}", "", "float rawHandMaskColor(vec2 uv, float itemOnly, float irisDepthMode, out vec3 outColor) {", "    outColor = texture(AfterSampler, uv).rgb;", "    float depth = texture(DepthSampler, uv).r;", "", "    if (irisDepthMode > 0.5) {", "        float depthMask = handDepthMaskAt(uv, irisDepthMode);", "        if (depthMask > 0.5) {", "            return 1.0;", "        }", "        float noHandDepth = texture(NoHandDepthSampler, uv).r;", "        if (depth >= 0.9999 || noHandDepth < 0.0001 || depth >= noHandDepth - 0.00002) {", "            return 0.0;", "        }", "    } else {", "        if (depth < 0.9999) {", "            return 1.0;", "        }", "    }", "", "    vec4 beforeColor = texture(BeforeSampler, uv);", "    vec4 afterColor = texture(AfterSampler, uv);", "    vec3 delta = abs(afterColor.rgb - beforeColor.rgb);", "    float peak = max(max(delta.r, delta.g), delta.b);", "    float luma = dot(delta, vec3(0.299, 0.587, 0.114));", "    float value = peak * 0.78 + luma * 0.88 + abs(afterColor.a - beforeColor.a);", "    float colorMask = smoothstep(0.004, 0.060, value);", "    float itemMask = smoothstep(0.035, 0.135, value);", "", "    if (irisDepthMode > 0.5) {", "        float depthMask = handDepthMaskAt(uv, irisDepthMode);", "        return mix(max(depthMask, colorMask), max(itemMask, depthMask * irisDepthMode), itemOnly);", "    } else {", "        return mix(colorMask, itemMask, itemOnly);", "    }", "}", "", "float nearbyDepthMaskFast(vec2 uv, vec2 px, float radiusPx, float irisDepthMode) {", "    float probe = radiusPx * 0.72;", "    float mask = handDepthMaskAt(uv, irisDepthMode);", "    mask = max(mask, handDepthMaskAt(clamp(uv + vec2(probe * px.x, 0.0), vec2(0.0), vec2(1.0)), irisDepthMode));", "    mask = max(mask, handDepthMaskAt(clamp(uv - vec2(probe * px.x, 0.0), vec2(0.0), vec2(1.0)), irisDepthMode));", "    mask = max(mask, handDepthMaskAt(clamp(uv + vec2(0.0, probe * px.y), vec2(0.0), vec2(1.0)), irisDepthMode));", "    mask = max(mask, handDepthMaskAt(clamp(uv - vec2(0.0, probe * px.y), vec2(0.0), vec2(1.0)), irisDepthMode));", "    return mask;", "}", "", "float frameDt() {", "    return clamp(flameColor.a, 0.0005, 0.05);", "}", "", "float hintFade() {", "", "    return exp(-28.68 * frameDt());", "}", "", "float previousTrailHint(vec2 uv, vec2 histUv, vec2 px, float wobble) {", "    float probe = px.x * (6.0 + wobble * 6.0);", "    float alpha = texture(PrevTrailSampler, uv).a;", "    alpha = max(alpha, texture(PrevTrailSampler, histUv).a);", "    alpha = max(alpha, texture(PrevTrailSampler, clamp(histUv + vec2(probe, 0.0), vec2(0.0), vec2(1.0))).a);", "    alpha = max(alpha, texture(PrevTrailSampler, clamp(histUv - vec2(probe, 0.0), vec2(0.0), vec2(1.0))).a);", "    return alpha;", "}", "", "vec4 previousTrailColor(vec2 histUv, vec2 softPx) {", "    vec4 previous = texture(PrevTrailSampler, histUv) * 0.34;", "    previous += texture(PrevTrailSampler, clamp(histUv + vec2(softPx.x, 0.0), vec2(0.0), vec2(1.0))) * 0.105;", "    previous += texture(PrevTrailSampler, clamp(histUv - vec2(softPx.x, 0.0), vec2(0.0), vec2(1.0))) * 0.105;", "    previous += texture(PrevTrailSampler, clamp(histUv + vec2(0.0, softPx.y), vec2(0.0), vec2(1.0))) * 0.105;", "    previous += texture(PrevTrailSampler, clamp(histUv - vec2(0.0, softPx.y), vec2(0.0), vec2(1.0))) * 0.105;", "    previous += texture(PrevTrailSampler, clamp(histUv + softPx, vec2(0.0), vec2(1.0))) * 0.045;", "    previous += texture(PrevTrailSampler, clamp(histUv - softPx, vec2(0.0), vec2(1.0))) * 0.045;", "    previous += texture(PrevTrailSampler, clamp(histUv + vec2(softPx.x, -softPx.y), vec2(0.0), vec2(1.0))) * 0.045;", "    previous += texture(PrevTrailSampler, clamp(histUv + vec2(-softPx.x, softPx.y), vec2(0.0), vec2(1.0))) * 0.045;", "    return previous;", "}", "", "void computeFlameField(vec2 uv, vec2 px, float radiusPx, float prevAlpha, vec3 prevColor, float itemOnly, float irisDepthMode,", "                       float centerMask, vec3 centerColor,", "                       out float envelope, out float currentMask, out vec3 itemColor) {", "    float maskSum = 0.0;", "    float maskWSum = 0.0;", "    vec3 colorSum = vec3(0.0);", "    float colorWSum = 0.0;", "", "    vec3 cc = centerColor;", "    float cm = centerMask;", "    maskSum += cm * 1.70;", "    maskWSum += 1.70;", "    float sat = max(max(cc.r, cc.g), cc.b) - min(min(cc.r, cc.g), cc.b);", "    float lum = dot(cc, vec3(0.299, 0.587, 0.114));", "    float cw = cm * (0.3 + sat * 1.5 + lum * 0.3) * 1.70;", "    colorSum += cc * cw;", "    colorWSum += cw;", "", "    for (int i = 0; i < 4; i++) {", "        vec2 dir = dirs[i];", "", "        vec2 nearUv = clamp(uv + dir * radiusPx * 0.45 * px, vec2(0.0), vec2(1.0));", "        vec3 nearColor;", "        float nearMask = rawHandMaskColor(nearUv, itemOnly, irisDepthMode, nearColor);", "        maskSum += nearMask * 0.70;", "        maskWSum += 0.70;", "        float nearSat = max(max(nearColor.r, nearColor.g), nearColor.b) - min(min(nearColor.r, nearColor.g), nearColor.b);", "        float nearLum = dot(nearColor, vec3(0.299, 0.587, 0.114));", "        float nearWeight = nearMask * 0.70 * (0.3 + nearSat * 1.5 + nearLum * 0.3);", "        colorSum += nearColor * nearWeight;", "        colorWSum += nearWeight;", "", "        vec2 farUv = clamp(uv + dir * radiusPx * px, vec2(0.0), vec2(1.0));", "        vec3 farColor;", "        float farMask = rawHandMaskColor(farUv, itemOnly, irisDepthMode, farColor);", "        maskSum += farMask * 0.30;", "        maskWSum += 0.30;", "        float farSat = max(max(farColor.r, farColor.g), farColor.b) - min(min(farColor.r, farColor.g), farColor.b);", "        float farLum = dot(farColor, vec3(0.299, 0.587, 0.114));", "        float farWeight = farMask * 0.30 * (0.3 + farSat * 1.5 + farLum * 0.3);", "        colorSum += farColor * farWeight;", "        colorWSum += farWeight;", "    }", "", "    float blurredMask = smoothstep(0.020, 0.68, maskSum / max(maskWSum, 0.001));", "    currentMask = blurredMask;", "    envelope = max(blurredMask, prevAlpha * hintFade());", "    itemColor = colorWSum > 0.001 ? colorSum / colorWSum : (prevAlpha > 0.01 ? prevColor : flameColor.rgb);", "}", "", "void main() {", "    float strength = params0.x;", "    float riseSpeed = params0.y;", "    float wobble = params0.z;", "    float flameLength = params0.w;", "    float brightness = params1.x;", "    float time = params1.y;", "    float packedColorMode = params1.z;", "    float irisDepthMode = step(20.0, packedColorMode);", "    packedColorMode -= irisDepthMode * 20.0;", "    float itemOnly = step(10.0, packedColorMode);", "    float colorMode = packedColorMode - itemOnly * 10.0;", "    float colorAlpha = params1.w;", "    vec2 px = screen.zw;", "    float lengthCurve = clamp(flameLength / 2.5, 0.0, 1.0);", "    float radiusPx = mix(22.0, 62.0, lengthCurve);", "", "    float rise = 0.05 + smoothstep(0.0, 2.0, riseSpeed) * 0.80;", "", "    float stepScale = clamp(frameDt() * 60.0, 0.25, 3.0);", "    float nearCurrentHand = handDepthMaskAt(texCoord, irisDepthMode);", "", "    if (nearCurrentHand < 0.5) {", "        vec2 quickHistUv = clamp(texCoord + vec2(0.0, -px.y * rise * stepScale), vec2(0.0), vec2(1.0));", "        if (previousTrailHint(texCoord, quickHistUv, px, wobble) < 0.0015) {", "            fragColor = vec4(0.0);", "            return;", "        }", "    }", "", "    vec3 quickColor;", "    float quickMask = rawHandMaskColor(texCoord, itemOnly, irisDepthMode, quickColor);", "", "    float curl = noise(texCoord * vec2(8.0, 6.0) + vec2(time * 0.20, time * 0.11)) - 0.5;", "    vec2 drift = vec2(", "        sin(texCoord.y * 18.0 + time * 2.35) * px.x * wobble * 2.6 + curl * px.x * (2.0 + wobble * 3.2),", "        -px.y * rise", "    ) * stepScale;", "    vec2 histUv = clamp(texCoord + drift, vec2(0.0), vec2(1.0));", "", "    vec2 softPx = px * (2.1 + wobble * 0.65);", "    vec4 previous = previousTrailColor(histUv, softPx);", "", "    float envelope;", "    float currentMask;", "    vec3 itemColor;", "    if (quickMask < 0.01 && nearCurrentHand < 0.5) {", "        currentMask = 0.0;", "        envelope = previous.a * hintFade();", "        itemColor = previous.a > 0.01 ? previous.rgb : flameColor.rgb;", "    } else {", "        computeFlameField(texCoord, px, radiusPx, previous.a, previous.rgb, itemOnly, irisDepthMode, quickMask, quickColor, envelope, currentMask, itemColor);", "    }", "", "    float flow = time * (0.08 + riseSpeed * 0.50);", "    float lateralWobble = sin(texCoord.y * 28.0 + time * (1.5 + wobble * 2.5)) * wobble * 0.22;", "    vec2 flameUv = vec2(texCoord.x + lateralWobble, texCoord.y - flow);", "", "    float n1 = fbm3(vec2(flameUv.x * 4.4, flameUv.y * (5.2 + flameLength * 2.7)));", "    float n2 = fbm2(vec2(flameUv.x * 8.2 + 3.7, flameUv.y * 9.5 - time * 0.35));", "    float fireNoise = n1 * 0.72 + n2 * 0.28;", "", "    float solidZone = smoothstep(0.15, 0.55, envelope);", "    float flameShape = mix(fireNoise, 1.0, solidZone * 0.78);", "    flameShape = smoothstep(0.18, 0.78, flameShape);", "", "    float source = currentMask * mix(0.64, 1.35, flameShape);", "    source = clamp(source, 0.0, 1.0);", "", "    float strengthCurve = 1.0 - exp(-clamp(strength, 0.0, 2.0) * 0.95);", "    float brightnessCurve = 0.45 + (1.0 - exp(-clamp(brightness, 0.0, 2.0) * 0.85)) * 0.70;", "    source *= strengthCurve * brightnessCurve * 1.22 * colorAlpha;", "    source = clamp(source, 0.0, 0.92);", "", "    vec3 baseColor;", "    if (colorMode < 0.5) {", "        baseColor = itemColor;", "        float maxVal = max(baseColor.r, max(baseColor.g, baseColor.b));", "        if (maxVal > 0.001) {", "            baseColor *= 0.65 / maxVal;", "        }", "    } else {", "        baseColor = gradColor1.a > 0.5", "                ? gradientColor(handGradUV(texCoord)).rgb", "                : flameColor.rgb;", "", "        float luma = dot(baseColor, vec3(0.299, 0.587, 0.114));", "        if (luma > 0.45) {", "            baseColor *= 0.45 / luma;", "        }", "    }", "    float distFromItem = 1.0 - clamp(envelope * 1.5, 0.0, 1.0);", "    vec3 coreColor = min(baseColor * 1.4 + vec3(0.06), vec3(1.0));", "    vec3 midColor = baseColor * 0.85;", "    vec3 tipColor = baseColor * 0.35;", "    vec3 fireColor = mix(coreColor, midColor, smoothstep(0.0, 0.35, distFromItem));", "    fireColor = mix(fireColor, tipColor, smoothstep(0.25, 0.9, distFromItem));", "    fireColor *= 0.92 + 0.08 * sin(time * 7.3 + texCoord.x * 20.0 + texCoord.y * 15.0);", "", "    float dt = frameDt();", "    float historyFade = exp(dt * mix(-4.353, -3.078, lengthCurve));", "    float bfBase = clamp(source * 0.56 + 0.10, 0.08, 0.66);", "    float blendFactor = 1.0 - exp(60.0 * log(1.0 - bfBase) * dt);", "    float alpha = clamp(mix(previous.a * historyFade, source, blendFactor), 0.0, 0.95);", "    alpha = max(alpha, previous.a * historyFade);", "", "    float colorBlend = blendFactor * smoothstep(0.015, 0.22, currentMask);", "    vec3 color = alpha > 0.001 ? mix(previous.rgb, fireColor, colorBlend) : vec3(0.0);", "    fragColor = vec4(color, alpha);", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }
}

