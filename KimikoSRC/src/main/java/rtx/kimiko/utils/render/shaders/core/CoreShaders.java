/*
 * Decompiled with CFR 0.152.
 * 
 * Could not load the following classes:
 *  kotlin.Metadata
 *  kotlin.collections.CollectionsKt
 *  kotlin.jvm.JvmStatic
 *  kotlin.jvm.internal.Intrinsics
 *  org.jetbrains.annotations.NotNull
 */
package rtx.kimiko.utils.render.shaders.core;

import java.util.Map;
import kotlin.Metadata;
import kotlin.collections.CollectionsKt;
import kotlin.jvm.JvmStatic;
import kotlin.jvm.internal.Intrinsics;
import org.jetbrains.annotations.NotNull;

@Metadata(mv={2, 4, 0}, k=1, xi=48, d1={"\u0000\"\n\u0002\u0018\u0002\n\u0002\u0010\u0000\n\u0002\b\u0002\n\u0002\u0010%\n\u0002\u0010\u000e\n\u0000\n\u0002\u0010\u0002\n\u0002\u0018\u0002\n\u0002\b}\b\u00c6\u0002\u0018\u00002\u00020\u0001B\t\b\u0002\u00a2\u0006\u0004\b\u0002\u0010\u0003J'\u0010\t\u001a\u00020\u00072\u0012\u0010\u0006\u001a\u000e\u0012\u0004\u0012\u00020\u0005\u0012\u0004\u0012\u00020\u00050\u0004H\u0007b\u0002\b\b\u00a2\u0006\u0004\b\t\u0010\nJ\u001b\u0010\f\u001a\u00020\u00052\u0006\u0010\u000b\u001a\u00020\u0005H\u0007b\u0002\b\b\u00a2\u0006\u0004\b\f\u0010\rJ\u000f\u0010\u000e\u001a\u00020\u0005H\u0002\u00a2\u0006\u0004\b\u000e\u0010\u000fJ\u000f\u0010\u0010\u001a\u00020\u0005H\u0002\u00a2\u0006\u0004\b\u0010\u0010\u000fJ\u000f\u0010\u0011\u001a\u00020\u0005H\u0002\u00a2\u0006\u0004\b\u0011\u0010\u000fJ\u000f\u0010\u0012\u001a\u00020\u0005H\u0002\u00a2\u0006\u0004\b\u0012\u0010\u000fJ\u000f\u0010\u0013\u001a\u00020\u0005H\u0002\u00a2\u0006\u0004\b\u0013\u0010\u000fJ\u000f\u0010\u0014\u001a\u00020\u0005H\u0002\u00a2\u0006\u0004\b\u0014\u0010\u000fJ\u000f\u0010\u0015\u001a\u00020\u0005H\u0002\u00a2\u0006\u0004\b\u0015\u0010\u000fJ\u000f\u0010\u0016\u001a\u00020\u0005H\u0002\u00a2\u0006\u0004\b\u0016\u0010\u000fJ\u000f\u0010\u0017\u001a\u00020\u0005H\u0002\u00a2\u0006\u0004\b\u0017\u0010\u000fJ\u000f\u0010\u0018\u001a\u00020\u0005H\u0002\u00a2\u0006\u0004\b\u0018\u0010\u000fJ\u000f\u0010\u0019\u001a\u00020\u0005H\u0002\u00a2\u0006\u0004\b\u0019\u0010\u000fJ\u000f\u0010\u001a\u001a\u00020\u0005H\u0002\u00a2\u0006\u0004\b\u001a\u0010\u000fJ\u000f\u0010\u001b\u001a\u00020\u0005H\u0002\u00a2\u0006\u0004\b\u001b\u0010\u000fJ\u000f\u0010\u001c\u001a\u00020\u0005H\u0002\u00a2\u0006\u0004\b\u001c\u0010\u000fJ\u000f\u0010\u001d\u001a\u00020\u0005H\u0002\u00a2\u0006\u0004\b\u001d\u0010\u000fJ\u000f\u0010\u001e\u001a\u00020\u0005H\u0002\u00a2\u0006\u0004\b\u001e\u0010\u000fJ\u000f\u0010\u001f\u001a\u00020\u0005H\u0002\u00a2\u0006\u0004\b\u001f\u0010\u000fJ\u000f\u0010 \u001a\u00020\u0005H\u0002\u00a2\u0006\u0004\b \u0010\u000fJ\u000f\u0010!\u001a\u00020\u0005H\u0002\u00a2\u0006\u0004\b!\u0010\u000fJ\u000f\u0010\"\u001a\u00020\u0005H\u0002\u00a2\u0006\u0004\b\"\u0010\u000fJ\u000f\u0010#\u001a\u00020\u0005H\u0002\u00a2\u0006\u0004\b#\u0010\u000fJ\u000f\u0010$\u001a\u00020\u0005H\u0002\u00a2\u0006\u0004\b$\u0010\u000fJ\u000f\u0010%\u001a\u00020\u0005H\u0002\u00a2\u0006\u0004\b%\u0010\u000fJ\u000f\u0010&\u001a\u00020\u0005H\u0002\u00a2\u0006\u0004\b&\u0010\u000fJ\u000f\u0010'\u001a\u00020\u0005H\u0002\u00a2\u0006\u0004\b'\u0010\u000fJ\u000f\u0010(\u001a\u00020\u0005H\u0002\u00a2\u0006\u0004\b(\u0010\u000fJ\u000f\u0010)\u001a\u00020\u0005H\u0002\u00a2\u0006\u0004\b)\u0010\u000fJ\u000f\u0010*\u001a\u00020\u0005H\u0002\u00a2\u0006\u0004\b*\u0010\u000fJ\u000f\u0010+\u001a\u00020\u0005H\u0002\u00a2\u0006\u0004\b+\u0010\u000fJ\u000f\u0010,\u001a\u00020\u0005H\u0002\u00a2\u0006\u0004\b,\u0010\u000fJ\u000f\u0010-\u001a\u00020\u0005H\u0002\u00a2\u0006\u0004\b-\u0010\u000fJ\u000f\u0010.\u001a\u00020\u0005H\u0002\u00a2\u0006\u0004\b.\u0010\u000fJ\u000f\u0010/\u001a\u00020\u0005H\u0002\u00a2\u0006\u0004\b/\u0010\u000fJ\u000f\u00100\u001a\u00020\u0005H\u0002\u00a2\u0006\u0004\b0\u0010\u000fJ\u000f\u00101\u001a\u00020\u0005H\u0002\u00a2\u0006\u0004\b1\u0010\u000fJ\u000f\u00102\u001a\u00020\u0005H\u0002\u00a2\u0006\u0004\b2\u0010\u000fJ\u000f\u00103\u001a\u00020\u0005H\u0002\u00a2\u0006\u0004\b3\u0010\u000fJ\u000f\u00104\u001a\u00020\u0005H\u0002\u00a2\u0006\u0004\b4\u0010\u000fJ\u000f\u00105\u001a\u00020\u0005H\u0002\u00a2\u0006\u0004\b5\u0010\u000fJ\u000f\u00106\u001a\u00020\u0005H\u0002\u00a2\u0006\u0004\b6\u0010\u000fJ\u000f\u00107\u001a\u00020\u0005H\u0002\u00a2\u0006\u0004\b7\u0010\u000fJ\u000f\u00108\u001a\u00020\u0005H\u0002\u00a2\u0006\u0004\b8\u0010\u000fJ\u000f\u00109\u001a\u00020\u0005H\u0002\u00a2\u0006\u0004\b9\u0010\u000fJ\u000f\u0010:\u001a\u00020\u0005H\u0002\u00a2\u0006\u0004\b:\u0010\u000fJ\u000f\u0010;\u001a\u00020\u0005H\u0002\u00a2\u0006\u0004\b;\u0010\u000fJ\u000f\u0010<\u001a\u00020\u0005H\u0002\u00a2\u0006\u0004\b<\u0010\u000fJ\u000f\u0010=\u001a\u00020\u0005H\u0002\u00a2\u0006\u0004\b=\u0010\u000fJ\u000f\u0010>\u001a\u00020\u0005H\u0002\u00a2\u0006\u0004\b>\u0010\u000fJ\u000f\u0010?\u001a\u00020\u0005H\u0002\u00a2\u0006\u0004\b?\u0010\u000fJ\u000f\u0010@\u001a\u00020\u0005H\u0002\u00a2\u0006\u0004\b@\u0010\u000fJ\u000f\u0010A\u001a\u00020\u0005H\u0002\u00a2\u0006\u0004\bA\u0010\u000fJ\u000f\u0010B\u001a\u00020\u0005H\u0002\u00a2\u0006\u0004\bB\u0010\u000fJ\u000f\u0010C\u001a\u00020\u0005H\u0002\u00a2\u0006\u0004\bC\u0010\u000fJ\u000f\u0010D\u001a\u00020\u0005H\u0002\u00a2\u0006\u0004\bD\u0010\u000fJ\u000f\u0010E\u001a\u00020\u0005H\u0002\u00a2\u0006\u0004\bE\u0010\u000fJ\u000f\u0010F\u001a\u00020\u0005H\u0002\u00a2\u0006\u0004\bF\u0010\u000fJ\u000f\u0010G\u001a\u00020\u0005H\u0002\u00a2\u0006\u0004\bG\u0010\u000fJ\u000f\u0010H\u001a\u00020\u0005H\u0002\u00a2\u0006\u0004\bH\u0010\u000fJ\u000f\u0010I\u001a\u00020\u0005H\u0002\u00a2\u0006\u0004\bI\u0010\u000fJ\u000f\u0010J\u001a\u00020\u0005H\u0002\u00a2\u0006\u0004\bJ\u0010\u000fJ\u000f\u0010K\u001a\u00020\u0005H\u0002\u00a2\u0006\u0004\bK\u0010\u000fJ\u000f\u0010L\u001a\u00020\u0005H\u0002\u00a2\u0006\u0004\bL\u0010\u000fJ\u000f\u0010M\u001a\u00020\u0005H\u0002\u00a2\u0006\u0004\bM\u0010\u000fJ\u000f\u0010N\u001a\u00020\u0005H\u0002\u00a2\u0006\u0004\bN\u0010\u000fJ\u000f\u0010O\u001a\u00020\u0005H\u0002\u00a2\u0006\u0004\bO\u0010\u000fJ\u000f\u0010P\u001a\u00020\u0005H\u0002\u00a2\u0006\u0004\bP\u0010\u000fJ\u000f\u0010Q\u001a\u00020\u0005H\u0002\u00a2\u0006\u0004\bQ\u0010\u000fJ\u000f\u0010R\u001a\u00020\u0005H\u0002\u00a2\u0006\u0004\bR\u0010\u000fJ\u000f\u0010S\u001a\u00020\u0005H\u0002\u00a2\u0006\u0004\bS\u0010\u000fJ\u000f\u0010T\u001a\u00020\u0005H\u0002\u00a2\u0006\u0004\bT\u0010\u000fJ\u000f\u0010U\u001a\u00020\u0005H\u0002\u00a2\u0006\u0004\bU\u0010\u000fJ\u000f\u0010V\u001a\u00020\u0005H\u0002\u00a2\u0006\u0004\bV\u0010\u000fJ\u000f\u0010W\u001a\u00020\u0005H\u0002\u00a2\u0006\u0004\bW\u0010\u000fJ\u000f\u0010X\u001a\u00020\u0005H\u0002\u00a2\u0006\u0004\bX\u0010\u000fJ\u000f\u0010Y\u001a\u00020\u0005H\u0002\u00a2\u0006\u0004\bY\u0010\u000fJ\u000f\u0010Z\u001a\u00020\u0005H\u0002\u00a2\u0006\u0004\bZ\u0010\u000fJ\u000f\u0010[\u001a\u00020\u0005H\u0002\u00a2\u0006\u0004\b[\u0010\u000fJ\u000f\u0010\\\u001a\u00020\u0005H\u0002\u00a2\u0006\u0004\b\\\u0010\u000fJ\u000f\u0010]\u001a\u00020\u0005H\u0002\u00a2\u0006\u0004\b]\u0010\u000fJ\u000f\u0010^\u001a\u00020\u0005H\u0002\u00a2\u0006\u0004\b^\u0010\u000fJ\u000f\u0010_\u001a\u00020\u0005H\u0002\u00a2\u0006\u0004\b_\u0010\u000fJ\u000f\u0010`\u001a\u00020\u0005H\u0002\u00a2\u0006\u0004\b`\u0010\u000fJ\u000f\u0010a\u001a\u00020\u0005H\u0002\u00a2\u0006\u0004\ba\u0010\u000fJ\u000f\u0010b\u001a\u00020\u0005H\u0002\u00a2\u0006\u0004\bb\u0010\u000fJ\u000f\u0010c\u001a\u00020\u0005H\u0002\u00a2\u0006\u0004\bc\u0010\u000fJ\u000f\u0010d\u001a\u00020\u0005H\u0002\u00a2\u0006\u0004\bd\u0010\u000fJ\u000f\u0010e\u001a\u00020\u0005H\u0002\u00a2\u0006\u0004\be\u0010\u000fJ\u000f\u0010f\u001a\u00020\u0005H\u0002\u00a2\u0006\u0004\bf\u0010\u000fJ\u000f\u0010g\u001a\u00020\u0005H\u0002\u00a2\u0006\u0004\bg\u0010\u000fJ\u000f\u0010h\u001a\u00020\u0005H\u0002\u00a2\u0006\u0004\bh\u0010\u000fJ\u000f\u0010i\u001a\u00020\u0005H\u0002\u00a2\u0006\u0004\bi\u0010\u000fJ\u000f\u0010j\u001a\u00020\u0005H\u0002\u00a2\u0006\u0004\bj\u0010\u000fJ\u000f\u0010k\u001a\u00020\u0005H\u0002\u00a2\u0006\u0004\bk\u0010\u000fJ\u000f\u0010l\u001a\u00020\u0005H\u0002\u00a2\u0006\u0004\bl\u0010\u000fJ\u000f\u0010m\u001a\u00020\u0005H\u0002\u00a2\u0006\u0004\bm\u0010\u000fJ\u000f\u0010n\u001a\u00020\u0005H\u0002\u00a2\u0006\u0004\bn\u0010\u000fJ\u000f\u0010o\u001a\u00020\u0005H\u0002\u00a2\u0006\u0004\bo\u0010\u000fJ\u000f\u0010p\u001a\u00020\u0005H\u0002\u00a2\u0006\u0004\bp\u0010\u000fJ\u000f\u0010q\u001a\u00020\u0005H\u0002\u00a2\u0006\u0004\bq\u0010\u000fJ\u000f\u0010r\u001a\u00020\u0005H\u0002\u00a2\u0006\u0004\br\u0010\u000fJ\u000f\u0010s\u001a\u00020\u0005H\u0002\u00a2\u0006\u0004\bs\u0010\u000fJ\u000f\u0010t\u001a\u00020\u0005H\u0002\u00a2\u0006\u0004\bt\u0010\u000fJ\u000f\u0010u\u001a\u00020\u0005H\u0002\u00a2\u0006\u0004\bu\u0010\u000fJ\u000f\u0010v\u001a\u00020\u0005H\u0002\u00a2\u0006\u0004\bv\u0010\u000fJ\u000f\u0010w\u001a\u00020\u0005H\u0002\u00a2\u0006\u0004\bw\u0010\u000fJ\u000f\u0010x\u001a\u00020\u0005H\u0002\u00a2\u0006\u0004\bx\u0010\u000fJ\u000f\u0010y\u001a\u00020\u0005H\u0002\u00a2\u0006\u0004\by\u0010\u000fJ\u000f\u0010z\u001a\u00020\u0005H\u0002\u00a2\u0006\u0004\bz\u0010\u000fJ\u000f\u0010{\u001a\u00020\u0005H\u0002\u00a2\u0006\u0004\b{\u0010\u000fJ\u000f\u0010|\u001a\u00020\u0005H\u0002\u00a2\u0006\u0004\b|\u0010\u000fJ\u000f\u0010}\u001a\u00020\u0005H\u0002\u00a2\u0006\u0004\b}\u0010\u000fJ\u000f\u0010~\u001a\u00020\u0005H\u0002\u00a2\u0006\u0004\b~\u0010\u000fJ\u000f\u0010\u007f\u001a\u00020\u0005H\u0002\u00a2\u0006\u0004\b\u007f\u0010\u000fJ\u0011\u0010\u0080\u0001\u001a\u00020\u0005H\u0002\u00a2\u0006\u0005\b\u0080\u0001\u0010\u000fJ\u0011\u0010\u0081\u0001\u001a\u00020\u0005H\u0002\u00a2\u0006\u0005\b\u0081\u0001\u0010\u000fJ\u0011\u0010\u0082\u0001\u001a\u00020\u0005H\u0002\u00a2\u0006\u0005\b\u0082\u0001\u0010\u000fJ\u0011\u0010\u0083\u0001\u001a\u00020\u0005H\u0002\u00a2\u0006\u0005\b\u0083\u0001\u0010\u000fJ\u0011\u0010\u0084\u0001\u001a\u00020\u0005H\u0002\u00a2\u0006\u0005\b\u0084\u0001\u0010\u000f\u00a8\u0006\u0085\u0001"}, d2={"Lrtx/kimiko/utils/render/shaders/core/CoreShaders;", "", "<init>", "()V", "", "", "sources", "", "Lkotlin/jvm/JvmStatic;", "register", "(Ljava/util/Map;)V", "key", "source", "(Ljava/lang/String;)Ljava/lang/String;", "s0", "()Ljava/lang/String;", "s1", "s2", "s3", "s4", "s5", "s6", "s7", "s8", "s9", "s10", "s11", "s12", "s13", "s14", "s15", "s16", "s17", "s18", "s19", "s20", "s21", "s22", "s23", "s24", "s25", "s26", "s27", "s28", "s29", "s30", "s31", "s32", "s33", "s34", "s35", "s36", "s37", "s38", "s39", "s40", "s41", "s42", "s43", "s44", "s45", "s46", "s47", "s48", "s49", "s50", "s51", "s52", "s53", "s54", "s55", "s56", "s57", "s58", "s59", "s60", "s61", "s62", "s63", "s64", "s65", "s66", "s67", "s68", "s69", "s70", "s71", "s72", "s73", "s74", "s75", "s76", "s77", "s78", "s79", "s80", "s81", "s82", "s83", "s84", "s85", "s86", "s87", "s88", "s89", "s90", "s91", "s92", "s93", "s94", "s95", "s96", "s97", "s98", "s99", "s100", "s101", "s102", "s103", "s104", "s105", "s106", "s107", "s108", "s109", "s110", "s111", "s112", "s113", "s114", "s115", "s116", "s117", "rtx.kimiko:kimiko"})
public final class CoreShaders {
    @NotNull
    public static final CoreShaders INSTANCE = new CoreShaders();

    private CoreShaders() {
    }

    @JvmStatic
    public static final void register(@NotNull Map<String, String> sources) {
        Intrinsics.checkNotNullParameter(sources, (String)"sources");
        sources.put("core/arc_divider.glsl", INSTANCE.s0());
        sources.put("core/arc_divider.vsh", INSTANCE.s1());
        sources.put("core/arc_divider.fsh", INSTANCE.s2());
        sources.put("core/arc_rect.glsl", INSTANCE.s3());
        sources.put("core/arc_rect.vsh", INSTANCE.s4());
        sources.put("core/arc_rect.fsh", INSTANCE.s5());
        sources.put("core/baritone_path.glsl", INSTANCE.s6());
        sources.put("core/baritone_path.vsh", INSTANCE.s7());
        sources.put("core/baritone_path.fsh", INSTANCE.s8());
        sources.put("core/batched_blur.glsl", INSTANCE.s9());
        sources.put("core/batched_blur.vsh", INSTANCE.s10());
        sources.put("core/batched_blur.fsh", INSTANCE.s11());
        sources.put("core/checkbox.glsl", INSTANCE.s12());
        sources.put("core/checkbox.vsh", INSTANCE.s13());
        sources.put("core/checkbox.fsh", INSTANCE.s14());
        sources.put("core/circle.glsl", INSTANCE.s15());
        sources.put("core/circle.vsh", INSTANCE.s16());
        sources.put("core/circle.fsh", INSTANCE.s17());
        sources.put("core/circle_adaptive.glsl", INSTANCE.s18());
        sources.put("core/circle_adaptive.vsh", INSTANCE.s19());
        sources.put("core/circle_adaptive.fsh", INSTANCE.s20());
        sources.put("core/effect_icon.glsl", INSTANCE.s21());
        sources.put("core/effect_icon.vsh", INSTANCE.s22());
        sources.put("core/effect_icon.fsh", INSTANCE.s23());
        sources.put("core/glass.glsl", INSTANCE.s24());
        sources.put("core/glass.vsh", INSTANCE.s25());
        sources.put("core/glass.fsh", INSTANCE.s26());
        sources.put("core/image.glsl", INSTANCE.s27());
        sources.put("core/image.vsh", INSTANCE.s28());
        sources.put("core/image.fsh", INSTANCE.s29());
        sources.put("core/item.glsl", INSTANCE.s30());
        sources.put("core/item.vsh", INSTANCE.s31());
        sources.put("core/item.fsh", INSTANCE.s32());
        sources.put("core/kawase_down.glsl", INSTANCE.s33());
        sources.put("core/kawase_down.vsh", INSTANCE.s34());
        sources.put("core/kawase_down.fsh", INSTANCE.s35());
        sources.put("core/kawase_up.glsl", INSTANCE.s36());
        sources.put("core/kawase_up.fsh", INSTANCE.s37());
        sources.put("core/line.glsl", INSTANCE.s38());
        sources.put("core/line.vsh", INSTANCE.s39());
        sources.put("core/line.fsh", INSTANCE.s40());
        sources.put("core/msdf_shimmer.glsl", INSTANCE.s41());
        sources.put("core/msdf_shimmer.vsh", INSTANCE.s42());
        sources.put("core/msdf_shimmer.fsh", INSTANCE.s43());
        sources.put("core/msdf_text.glsl", INSTANCE.s44());
        sources.put("core/msdf_text.vsh", INSTANCE.s45());
        sources.put("core/msdf_text.fsh", INSTANCE.s46());
        sources.put("core/msdf_text_adaptive.glsl", INSTANCE.s47());
        sources.put("core/msdf_text_adaptive.vsh", INSTANCE.s48());
        sources.put("core/msdf_text_adaptive.fsh", INSTANCE.s49());
        sources.put("core/msdf_wave.glsl", INSTANCE.s50());
        sources.put("core/msdf_wave.vsh", INSTANCE.s51());
        sources.put("core/msdf_wave.fsh", INSTANCE.s52());
        sources.put("core/outline_360.glsl", INSTANCE.s53());
        sources.put("core/outline_360.vsh", INSTANCE.s54());
        sources.put("core/outline_360.fsh", INSTANCE.s55());
        sources.put("core/outline_default.glsl", INSTANCE.s56());
        sources.put("core/outline_default.vsh", INSTANCE.s57());
        sources.put("core/outline_default.fsh", INSTANCE.s58());
        sources.put("core/outline_glass.glsl", INSTANCE.s59());
        sources.put("core/outline_glass.vsh", INSTANCE.s60());
        sources.put("core/outline_glass.fsh", INSTANCE.s61());
        sources.put("core/picker.glsl", INSTANCE.s62());
        sources.put("core/picker.vsh", INSTANCE.s63());
        sources.put("core/picker.fsh", INSTANCE.s64());
        sources.put("core/portal_live.glsl", INSTANCE.s65());
        sources.put("core/portal_live.vsh", INSTANCE.s66());
        sources.put("core/portal_live_rim.glsl", INSTANCE.s67());
        sources.put("core/portal_live_rim.fsh", INSTANCE.s68());
        sources.put("core/portal_live_shard.glsl", INSTANCE.s69());
        sources.put("core/portal_live_shard.fsh", INSTANCE.s70());
        sources.put("core/portal_live_shard_rim.glsl", INSTANCE.s71());
        sources.put("core/portal_live_shard_rim.fsh", INSTANCE.s72());
        sources.put("core/portal_live_video.glsl", INSTANCE.s73());
        sources.put("core/portal_live_video.fsh", INSTANCE.s74());
        sources.put("core/portal_live_wait.glsl", INSTANCE.s75());
        sources.put("core/portal_live_wait.fsh", INSTANCE.s76());
        sources.put("core/rect_default.glsl", INSTANCE.s77());
        sources.put("core/rect_default.vsh", INSTANCE.s78());
        sources.put("core/rect_default.fsh", INSTANCE.s79());
        sources.put("core/rect_half_icon.glsl", INSTANCE.s80());
        sources.put("core/rect_half_icon.vsh", INSTANCE.s81());
        sources.put("core/rect_half_icon.fsh", INSTANCE.s82());
        sources.put("core/rect_halftone.glsl", INSTANCE.s83());
        sources.put("core/rect_halftone.vsh", INSTANCE.s84());
        sources.put("core/rect_halftone.fsh", INSTANCE.s85());
        sources.put("core/ripple.glsl", INSTANCE.s86());
        sources.put("core/ripple.vsh", INSTANCE.s87());
        sources.put("core/ripple.fsh", INSTANCE.s88());
        sources.put("core/shimmer.glsl", INSTANCE.s89());
        sources.put("core/shimmer.vsh", INSTANCE.s90());
        sources.put("core/shimmer.fsh", INSTANCE.s91());
        sources.put("core/table_panel.glsl", INSTANCE.s92());
        sources.put("core/table_panel.vsh", INSTANCE.s93());
        sources.put("core/table_panel.fsh", INSTANCE.s94());
        sources.put("core/terrain_wave.glsl", INSTANCE.s95());
        sources.put("core/terrain_wave.vsh", INSTANCE.s96());
        sources.put("core/text_atlas.glsl", INSTANCE.s97());
        sources.put("core/text_atlas.vsh", INSTANCE.s98());
        sources.put("core/text_atlas.fsh", INSTANCE.s99());
        sources.put("core/text_atlas_fade.glsl", INSTANCE.s100());
        sources.put("core/text_atlas_fade.vsh", INSTANCE.s101());
        sources.put("core/text_atlas_fade.fsh", INSTANCE.s102());
        sources.put("core/trail_echo_glass.glsl", INSTANCE.s103());
        sources.put("core/trail_echo_glass.vsh", INSTANCE.s104());
        sources.put("core/trail_echo_glass.fsh", INSTANCE.s105());
        sources.put("core/underhand_rect.glsl", INSTANCE.s106());
        sources.put("core/underhand_rect.vsh", INSTANCE.s107());
        sources.put("core/underhand_rect.fsh", INSTANCE.s108());
        sources.put("core/veil_union.glsl", INSTANCE.s109());
        sources.put("core/veil_union.vsh", INSTANCE.s110());
        sources.put("core/veil_union.fsh", INSTANCE.s111());
        sources.put("core/world_border_fade.glsl", INSTANCE.s112());
        sources.put("core/world_border_fade.vsh", INSTANCE.s113());
        sources.put("core/world_border_fade.fsh", INSTANCE.s114());
        sources.put("core/zippy.glsl", INSTANCE.s115());
        sources.put("core/zippy.vsh", INSTANCE.s116());
        sources.put("core/zippy.fsh", INSTANCE.s117());
    }

    @JvmStatic
    @NotNull
    public static final String source(@NotNull String key) {
        Intrinsics.checkNotNullParameter((Object)key, (String)"key");
        return switch (key) {
            case "core/arc_divider.glsl" -> INSTANCE.s0();
            case "core/arc_divider.vsh" -> INSTANCE.s1();
            case "core/arc_divider.fsh" -> INSTANCE.s2();
            case "core/arc_rect.glsl" -> INSTANCE.s3();
            case "core/arc_rect.vsh" -> INSTANCE.s4();
            case "core/arc_rect.fsh" -> INSTANCE.s5();
            case "core/baritone_path.glsl" -> INSTANCE.s6();
            case "core/baritone_path.vsh" -> INSTANCE.s7();
            case "core/baritone_path.fsh" -> INSTANCE.s8();
            case "core/batched_blur.glsl" -> INSTANCE.s9();
            case "core/batched_blur.vsh" -> INSTANCE.s10();
            case "core/batched_blur.fsh" -> INSTANCE.s11();
            case "core/checkbox.glsl" -> INSTANCE.s12();
            case "core/checkbox.vsh" -> INSTANCE.s13();
            case "core/checkbox.fsh" -> INSTANCE.s14();
            case "core/circle.glsl" -> INSTANCE.s15();
            case "core/circle.vsh" -> INSTANCE.s16();
            case "core/circle.fsh" -> INSTANCE.s17();
            case "core/circle_adaptive.glsl" -> INSTANCE.s18();
            case "core/circle_adaptive.vsh" -> INSTANCE.s19();
            case "core/circle_adaptive.fsh" -> INSTANCE.s20();
            case "core/effect_icon.glsl" -> INSTANCE.s21();
            case "core/effect_icon.vsh" -> INSTANCE.s22();
            case "core/effect_icon.fsh" -> INSTANCE.s23();
            case "core/glass.glsl" -> INSTANCE.s24();
            case "core/glass.vsh" -> INSTANCE.s25();
            case "core/glass.fsh" -> INSTANCE.s26();
            case "core/image.glsl" -> INSTANCE.s27();
            case "core/image.vsh" -> INSTANCE.s28();
            case "core/image.fsh" -> INSTANCE.s29();
            case "core/item.glsl" -> INSTANCE.s30();
            case "core/item.vsh" -> INSTANCE.s31();
            case "core/item.fsh" -> INSTANCE.s32();
            case "core/kawase_down.glsl" -> INSTANCE.s33();
            case "core/kawase_down.vsh" -> INSTANCE.s34();
            case "core/kawase_down.fsh" -> INSTANCE.s35();
            case "core/kawase_up.glsl" -> INSTANCE.s36();
            case "core/kawase_up.fsh" -> INSTANCE.s37();
            case "core/line.glsl" -> INSTANCE.s38();
            case "core/line.vsh" -> INSTANCE.s39();
            case "core/line.fsh" -> INSTANCE.s40();
            case "core/msdf_shimmer.glsl" -> INSTANCE.s41();
            case "core/msdf_shimmer.vsh" -> INSTANCE.s42();
            case "core/msdf_shimmer.fsh" -> INSTANCE.s43();
            case "core/msdf_text.glsl" -> INSTANCE.s44();
            case "core/msdf_text.vsh" -> INSTANCE.s45();
            case "core/msdf_text.fsh" -> INSTANCE.s46();
            case "core/msdf_text_adaptive.glsl" -> INSTANCE.s47();
            case "core/msdf_text_adaptive.vsh" -> INSTANCE.s48();
            case "core/msdf_text_adaptive.fsh" -> INSTANCE.s49();
            case "core/msdf_wave.glsl" -> INSTANCE.s50();
            case "core/msdf_wave.vsh" -> INSTANCE.s51();
            case "core/msdf_wave.fsh" -> INSTANCE.s52();
            case "core/outline_360.glsl" -> INSTANCE.s53();
            case "core/outline_360.vsh" -> INSTANCE.s54();
            case "core/outline_360.fsh" -> INSTANCE.s55();
            case "core/outline_default.glsl" -> INSTANCE.s56();
            case "core/outline_default.vsh" -> INSTANCE.s57();
            case "core/outline_default.fsh" -> INSTANCE.s58();
            case "core/outline_glass.glsl" -> INSTANCE.s59();
            case "core/outline_glass.vsh" -> INSTANCE.s60();
            case "core/outline_glass.fsh" -> INSTANCE.s61();
            case "core/picker.glsl" -> INSTANCE.s62();
            case "core/picker.vsh" -> INSTANCE.s63();
            case "core/picker.fsh" -> INSTANCE.s64();
            case "core/portal_live.glsl" -> INSTANCE.s65();
            case "core/portal_live.vsh" -> INSTANCE.s66();
            case "core/portal_live_rim.glsl" -> INSTANCE.s67();
            case "core/portal_live_rim.fsh" -> INSTANCE.s68();
            case "core/portal_live_shard.glsl" -> INSTANCE.s69();
            case "core/portal_live_shard.fsh" -> INSTANCE.s70();
            case "core/portal_live_shard_rim.glsl" -> INSTANCE.s71();
            case "core/portal_live_shard_rim.fsh" -> INSTANCE.s72();
            case "core/portal_live_video.glsl" -> INSTANCE.s73();
            case "core/portal_live_video.fsh" -> INSTANCE.s74();
            case "core/portal_live_wait.glsl" -> INSTANCE.s75();
            case "core/portal_live_wait.fsh" -> INSTANCE.s76();
            case "core/rect_default.glsl" -> INSTANCE.s77();
            case "core/rect_default.vsh" -> INSTANCE.s78();
            case "core/rect_default.fsh" -> INSTANCE.s79();
            case "core/rect_half_icon.glsl" -> INSTANCE.s80();
            case "core/rect_half_icon.vsh" -> INSTANCE.s81();
            case "core/rect_half_icon.fsh" -> INSTANCE.s82();
            case "core/rect_halftone.glsl" -> INSTANCE.s83();
            case "core/rect_halftone.vsh" -> INSTANCE.s84();
            case "core/rect_halftone.fsh" -> INSTANCE.s85();
            case "core/ripple.glsl" -> INSTANCE.s86();
            case "core/ripple.vsh" -> INSTANCE.s87();
            case "core/ripple.fsh" -> INSTANCE.s88();
            case "core/shimmer.glsl" -> INSTANCE.s89();
            case "core/shimmer.vsh" -> INSTANCE.s90();
            case "core/shimmer.fsh" -> INSTANCE.s91();
            case "core/table_panel.glsl" -> INSTANCE.s92();
            case "core/table_panel.vsh" -> INSTANCE.s93();
            case "core/table_panel.fsh" -> INSTANCE.s94();
            case "core/terrain_wave.glsl" -> INSTANCE.s95();
            case "core/terrain_wave.vsh" -> INSTANCE.s96();
            case "core/text_atlas.glsl" -> INSTANCE.s97();
            case "core/text_atlas.vsh" -> INSTANCE.s98();
            case "core/text_atlas.fsh" -> INSTANCE.s99();
            case "core/text_atlas_fade.glsl" -> INSTANCE.s100();
            case "core/text_atlas_fade.vsh" -> INSTANCE.s101();
            case "core/text_atlas_fade.fsh" -> INSTANCE.s102();
            case "core/trail_echo_glass.glsl" -> INSTANCE.s103();
            case "core/trail_echo_glass.vsh" -> INSTANCE.s104();
            case "core/trail_echo_glass.fsh" -> INSTANCE.s105();
            case "core/underhand_rect.glsl" -> INSTANCE.s106();
            case "core/underhand_rect.vsh" -> INSTANCE.s107();
            case "core/underhand_rect.fsh" -> INSTANCE.s108();
            case "core/veil_union.glsl" -> INSTANCE.s109();
            case "core/veil_union.vsh" -> INSTANCE.s110();
            case "core/veil_union.fsh" -> INSTANCE.s111();
            case "core/world_border_fade.glsl" -> INSTANCE.s112();
            case "core/world_border_fade.vsh" -> INSTANCE.s113();
            case "core/world_border_fade.fsh" -> INSTANCE.s114();
            case "core/zippy.glsl" -> INSTANCE.s115();
            case "core/zippy.vsh" -> INSTANCE.s116();
            case "core/zippy.fsh" -> INSTANCE.s117();
            default -> throw new IllegalArgumentException(key);
        };
    }

    private final String s0() {
        Object[] objectArray = new String[]{"//!vertex", "#version 150", "", "#moj_import <minecraft:dynamictransforms.glsl>", "#moj_import <minecraft:projection.glsl>", "", "in vec3 Position;", "in vec4 Color;", "in float LineWidth;", "", "out vec2 FragCoord;", "flat out int QuadIndex;", "", "void main() {", "    gl_Position = ProjMat * ModelViewMat * vec4(Position, 1.0);", "    FragCoord = Color.rg;", "    QuadIndex = max(int(LineWidth + 0.5) - 1, 0);", "}", "//!fragment", "#version 150", "", "in vec2 FragCoord;", "flat in int QuadIndex;", "", "layout(std140) uniform ArcParamsArray {", "    vec4 params[1024];", "};", "", "out vec4 OutColor;", "", "float parabolaDistance(vec2 p, float k, float halfW) {", "", "    float best = 1e9;", "    const int STEPS = 32;", "    for (int i = 0; i <= STEPS; i++) {", "        float t = float(i) / float(STEPS);", "        float x = mix(-halfW, halfW, t);", "        float y = k * x * x;", "        best = min(best, distance(p, vec2(x, y)));", "    }", "    return best;", "}", "", "void main() {", "    int base = QuadIndex * 2;", "    vec4 geom = params[base];", "    vec4 color = params[base + 1];", "", "    float halfW = max(geom.x, 0.5);", "    float lift = geom.y;", "    float thickness = max(geom.z, 0.5);", "    float feather = max(geom.w, 0.75);", "", "    float padX = thickness + feather;", "    float padY = thickness + feather;", "    float spanX = halfW + padX;", "    float spanY = lift + padY;", "    vec2 local = (FragCoord - vec2(0.5)) * vec2(spanX * 2.0, spanY * 2.0);", "", "    float k = halfW > 0.0 ? lift / (halfW * halfW) : 0.0;", "", "    vec2 p = vec2(local.x, local.y + lift * 0.5);", "    float dist = parabolaDistance(p, k, halfW);", "", "    float half = thickness * 0.5;", "    float alpha = 1.0 - smoothstep(half - feather, half + feather, dist);", "", "    float edge = 1.0 - clamp((abs(local.x) - (halfW - feather * 4.0)) / (feather * 4.0), 0.0, 1.0);", "    alpha *= edge;", "", "    color.a *= alpha;", "    if (color.a <= 0.001) {", "        discard;", "    }", "    OutColor = color;", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }

    private final String s1() {
        Object[] objectArray = new String[]{"#version 150", "", "#moj_import <minecraft:dynamictransforms.glsl>", "#moj_import <minecraft:projection.glsl>", "", "in vec3 Position;", "in vec4 Color;", "in float LineWidth;", "", "out vec2 FragCoord;", "flat out int QuadIndex;", "", "void main() {", "    gl_Position = ProjMat * ModelViewMat * vec4(Position, 1.0);", "    FragCoord = Color.rg;", "    QuadIndex = max(int(LineWidth + 0.5) - 1, 0);", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }

    private final String s2() {
        Object[] objectArray = new String[]{"#version 150", "", "in vec2 FragCoord;", "flat in int QuadIndex;", "", "layout(std140) uniform ArcParamsArray {", "    vec4 params[1024];", "};", "", "out vec4 OutColor;", "", "float parabolaDistance(vec2 p, float k, float halfW) {", "", "    float best = 1e9;", "    const int STEPS = 32;", "    for (int i = 0; i <= STEPS; i++) {", "        float t = float(i) / float(STEPS);", "        float x = mix(-halfW, halfW, t);", "        float y = k * x * x;", "        best = min(best, distance(p, vec2(x, y)));", "    }", "    return best;", "}", "", "void main() {", "    int base = QuadIndex * 2;", "    vec4 geom = params[base];", "    vec4 color = params[base + 1];", "", "    float halfW = max(geom.x, 0.5);", "    float lift = geom.y;", "    float thickness = max(geom.z, 0.5);", "    float feather = max(geom.w, 0.75);", "", "    float padX = thickness + feather;", "    float padY = thickness + feather;", "    float spanX = halfW + padX;", "    float spanY = lift + padY;", "    vec2 local = (FragCoord - vec2(0.5)) * vec2(spanX * 2.0, spanY * 2.0);", "", "    float k = halfW > 0.0 ? lift / (halfW * halfW) : 0.0;", "", "    vec2 p = vec2(local.x, local.y + lift * 0.5);", "    float dist = parabolaDistance(p, k, halfW);", "", "    float half = thickness * 0.5;", "    float alpha = 1.0 - smoothstep(half - feather, half + feather, dist);", "", "    float edge = 1.0 - clamp((abs(local.x) - (halfW - feather * 4.0)) / (feather * 4.0), 0.0, 1.0);", "    alpha *= edge;", "", "    color.a *= alpha;", "    if (color.a <= 0.001) {", "        discard;", "    }", "    OutColor = color;", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }

    private final String s3() {
        Object[] objectArray = new String[]{"//!vertex", "#version 150", "", "#moj_import <minecraft:dynamictransforms.glsl>", "#moj_import <minecraft:projection.glsl>", "", "in vec3 Position;", "in vec4 Color;", "in float LineWidth;", "", "out vec2 FragCoord;", "flat out int QuadIndex;", "", "void main() {", "    gl_Position = ProjMat * ModelViewMat * vec4(Position, 1.0);", "    FragCoord = Color.rg;", "    QuadIndex = max(int(LineWidth + 0.5) - 1, 0);", "}", "//!fragment", "#version 150", "", "in vec2 FragCoord;", "flat in int QuadIndex;", "", "layout(std140) uniform ArcRectParamsArray {", "    vec4 params[1536];", "};", "", "out vec4 OutColor;", "", "const float TAU = 6.28318530718;", "", "vec4 stopColor(vec4 a, vec4 b, vec4 c, vec4 d, float t) {", "    float f = clamp(t, 0.0, 1.0) * 3.0;", "    vec4 ab = mix(a, b, clamp(f, 0.0, 1.0));", "    vec4 bc = mix(ab, c, clamp(f - 1.0, 0.0, 1.0));", "    return mix(bc, d, clamp(f - 2.0, 0.0, 1.0));", "}", "", "void main() {", "    int base = QuadIndex * 6;", "    vec4 shape = params[base];", "    vec4 angles = params[base + 1];", "", "    float radius = max(shape.x, 0.0);", "    float halfThickness = max(shape.y, 0.05) * 0.5;", "    float feather = max(shape.z, 0.35);", "    float capRound = shape.w;", "", "    float start = angles.x;", "    float sweep = clamp(angles.y, 0.0, TAU);", "    float gradientShift = angles.z;", "    float extent = radius + halfThickness + feather;", "", "    vec2 coord = clamp(FragCoord, vec2(0.0), vec2(1.0));", "    vec2 local = (coord - vec2(0.5)) * (extent * 2.0);", "", "    float dist = length(local);", "    float radial = abs(dist - radius);", "", "    float angle = atan(local.x, -local.y);", "    float delta = mod(angle - start, TAU);", "", "    float full = step(TAU - 0.0005, sweep);", "    float outside = max(0.0, min(delta - sweep, TAU - delta)) * radius * (1.0 - full);", "", "    float squared = max(radial, outside);", "    float rounded = length(vec2(radial, outside));", "    float sd = mix(squared, rounded, step(0.5, capRound));", "", "    float alpha = 1.0 - smoothstep(halfThickness - feather, halfThickness + feather, sd);", "    if (alpha <= 0.001) {", "        discard;", "    }", "", "    float span = mix(clamp(delta / max(sweep, 0.0001), 0.0, 1.0), delta / TAU, full);", "    float t = gradientShift == 0.0 ? span : fract(span + gradientShift);", "", "    vec4 color = stopColor(params[base + 2], params[base + 3], params[base + 4], params[base + 5], t);", "    color.a *= alpha;", "    if (color.a <= 0.001) {", "        discard;", "    }", "", "    OutColor = color;", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }

    private final String s4() {
        Object[] objectArray = new String[]{"#version 150", "", "#moj_import <minecraft:dynamictransforms.glsl>", "#moj_import <minecraft:projection.glsl>", "", "in vec3 Position;", "in vec4 Color;", "in float LineWidth;", "", "out vec2 FragCoord;", "flat out int QuadIndex;", "", "void main() {", "    gl_Position = ProjMat * ModelViewMat * vec4(Position, 1.0);", "    FragCoord = Color.rg;", "    QuadIndex = max(int(LineWidth + 0.5) - 1, 0);", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }

    private final String s5() {
        Object[] objectArray = new String[]{"#version 150", "", "in vec2 FragCoord;", "flat in int QuadIndex;", "", "layout(std140) uniform ArcRectParamsArray {", "    vec4 params[1536];", "};", "", "out vec4 OutColor;", "", "const float TAU = 6.28318530718;", "", "vec4 stopColor(vec4 a, vec4 b, vec4 c, vec4 d, float t) {", "    float f = clamp(t, 0.0, 1.0) * 3.0;", "    vec4 ab = mix(a, b, clamp(f, 0.0, 1.0));", "    vec4 bc = mix(ab, c, clamp(f - 1.0, 0.0, 1.0));", "    return mix(bc, d, clamp(f - 2.0, 0.0, 1.0));", "}", "", "void main() {", "    int base = QuadIndex * 6;", "    vec4 shape = params[base];", "    vec4 angles = params[base + 1];", "", "    float radius = max(shape.x, 0.0);", "    float halfThickness = max(shape.y, 0.05) * 0.5;", "    float feather = max(shape.z, 0.35);", "    float capRound = shape.w;", "", "    float start = angles.x;", "    float sweep = clamp(angles.y, 0.0, TAU);", "    float gradientShift = angles.z;", "    float extent = radius + halfThickness + feather;", "", "    vec2 coord = clamp(FragCoord, vec2(0.0), vec2(1.0));", "    vec2 local = (coord - vec2(0.5)) * (extent * 2.0);", "", "    float dist = length(local);", "    float radial = abs(dist - radius);", "", "    float angle = atan(local.x, -local.y);", "    float delta = mod(angle - start, TAU);", "", "    float full = step(TAU - 0.0005, sweep);", "    float outside = max(0.0, min(delta - sweep, TAU - delta)) * radius * (1.0 - full);", "", "    float squared = max(radial, outside);", "    float rounded = length(vec2(radial, outside));", "    float sd = mix(squared, rounded, step(0.5, capRound));", "", "    float alpha = 1.0 - smoothstep(halfThickness - feather, halfThickness + feather, sd);", "    if (alpha <= 0.001) {", "        discard;", "    }", "", "    float span = mix(clamp(delta / max(sweep, 0.0001), 0.0, 1.0), delta / TAU, full);", "    float t = gradientShift == 0.0 ? span : fract(span + gradientShift);", "", "    vec4 color = stopColor(params[base + 2], params[base + 3], params[base + 4], params[base + 5], t);", "    color.a *= alpha;", "    if (color.a <= 0.001) {", "        discard;", "    }", "", "    OutColor = color;", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }

    private final String s6() {
        Object[] objectArray = new String[]{"//!vertex", "#version 330", "", "#moj_import <minecraft:fog.glsl>", "#moj_import <minecraft:globals.glsl>", "#moj_import <minecraft:dynamictransforms.glsl>", "#moj_import <minecraft:projection.glsl>", "", "in vec3 Position;", "in vec4 Color;", "", "out float sphericalVertexDistance;", "out float cylindricalVertexDistance;", "out vec4 vertexColor;", "out float pathPhase;", "", "void main() {", "    gl_Position = ProjMat * ModelViewMat * vec4(Position, 1.0);", "    sphericalVertexDistance = fog_spherical_distance(Position);", "    cylindricalVertexDistance = fog_cylindrical_distance(Position);", "    vertexColor = Color;", "    pathPhase = dot(Position, vec3(0.041, 0.029, 0.037)) + GameTime * 18.0;", "}", "//!fragment", "#version 330", "", "#moj_import <minecraft:fog.glsl>", "#moj_import <minecraft:dynamictransforms.glsl>", "", "in float sphericalVertexDistance;", "in float cylindricalVertexDistance;", "in vec4 vertexColor;", "in float pathPhase;", "", "out vec4 fragColor;", "", "void main() {", "    vec4 color = vertexColor * ColorModulator;", "    float pulse = 0.985 + sin(pathPhase * 6.2831853) * 0.015;", "    float smokeA = sin(gl_FragCoord.x * 0.071 + pathPhase * 2.1);", "    float smokeB = sin(gl_FragCoord.y * 0.053 - pathPhase * 1.7);", "    float smoke = 0.94 + smokeA * smokeB * 0.06;", "    float luma = dot(color.rgb, vec3(0.299, 0.587, 0.114));", "    vec3 saturated = mix(vec3(luma), color.rgb, 1.06);", "    float energy = smoothstep(0.0, 0.8, color.a) * pulse * smoke;", "    vec3 lit = saturated * (0.84 + energy * 0.12);", "    vec4 shaded = vec4(clamp(lit, 0.0, 1.0), clamp(color.a * pulse * smoke, 0.0, 1.0));", "", "    fragColor = apply_fog(shaded, sphericalVertexDistance, cylindricalVertexDistance, FogEnvironmentalStart, FogEnvironmentalEnd, FogRenderDistanceStart, FogRenderDistanceEnd, FogColor);", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }

    private final String s7() {
        Object[] objectArray = new String[]{"#version 330", "", "#moj_import <minecraft:fog.glsl>", "#moj_import <minecraft:globals.glsl>", "#moj_import <minecraft:dynamictransforms.glsl>", "#moj_import <minecraft:projection.glsl>", "", "in vec3 Position;", "in vec4 Color;", "", "out float sphericalVertexDistance;", "out float cylindricalVertexDistance;", "out vec4 vertexColor;", "out float pathPhase;", "", "void main() {", "    gl_Position = ProjMat * ModelViewMat * vec4(Position, 1.0);", "    sphericalVertexDistance = fog_spherical_distance(Position);", "    cylindricalVertexDistance = fog_cylindrical_distance(Position);", "    vertexColor = Color;", "    pathPhase = dot(Position, vec3(0.041, 0.029, 0.037)) + GameTime * 18.0;", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }

    private final String s8() {
        Object[] objectArray = new String[]{"#version 330", "", "#moj_import <minecraft:fog.glsl>", "#moj_import <minecraft:dynamictransforms.glsl>", "", "in float sphericalVertexDistance;", "in float cylindricalVertexDistance;", "in vec4 vertexColor;", "in float pathPhase;", "", "out vec4 fragColor;", "", "void main() {", "    vec4 color = vertexColor * ColorModulator;", "    float pulse = 0.985 + sin(pathPhase * 6.2831853) * 0.015;", "    float smokeA = sin(gl_FragCoord.x * 0.071 + pathPhase * 2.1);", "    float smokeB = sin(gl_FragCoord.y * 0.053 - pathPhase * 1.7);", "    float smoke = 0.94 + smokeA * smokeB * 0.06;", "    float luma = dot(color.rgb, vec3(0.299, 0.587, 0.114));", "    vec3 saturated = mix(vec3(luma), color.rgb, 1.06);", "    float energy = smoothstep(0.0, 0.8, color.a) * pulse * smoke;", "    vec3 lit = saturated * (0.84 + energy * 0.12);", "    vec4 shaded = vec4(clamp(lit, 0.0, 1.0), clamp(color.a * pulse * smoke, 0.0, 1.0));", "", "    fragColor = apply_fog(shaded, sphericalVertexDistance, cylindricalVertexDistance, FogEnvironmentalStart, FogEnvironmentalEnd, FogRenderDistanceStart, FogRenderDistanceEnd, FogColor);", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }

    private final String s9() {
        Object[] objectArray = new String[]{"//!vertex", "#version 150", "", "#moj_import <minecraft:dynamictransforms.glsl>", "#moj_import <minecraft:projection.glsl>", "", "in vec3 Position;", "in vec2 UV0;", "in vec4 Color;", "in float LineWidth;", "", "out vec2 FragCoord;", "out vec4 FragColor;", "flat out int QuadIndex;", "", "void main() {", "    gl_Position = ProjMat * ModelViewMat * vec4(Position, 1.0);", "    FragCoord = UV0;", "    FragColor = Color;", "    QuadIndex = max(int(LineWidth + 0.5) - 1, 0);", "}", "//!fragment", "#version 150", "", "#moj_import <kimiko:common.glsl>", "", "in vec2 FragCoord;", "in vec4 FragColor;", "flat in int QuadIndex;", "", "uniform sampler2D Sampler0;", "", "layout(std140) uniform BlurParamsArray {", "    vec4 params[2048];", "};", "", "layout(std140) uniform BlurRegion {", "    vec4 Region;", "};", "", "out vec4 OutColor;", "", "void main() {", "    int idx = QuadIndex;", "    vec4 Radius = params[idx * 2];", "    vec4 sizeSmooth = params[idx * 2 + 1];", "    vec2 Size = sizeSmooth.xy;", "    float Smoothness = sizeSmooth.z;", "", "    vec2 texCoord = clamp((gl_FragCoord.xy - Region.xy) / max(Region.zw, vec2(1.0)), vec2(0.0), vec2(1.0));", "    vec3 blurred = texture(Sampler0, texCoord).rgb;", "", "    float tintStrength = clamp(FragColor.a * (1.0 - dot(FragColor.rgb, vec3(0.299, 0.587, 0.114))) * 0.72, 0.0, 0.82);", "    vec4 color = vec4(mix(blurred, FragColor.rgb, tintStrength), FragColor.a);", "    color.a *= ralpha(Size, FragCoord, Radius, Smoothness);", "", "    if (color.a == 0.0) { discard; }", "    OutColor = color;", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }

    private final String s10() {
        Object[] objectArray = new String[]{"#version 150", "", "#moj_import <minecraft:dynamictransforms.glsl>", "#moj_import <minecraft:projection.glsl>", "", "in vec3 Position;", "in vec2 UV0;", "in vec4 Color;", "in float LineWidth;", "", "out vec2 FragCoord;", "out vec4 FragColor;", "flat out int QuadIndex;", "", "void main() {", "    gl_Position = ProjMat * ModelViewMat * vec4(Position, 1.0);", "    FragCoord = UV0;", "    FragColor = Color;", "    QuadIndex = max(int(LineWidth + 0.5) - 1, 0);", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }

    private final String s11() {
        Object[] objectArray = new String[]{"#version 150", "", "#moj_import <kimiko:common.glsl>", "", "in vec2 FragCoord;", "in vec4 FragColor;", "flat in int QuadIndex;", "", "uniform sampler2D Sampler0;", "", "layout(std140) uniform BlurParamsArray {", "    vec4 params[2048];", "};", "", "layout(std140) uniform BlurRegion {", "    vec4 Region;", "};", "", "out vec4 OutColor;", "", "void main() {", "    int idx = QuadIndex;", "    vec4 Radius = params[idx * 2];", "    vec4 sizeSmooth = params[idx * 2 + 1];", "    vec2 Size = sizeSmooth.xy;", "    float Smoothness = sizeSmooth.z;", "", "    vec2 texCoord = clamp((gl_FragCoord.xy - Region.xy) / max(Region.zw, vec2(1.0)), vec2(0.0), vec2(1.0));", "    vec3 blurred = texture(Sampler0, texCoord).rgb;", "", "    float tintStrength = clamp(FragColor.a * (1.0 - dot(FragColor.rgb, vec3(0.299, 0.587, 0.114))) * 0.72, 0.0, 0.82);", "    vec4 color = vec4(mix(blurred, FragColor.rgb, tintStrength), FragColor.a);", "    color.a *= ralpha(Size, FragCoord, Radius, Smoothness);", "", "    if (color.a == 0.0) { discard; }", "    OutColor = color;", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }

    private final String s12() {
        Object[] objectArray = new String[]{"//!vertex", "#version 150", "", "#moj_import <minecraft:dynamictransforms.glsl>", "#moj_import <minecraft:projection.glsl>", "", "in vec3 Position;", "", "out vec2 FragCoord;", "flat out int QuadIndex;", "", "const vec2[4] RECT_VERTICES_COORDS = vec2[] (", "    vec2(0.0, 0.0),", "    vec2(0.0, 1.0),", "    vec2(1.0, 1.0),", "    vec2(1.0, 0.0)", ");", "", "void main() {", "    gl_Position = ProjMat * ModelViewMat * vec4(Position, 1.0);", "    FragCoord = RECT_VERTICES_COORDS[gl_VertexID % 4];", "    QuadIndex = gl_VertexID / 4;", "}", "//!fragment", "#version 150", "", "#moj_import <kimiko:common.glsl>", "", "in vec2 FragCoord;", "flat in int QuadIndex;", "", "layout(std140) uniform CheckboxParamsArray {", "    vec4 params[1536];", "};", "", "out vec4 OutColor;", "", "float segmentDistance(vec2 p, vec2 a, vec2 b) {", "    vec2 pa = p - a;", "    vec2 ba = b - a;", "    float h = clamp(dot(pa, ba) / max(dot(ba, ba), 0.0001), 0.0, 1.0);", "    return length(pa - ba * h);", "}", "", "void main() {", "    int base = QuadIndex * 3;", "    vec4 shape = params[base];", "    vec4 offColor = params[base + 1];", "    vec4 onColor = params[base + 2];", "", "    vec2 coord = clamp(FragCoord, vec2(0.0), vec2(1.0));", "    vec2 size = max(shape.yz, vec2(1.0));", "    float progress = clamp(shape.w, 0.0, 1.0);", "    float alpha = ralpha(size, coord, vec4(max(shape.x, 0.0)), 1.0);", "    vec4 color = mix(offColor, onColor, progress);", "", "    vec2 p = coord * size;", "    float stroke = max(1.0, min(size.x, size.y) * 0.12);", "    float first = segmentDistance(p, size * vec2(0.27, 0.53), size * vec2(0.42, 0.68));", "    float second = segmentDistance(p, size * vec2(0.42, 0.68), size * vec2(0.74, 0.34));", "    float check = (1.0 - smoothstep(stroke, stroke + 0.85, min(first, second))) * smoothstep(0.08, 1.0, progress);", "", "    color.a *= alpha * (1.0 - check);", "", "    if (color.a <= 0.001) {", "        discard;", "    }", "", "    OutColor = color;", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }

    private final String s13() {
        Object[] objectArray = new String[]{"#version 150", "", "#moj_import <minecraft:dynamictransforms.glsl>", "#moj_import <minecraft:projection.glsl>", "", "in vec3 Position;", "", "out vec2 FragCoord;", "flat out int QuadIndex;", "", "const vec2[4] RECT_VERTICES_COORDS = vec2[] (", "    vec2(0.0, 0.0),", "    vec2(0.0, 1.0),", "    vec2(1.0, 1.0),", "    vec2(1.0, 0.0)", ");", "", "void main() {", "    gl_Position = ProjMat * ModelViewMat * vec4(Position, 1.0);", "    FragCoord = RECT_VERTICES_COORDS[gl_VertexID % 4];", "    QuadIndex = gl_VertexID / 4;", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }

    private final String s14() {
        Object[] objectArray = new String[]{"#version 150", "", "#moj_import <kimiko:common.glsl>", "", "in vec2 FragCoord;", "flat in int QuadIndex;", "", "layout(std140) uniform CheckboxParamsArray {", "    vec4 params[1536];", "};", "", "out vec4 OutColor;", "", "float segmentDistance(vec2 p, vec2 a, vec2 b) {", "    vec2 pa = p - a;", "    vec2 ba = b - a;", "    float h = clamp(dot(pa, ba) / max(dot(ba, ba), 0.0001), 0.0, 1.0);", "    return length(pa - ba * h);", "}", "", "void main() {", "    int base = QuadIndex * 3;", "    vec4 shape = params[base];", "    vec4 offColor = params[base + 1];", "    vec4 onColor = params[base + 2];", "", "    vec2 coord = clamp(FragCoord, vec2(0.0), vec2(1.0));", "    vec2 size = max(shape.yz, vec2(1.0));", "    float progress = clamp(shape.w, 0.0, 1.0);", "    float alpha = ralpha(size, coord, vec4(max(shape.x, 0.0)), 1.0);", "    vec4 color = mix(offColor, onColor, progress);", "", "    vec2 p = coord * size;", "    float stroke = max(1.0, min(size.x, size.y) * 0.12);", "    float first = segmentDistance(p, size * vec2(0.27, 0.53), size * vec2(0.42, 0.68));", "    float second = segmentDistance(p, size * vec2(0.42, 0.68), size * vec2(0.74, 0.34));", "    float check = (1.0 - smoothstep(stroke, stroke + 0.85, min(first, second))) * smoothstep(0.08, 1.0, progress);", "", "    color.a *= alpha * (1.0 - check);", "", "    if (color.a <= 0.001) {", "        discard;", "    }", "", "    OutColor = color;", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }

    private final String s15() {
        Object[] objectArray = new String[]{"//!vertex", "#version 150", "", "#moj_import <minecraft:dynamictransforms.glsl>", "#moj_import <minecraft:projection.glsl>", "", "in vec3 Position;", "in vec4 Color;", "in float LineWidth;", "", "out vec2 FragCoord;", "flat out int QuadIndex;", "", "void main() {", "    gl_Position = ProjMat * ModelViewMat * vec4(Position, 1.0);", "    FragCoord = Color.rg;", "    QuadIndex = max(int(LineWidth + 0.5) - 1, 0);", "}", "//!fragment", "#version 150", "", "in vec2 FragCoord;", "flat in int QuadIndex;", "", "layout(std140) uniform CircleParamsArray {", "    vec4 params[1024];", "};", "", "out vec4 OutColor;", "", "void main() {", "    int base = QuadIndex * 2;", "    vec4 shape = params[base];", "    vec4 color = params[base + 1];", "", "    float radius = max(shape.x, 0.0);", "    float feather = max(shape.y, 0.5);", "    float thickness = max(shape.z, 0.0);", "    float extent = radius + feather;", "", "    vec2 coord = clamp(FragCoord, vec2(0.0), vec2(1.0));", "    vec2 local = (coord - vec2(0.5)) * (extent * 2.0);", "    float dist = length(local);", "    float outer = 1.0 - smoothstep(radius - feather, radius + feather, dist);", "    float alpha = outer;", "", "    if (thickness > 0.0) {", "        float innerRadius = max(radius - thickness, 0.0);", "        float inner = 1.0 - smoothstep(innerRadius - feather, innerRadius + feather, dist);", "        alpha = clamp(outer - inner, 0.0, 1.0);", "    }", "", "    color.a *= alpha;", "    if (color.a <= 0.001) {", "        discard;", "    }", "", "    OutColor = color;", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }

    private final String s16() {
        Object[] objectArray = new String[]{"#version 150", "", "#moj_import <minecraft:dynamictransforms.glsl>", "#moj_import <minecraft:projection.glsl>", "", "in vec3 Position;", "in vec4 Color;", "in float LineWidth;", "", "out vec2 FragCoord;", "flat out int QuadIndex;", "", "void main() {", "    gl_Position = ProjMat * ModelViewMat * vec4(Position, 1.0);", "    FragCoord = Color.rg;", "    QuadIndex = max(int(LineWidth + 0.5) - 1, 0);", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }

    private final String s17() {
        Object[] objectArray = new String[]{"#version 150", "", "in vec2 FragCoord;", "flat in int QuadIndex;", "", "layout(std140) uniform CircleParamsArray {", "    vec4 params[1024];", "};", "", "out vec4 OutColor;", "", "void main() {", "    int base = QuadIndex * 2;", "    vec4 shape = params[base];", "    vec4 color = params[base + 1];", "", "    float radius = max(shape.x, 0.0);", "    float feather = max(shape.y, 0.5);", "    float thickness = max(shape.z, 0.0);", "    float extent = radius + feather;", "", "    vec2 coord = clamp(FragCoord, vec2(0.0), vec2(1.0));", "    vec2 local = (coord - vec2(0.5)) * (extent * 2.0);", "    float dist = length(local);", "    float outer = 1.0 - smoothstep(radius - feather, radius + feather, dist);", "    float alpha = outer;", "", "    if (thickness > 0.0) {", "        float innerRadius = max(radius - thickness, 0.0);", "        float inner = 1.0 - smoothstep(innerRadius - feather, innerRadius + feather, dist);", "        alpha = clamp(outer - inner, 0.0, 1.0);", "    }", "", "    color.a *= alpha;", "    if (color.a <= 0.001) {", "        discard;", "    }", "", "    OutColor = color;", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }

    private final String s18() {
        Object[] objectArray = new String[]{"//!vertex", "#version 150", "", "#moj_import <minecraft:dynamictransforms.glsl>", "#moj_import <minecraft:projection.glsl>", "", "in vec3 Position;", "in vec4 Color;", "in float LineWidth;", "", "out vec2 FragCoord;", "flat out int QuadIndex;", "", "void main() {", "    gl_Position = ProjMat * ModelViewMat * vec4(Position, 1.0);", "    FragCoord = Color.rg;", "    QuadIndex = max(int(LineWidth + 0.5) - 1, 0);", "}", "", "//!fragment", "#version 150", "", "#moj_import <minecraft:dynamictransforms.glsl>", "", "uniform sampler2D Sampler0;", "", "in vec2 FragCoord;", "flat in int QuadIndex;", "", "layout(std140) uniform CircleParamsArray {", "    vec4 params[1024];", "};", "", "out vec4 OutColor;", "", "vec3 backdropAt(vec2 uv) {", "    vec2 texel = 1.0 / max(vec2(textureSize(Sampler0, 0)), vec2(1.0));", "    vec3 color = texture(Sampler0, uv).rgb * 0.40;", "    color += texture(Sampler0, clamp(uv + vec2(texel.x, 0.0), 0.0, 1.0)).rgb * 0.15;", "    color += texture(Sampler0, clamp(uv - vec2(texel.x, 0.0), 0.0, 1.0)).rgb * 0.15;", "    color += texture(Sampler0, clamp(uv + vec2(0.0, texel.y), 0.0, 1.0)).rgb * 0.15;", "    color += texture(Sampler0, clamp(uv - vec2(0.0, texel.y), 0.0, 1.0)).rgb * 0.15;", "    return color;", "}", "", "void main() {", "    int base = QuadIndex * 2;", "    vec4 shape = params[base];", "    vec4 color = params[base + 1];", "", "    float radius = max(shape.x, 0.0);", "    float feather = max(shape.y, 0.5);", "    float thickness = max(shape.z, 0.0);", "    float extent = radius + feather;", "", "    vec2 coord = clamp(FragCoord, vec2(0.0), vec2(1.0));", "    vec2 local = (coord - vec2(0.5)) * (extent * 2.0);", "    float dist = length(local);", "    float outer = 1.0 - smoothstep(radius - feather, radius + feather, dist);", "    float alpha = outer;", "", "    if (thickness > 0.0) {", "        float innerRadius = max(radius - thickness, 0.0);", "        float inner = 1.0 - smoothstep(innerRadius - feather, innerRadius + feather, dist);", "        alpha = clamp(outer - inner, 0.0, 1.0);", "    }", "", "    color.a *= alpha;", "    if (color.a <= 0.001) {", "        discard;", "    }", "", "    vec2 sceneSize = max(vec2(textureSize(Sampler0, 0)) * 2.0, vec2(1.0));", "    vec2 sceneUv = clamp(gl_FragCoord.xy / sceneSize, vec2(0.0), vec2(1.0));", "    float luminance = dot(backdropAt(sceneUv), vec3(0.2126, 0.7152, 0.0722));", "    float gray = clamp(1.0 - luminance * 1.35, 0.30, 1.0);", "    OutColor = vec4(vec3(gray), color.a) * ColorModulator;", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }

    private final String s19() {
        Object[] objectArray = new String[]{"#version 150", "", "#moj_import <minecraft:dynamictransforms.glsl>", "#moj_import <minecraft:projection.glsl>", "", "in vec3 Position;", "in vec4 Color;", "in float LineWidth;", "", "out vec2 FragCoord;", "flat out int QuadIndex;", "", "void main() {", "    gl_Position = ProjMat * ModelViewMat * vec4(Position, 1.0);", "    FragCoord = Color.rg;", "    QuadIndex = max(int(LineWidth + 0.5) - 1, 0);", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }

    private final String s20() {
        Object[] objectArray = new String[]{"#version 150", "", "#moj_import <minecraft:dynamictransforms.glsl>", "", "uniform sampler2D Sampler0;", "", "in vec2 FragCoord;", "flat in int QuadIndex;", "", "layout(std140) uniform CircleParamsArray {", "    vec4 params[1024];", "};", "", "out vec4 OutColor;", "", "vec3 backdropAt(vec2 uv) {", "    vec2 texel = 1.0 / max(vec2(textureSize(Sampler0, 0)), vec2(1.0));", "    vec3 color = texture(Sampler0, uv).rgb * 0.40;", "    color += texture(Sampler0, clamp(uv + vec2(texel.x, 0.0), 0.0, 1.0)).rgb * 0.15;", "    color += texture(Sampler0, clamp(uv - vec2(texel.x, 0.0), 0.0, 1.0)).rgb * 0.15;", "    color += texture(Sampler0, clamp(uv + vec2(0.0, texel.y), 0.0, 1.0)).rgb * 0.15;", "    color += texture(Sampler0, clamp(uv - vec2(0.0, texel.y), 0.0, 1.0)).rgb * 0.15;", "    return color;", "}", "", "void main() {", "    int base = QuadIndex * 2;", "    vec4 shape = params[base];", "    vec4 color = params[base + 1];", "", "    float radius = max(shape.x, 0.0);", "    float feather = max(shape.y, 0.5);", "    float thickness = max(shape.z, 0.0);", "    float extent = radius + feather;", "", "    vec2 coord = clamp(FragCoord, vec2(0.0), vec2(1.0));", "    vec2 local = (coord - vec2(0.5)) * (extent * 2.0);", "    float dist = length(local);", "    float outer = 1.0 - smoothstep(radius - feather, radius + feather, dist);", "    float alpha = outer;", "", "    if (thickness > 0.0) {", "        float innerRadius = max(radius - thickness, 0.0);", "        float inner = 1.0 - smoothstep(innerRadius - feather, innerRadius + feather, dist);", "        alpha = clamp(outer - inner, 0.0, 1.0);", "    }", "", "    color.a *= alpha;", "    if (color.a <= 0.001) {", "        discard;", "    }", "", "    vec2 sceneSize = max(vec2(textureSize(Sampler0, 0)) * 2.0, vec2(1.0));", "    vec2 sceneUv = clamp(gl_FragCoord.xy / sceneSize, vec2(0.0), vec2(1.0));", "    float luminance = dot(backdropAt(sceneUv), vec3(0.2126, 0.7152, 0.0722));", "    float gray = clamp(1.0 - luminance * 1.35, 0.30, 1.0);", "    OutColor = vec4(vec3(gray), color.a) * ColorModulator;", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }

    private final String s21() {
        Object[] objectArray = new String[]{"//!vertex", "#version 150", "", "#moj_import <minecraft:dynamictransforms.glsl>", "#moj_import <minecraft:projection.glsl>", "", "in vec3 Position;", "in vec2 UV0;", "in vec4 Color;", "", "out vec2 TexCoord;", "out vec4 FragColor;", "", "void main() {", "    gl_Position = ProjMat * ModelViewMat * vec4(Position, 1.0);", "    TexCoord = UV0;", "    FragColor = Color;", "}", "//!fragment", "#version 150", "", "#moj_import <minecraft:dynamictransforms.glsl>", "", "uniform sampler2D Sampler0;", "", "in vec2 TexCoord;", "in vec4 FragColor;", "", "out vec4 OutColor;", "", "void main() {", "    vec4 color = texture(Sampler0, TexCoord) * FragColor;", "    if (color.a <= 0.001) {", "        discard;", "    }", "", "    OutColor = color * ColorModulator;", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }

    private final String s22() {
        Object[] objectArray = new String[]{"#version 150", "", "#moj_import <minecraft:dynamictransforms.glsl>", "#moj_import <minecraft:projection.glsl>", "", "in vec3 Position;", "in vec2 UV0;", "in vec4 Color;", "", "out vec2 TexCoord;", "out vec4 FragColor;", "", "void main() {", "    gl_Position = ProjMat * ModelViewMat * vec4(Position, 1.0);", "    TexCoord = UV0;", "    FragColor = Color;", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }

    private final String s23() {
        Object[] objectArray = new String[]{"#version 150", "", "#moj_import <minecraft:dynamictransforms.glsl>", "", "uniform sampler2D Sampler0;", "", "in vec2 TexCoord;", "in vec4 FragColor;", "", "out vec4 OutColor;", "", "void main() {", "    vec4 color = texture(Sampler0, TexCoord) * FragColor;", "    if (color.a <= 0.001) {", "        discard;", "    }", "", "    OutColor = color * ColorModulator;", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }

    private final String s24() {
        Object[] objectArray = new String[]{"//!vertex", "#version 150", "", "#moj_import <minecraft:dynamictransforms.glsl>", "#moj_import <minecraft:projection.glsl>", "", "in vec3 Position;", "in vec4 Color;", "in float LineWidth;", "", "out vec2 FragCoord;", "flat out int QuadIndex;", "", "layout(std140) uniform GlassParamsArray {", "    vec4 params[2560];", "};", "", "void main() {", "    int index = max(int(LineWidth + 0.5) - 1, 0);", "    int base = index * 5;", "    vec4 flagsDistortZ = params[base + 4];", "", "    gl_Position = ProjMat * ModelViewMat * vec4(Position.xy, flagsDistortZ.z, 1.0);", "    FragCoord = Color.rg;", "    QuadIndex = index;", "}", "//!fragment", "#version 150", "", "#moj_import <kimiko:common.glsl>", "", "in vec2 FragCoord;", "flat in int QuadIndex;", "", "uniform sampler2D Sampler0;", "", "layout(std140) uniform GlassParamsArray {", "    vec4 params[2560];", "};", "", "layout(std140) uniform BlurRegion {", "    vec4 Region;", "};", "", "out vec4 OutColor;", "", "void main() {", "    int base = QuadIndex * 5;", "    vec4 radius = max(params[base], vec4(0.0));", "    vec4 sizeSmoothCorner = params[base + 1];", "    vec4 alphaPowerMix = params[base + 2];", "    vec4 fresnelColor = params[base + 3];", "    vec4 flagsDistortZ = params[base + 4];", "", "    vec2 size = max(sizeSmoothCorner.xy, vec2(1.0));", "    float smoothness = sizeSmoothCorner.z;", "    float cornerSmoothness = max(sizeSmoothCorner.w, 0.001);", "    float globalAlpha = clamp(alphaPowerMix.x, 0.0, 1.0);", "    float fresnelPower = max(alphaPowerMix.y, 0.001);", "    float baseAlpha = clamp(alphaPowerMix.z, 0.0, 1.0);", "    float fresnelMix = clamp(alphaPowerMix.w, 0.0, 1.0);", "    float fresnelInvert = flagsDistortZ.x;", "    float distortStrength = flagsDistortZ.y;", "", "    vec2 coord = clamp(FragCoord, vec2(0.0), vec2(1.0));", "    vec2 center = size * 0.5;", "    vec2 halfSize = max(center - 1.0, vec2(0.0));", "    vec2 pos = center - coord * size;", "", "    float alpha = ralpha(size, coord, radius, smoothness);", "    float distToEdge = abs(rdist(pos, halfSize, radius));", "    float maxDistNorm = max(min(halfSize.x, halfSize.y), 0.001);", "    float edgeGradient = 1.0 - clamp(distToEdge / maxDistNorm, 0.0, 1.0);", "    float fresnelBase = (fresnelInvert > 0.5) ? edgeGradient : (1.0 - edgeGradient);", "", "    float fresnel;", "    if (fresnelPower > 20.0) {", "        fresnel = exp(fresnelPower * log(clamp(fresnelBase, 0.001, 1.0)));", "    } else {", "        fresnel = pow(clamp(fresnelBase, 0.0, 1.0), fresnelPower);", "    }", "    fresnel = clamp(fresnel, 0.0, 1.0);", "", "    vec2 dir = (length(pos) > 0.001) ? normalize(-pos) : vec2(0.0);", "    vec2 texCoord = (gl_FragCoord.xy - Region.xy) / max(Region.zw, vec2(1.0));", "", "    float cornerRad = (pos.x > 0.0) ? ((pos.y > 0.0) ? radius.x : radius.w)", "                                    : ((pos.y > 0.0) ? radius.y : radius.z);", "    cornerRad = max(cornerRad, 1.0);", "    vec2 edgeXY = halfSize - abs(pos);", "    float cornerness = (1.0 - smoothstep(0.0, cornerRad, edgeXY.x)) * (1.0 - smoothstep(0.0, cornerRad, edgeXY.y));", "", "    vec2 ofs = dir * fresnel * distortStrength * (1.0 - 0.5 * cornerness);", "    vec2 sampleUv = clamp(texCoord + ofs, vec2(0.0), vec2(1.0));", "    vec4 baseCol = texture(Sampler0, sampleUv);", "", "    vec2 tap = vec2(2.2) / max(Region.zw, vec2(1.0));", "    vec4 smCol = baseCol * 0.20;", "    smCol += texture(Sampler0, clamp(sampleUv + vec2(tap.x, 0.0), 0.0, 1.0)) * 0.12;", "    smCol += texture(Sampler0, clamp(sampleUv - vec2(tap.x, 0.0), 0.0, 1.0)) * 0.12;", "    smCol += texture(Sampler0, clamp(sampleUv + vec2(0.0, tap.y), 0.0, 1.0)) * 0.12;", "    smCol += texture(Sampler0, clamp(sampleUv - vec2(0.0, tap.y), 0.0, 1.0)) * 0.12;", "    smCol += texture(Sampler0, clamp(sampleUv + tap, 0.0, 1.0)) * 0.08;", "    smCol += texture(Sampler0, clamp(sampleUv - tap, 0.0, 1.0)) * 0.08;", "    smCol += texture(Sampler0, clamp(sampleUv + vec2(tap.x, -tap.y), 0.0, 1.0)) * 0.08;", "    smCol += texture(Sampler0, clamp(sampleUv + vec2(-tap.x, tap.y), 0.0, 1.0)) * 0.08;", "    vec4 texColor = mix(baseCol, smCol, cornerness);", "", "    vec3 finalColor = mix(texColor.rgb, fresnelColor.rgb, fresnel * fresnelMix);", "    float finalAlpha = mix(baseAlpha, fresnelColor.a, fresnel) * alpha * globalAlpha;", "", "    if (finalAlpha < 0.001) {", "        discard;", "    }", "", "    OutColor = vec4(finalColor, finalAlpha);", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }

    private final String s25() {
        Object[] objectArray = new String[]{"#version 150", "", "#moj_import <minecraft:dynamictransforms.glsl>", "#moj_import <minecraft:projection.glsl>", "", "in vec3 Position;", "in vec4 Color;", "in float LineWidth;", "", "out vec2 FragCoord;", "flat out int QuadIndex;", "", "layout(std140) uniform GlassParamsArray {", "    vec4 params[2560];", "};", "", "void main() {", "    int index = max(int(LineWidth + 0.5) - 1, 0);", "    int base = index * 5;", "    vec4 flagsDistortZ = params[base + 4];", "", "    gl_Position = ProjMat * ModelViewMat * vec4(Position.xy, flagsDistortZ.z, 1.0);", "    FragCoord = Color.rg;", "    QuadIndex = index;", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }

    private final String s26() {
        Object[] objectArray = new String[]{"#version 150", "", "#moj_import <kimiko:common.glsl>", "", "in vec2 FragCoord;", "flat in int QuadIndex;", "", "uniform sampler2D Sampler0;", "", "layout(std140) uniform GlassParamsArray {", "    vec4 params[2560];", "};", "", "layout(std140) uniform BlurRegion {", "    vec4 Region;", "};", "", "out vec4 OutColor;", "", "void main() {", "    int base = QuadIndex * 5;", "    vec4 radius = max(params[base], vec4(0.0));", "    vec4 sizeSmoothCorner = params[base + 1];", "    vec4 alphaPowerMix = params[base + 2];", "    vec4 fresnelColor = params[base + 3];", "    vec4 flagsDistortZ = params[base + 4];", "", "    vec2 size = max(sizeSmoothCorner.xy, vec2(1.0));", "    float smoothness = sizeSmoothCorner.z;", "    float cornerSmoothness = max(sizeSmoothCorner.w, 0.001);", "    float globalAlpha = clamp(alphaPowerMix.x, 0.0, 1.0);", "    float fresnelPower = max(alphaPowerMix.y, 0.001);", "    float baseAlpha = clamp(alphaPowerMix.z, 0.0, 1.0);", "    float fresnelMix = clamp(alphaPowerMix.w, 0.0, 1.0);", "    float fresnelInvert = flagsDistortZ.x;", "    float distortStrength = flagsDistortZ.y;", "", "    vec2 coord = clamp(FragCoord, vec2(0.0), vec2(1.0));", "    vec2 center = size * 0.5;", "    vec2 halfSize = max(center - 1.0, vec2(0.0));", "    vec2 pos = center - coord * size;", "", "    float alpha = ralpha(size, coord, radius, smoothness);", "    float distToEdge = abs(rdist(pos, halfSize, radius));", "    float maxDistNorm = max(min(halfSize.x, halfSize.y), 0.001);", "    float edgeGradient = 1.0 - clamp(distToEdge / maxDistNorm, 0.0, 1.0);", "    float fresnelBase = (fresnelInvert > 0.5) ? edgeGradient : (1.0 - edgeGradient);", "", "    float fresnel;", "    if (fresnelPower > 20.0) {", "        fresnel = exp(fresnelPower * log(clamp(fresnelBase, 0.001, 1.0)));", "    } else {", "        fresnel = pow(clamp(fresnelBase, 0.0, 1.0), fresnelPower);", "    }", "    fresnel = clamp(fresnel, 0.0, 1.0);", "", "    vec2 dir = (length(pos) > 0.001) ? normalize(-pos) : vec2(0.0);", "    vec2 texCoord = (gl_FragCoord.xy - Region.xy) / max(Region.zw, vec2(1.0));", "", "    float cornerRad = (pos.x > 0.0) ? ((pos.y > 0.0) ? radius.x : radius.w)", "                                    : ((pos.y > 0.0) ? radius.y : radius.z);", "    cornerRad = max(cornerRad, 1.0);", "    vec2 edgeXY = halfSize - abs(pos);", "    float cornerness = (1.0 - smoothstep(0.0, cornerRad, edgeXY.x)) * (1.0 - smoothstep(0.0, cornerRad, edgeXY.y));", "", "    vec2 ofs = dir * fresnel * distortStrength * (1.0 - 0.5 * cornerness);", "    vec2 sampleUv = clamp(texCoord + ofs, vec2(0.0), vec2(1.0));", "    vec4 baseCol = texture(Sampler0, sampleUv);", "", "    vec2 tap = vec2(2.2) / max(Region.zw, vec2(1.0));", "    vec4 smCol = baseCol * 0.20;", "    smCol += texture(Sampler0, clamp(sampleUv + vec2(tap.x, 0.0), 0.0, 1.0)) * 0.12;", "    smCol += texture(Sampler0, clamp(sampleUv - vec2(tap.x, 0.0), 0.0, 1.0)) * 0.12;", "    smCol += texture(Sampler0, clamp(sampleUv + vec2(0.0, tap.y), 0.0, 1.0)) * 0.12;", "    smCol += texture(Sampler0, clamp(sampleUv - vec2(0.0, tap.y), 0.0, 1.0)) * 0.12;", "    smCol += texture(Sampler0, clamp(sampleUv + tap, 0.0, 1.0)) * 0.08;", "    smCol += texture(Sampler0, clamp(sampleUv - tap, 0.0, 1.0)) * 0.08;", "    smCol += texture(Sampler0, clamp(sampleUv + vec2(tap.x, -tap.y), 0.0, 1.0)) * 0.08;", "    smCol += texture(Sampler0, clamp(sampleUv + vec2(-tap.x, tap.y), 0.0, 1.0)) * 0.08;", "    vec4 texColor = mix(baseCol, smCol, cornerness);", "", "    vec3 finalColor = mix(texColor.rgb, fresnelColor.rgb, fresnel * fresnelMix);", "    float finalAlpha = mix(baseAlpha, fresnelColor.a, fresnel) * alpha * globalAlpha;", "", "    if (finalAlpha < 0.001) {", "        discard;", "    }", "", "    OutColor = vec4(finalColor, finalAlpha);", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }

    private final String s27() {
        Object[] objectArray = new String[]{"//!vertex", "#version 150", "", "#moj_import <minecraft:dynamictransforms.glsl>", "#moj_import <minecraft:projection.glsl>", "", "in vec3 Position;", "in vec2 UV0;", "in vec4 Color;", "in float LineWidth;", "", "out vec2 TexCoord;", "out vec2 FragCoord;", "flat out int QuadIndex;", "", "void main() {", "    gl_Position = ProjMat * ModelViewMat * vec4(Position, 1.0);", "    TexCoord = UV0;", "    FragCoord = Color.rg;", "    QuadIndex = max(int(LineWidth + 0.5) - 1, 0);", "}", "//!fragment", "#version 150", "", "#moj_import <kimiko:common.glsl>", "", "#moj_import <minecraft:dynamictransforms.glsl>", "", "uniform sampler2D Sampler0;", "", "in vec2 TexCoord;", "in vec2 FragCoord;", "flat in int QuadIndex;", "", "layout(std140) uniform ImageParamsArray {", "    vec4 params[3584];", "};", "", "out vec4 OutColor;", "", "vec4 imageColor(vec2 coord, vec4 topLeft, vec4 topRight, vec4 bottomRight, vec4 bottomLeft) {", "    return mix(mix(topLeft, topRight, coord.x), mix(bottomLeft, bottomRight, coord.x), coord.y);", "}", "", "// Supersampled fetch for sub-rect (atlas) draws, tuned to stay SHARP:", "// - at 1:1 or magnification it is a single bilinear tap (no extra blur at all);", "// - when minified, a 3x3 grid of taps covers only the part of the pixel footprint that a", "//   bilinear tap does not already cover, so the effective kernel matches the footprint", "//   instead of exceeding it;", "// - a light unsharp step pulls micro-contrast back toward the centre tap;", "// - every tap is clamped inside the sub-rect so neighbouring atlas cells never bleed in.", "vec4 sampleHq(vec4 uvRect) {", "    vec2 texSize = vec2(textureSize(Sampler0, 0));", "    vec2 texel = 1.0 / texSize;", "    vec2 lo = min(uvRect.xy, uvRect.zw) + texel * 0.5;", "    vec2 hi = max(uvRect.xy, uvRect.zw) - texel * 0.5;", "    hi = max(hi, lo);", "", "    vec4 center = texture(Sampler0, clamp(TexCoord, lo, hi));", "", "    vec2 dx = dFdx(TexCoord);", "    vec2 dy = dFdy(TexCoord);", "    float fx = length(dx * texSize);", "    float fy = length(dy * texSize);", "    if (max(fx, fy) <= 1.15) {", "        return center;", "    }", "", "    vec2 ox = dx * (0.5 * max(1.0 - 1.0 / max(fx, 1.0), 0.0));", "    vec2 oy = dy * (0.5 * max(1.0 - 1.0 / max(fy, 1.0), 0.0));", "", "    vec4 acc = vec4(0.0);", "    for (int i = -1; i <= 1; i++) {", "        for (int j = -1; j <= 1; j++) {", "            acc += texture(Sampler0, clamp(TexCoord + float(i) * ox + float(j) * oy, lo, hi));", "        }", "    }", "    vec4 soft = acc / 9.0;", "", "    return soft + (center - soft) * 0.30;", "}", "", "void main() {", "    int base = QuadIndex * 7;", "    vec4 radius = params[base];", "    vec4 sizeSmooth = params[base + 1];", "    vec4 topLeft = params[base + 2];", "    vec4 topRight = params[base + 3];", "    vec4 bottomRight = params[base + 4];", "    vec4 bottomLeft = params[base + 5];", "    vec4 uvRect = params[base + 6];", "", "    vec2 coord = clamp(FragCoord, vec2(0.0), vec2(1.0));", "    float shapeAlpha = ralpha(max(sizeSmooth.xy, vec2(1.0)), coord, radius, sizeSmooth.z);", "    vec4 textureColor = sizeSmooth.w > 0.5 ? sampleHq(uvRect) : texture(Sampler0, TexCoord);", "    vec4 color = textureColor * imageColor(coord, topLeft, topRight, bottomRight, bottomLeft);", "    color.a *= shapeAlpha;", "", "    OutColor = color * ColorModulator;", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }

    private final String s28() {
        Object[] objectArray = new String[]{"#version 150", "", "#moj_import <minecraft:dynamictransforms.glsl>", "#moj_import <minecraft:projection.glsl>", "", "in vec3 Position;", "in vec2 UV0;", "in vec4 Color;", "in float LineWidth;", "", "out vec2 TexCoord;", "out vec2 FragCoord;", "flat out int QuadIndex;", "", "void main() {", "    gl_Position = ProjMat * ModelViewMat * vec4(Position, 1.0);", "    TexCoord = UV0;", "    FragCoord = Color.rg;", "    QuadIndex = max(int(LineWidth + 0.5) - 1, 0);", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }

    private final String s29() {
        Object[] objectArray = new String[]{"#version 150", "", "#moj_import <kimiko:common.glsl>", "", "#moj_import <minecraft:dynamictransforms.glsl>", "", "uniform sampler2D Sampler0;", "", "in vec2 TexCoord;", "in vec2 FragCoord;", "flat in int QuadIndex;", "", "layout(std140) uniform ImageParamsArray {", "    vec4 params[3584];", "};", "", "out vec4 OutColor;", "", "vec4 imageColor(vec2 coord, vec4 topLeft, vec4 topRight, vec4 bottomRight, vec4 bottomLeft) {", "    return mix(mix(topLeft, topRight, coord.x), mix(bottomLeft, bottomRight, coord.x), coord.y);", "}", "", "// Supersampled fetch for sub-rect (atlas) draws, tuned to stay SHARP:", "// - at 1:1 or magnification it is a single bilinear tap (no extra blur at all);", "// - when minified, a 3x3 grid of taps covers only the part of the pixel footprint that a", "//   bilinear tap does not already cover, so the effective kernel matches the footprint", "//   instead of exceeding it;", "// - a light unsharp step pulls micro-contrast back toward the centre tap;", "// - every tap is clamped inside the sub-rect so neighbouring atlas cells never bleed in.", "vec4 sampleHq(vec4 uvRect) {", "    vec2 texSize = vec2(textureSize(Sampler0, 0));", "    vec2 texel = 1.0 / texSize;", "    vec2 lo = min(uvRect.xy, uvRect.zw) + texel * 0.5;", "    vec2 hi = max(uvRect.xy, uvRect.zw) - texel * 0.5;", "    hi = max(hi, lo);", "", "    vec4 center = texture(Sampler0, clamp(TexCoord, lo, hi));", "", "    vec2 dx = dFdx(TexCoord);", "    vec2 dy = dFdy(TexCoord);", "    float fx = length(dx * texSize);", "    float fy = length(dy * texSize);", "    if (max(fx, fy) <= 1.15) {", "        return center;", "    }", "", "    vec2 ox = dx * (0.5 * max(1.0 - 1.0 / max(fx, 1.0), 0.0));", "    vec2 oy = dy * (0.5 * max(1.0 - 1.0 / max(fy, 1.0), 0.0));", "", "    vec4 acc = vec4(0.0);", "    for (int i = -1; i <= 1; i++) {", "        for (int j = -1; j <= 1; j++) {", "            acc += texture(Sampler0, clamp(TexCoord + float(i) * ox + float(j) * oy, lo, hi));", "        }", "    }", "    vec4 soft = acc / 9.0;", "", "    return soft + (center - soft) * 0.30;", "}", "", "void main() {", "    int base = QuadIndex * 7;", "    vec4 radius = params[base];", "    vec4 sizeSmooth = params[base + 1];", "    vec4 topLeft = params[base + 2];", "    vec4 topRight = params[base + 3];", "    vec4 bottomRight = params[base + 4];", "    vec4 bottomLeft = params[base + 5];", "    vec4 uvRect = params[base + 6];", "", "    vec2 coord = clamp(FragCoord, vec2(0.0), vec2(1.0));", "    float shapeAlpha = ralpha(max(sizeSmooth.xy, vec2(1.0)), coord, radius, sizeSmooth.z);", "    vec4 textureColor = sizeSmooth.w > 0.5 ? sampleHq(uvRect) : texture(Sampler0, TexCoord);", "    vec4 color = textureColor * imageColor(coord, topLeft, topRight, bottomRight, bottomLeft);", "    color.a *= shapeAlpha;", "", "    OutColor = color * ColorModulator;", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }

    private final String s30() {
        Object[] objectArray = new String[]{"//!vertex", "#version 150", "", "#moj_import <minecraft:dynamictransforms.glsl>", "#moj_import <minecraft:projection.glsl>", "", "in vec3 Position;", "in vec2 UV0;", "in vec4 Color;", "in float LineWidth;", "", "out vec2 TexCoord;", "out vec4 FragColor;", "flat out int QuadIndex;", "", "void main() {", "    gl_Position = ProjMat * ModelViewMat * vec4(Position, 1.0);", "    TexCoord = UV0;", "    FragColor = Color;", "    QuadIndex = max(int(LineWidth + 0.5) - 1, 0);", "}", "//!fragment", "#version 150", "", "#moj_import <minecraft:dynamictransforms.glsl>", "", "uniform sampler2D Sampler0;", "", "in vec2 TexCoord;", "in vec4 FragColor;", "flat in int QuadIndex;", "", "layout(std140) uniform ItemParamsArray {", "    vec4 params[2048];", "};", "", "out vec4 OutColor;", "", "void main() {", "    vec4 textureColor = texture(Sampler0, TexCoord);", "    vec4 color = textureColor * FragColor;", "", "    float glintStrength = params[QuadIndex].x;", "    if (glintStrength > 0.001 && color.a > 0.001) {", "        float time = params[QuadIndex].y;", "        float stripeA = sin((TexCoord.x + TexCoord.y) * 86.0 - time * 5.0);", "        float stripeB = sin((TexCoord.x * 1.7 - TexCoord.y) * 54.0 + time * 3.1);", "        float mask = smoothstep(0.18, 0.92, stripeA * 0.5 + stripeB * 0.25 + 0.5);", "        vec3 glintColor = mix(vec3(0.42, 0.18, 0.95), vec3(0.95, 0.72, 1.0), mask);", "        color.rgb = mix(color.rgb, glintColor, mask * glintStrength * color.a);", "    }", "", "    if (color.a <= 0.001) {", "        discard;", "    }", "", "    OutColor = color * ColorModulator;", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }

    private final String s31() {
        Object[] objectArray = new String[]{"#version 150", "", "#moj_import <minecraft:dynamictransforms.glsl>", "#moj_import <minecraft:projection.glsl>", "", "in vec3 Position;", "in vec2 UV0;", "in vec4 Color;", "in float LineWidth;", "", "out vec2 TexCoord;", "out vec4 FragColor;", "flat out int QuadIndex;", "", "void main() {", "    gl_Position = ProjMat * ModelViewMat * vec4(Position, 1.0);", "    TexCoord = UV0;", "    FragColor = Color;", "    QuadIndex = max(int(LineWidth + 0.5) - 1, 0);", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }

    private final String s32() {
        Object[] objectArray = new String[]{"#version 150", "", "#moj_import <minecraft:dynamictransforms.glsl>", "", "uniform sampler2D Sampler0;", "", "in vec2 TexCoord;", "in vec4 FragColor;", "flat in int QuadIndex;", "", "layout(std140) uniform ItemParamsArray {", "    vec4 params[2048];", "};", "", "out vec4 OutColor;", "", "void main() {", "    vec4 textureColor = texture(Sampler0, TexCoord);", "    vec4 color = textureColor * FragColor;", "", "    float glintStrength = params[QuadIndex].x;", "    if (glintStrength > 0.001 && color.a > 0.001) {", "        float time = params[QuadIndex].y;", "        float stripeA = sin((TexCoord.x + TexCoord.y) * 86.0 - time * 5.0);", "        float stripeB = sin((TexCoord.x * 1.7 - TexCoord.y) * 54.0 + time * 3.1);", "        float mask = smoothstep(0.18, 0.92, stripeA * 0.5 + stripeB * 0.25 + 0.5);", "        vec3 glintColor = mix(vec3(0.42, 0.18, 0.95), vec3(0.95, 0.72, 1.0), mask);", "        color.rgb = mix(color.rgb, glintColor, mask * glintStrength * color.a);", "    }", "", "    if (color.a <= 0.001) {", "        discard;", "    }", "", "    OutColor = color * ColorModulator;", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }

    private final String s33() {
        Object[] objectArray = new String[]{"//!vertex", "#version 150", "", "in vec3 Position;", "", "out vec2 TexCoord;", "", "void main() {", "    gl_Position = vec4(Position.xy, 0.0, 1.0);", "    TexCoord = Position.xy * 0.5 + 0.5;", "}", "//!fragment", "#version 150", "", "uniform sampler2D Sampler0;", "", "layout(std140) uniform KawaseParams {", "    vec4 SourceRect;", "    vec4 HalfPixel;", "};", "", "in vec2 TexCoord;", "", "out vec4 OutColor;", "", "void main() {", "    vec2 sourceCoord = SourceRect.xy + TexCoord * SourceRect.zw;", "    vec4 sum = texture(Sampler0, sourceCoord) * 4.0;", "    sum += texture(Sampler0, sourceCoord - HalfPixel.xy);", "    sum += texture(Sampler0, sourceCoord + HalfPixel.xy);", "    sum += texture(Sampler0, sourceCoord + vec2(HalfPixel.x, -HalfPixel.y));", "    sum += texture(Sampler0, sourceCoord - vec2(HalfPixel.x, -HalfPixel.y));", "    OutColor = sum / 8.0;", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }

    private final String s34() {
        Object[] objectArray = new String[]{"#version 150", "", "in vec3 Position;", "", "out vec2 TexCoord;", "", "void main() {", "    gl_Position = vec4(Position.xy, 0.0, 1.0);", "    TexCoord = Position.xy * 0.5 + 0.5;", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }

    private final String s35() {
        Object[] objectArray = new String[]{"#version 150", "", "uniform sampler2D Sampler0;", "", "layout(std140) uniform KawaseParams {", "    vec4 SourceRect;", "    vec4 HalfPixel;", "};", "", "in vec2 TexCoord;", "", "out vec4 OutColor;", "", "void main() {", "    vec2 sourceCoord = SourceRect.xy + TexCoord * SourceRect.zw;", "    vec4 sum = texture(Sampler0, sourceCoord) * 4.0;", "    sum += texture(Sampler0, sourceCoord - HalfPixel.xy);", "    sum += texture(Sampler0, sourceCoord + HalfPixel.xy);", "    sum += texture(Sampler0, sourceCoord + vec2(HalfPixel.x, -HalfPixel.y));", "    sum += texture(Sampler0, sourceCoord - vec2(HalfPixel.x, -HalfPixel.y));", "    OutColor = sum / 8.0;", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }

    private final String s36() {
        Object[] objectArray = new String[]{"//!fragment", "#version 150", "", "uniform sampler2D Sampler0;", "", "layout(std140) uniform KawaseParams {", "    vec4 SourceRect;", "    vec4 HalfPixel;", "};", "", "in vec2 TexCoord;", "", "out vec4 OutColor;", "", "void main() {", "    vec2 sourceCoord = SourceRect.xy + TexCoord * SourceRect.zw;", "    vec4 sum = texture(Sampler0, sourceCoord + vec2(-HalfPixel.x * 2.0, 0.0));", "    sum += texture(Sampler0, sourceCoord + vec2(-HalfPixel.x, HalfPixel.y)) * 2.0;", "    sum += texture(Sampler0, sourceCoord + vec2(0.0, HalfPixel.y * 2.0));", "    sum += texture(Sampler0, sourceCoord + vec2(HalfPixel.x, HalfPixel.y)) * 2.0;", "    sum += texture(Sampler0, sourceCoord + vec2(HalfPixel.x * 2.0, 0.0));", "    sum += texture(Sampler0, sourceCoord + vec2(HalfPixel.x, -HalfPixel.y)) * 2.0;", "    sum += texture(Sampler0, sourceCoord + vec2(0.0, -HalfPixel.y * 2.0));", "    sum += texture(Sampler0, sourceCoord + vec2(-HalfPixel.x, -HalfPixel.y)) * 2.0;", "    OutColor = sum / 12.0;", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }

    private final String s37() {
        Object[] objectArray = new String[]{"#version 150", "", "uniform sampler2D Sampler0;", "", "layout(std140) uniform KawaseParams {", "    vec4 SourceRect;", "    vec4 HalfPixel;", "};", "", "in vec2 TexCoord;", "", "out vec4 OutColor;", "", "void main() {", "    vec2 sourceCoord = SourceRect.xy + TexCoord * SourceRect.zw;", "    vec4 sum = texture(Sampler0, sourceCoord + vec2(-HalfPixel.x * 2.0, 0.0));", "    sum += texture(Sampler0, sourceCoord + vec2(-HalfPixel.x, HalfPixel.y)) * 2.0;", "    sum += texture(Sampler0, sourceCoord + vec2(0.0, HalfPixel.y * 2.0));", "    sum += texture(Sampler0, sourceCoord + vec2(HalfPixel.x, HalfPixel.y)) * 2.0;", "    sum += texture(Sampler0, sourceCoord + vec2(HalfPixel.x * 2.0, 0.0));", "    sum += texture(Sampler0, sourceCoord + vec2(HalfPixel.x, -HalfPixel.y)) * 2.0;", "    sum += texture(Sampler0, sourceCoord + vec2(0.0, -HalfPixel.y * 2.0));", "    sum += texture(Sampler0, sourceCoord + vec2(-HalfPixel.x, -HalfPixel.y)) * 2.0;", "    OutColor = sum / 12.0;", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }

    private final String s38() {
        Object[] objectArray = new String[]{"//!vertex", "#version 150", "", "#moj_import <minecraft:dynamictransforms.glsl>", "#moj_import <minecraft:projection.glsl>", "", "in vec3 Position;", "in vec4 Color;", "in float LineWidth;", "", "out vec2 FragCoord;", "flat out int QuadIndex;", "", "void main() {", "    gl_Position = ProjMat * ModelViewMat * vec4(Position, 1.0);", "    FragCoord = Color.rg;", "    QuadIndex = max(int(LineWidth + 0.5) - 1, 0);", "}", "//!fragment", "#version 150", "", "#moj_import <minecraft:dynamictransforms.glsl>", "", "in vec2 FragCoord;", "flat in int QuadIndex;", "", "layout(std140) uniform LineParamsArray {", "", "    vec4 params[2048];", "};", "", "out vec4 OutColor;", "", "void main() {", "    int base = QuadIndex * 2;", "    vec4 color = params[base];", "    vec4 meta = params[base + 1];", "    vec2 size = max(meta.xy, vec2(1.0));", "    float fadeStart = clamp(meta.z, 0.0, 1.0);", "    float fadeEnd = clamp(meta.w, 0.0, 1.0);", "", "    vec2 coord = clamp(FragCoord, vec2(0.0), vec2(1.0));", "    vec2 halfSize = size * 0.5;", "", "    vec2 pos = halfSize - coord * size;", "    vec2 q = abs(pos) - halfSize;", "    float dist = length(max(q, vec2(0.0))) + min(max(q.x, q.y), 0.0);", "", "    float feather = max(fwidth(dist) * 0.5, 0.6);", "    float alpha = 1.0 - smoothstep(-feather, feather, dist);", "", "    alpha *= mix(fadeStart, fadeEnd, coord.x);", "", "    color.a *= alpha;", "    if (color.a <= 0.001) {", "        discard;", "    }", "    OutColor = color * ColorModulator;", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }

    private final String s39() {
        Object[] objectArray = new String[]{"#version 150", "", "#moj_import <minecraft:dynamictransforms.glsl>", "#moj_import <minecraft:projection.glsl>", "", "in vec3 Position;", "in vec4 Color;", "in float LineWidth;", "", "out vec2 FragCoord;", "flat out int QuadIndex;", "", "void main() {", "    gl_Position = ProjMat * ModelViewMat * vec4(Position, 1.0);", "    FragCoord = Color.rg;", "    QuadIndex = max(int(LineWidth + 0.5) - 1, 0);", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }

    private final String s40() {
        Object[] objectArray = new String[]{"#version 150", "", "#moj_import <minecraft:dynamictransforms.glsl>", "", "in vec2 FragCoord;", "flat in int QuadIndex;", "", "layout(std140) uniform LineParamsArray {", "", "    vec4 params[2048];", "};", "", "out vec4 OutColor;", "", "void main() {", "    int base = QuadIndex * 2;", "    vec4 color = params[base];", "    vec4 meta = params[base + 1];", "    vec2 size = max(meta.xy, vec2(1.0));", "    float fadeStart = clamp(meta.z, 0.0, 1.0);", "    float fadeEnd = clamp(meta.w, 0.0, 1.0);", "", "    vec2 coord = clamp(FragCoord, vec2(0.0), vec2(1.0));", "    vec2 halfSize = size * 0.5;", "", "    vec2 pos = halfSize - coord * size;", "    vec2 q = abs(pos) - halfSize;", "    float dist = length(max(q, vec2(0.0))) + min(max(q.x, q.y), 0.0);", "", "    float feather = max(fwidth(dist) * 0.5, 0.6);", "    float alpha = 1.0 - smoothstep(-feather, feather, dist);", "", "    alpha *= mix(fadeStart, fadeEnd, coord.x);", "", "    color.a *= alpha;", "    if (color.a <= 0.001) {", "        discard;", "    }", "    OutColor = color * ColorModulator;", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }

    private final String s41() {
        Object[] objectArray = new String[]{"//!vertex", "#version 150", "", "#moj_import <minecraft:dynamictransforms.glsl>", "#moj_import <minecraft:projection.glsl>", "", "in vec3 Position;", "in vec2 UV0;", "in vec4 Color;", "in float LineWidth;", "", "out vec2 TexCoord;", "out vec4 VertexColor;", "out float LinePos;", "", "void main() {", "    gl_Position = ProjMat * ModelViewMat * vec4(Position, 1.0);", "    TexCoord    = UV0;", "    VertexColor = Color;", "    LinePos     = LineWidth;", "}", "//!fragment", "#version 150", "", "#moj_import <minecraft:dynamictransforms.glsl>", "", "uniform sampler2D Sampler0;", "", "in vec2  TexCoord;", "", "in vec4  VertexColor;", "", "in float LinePos;", "", "out vec4 OutColor;", "", "const float DISTANCE_RANGE = 4.0;", "const float EDGE_SHARPNESS = 1.45;", "", "float median(float r, float g, float b) {", "    return max(min(r, g), min(max(r, g), b));", "}", "", "float sampleDistance(vec2 uv, float sdfBlend) {", "    vec4 t = texture(Sampler0, uv);", "    return mix(median(t.r, t.g, t.b), t.a, sdfBlend);", "}", "", "float textCoverage(vec2 uv, vec2 dx, vec2 dy) {", "    vec2 atlasSize = vec2(textureSize(Sampler0, 0));", "    vec2 unitRange = vec2(DISTANCE_RANGE) / atlasSize;", "    vec2 fw = abs(dx) + abs(dy);", "    vec2 screenTexSize = vec2(1.0) / max(fw, vec2(1.0e-6));", "    float spr = max(0.5 * dot(unitRange, screenTexSize), 1.0);", "", "    float texelsPerPx = max(length(dx * atlasSize), length(dy * atlasSize));", "    float sdfBlend = clamp(texelsPerPx - 1.0, 0.0, 1.0);", "    float sharpness = mix(1.0, EDGE_SHARPNESS, clamp((spr - 1.6) / 1.4, 0.0, 1.0));", "", "    if (texelsPerPx > 1.05) {", "        vec2 o1 = 0.125 * dx + 0.375 * dy;", "        vec2 o2 = 0.375 * dx - 0.125 * dy;", "        float c0 = clamp(spr * (sampleDistance(uv + o1, sdfBlend) - 0.5) * sharpness + 0.5, 0.0, 1.0);", "        float c1 = clamp(spr * (sampleDistance(uv - o1, sdfBlend) - 0.5) * sharpness + 0.5, 0.0, 1.0);", "        float c2 = clamp(spr * (sampleDistance(uv + o2, sdfBlend) - 0.5) * sharpness + 0.5, 0.0, 1.0);", "        float c3 = clamp(spr * (sampleDistance(uv - o2, sdfBlend) - 0.5) * sharpness + 0.5, 0.0, 1.0);", "        return 0.25 * (c0 + c1 + c2 + c3);", "    }", "    return clamp(spr * (sampleDistance(uv, sdfBlend) - 0.5) * sharpness + 0.5, 0.0, 1.0);", "}", "", "void main() {", "    vec2 dx = dFdx(TexCoord);", "    vec2 dy = dFdy(TexCoord);", "    float coverage = textCoverage(TexCoord, dx, dy);", "", "    float baseOpacity   = VertexColor.a;", "    float progress      = VertexColor.r * 2.0 - 0.5;", "    float halfW         = VertexColor.g;", "    float shimIntensity = VertexColor.b;", "", "    float packedLine = LinePos;", "    float linePos = mod(packedLine, 64.0) / 63.0;", "    float tintB   = mod(floor(packedLine / 64.0), 64.0) / 63.0;", "    float tintG   = mod(floor(packedLine / 4096.0), 64.0) / 63.0;", "    float tintR   = floor(packedLine / 262144.0) / 63.0;", "", "    float dist  = abs(linePos - progress);", "    float spot  = smoothstep(halfW, 0.0, dist);", "    spot        = spot * spot;", "", "    float alpha = coverage * spot * shimIntensity * baseOpacity;", "    if (alpha <= 0.001) discard;", "", "    OutColor = vec4(tintR, tintG, tintB, alpha) * ColorModulator;", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }

    private final String s42() {
        Object[] objectArray = new String[]{"#version 150", "", "#moj_import <minecraft:dynamictransforms.glsl>", "#moj_import <minecraft:projection.glsl>", "", "in vec3 Position;", "in vec2 UV0;", "in vec4 Color;", "in float LineWidth;", "", "out vec2 TexCoord;", "out vec4 VertexColor;", "out float LinePos;", "", "void main() {", "    gl_Position = ProjMat * ModelViewMat * vec4(Position, 1.0);", "    TexCoord    = UV0;", "    VertexColor = Color;", "    LinePos     = LineWidth;", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }

    private final String s43() {
        Object[] objectArray = new String[]{"#version 150", "", "#moj_import <minecraft:dynamictransforms.glsl>", "", "uniform sampler2D Sampler0;", "", "in vec2  TexCoord;", "", "in vec4  VertexColor;", "", "in float LinePos;", "", "out vec4 OutColor;", "", "const float DISTANCE_RANGE = 4.0;", "const float EDGE_SHARPNESS = 1.45;", "", "float median(float r, float g, float b) {", "    return max(min(r, g), min(max(r, g), b));", "}", "", "float sampleDistance(vec2 uv, float sdfBlend) {", "    vec4 t = texture(Sampler0, uv);", "    return mix(median(t.r, t.g, t.b), t.a, sdfBlend);", "}", "", "float textCoverage(vec2 uv, vec2 dx, vec2 dy) {", "    vec2 atlasSize = vec2(textureSize(Sampler0, 0));", "    vec2 unitRange = vec2(DISTANCE_RANGE) / atlasSize;", "    vec2 fw = abs(dx) + abs(dy);", "    vec2 screenTexSize = vec2(1.0) / max(fw, vec2(1.0e-6));", "    float spr = max(0.5 * dot(unitRange, screenTexSize), 1.0);", "", "    float texelsPerPx = max(length(dx * atlasSize), length(dy * atlasSize));", "    float sdfBlend = clamp(texelsPerPx - 1.0, 0.0, 1.0);", "    float sharpness = mix(1.0, EDGE_SHARPNESS, clamp((spr - 1.6) / 1.4, 0.0, 1.0));", "", "    if (texelsPerPx > 1.05) {", "        vec2 o1 = 0.125 * dx + 0.375 * dy;", "        vec2 o2 = 0.375 * dx - 0.125 * dy;", "        float c0 = clamp(spr * (sampleDistance(uv + o1, sdfBlend) - 0.5) * sharpness + 0.5, 0.0, 1.0);", "        float c1 = clamp(spr * (sampleDistance(uv - o1, sdfBlend) - 0.5) * sharpness + 0.5, 0.0, 1.0);", "        float c2 = clamp(spr * (sampleDistance(uv + o2, sdfBlend) - 0.5) * sharpness + 0.5, 0.0, 1.0);", "        float c3 = clamp(spr * (sampleDistance(uv - o2, sdfBlend) - 0.5) * sharpness + 0.5, 0.0, 1.0);", "        return 0.25 * (c0 + c1 + c2 + c3);", "    }", "    return clamp(spr * (sampleDistance(uv, sdfBlend) - 0.5) * sharpness + 0.5, 0.0, 1.0);", "}", "", "void main() {", "    vec2 dx = dFdx(TexCoord);", "    vec2 dy = dFdy(TexCoord);", "    float coverage = textCoverage(TexCoord, dx, dy);", "", "    float baseOpacity   = VertexColor.a;", "    float progress      = VertexColor.r * 2.0 - 0.5;", "    float halfW         = VertexColor.g;", "    float shimIntensity = VertexColor.b;", "", "    float packedLine = LinePos;", "    float linePos = mod(packedLine, 64.0) / 63.0;", "    float tintB   = mod(floor(packedLine / 64.0), 64.0) / 63.0;", "    float tintG   = mod(floor(packedLine / 4096.0), 64.0) / 63.0;", "    float tintR   = floor(packedLine / 262144.0) / 63.0;", "", "    float dist  = abs(linePos - progress);", "    float spot  = smoothstep(halfW, 0.0, dist);", "    spot        = spot * spot;", "", "    float alpha = coverage * spot * shimIntensity * baseOpacity;", "    if (alpha <= 0.001) discard;", "", "    OutColor = vec4(tintR, tintG, tintB, alpha) * ColorModulator;", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }

    private final String s44() {
        Object[] objectArray = new String[]{"//!vertex", "#version 150", "", "#moj_import <minecraft:dynamictransforms.glsl>", "#moj_import <minecraft:projection.glsl>", "", "in vec3 Position;", "in vec2 UV0;", "in vec4 Color;", "in float LineWidth;", "", "out vec2 TexCoord;", "out vec4 VertexColor;", "out float AtlasUnitRange;", "", "void main() {", "    gl_Position = ProjMat * ModelViewMat * vec4(Position, 1.0);", "    TexCoord = UV0;", "    VertexColor = Color;", "    AtlasUnitRange = LineWidth;", "}", "//!fragment", "#version 150", "", "#moj_import <minecraft:dynamictransforms.glsl>", "", "uniform sampler2D Sampler0;", "", "in vec2 TexCoord;", "in vec4 VertexColor;", "in float AtlasUnitRange;", "", "out vec4 OutColor;", "", "const float DISTANCE_RANGE = 4.0;", "const float EDGE_SHARPNESS = 1.45;", "", "float median(float r, float g, float b) {", "    return max(min(r, g), min(max(r, g), b));", "}", "", "float sampleDistance(vec2 uv, float sdfBlend) {", "    vec4 t = texture(Sampler0, uv);", "    return mix(median(t.r, t.g, t.b), t.a, sdfBlend);", "}", "", "float textCoverage(vec2 uv, vec2 dx, vec2 dy) {", "    vec2 atlasSize = vec2(textureSize(Sampler0, 0));", "    vec2 unitRange = AtlasUnitRange > 0.0 ? vec2(AtlasUnitRange) : vec2(DISTANCE_RANGE) / atlasSize;", "    vec2 fw = abs(dx) + abs(dy);", "    vec2 screenTexSize = vec2(1.0) / max(fw, vec2(1.0e-6));", "    float spr = max(0.5 * dot(unitRange, screenTexSize), 1.0);", "", "    float texelsPerPx = max(length(dx * atlasSize), length(dy * atlasSize));", "    float sdfBlend = clamp(texelsPerPx - 1.0, 0.0, 1.0);", "    float sharpness = mix(1.0, EDGE_SHARPNESS, clamp((spr - 1.6) / 1.4, 0.0, 1.0));", "", "    if (texelsPerPx > 1.05) {", "        vec2 o1 = 0.125 * dx + 0.375 * dy;", "        vec2 o2 = 0.375 * dx - 0.125 * dy;", "        float c0 = clamp(spr * (sampleDistance(uv + o1, sdfBlend) - 0.5) * sharpness + 0.5, 0.0, 1.0);", "        float c1 = clamp(spr * (sampleDistance(uv - o1, sdfBlend) - 0.5) * sharpness + 0.5, 0.0, 1.0);", "        float c2 = clamp(spr * (sampleDistance(uv + o2, sdfBlend) - 0.5) * sharpness + 0.5, 0.0, 1.0);", "        float c3 = clamp(spr * (sampleDistance(uv - o2, sdfBlend) - 0.5) * sharpness + 0.5, 0.0, 1.0);", "        return 0.25 * (c0 + c1 + c2 + c3);", "    }", "    return clamp(spr * (sampleDistance(uv, sdfBlend) - 0.5) * sharpness + 0.5, 0.0, 1.0);", "}", "", "void main() {", "    vec2 dx = dFdx(TexCoord);", "    vec2 dy = dFdy(TexCoord);", "    float coverage = textCoverage(TexCoord, dx, dy);", "", "    float alpha = coverage * VertexColor.a;", "    if (alpha <= 0.001) {", "        discard;", "    }", "", "    OutColor = vec4(VertexColor.rgb, alpha) * ColorModulator;", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }

    private final String s45() {
        Object[] objectArray = new String[]{"#version 150", "", "#moj_import <minecraft:dynamictransforms.glsl>", "#moj_import <minecraft:projection.glsl>", "", "in vec3 Position;", "in vec2 UV0;", "in vec4 Color;", "in float LineWidth;", "", "out vec2 TexCoord;", "out vec4 VertexColor;", "out float AtlasUnitRange;", "", "void main() {", "    gl_Position = ProjMat * ModelViewMat * vec4(Position, 1.0);", "    TexCoord = UV0;", "    VertexColor = Color;", "    AtlasUnitRange = LineWidth;", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }

    private final String s46() {
        Object[] objectArray = new String[]{"#version 150", "", "#moj_import <minecraft:dynamictransforms.glsl>", "", "uniform sampler2D Sampler0;", "", "in vec2 TexCoord;", "in vec4 VertexColor;", "in float AtlasUnitRange;", "", "out vec4 OutColor;", "", "const float DISTANCE_RANGE = 4.0;", "const float EDGE_SHARPNESS = 1.45;", "", "float median(float r, float g, float b) {", "    return max(min(r, g), min(max(r, g), b));", "}", "", "float sampleDistance(vec2 uv, float sdfBlend) {", "    vec4 t = texture(Sampler0, uv);", "    return mix(median(t.r, t.g, t.b), t.a, sdfBlend);", "}", "", "float textCoverage(vec2 uv, vec2 dx, vec2 dy) {", "    vec2 atlasSize = vec2(textureSize(Sampler0, 0));", "    vec2 unitRange = AtlasUnitRange > 0.0 ? vec2(AtlasUnitRange) : vec2(DISTANCE_RANGE) / atlasSize;", "    vec2 fw = abs(dx) + abs(dy);", "    vec2 screenTexSize = vec2(1.0) / max(fw, vec2(1.0e-6));", "    float spr = max(0.5 * dot(unitRange, screenTexSize), 1.0);", "", "    float texelsPerPx = max(length(dx * atlasSize), length(dy * atlasSize));", "    float sdfBlend = clamp(texelsPerPx - 1.0, 0.0, 1.0);", "    float sharpness = mix(1.0, EDGE_SHARPNESS, clamp((spr - 1.6) / 1.4, 0.0, 1.0));", "", "    if (texelsPerPx > 1.05) {", "        vec2 o1 = 0.125 * dx + 0.375 * dy;", "        vec2 o2 = 0.375 * dx - 0.125 * dy;", "        float c0 = clamp(spr * (sampleDistance(uv + o1, sdfBlend) - 0.5) * sharpness + 0.5, 0.0, 1.0);", "        float c1 = clamp(spr * (sampleDistance(uv - o1, sdfBlend) - 0.5) * sharpness + 0.5, 0.0, 1.0);", "        float c2 = clamp(spr * (sampleDistance(uv + o2, sdfBlend) - 0.5) * sharpness + 0.5, 0.0, 1.0);", "        float c3 = clamp(spr * (sampleDistance(uv - o2, sdfBlend) - 0.5) * sharpness + 0.5, 0.0, 1.0);", "        return 0.25 * (c0 + c1 + c2 + c3);", "    }", "    return clamp(spr * (sampleDistance(uv, sdfBlend) - 0.5) * sharpness + 0.5, 0.0, 1.0);", "}", "", "void main() {", "    vec2 dx = dFdx(TexCoord);", "    vec2 dy = dFdy(TexCoord);", "    float coverage = textCoverage(TexCoord, dx, dy);", "", "    float alpha = coverage * VertexColor.a;", "    if (alpha <= 0.001) {", "        discard;", "    }", "", "    OutColor = vec4(VertexColor.rgb, alpha) * ColorModulator;", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }

    private final String s47() {
        Object[] objectArray = new String[]{"//!vertex", "#version 150", "", "#moj_import <minecraft:dynamictransforms.glsl>", "#moj_import <minecraft:projection.glsl>", "", "in vec3 Position;", "in vec2 UV0;", "in vec4 Color;", "in float LineWidth;", "", "out vec2 TexCoord;", "out vec4 VertexColor;", "out float AtlasUnitRange;", "", "void main() {", "    gl_Position = ProjMat * ModelViewMat * vec4(Position, 1.0);", "    TexCoord = UV0;", "    VertexColor = Color;", "    AtlasUnitRange = LineWidth;", "}", "", "//!fragment", "#version 150", "", "#moj_import <minecraft:dynamictransforms.glsl>", "", "uniform sampler2D Sampler0;", "uniform sampler2D Sampler1;", "", "in vec2 TexCoord;", "in vec4 VertexColor;", "in float AtlasUnitRange;", "", "out vec4 OutColor;", "", "const float DISTANCE_RANGE = 4.0;", "const float EDGE_SHARPNESS = 1.45;", "", "float median(float r, float g, float b) {", "    return max(min(r, g), min(max(r, g), b));", "}", "", "float sampleDistance(vec2 uv, float sdfBlend) {", "    vec4 t = texture(Sampler0, uv);", "    return mix(median(t.r, t.g, t.b), t.a, sdfBlend);", "}", "", "float textCoverage(vec2 uv, vec2 dx, vec2 dy) {", "    vec2 atlasSize = vec2(textureSize(Sampler0, 0));", "    vec2 unitRange = AtlasUnitRange > 0.0 ? vec2(AtlasUnitRange) : vec2(DISTANCE_RANGE) / atlasSize;", "    vec2 fw = abs(dx) + abs(dy);", "    vec2 screenTexSize = vec2(1.0) / max(fw, vec2(1.0e-6));", "    float spr = max(0.5 * dot(unitRange, screenTexSize), 1.0);", "    float texelsPerPx = max(length(dx * atlasSize), length(dy * atlasSize));", "    float sdfBlend = clamp(texelsPerPx - 1.0, 0.0, 1.0);", "    float sharpness = mix(1.0, EDGE_SHARPNESS, clamp((spr - 1.6) / 1.4, 0.0, 1.0));", "", "    if (texelsPerPx > 1.05) {", "        vec2 o1 = 0.125 * dx + 0.375 * dy;", "        vec2 o2 = 0.375 * dx - 0.125 * dy;", "        float c0 = clamp(spr * (sampleDistance(uv + o1, sdfBlend) - 0.5) * sharpness + 0.5, 0.0, 1.0);", "        float c1 = clamp(spr * (sampleDistance(uv - o1, sdfBlend) - 0.5) * sharpness + 0.5, 0.0, 1.0);", "        float c2 = clamp(spr * (sampleDistance(uv + o2, sdfBlend) - 0.5) * sharpness + 0.5, 0.0, 1.0);", "        float c3 = clamp(spr * (sampleDistance(uv - o2, sdfBlend) - 0.5) * sharpness + 0.5, 0.0, 1.0);", "        return 0.25 * (c0 + c1 + c2 + c3);", "    }", "    return clamp(spr * (sampleDistance(uv, sdfBlend) - 0.5) * sharpness + 0.5, 0.0, 1.0);", "}", "", "vec3 backdropAt(vec2 uv) {", "    vec2 texel = 1.0 / max(vec2(textureSize(Sampler1, 0)), vec2(1.0));", "    vec3 color = texture(Sampler1, uv).rgb * 0.40;", "    color += texture(Sampler1, clamp(uv + vec2(texel.x, 0.0), 0.0, 1.0)).rgb * 0.15;", "    color += texture(Sampler1, clamp(uv - vec2(texel.x, 0.0), 0.0, 1.0)).rgb * 0.15;", "    color += texture(Sampler1, clamp(uv + vec2(0.0, texel.y), 0.0, 1.0)).rgb * 0.15;", "    color += texture(Sampler1, clamp(uv - vec2(0.0, texel.y), 0.0, 1.0)).rgb * 0.15;", "    return color;", "}", "", "void main() {", "    vec2 dx = dFdx(TexCoord);", "    vec2 dy = dFdy(TexCoord);", "    float coverage = textCoverage(TexCoord, dx, dy);", "    float alpha = coverage * VertexColor.a;", "    if (alpha <= 0.001) {", "        discard;", "    }", "", "    // The backdrop is the same half-resolution blurred scene texture used by", "    // clientRect. Its luminance drives the text between white and dark gray on", "    // every fragment, so moving the watermark through the world updates it.", "    vec2 sceneSize = max(vec2(textureSize(Sampler1, 0)) * 2.0, vec2(1.0));", "    vec2 sceneUv = clamp(gl_FragCoord.xy / sceneSize, vec2(0.0), vec2(1.0));", "    float luminance = dot(backdropAt(sceneUv), vec3(0.2126, 0.7152, 0.0722));", "    // VertexColor.rgb is an optional loader-slice light factor. A white", "    // factor preserves the pure backdrop contrast used by normal text;", "    // lower factors blend toward the Uiverse-style gray shadow.", "    float sceneGray = clamp(1.0 - luminance * 1.35, 0.30, 1.0);", "    float sliceFactor = clamp(dot(VertexColor.rgb, vec3(0.3333333)), 0.0, 1.0);", "    float shadowGray = clamp(sceneGray * 0.68, 0.18, 0.72);", "    float gray = mix(shadowGray, sceneGray, sliceFactor);", "    OutColor = vec4(vec3(gray), alpha) * ColorModulator;", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }

    private final String s48() {
        Object[] objectArray = new String[]{"#version 150", "", "#moj_import <minecraft:dynamictransforms.glsl>", "#moj_import <minecraft:projection.glsl>", "", "in vec3 Position;", "in vec2 UV0;", "in vec4 Color;", "in float LineWidth;", "", "out vec2 TexCoord;", "out vec4 VertexColor;", "out float AtlasUnitRange;", "", "void main() {", "    gl_Position = ProjMat * ModelViewMat * vec4(Position, 1.0);", "    TexCoord = UV0;", "    VertexColor = Color;", "    AtlasUnitRange = LineWidth;", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }

    private final String s49() {
        Object[] objectArray = new String[]{"#version 150", "", "#moj_import <minecraft:dynamictransforms.glsl>", "", "uniform sampler2D Sampler0;", "uniform sampler2D Sampler1;", "", "in vec2 TexCoord;", "in vec4 VertexColor;", "in float AtlasUnitRange;", "", "out vec4 OutColor;", "", "const float DISTANCE_RANGE = 4.0;", "const float EDGE_SHARPNESS = 1.45;", "", "float median(float r, float g, float b) {", "    return max(min(r, g), min(max(r, g), b));", "}", "", "float sampleDistance(vec2 uv, float sdfBlend) {", "    vec4 t = texture(Sampler0, uv);", "    return mix(median(t.r, t.g, t.b), t.a, sdfBlend);", "}", "", "float textCoverage(vec2 uv, vec2 dx, vec2 dy) {", "    vec2 atlasSize = vec2(textureSize(Sampler0, 0));", "    vec2 unitRange = AtlasUnitRange > 0.0 ? vec2(AtlasUnitRange) : vec2(DISTANCE_RANGE) / atlasSize;", "    vec2 fw = abs(dx) + abs(dy);", "    vec2 screenTexSize = vec2(1.0) / max(fw, vec2(1.0e-6));", "    float spr = max(0.5 * dot(unitRange, screenTexSize), 1.0);", "    float texelsPerPx = max(length(dx * atlasSize), length(dy * atlasSize));", "    float sdfBlend = clamp(texelsPerPx - 1.0, 0.0, 1.0);", "    float sharpness = mix(1.0, EDGE_SHARPNESS, clamp((spr - 1.6) / 1.4, 0.0, 1.0));", "", "    if (texelsPerPx > 1.05) {", "        vec2 o1 = 0.125 * dx + 0.375 * dy;", "        vec2 o2 = 0.375 * dx - 0.125 * dy;", "        float c0 = clamp(spr * (sampleDistance(uv + o1, sdfBlend) - 0.5) * sharpness + 0.5, 0.0, 1.0);", "        float c1 = clamp(spr * (sampleDistance(uv - o1, sdfBlend) - 0.5) * sharpness + 0.5, 0.0, 1.0);", "        float c2 = clamp(spr * (sampleDistance(uv + o2, sdfBlend) - 0.5) * sharpness + 0.5, 0.0, 1.0);", "        float c3 = clamp(spr * (sampleDistance(uv - o2, sdfBlend) - 0.5) * sharpness + 0.5, 0.0, 1.0);", "        return 0.25 * (c0 + c1 + c2 + c3);", "    }", "    return clamp(spr * (sampleDistance(uv, sdfBlend) - 0.5) * sharpness + 0.5, 0.0, 1.0);", "}", "", "vec3 backdropAt(vec2 uv) {", "    vec2 texel = 1.0 / max(vec2(textureSize(Sampler1, 0)), vec2(1.0));", "    vec3 color = texture(Sampler1, uv).rgb * 0.40;", "    color += texture(Sampler1, clamp(uv + vec2(texel.x, 0.0), 0.0, 1.0)).rgb * 0.15;", "    color += texture(Sampler1, clamp(uv - vec2(texel.x, 0.0), 0.0, 1.0)).rgb * 0.15;", "    color += texture(Sampler1, clamp(uv + vec2(0.0, texel.y), 0.0, 1.0)).rgb * 0.15;", "    color += texture(Sampler1, clamp(uv - vec2(0.0, texel.y), 0.0, 1.0)).rgb * 0.15;", "    return color;", "}", "", "void main() {", "    vec2 dx = dFdx(TexCoord);", "    vec2 dy = dFdy(TexCoord);", "    float coverage = textCoverage(TexCoord, dx, dy);", "    float alpha = coverage * VertexColor.a;", "    if (alpha <= 0.001) {", "        discard;", "    }", "", "    // The backdrop is the same half-resolution blurred scene texture used by", "    // clientRect. Its luminance drives the text between white and dark gray on", "    // every fragment, so moving the watermark through the world updates it.", "    vec2 sceneSize = max(vec2(textureSize(Sampler1, 0)) * 2.0, vec2(1.0));", "    vec2 sceneUv = clamp(gl_FragCoord.xy / sceneSize, vec2(0.0), vec2(1.0));", "    float luminance = dot(backdropAt(sceneUv), vec3(0.2126, 0.7152, 0.0722));", "    // VertexColor.rgb is an optional loader-slice light factor. A white", "    // factor preserves the pure backdrop contrast used by normal text;", "    // lower factors blend toward the Uiverse-style gray shadow.", "    float sceneGray = clamp(1.0 - luminance * 1.35, 0.30, 1.0);", "    float sliceFactor = clamp(dot(VertexColor.rgb, vec3(0.3333333)), 0.0, 1.0);", "    float shadowGray = clamp(sceneGray * 0.68, 0.18, 0.72);", "    float gray = mix(shadowGray, sceneGray, sliceFactor);", "    OutColor = vec4(vec3(gray), alpha) * ColorModulator;", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }

    private final String s50() {
        Object[] objectArray = new String[]{"//!vertex", "#version 150", "", "#moj_import <minecraft:dynamictransforms.glsl>", "#moj_import <minecraft:projection.glsl>", "", "in vec3 Position;", "in vec2 UV0;", "in vec4 Color;", "in float LineWidth;", "", "out vec2 TexCoord;", "out vec4 VertexColor;", "out float Phase;", "out float ScreenY;", "", "void main() {", "    gl_Position = ProjMat * ModelViewMat * vec4(Position, 1.0);", "    TexCoord    = UV0;", "    VertexColor = Color;", "    Phase       = LineWidth;", "    ScreenY     = Position.y;", "}", "//!fragment", "#version 150", "", "#moj_import <minecraft:dynamictransforms.glsl>", "", "uniform sampler2D Sampler0;", "", "in vec2 TexCoord;", "in vec4 VertexColor;", "in float Phase;", "in float ScreenY;", "", "out vec4 OutColor;", "", "const float DISTANCE_RANGE = 4.0;", "const float EDGE_SHARPNESS = 1.45;", "", "float median(float r, float g, float b) {", "    return max(min(r, g), min(max(r, g), b));", "}", "", "float sampleDistance(vec2 uv, float sdfBlend) {", "    vec4 t = texture(Sampler0, uv);", "    return mix(median(t.r, t.g, t.b), t.a, sdfBlend);", "}", "", "float textCoverage(vec2 uv, vec2 dx, vec2 dy) {", "    vec2 atlasSize = vec2(textureSize(Sampler0, 0));", "    vec2 unitRange = vec2(DISTANCE_RANGE) / atlasSize;", "    vec2 fw = abs(dx) + abs(dy);", "    vec2 screenTexSize = vec2(1.0) / max(fw, vec2(1.0e-6));", "    float spr = max(0.5 * dot(unitRange, screenTexSize), 1.0);", "", "    float texelsPerPx = max(length(dx * atlasSize), length(dy * atlasSize));", "    float sdfBlend = clamp(texelsPerPx - 1.0, 0.0, 1.0);", "    float sharpness = mix(1.0, EDGE_SHARPNESS, clamp((spr - 1.6) / 1.4, 0.0, 1.0));", "", "    if (texelsPerPx > 1.05) {", "        vec2 o1 = 0.125 * dx + 0.375 * dy;", "        vec2 o2 = 0.375 * dx - 0.125 * dy;", "        float c0 = clamp(spr * (sampleDistance(uv + o1, sdfBlend) - 0.5) * sharpness + 0.5, 0.0, 1.0);", "        float c1 = clamp(spr * (sampleDistance(uv - o1, sdfBlend) - 0.5) * sharpness + 0.5, 0.0, 1.0);", "        float c2 = clamp(spr * (sampleDistance(uv + o2, sdfBlend) - 0.5) * sharpness + 0.5, 0.0, 1.0);", "        float c3 = clamp(spr * (sampleDistance(uv - o2, sdfBlend) - 0.5) * sharpness + 0.5, 0.0, 1.0);", "        return 0.25 * (c0 + c1 + c2 + c3);", "    }", "    return clamp(spr * (sampleDistance(uv, sdfBlend) - 0.5) * sharpness + 0.5, 0.0, 1.0);", "}", "", "void main() {", "    vec2 dx = dFdx(TexCoord);", "    vec2 dy = dFdy(TexCoord);", "    float coverage = textCoverage(TexCoord, dx, dy);", "", "    float alpha = coverage * VertexColor.a;", "    if (alpha <= 0.001) {", "        discard;", "    }", "", "    const float FREQ  = 0.05;", "    const float SHARP = 1.0;", "    const float MAXW  = 0.85;", "", "    float s = 0.5 + 0.5 * sin(ScreenY * FREQ - Phase);", "    s = pow(s, SHARP);", "    float w = s * MAXW;", "    vec3 col = mix(VertexColor.rgb, vec3(1.0), w);", "", "    OutColor = vec4(col, alpha) * ColorModulator;", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }

    private final String s51() {
        Object[] objectArray = new String[]{"#version 150", "", "#moj_import <minecraft:dynamictransforms.glsl>", "#moj_import <minecraft:projection.glsl>", "", "in vec3 Position;", "in vec2 UV0;", "in vec4 Color;", "in float LineWidth;", "", "out vec2 TexCoord;", "out vec4 VertexColor;", "out float Phase;", "out float ScreenY;", "", "void main() {", "    gl_Position = ProjMat * ModelViewMat * vec4(Position, 1.0);", "    TexCoord    = UV0;", "    VertexColor = Color;", "    Phase       = LineWidth;", "    ScreenY     = Position.y;", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }

    private final String s52() {
        Object[] objectArray = new String[]{"#version 150", "", "#moj_import <minecraft:dynamictransforms.glsl>", "", "uniform sampler2D Sampler0;", "", "in vec2 TexCoord;", "in vec4 VertexColor;", "in float Phase;", "in float ScreenY;", "", "out vec4 OutColor;", "", "const float DISTANCE_RANGE = 4.0;", "const float EDGE_SHARPNESS = 1.45;", "", "float median(float r, float g, float b) {", "    return max(min(r, g), min(max(r, g), b));", "}", "", "float sampleDistance(vec2 uv, float sdfBlend) {", "    vec4 t = texture(Sampler0, uv);", "    return mix(median(t.r, t.g, t.b), t.a, sdfBlend);", "}", "", "float textCoverage(vec2 uv, vec2 dx, vec2 dy) {", "    vec2 atlasSize = vec2(textureSize(Sampler0, 0));", "    vec2 unitRange = vec2(DISTANCE_RANGE) / atlasSize;", "    vec2 fw = abs(dx) + abs(dy);", "    vec2 screenTexSize = vec2(1.0) / max(fw, vec2(1.0e-6));", "    float spr = max(0.5 * dot(unitRange, screenTexSize), 1.0);", "", "    float texelsPerPx = max(length(dx * atlasSize), length(dy * atlasSize));", "    float sdfBlend = clamp(texelsPerPx - 1.0, 0.0, 1.0);", "    float sharpness = mix(1.0, EDGE_SHARPNESS, clamp((spr - 1.6) / 1.4, 0.0, 1.0));", "", "    if (texelsPerPx > 1.05) {", "        vec2 o1 = 0.125 * dx + 0.375 * dy;", "        vec2 o2 = 0.375 * dx - 0.125 * dy;", "        float c0 = clamp(spr * (sampleDistance(uv + o1, sdfBlend) - 0.5) * sharpness + 0.5, 0.0, 1.0);", "        float c1 = clamp(spr * (sampleDistance(uv - o1, sdfBlend) - 0.5) * sharpness + 0.5, 0.0, 1.0);", "        float c2 = clamp(spr * (sampleDistance(uv + o2, sdfBlend) - 0.5) * sharpness + 0.5, 0.0, 1.0);", "        float c3 = clamp(spr * (sampleDistance(uv - o2, sdfBlend) - 0.5) * sharpness + 0.5, 0.0, 1.0);", "        return 0.25 * (c0 + c1 + c2 + c3);", "    }", "    return clamp(spr * (sampleDistance(uv, sdfBlend) - 0.5) * sharpness + 0.5, 0.0, 1.0);", "}", "", "void main() {", "    vec2 dx = dFdx(TexCoord);", "    vec2 dy = dFdy(TexCoord);", "    float coverage = textCoverage(TexCoord, dx, dy);", "", "    float alpha = coverage * VertexColor.a;", "    if (alpha <= 0.001) {", "        discard;", "    }", "", "    const float FREQ  = 0.05;", "    const float SHARP = 1.0;", "    const float MAXW  = 0.85;", "", "    float s = 0.5 + 0.5 * sin(ScreenY * FREQ - Phase);", "    s = pow(s, SHARP);", "    float w = s * MAXW;", "    vec3 col = mix(VertexColor.rgb, vec3(1.0), w);", "", "    OutColor = vec4(col, alpha) * ColorModulator;", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }

    private final String s53() {
        Object[] objectArray = new String[]{"//!vertex", "#version 150", "", "#moj_import <minecraft:dynamictransforms.glsl>", "#moj_import <minecraft:projection.glsl>", "", "in vec3 Position;", "in vec4 Color;", "in float LineWidth;", "", "out vec2 FragCoord;", "flat out int QuadIndex;", "", "void main() {", "    gl_Position = ProjMat * ModelViewMat * vec4(Position, 1.0);", "    FragCoord = Color.rg;", "    QuadIndex = max(int(LineWidth + 0.5) - 1, 0);", "}", "//!fragment", "#version 150", "", "#moj_import <kimiko:common.glsl>", "", "in vec2 FragCoord;", "flat in int QuadIndex;", "", "const int MAX_OUTLINE_360_RANGES = 1024;", "", "layout(std140) uniform Outline360ParamsArray {", "    vec4 params[1024];", "};", "", "layout(std140) uniform Outline360RangesArray {", "    vec4 ranges[3072];", "};", "", "out vec4 OutColor;", "", "float outlineAlpha(vec2 coord, vec2 size, vec4 radius, float thickness, float smoothness) {", "    float outer = ralpha(size, coord, radius, smoothness);", "    vec2 innerSize = size - vec2(thickness * 2.0);", "    if (innerSize.x <= 0.0 || innerSize.y <= 0.0) {", "        return outer;", "    }", "", "    vec2 local = coord * size;", "    vec2 innerCoord = (local - vec2(thickness)) / innerSize;", "    vec4 innerRadius = max(radius - vec4(thickness), vec4(0.0));", "    float inner = ralpha(innerSize, innerCoord, innerRadius, smoothness);", "    return clamp(outer - inner, 0.0, 1.0);", "}", "", "float contourAngleDegrees(vec2 coord, vec2 size, vec4 radius, float thickness, float offsetDegrees) {", "    vec2 local = coord * size;", "    float halfThickness = thickness * 0.5;", "    vec2 samplePoint = local;", "", "    if (radius.y > 0.0 && local.x >= size.x - radius.y && local.y <= radius.y) {", "        vec2 center = vec2(size.x - radius.y, radius.y);", "        vec2 dir = local - center;", "        samplePoint = center + normalize(dir + vec2(0.0001, -0.0001)) * max(radius.y - halfThickness, 0.0);", "    } else if (radius.x > 0.0 && local.x <= radius.x && local.y <= radius.x) {", "        vec2 center = vec2(radius.x, radius.x);", "        vec2 dir = local - center;", "        samplePoint = center + normalize(dir + vec2(-0.0001, -0.0001)) * max(radius.x - halfThickness, 0.0);", "    } else if (radius.w > 0.0 && local.x <= radius.w && local.y >= size.y - radius.w) {", "        vec2 center = vec2(radius.w, size.y - radius.w);", "        vec2 dir = local - center;", "        samplePoint = center + normalize(dir + vec2(-0.0001, 0.0001)) * max(radius.w - halfThickness, 0.0);", "    } else if (radius.z > 0.0 && local.x >= size.x - radius.z && local.y >= size.y - radius.z) {", "        vec2 center = vec2(size.x - radius.z, size.y - radius.z);", "        vec2 dir = local - center;", "        samplePoint = center + normalize(dir + vec2(0.0001, 0.0001)) * max(radius.z - halfThickness, 0.0);", "    } else {", "        float left = local.x;", "        float right = size.x - local.x;", "        float top = local.y;", "        float bottom = size.y - local.y;", "        float nearest = min(min(left, right), min(top, bottom));", "", "        if (nearest == top) {", "            samplePoint.y = halfThickness;", "        } else if (nearest == right) {", "            samplePoint.x = size.x - halfThickness;", "        } else if (nearest == bottom) {", "            samplePoint.y = size.y - halfThickness;", "        } else {", "            samplePoint.x = halfThickness;", "        }", "    }", "", "    vec2 p = samplePoint - size * 0.5;", "    float angle = degrees(atan(-p.y, p.x)) + offsetDegrees;", "    return mod(angle + 360.0, 360.0);", "}", "", "float rangeFactor(float angle, vec4 range, float blendDegrees, out float spanT) {", "    float start = mod(range.x + 360.0, 360.0);", "    float finish = mod(range.y + 360.0, 360.0);", "    float span = mod(finish - start + 360.0, 360.0);", "    if (span <= 0.001 || span >= 359.999) {", "        spanT = span >= 359.999 ? mod(angle - start + 360.0, 360.0) / 360.0 : 0.0;", "        return 1.0;", "    }", "", "    float delta = mod(angle - start + 360.0, 360.0);", "    spanT = clamp(delta / span, 0.0, 1.0);", "    if (delta > span) {", "        return 0.0;", "    }", "", "    float blendIn = range.z < 0.0 ? blendDegrees : range.z;", "    float blendOut = range.w < 0.0 ? blendDegrees : range.w;", "    float enter = smoothstep(0.0, min(max(blendIn, 0.001), span * 0.5), delta);", "    float exit = smoothstep(0.0, min(max(blendOut, 0.001), span * 0.5), span - delta);", "    return enter * exit;", "}", "", "vec4 color360(float angle, vec4 baseColor, int rangeOffset, int rangeCount, float blendDegrees) {", "    vec4 color = baseColor;", "    for (int i = 0; i < MAX_OUTLINE_360_RANGES; i++) {", "        if (i >= rangeCount) {", "            break;", "        }", "", "        int idx = (rangeOffset + i) * 3;", "        vec4 range = ranges[idx];", "        float spanT;", "        float factor = rangeFactor(angle, range, blendDegrees, spanT);", "        vec4 rangeColor = mix(ranges[idx + 1], ranges[idx + 2], spanT);", "        color = mix(color, rangeColor, factor);", "    }", "    return color;", "}", "", "void main() {", "    int base = QuadIndex * 4;", "    vec4 radius = params[base];", "    vec4 sizeThicknessSmooth = params[base + 1];", "    vec4 defaultColor = params[base + 2];", "    vec4 rangeInfo = params[base + 3];", "", "    vec2 coord = clamp(FragCoord, vec2(0.0), vec2(1.0));", "    vec2 size = max(sizeThicknessSmooth.xy, vec2(1.0));", "    float thickness = max(sizeThicknessSmooth.z, 0.0);", "    float alpha = outlineAlpha(coord, size, radius, thickness, sizeThicknessSmooth.w);", "    float angle = contourAngleDegrees(coord, size, radius, thickness, rangeInfo.w);", "", "    int rangeOffset = int(rangeInfo.x + 0.5);", "    int rangeCount = int(rangeInfo.y + 0.5);", "    vec4 color = color360(angle, defaultColor, rangeOffset, rangeCount, rangeInfo.z);", "    color.a *= alpha;", "", "    if (color.a <= 0.001) {", "        discard;", "    }", "", "    OutColor = color;", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }

    private final String s54() {
        Object[] objectArray = new String[]{"#version 150", "", "#moj_import <minecraft:dynamictransforms.glsl>", "#moj_import <minecraft:projection.glsl>", "", "in vec3 Position;", "in vec4 Color;", "in float LineWidth;", "", "out vec2 FragCoord;", "flat out int QuadIndex;", "", "void main() {", "    gl_Position = ProjMat * ModelViewMat * vec4(Position, 1.0);", "    FragCoord = Color.rg;", "    QuadIndex = max(int(LineWidth + 0.5) - 1, 0);", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }

    private final String s55() {
        Object[] objectArray = new String[]{"#version 150", "", "#moj_import <kimiko:common.glsl>", "", "in vec2 FragCoord;", "flat in int QuadIndex;", "", "const int MAX_OUTLINE_360_RANGES = 1024;", "", "layout(std140) uniform Outline360ParamsArray {", "    vec4 params[1024];", "};", "", "layout(std140) uniform Outline360RangesArray {", "    vec4 ranges[3072];", "};", "", "out vec4 OutColor;", "", "float outlineAlpha(vec2 coord, vec2 size, vec4 radius, float thickness, float smoothness) {", "    float outer = ralpha(size, coord, radius, smoothness);", "    vec2 innerSize = size - vec2(thickness * 2.0);", "    if (innerSize.x <= 0.0 || innerSize.y <= 0.0) {", "        return outer;", "    }", "", "    vec2 local = coord * size;", "    vec2 innerCoord = (local - vec2(thickness)) / innerSize;", "    vec4 innerRadius = max(radius - vec4(thickness), vec4(0.0));", "    float inner = ralpha(innerSize, innerCoord, innerRadius, smoothness);", "    return clamp(outer - inner, 0.0, 1.0);", "}", "", "float contourAngleDegrees(vec2 coord, vec2 size, vec4 radius, float thickness, float offsetDegrees) {", "    vec2 local = coord * size;", "    float halfThickness = thickness * 0.5;", "    vec2 samplePoint = local;", "", "    if (radius.y > 0.0 && local.x >= size.x - radius.y && local.y <= radius.y) {", "        vec2 center = vec2(size.x - radius.y, radius.y);", "        vec2 dir = local - center;", "        samplePoint = center + normalize(dir + vec2(0.0001, -0.0001)) * max(radius.y - halfThickness, 0.0);", "    } else if (radius.x > 0.0 && local.x <= radius.x && local.y <= radius.x) {", "        vec2 center = vec2(radius.x, radius.x);", "        vec2 dir = local - center;", "        samplePoint = center + normalize(dir + vec2(-0.0001, -0.0001)) * max(radius.x - halfThickness, 0.0);", "    } else if (radius.w > 0.0 && local.x <= radius.w && local.y >= size.y - radius.w) {", "        vec2 center = vec2(radius.w, size.y - radius.w);", "        vec2 dir = local - center;", "        samplePoint = center + normalize(dir + vec2(-0.0001, 0.0001)) * max(radius.w - halfThickness, 0.0);", "    } else if (radius.z > 0.0 && local.x >= size.x - radius.z && local.y >= size.y - radius.z) {", "        vec2 center = vec2(size.x - radius.z, size.y - radius.z);", "        vec2 dir = local - center;", "        samplePoint = center + normalize(dir + vec2(0.0001, 0.0001)) * max(radius.z - halfThickness, 0.0);", "    } else {", "        float left = local.x;", "        float right = size.x - local.x;", "        float top = local.y;", "        float bottom = size.y - local.y;", "        float nearest = min(min(left, right), min(top, bottom));", "", "        if (nearest == top) {", "            samplePoint.y = halfThickness;", "        } else if (nearest == right) {", "            samplePoint.x = size.x - halfThickness;", "        } else if (nearest == bottom) {", "            samplePoint.y = size.y - halfThickness;", "        } else {", "            samplePoint.x = halfThickness;", "        }", "    }", "", "    vec2 p = samplePoint - size * 0.5;", "    float angle = degrees(atan(-p.y, p.x)) + offsetDegrees;", "    return mod(angle + 360.0, 360.0);", "}", "", "float rangeFactor(float angle, vec4 range, float blendDegrees, out float spanT) {", "    float start = mod(range.x + 360.0, 360.0);", "    float finish = mod(range.y + 360.0, 360.0);", "    float span = mod(finish - start + 360.0, 360.0);", "    if (span <= 0.001 || span >= 359.999) {", "        spanT = span >= 359.999 ? mod(angle - start + 360.0, 360.0) / 360.0 : 0.0;", "        return 1.0;", "    }", "", "    float delta = mod(angle - start + 360.0, 360.0);", "    spanT = clamp(delta / span, 0.0, 1.0);", "    if (delta > span) {", "        return 0.0;", "    }", "", "    float blendIn = range.z < 0.0 ? blendDegrees : range.z;", "    float blendOut = range.w < 0.0 ? blendDegrees : range.w;", "    float enter = smoothstep(0.0, min(max(blendIn, 0.001), span * 0.5), delta);", "    float exit = smoothstep(0.0, min(max(blendOut, 0.001), span * 0.5), span - delta);", "    return enter * exit;", "}", "", "vec4 color360(float angle, vec4 baseColor, int rangeOffset, int rangeCount, float blendDegrees) {", "    vec4 color = baseColor;", "    for (int i = 0; i < MAX_OUTLINE_360_RANGES; i++) {", "        if (i >= rangeCount) {", "            break;", "        }", "", "        int idx = (rangeOffset + i) * 3;", "        vec4 range = ranges[idx];", "        float spanT;", "        float factor = rangeFactor(angle, range, blendDegrees, spanT);", "        vec4 rangeColor = mix(ranges[idx + 1], ranges[idx + 2], spanT);", "        color = mix(color, rangeColor, factor);", "    }", "    return color;", "}", "", "void main() {", "    int base = QuadIndex * 4;", "    vec4 radius = params[base];", "    vec4 sizeThicknessSmooth = params[base + 1];", "    vec4 defaultColor = params[base + 2];", "    vec4 rangeInfo = params[base + 3];", "", "    vec2 coord = clamp(FragCoord, vec2(0.0), vec2(1.0));", "    vec2 size = max(sizeThicknessSmooth.xy, vec2(1.0));", "    float thickness = max(sizeThicknessSmooth.z, 0.0);", "    float alpha = outlineAlpha(coord, size, radius, thickness, sizeThicknessSmooth.w);", "    float angle = contourAngleDegrees(coord, size, radius, thickness, rangeInfo.w);", "", "    int rangeOffset = int(rangeInfo.x + 0.5);", "    int rangeCount = int(rangeInfo.y + 0.5);", "    vec4 color = color360(angle, defaultColor, rangeOffset, rangeCount, rangeInfo.z);", "    color.a *= alpha;", "", "    if (color.a <= 0.001) {", "        discard;", "    }", "", "    OutColor = color;", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }

    private final String s56() {
        Object[] objectArray = new String[]{"//!vertex", "#version 150", "", "#moj_import <minecraft:dynamictransforms.glsl>", "#moj_import <minecraft:projection.glsl>", "", "in vec3 Position;", "in vec4 Color;", "in float LineWidth;", "", "out vec2 FragCoord;", "out vec2 ScreenPos;", "flat out int QuadIndex;", "", "void main() {", "    gl_Position = ProjMat * ModelViewMat * vec4(Position, 1.0);", "    FragCoord = Color.rg;", "    ScreenPos = Position.xy;", "    QuadIndex = max(int(LineWidth + 0.5) - 1, 0);", "}", "//!fragment", "#version 150", "", "#moj_import <kimiko:common.glsl>", "", "in vec2 FragCoord;", "in vec2 ScreenPos;", "flat in int QuadIndex;", "", "layout(std140) uniform OutlineParamsArray {", "    vec4 params[4000];", "};", "", "layout(std140) uniform PaletteParams {", "    vec4 paletteMeta;", "    vec4 paletteColors[6];", "    vec4 paletteMeta2;", "    vec4 paletteLayerA[6];", "    vec4 paletteWaveA;", "    vec4 paletteWave2;", "    vec4 paletteLayerB[6];", "    vec4 paletteWaveB;", "    vec4 palettePadB;", "    vec4 paletteLayerC[6];", "    vec4 paletteWaveC;", "    vec4 palettePadC;", "    vec4 paletteLayerD[6];", "    vec4 paletteWaveD;", "    vec4 palettePadD;", "    vec4 paletteLayerE[6];", "    vec4 paletteWaveE;", "    vec4 palettePadE;", "    vec4 paletteLayerF[6];", "    vec4 paletteWaveF;", "    vec4 palettePadF;", "};", "", "", "out vec4 OutColor;", "", "float kLayerA = 0.0;", "float kLayerB = 0.0;", "float kLayerC = 0.0;", "float kLayerD = 0.0;", "float kLayerE = 0.0;", "float kLayerF = 0.0;", "", "float themeLayerCoverage(vec4 wave, vec2 fragXY) {", "    if (wave.w < 0.5) {", "        return 0.0;", "    }", "    if (wave.w > 1.5) {", "        return clamp(wave.z, 0.0, 1.0);", "    }", "    vec2 res = max(paletteWave2.xy, vec2(1.0));", "    float aspect = res.x / res.y;", "    vec2 d = (fragXY / res - wave.xy) * vec2(aspect, 1.0);", "    float wf = max(paletteWave2.z, 0.0005);", "    return 1.0 - smoothstep(wave.z - wf, wave.z + wf, length(d));", "}", "", "void themeWaveMix(vec2 fragXY) {", "    kLayerA = themeLayerCoverage(paletteWaveA, fragXY);", "    kLayerB = themeLayerCoverage(paletteWaveB, fragXY);", "    kLayerC = themeLayerCoverage(paletteWaveC, fragXY);", "    kLayerD = themeLayerCoverage(paletteWaveD, fragXY);", "    kLayerE = themeLayerCoverage(paletteWaveE, fragXY);", "    kLayerF = themeLayerCoverage(paletteWaveF, fragXY);", "}", "", "vec3 themeBlend(vec3 base, vec3 a, vec3 b, vec3 c, vec3 d, vec3 e, vec3 f) {", "    vec3 r = mix(base, a, kLayerA);", "    r = mix(r, b, kLayerB);", "    r = mix(r, c, kLayerC);", "    r = mix(r, d, kLayerD);", "    r = mix(r, e, kLayerE);", "    return mix(r, f, kLayerF);", "}", "", "vec3 paletteColorAt(int idx) {", "    if (idx <= 0) return themeBlend(paletteColors[0].rgb, paletteLayerA[0].rgb, paletteLayerB[0].rgb, paletteLayerC[0].rgb, paletteLayerD[0].rgb, paletteLayerE[0].rgb, paletteLayerF[0].rgb);", "    if (idx == 1) return themeBlend(paletteColors[1].rgb, paletteLayerA[1].rgb, paletteLayerB[1].rgb, paletteLayerC[1].rgb, paletteLayerD[1].rgb, paletteLayerE[1].rgb, paletteLayerF[1].rgb);", "    if (idx == 2) return themeBlend(paletteColors[2].rgb, paletteLayerA[2].rgb, paletteLayerB[2].rgb, paletteLayerC[2].rgb, paletteLayerD[2].rgb, paletteLayerE[2].rgb, paletteLayerF[2].rgb);", "    if (idx == 3) return themeBlend(paletteColors[3].rgb, paletteLayerA[3].rgb, paletteLayerB[3].rgb, paletteLayerC[3].rgb, paletteLayerD[3].rgb, paletteLayerE[3].rgb, paletteLayerF[3].rgb);", "    if (idx == 4) return themeBlend(paletteColors[4].rgb, paletteLayerA[4].rgb, paletteLayerB[4].rgb, paletteLayerC[4].rgb, paletteLayerD[4].rgb, paletteLayerE[4].rgb, paletteLayerF[4].rgb);", "    return themeBlend(paletteColors[5].rgb, paletteLayerA[5].rgb, paletteLayerB[5].rgb, paletteLayerC[5].rgb, paletteLayerD[5].rgb, paletteLayerE[5].rgb, paletteLayerF[5].rgb);", "}", "", "vec3 paletteRamp(float t) {", "    int count = int(paletteMeta.x + 0.5);", "    if (count <= 1) {", "        return paletteColorAt(0);", "    }", "    float f = clamp(t, 0.0, 1.0) * float(count - 1);", "    int i = clamp(int(floor(f)), 0, count - 1);", "    int j = min(i + 1, count - 1);", "    float frac = clamp(f - float(i), 0.0, 1.0);", "    frac = frac * frac * (3.0 - 2.0 * frac);", "    return mix(paletteColorAt(i), paletteColorAt(j), frac);", "}", "", "vec3 paletteLoop(float t) {", "    int count = int(paletteMeta.x + 0.5);", "    if (count <= 1) {", "        return paletteColorAt(0);", "    }", "    float f = fract(t) * float(count);", "    int i1 = clamp(int(floor(f)), 0, count - 1);", "    float u = clamp(f - float(i1), 0.0, 1.0);", "", "    int i0 = i1 - 1; if (i0 < 0) i0 += count;", "    int i2 = i1 + 1; if (i2 >= count) i2 -= count;", "    int i3 = i1 + 2; if (i3 >= count) i3 -= count;", "", "    vec3 c0 = paletteColorAt(i0);", "    vec3 c1 = paletteColorAt(i1);", "    vec3 c2 = paletteColorAt(i2);", "    vec3 c3 = paletteColorAt(i3);", "", "    float u2 = u * u;", "    float u3 = u2 * u;", "    vec3 cyclicCol = 0.5 * ((2.0 * c1)", "                      + (-c0 + c2) * u", "                      + (2.0 * c0 - 5.0 * c1 + 4.0 * c2 - c3) * u2", "                      + (-c0 + 3.0 * c1 - 3.0 * c2 + c3) * u3);", "", "    float tri = 0.5 - 0.5 * cos(6.2831853 * fract(t));", "    vec3 mirrorCol = paletteRamp(tri);", "", "    float closed = clamp(paletteMeta2.y, 0.0, 1.0);", "    return clamp(mix(mirrorCol, cyclicCol, closed), 0.0, 1.0);", "}", "", "vec3 boxGradient(vec2 uv, float phase) {", "    vec3 tl = paletteLoop(phase);", "    vec3 tr = paletteLoop(phase + 0.25);", "    vec3 br = paletteLoop(phase + 0.5);", "    vec3 bl = paletteLoop(phase + 0.75);", "    vec3 top = mix(tl, tr, uv.x);", "    vec3 bot = mix(bl, br, uv.x);", "    return mix(top, bot, uv.y);", "}", "", "vec3 meshGradient(vec2 uv, float phase, float aspect) {", "    float a = 6.2831853 * phase;", "    vec3 sum = vec3(0.0);", "    float wsum = 0.0;", "    for (int i = 0; i < 6; i++) {", "        float fi = float(i);", "        float kx = mod(fi, 2.0) < 0.5 ? 1.0 : 2.0;", "        float ky = mod(fi, 2.0) < 0.5 ? 2.0 : 1.0;", "        vec2 pos = vec2(0.5) + 0.34 * vec2(sin(a * kx + fi * 2.3999), cos(a * ky - fi * 1.618));", "        vec2 d = (uv - pos) * vec2(aspect, 1.0);", "        float w = 1.0 / (dot(d, d) * 5.0 + 0.06);", "        sum += paletteRamp(fi / 5.0) * w;", "        wsum += w;", "    }", "    return sum / wsum;", "}", "", "vec3 styleColor(float id, vec2 uv, float phase, float aspect, float tri) {", "    int s = int(id + 0.5);", "    if (s == 1) return meshGradient(uv, phase, aspect);", "    if (s == 2) return boxGradient(uv, phase * 20.0);", "    return paletteRamp(smoothstep(0.0, 1.0, tri)) * 0.86;", "}", "", "vec4 outlineColor(vec2 coord, vec4 topLeft, vec4 topRight, vec4 bottomRight, vec4 bottomLeft) {", "    vec4 top = mix(topLeft, topRight, coord.x);", "    vec4 bottom = mix(bottomLeft, bottomRight, coord.x);", "    return mix(top, bottom, coord.y);", "}", "", "float outlineAlpha(vec2 coord, vec2 size, vec4 radius, float thickness, float smoothness) {", "    float outer = ralpha(size, coord, radius, smoothness);", "    vec2 innerSize = size - vec2(thickness * 2.0);", "    if (innerSize.x <= 0.0 || innerSize.y <= 0.0) {", "        return outer;", "    }", "", "    vec2 local = coord * size;", "    vec2 innerCoord = (local - vec2(thickness)) / innerSize;", "    vec4 innerRadius = max(radius - vec4(thickness), vec4(0.0));", "    float inner = ralpha(innerSize, innerCoord, innerRadius, smoothness);", "    return clamp(outer - inner, 0.0, 1.0);", "}", "", "void main() {", "    int base = QuadIndex * 10;", "    vec4 radius = params[base];", "    vec4 sizeThicknessSmooth = params[base + 1];", "    vec4 topLeft = params[base + 2];", "    vec4 topRight = params[base + 3];", "    vec4 bottomRight = params[base + 4];", "    vec4 bottomLeft = params[base + 5];", "    vec4 scissorPosSize = params[base + 6];", "    vec4 scissorRadius = params[base + 7];", "    vec4 scissorRotation = params[base + 8];", "    vec4 scissorFade = params[base + 9];", "", "    vec2 size = max(sizeThicknessSmooth.xy, vec2(1.0));", "", "    const float AA_PAD = 1.5;", "    vec2 f = vec2(AA_PAD) / size;", "    vec2 shapeCoord = FragCoord * (vec2(1.0) + 2.0 * f) - f;", "    vec2 coord = clamp(shapeCoord, vec2(0.0), vec2(1.0));", "    float thickness = max(sizeThicknessSmooth.z, 0.0);", "    float alpha = outlineAlpha(shapeCoord, size, radius, thickness, sizeThicknessSmooth.w);", "", "    vec4 color = outlineColor(coord, topLeft, topRight, bottomRight, bottomLeft);", "", "    // Follow the fill's gradient when asked to, so an outline drawn around a", "    // client rect runs the same colours through the same style rather than", "    // fading between four corners of its own.", "    float paletteStrength = clamp(scissorRotation.z, 0.0, 1.0);", "    if (paletteStrength > 0.001 && int(paletteMeta.x + 0.5) >= 2) {", "        float colorOffset = scissorRotation.w;", "        vec2 gradCoord = coord;", "        float gradAspect = size.x / size.y;", "        themeWaveMix(gl_FragCoord.xy);", "", "        float tri = abs(fract(gradCoord.x + fract(colorOffset)) * 2.0 - 1.0);", "        vec3 targetCol = styleColor(paletteMeta.z, gradCoord, paletteMeta.y, gradAspect, tri);", "", "        float sweep = paletteMeta.w;", "        if (sweep >= 0.0) {", "            vec3 prevCol = styleColor(paletteMeta2.x, gradCoord, paletteMeta.y, gradAspect, tri);", "            float axis = gradCoord.x + (gradCoord.y - 0.5) * 0.30;", "            float head = sweep * 1.5 - 0.25;", "            float wipe = 1.0 - smoothstep(head - 0.04, head + 0.04, axis);", "            color.rgb = mix(color.rgb, mix(prevCol, targetCol, wipe), paletteStrength);", "        } else {", "            color.rgb = mix(color.rgb, targetCol, paletteStrength);", "        }", "    }", "", "    color.a *= alpha;", "", "    if (scissorPosSize.z > 0.0 && scissorPosSize.w > 0.0) {", "        vec2 clipPoint = ScreenPos;", "        if (abs(scissorRotation.y) > 0.0001) {", "            vec2 clipCenter = scissorPosSize.xy + scissorPosSize.zw * 0.5;", "            vec2 rel = ScreenPos - clipCenter;", "            clipPoint = clipCenter + vec2(", "                rel.x * scissorRotation.x + rel.y * scissorRotation.y,", "                -rel.x * scissorRotation.y + rel.y * scissorRotation.x", "            );", "        }", "        vec2 scissorCoord = (clipPoint - scissorPosSize.xy) / scissorPosSize.zw;", "        color.a *= ralpha(scissorPosSize.zw, scissorCoord, max(scissorRadius, vec4(0.0)), sizeThicknessSmooth.w);", "        if (scissorFade.x > 0.0) {", "            color.a *= smoothstep(0.0, scissorFade.x, clipPoint.y - scissorPosSize.y);", "        }", "    }", "", "    if (color.a <= 0.001) {", "        discard;", "    }", "", "    OutColor = color;", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }

    private final String s57() {
        Object[] objectArray = new String[]{"#version 150", "", "#moj_import <minecraft:dynamictransforms.glsl>", "#moj_import <minecraft:projection.glsl>", "", "in vec3 Position;", "in vec4 Color;", "in float LineWidth;", "", "out vec2 FragCoord;", "out vec2 ScreenPos;", "flat out int QuadIndex;", "", "void main() {", "    gl_Position = ProjMat * ModelViewMat * vec4(Position, 1.0);", "    FragCoord = Color.rg;", "    ScreenPos = Position.xy;", "    QuadIndex = max(int(LineWidth + 0.5) - 1, 0);", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }

    private final String s58() {
        Object[] objectArray = new String[]{"#version 150", "", "#moj_import <kimiko:common.glsl>", "", "in vec2 FragCoord;", "in vec2 ScreenPos;", "flat in int QuadIndex;", "", "layout(std140) uniform OutlineParamsArray {", "    vec4 params[4000];", "};", "", "layout(std140) uniform PaletteParams {", "    vec4 paletteMeta;", "    vec4 paletteColors[6];", "    vec4 paletteMeta2;", "    vec4 paletteLayerA[6];", "    vec4 paletteWaveA;", "    vec4 paletteWave2;", "    vec4 paletteLayerB[6];", "    vec4 paletteWaveB;", "    vec4 palettePadB;", "    vec4 paletteLayerC[6];", "    vec4 paletteWaveC;", "    vec4 palettePadC;", "    vec4 paletteLayerD[6];", "    vec4 paletteWaveD;", "    vec4 palettePadD;", "    vec4 paletteLayerE[6];", "    vec4 paletteWaveE;", "    vec4 palettePadE;", "    vec4 paletteLayerF[6];", "    vec4 paletteWaveF;", "    vec4 palettePadF;", "};", "", "", "out vec4 OutColor;", "", "float kLayerA = 0.0;", "float kLayerB = 0.0;", "float kLayerC = 0.0;", "float kLayerD = 0.0;", "float kLayerE = 0.0;", "float kLayerF = 0.0;", "", "float themeLayerCoverage(vec4 wave, vec2 fragXY) {", "    if (wave.w < 0.5) {", "        return 0.0;", "    }", "    if (wave.w > 1.5) {", "        return clamp(wave.z, 0.0, 1.0);", "    }", "    vec2 res = max(paletteWave2.xy, vec2(1.0));", "    float aspect = res.x / res.y;", "    vec2 d = (fragXY / res - wave.xy) * vec2(aspect, 1.0);", "    float wf = max(paletteWave2.z, 0.0005);", "    return 1.0 - smoothstep(wave.z - wf, wave.z + wf, length(d));", "}", "", "void themeWaveMix(vec2 fragXY) {", "    kLayerA = themeLayerCoverage(paletteWaveA, fragXY);", "    kLayerB = themeLayerCoverage(paletteWaveB, fragXY);", "    kLayerC = themeLayerCoverage(paletteWaveC, fragXY);", "    kLayerD = themeLayerCoverage(paletteWaveD, fragXY);", "    kLayerE = themeLayerCoverage(paletteWaveE, fragXY);", "    kLayerF = themeLayerCoverage(paletteWaveF, fragXY);", "}", "", "vec3 themeBlend(vec3 base, vec3 a, vec3 b, vec3 c, vec3 d, vec3 e, vec3 f) {", "    vec3 r = mix(base, a, kLayerA);", "    r = mix(r, b, kLayerB);", "    r = mix(r, c, kLayerC);", "    r = mix(r, d, kLayerD);", "    r = mix(r, e, kLayerE);", "    return mix(r, f, kLayerF);", "}", "", "vec3 paletteColorAt(int idx) {", "    if (idx <= 0) return themeBlend(paletteColors[0].rgb, paletteLayerA[0].rgb, paletteLayerB[0].rgb, paletteLayerC[0].rgb, paletteLayerD[0].rgb, paletteLayerE[0].rgb, paletteLayerF[0].rgb);", "    if (idx == 1) return themeBlend(paletteColors[1].rgb, paletteLayerA[1].rgb, paletteLayerB[1].rgb, paletteLayerC[1].rgb, paletteLayerD[1].rgb, paletteLayerE[1].rgb, paletteLayerF[1].rgb);", "    if (idx == 2) return themeBlend(paletteColors[2].rgb, paletteLayerA[2].rgb, paletteLayerB[2].rgb, paletteLayerC[2].rgb, paletteLayerD[2].rgb, paletteLayerE[2].rgb, paletteLayerF[2].rgb);", "    if (idx == 3) return themeBlend(paletteColors[3].rgb, paletteLayerA[3].rgb, paletteLayerB[3].rgb, paletteLayerC[3].rgb, paletteLayerD[3].rgb, paletteLayerE[3].rgb, paletteLayerF[3].rgb);", "    if (idx == 4) return themeBlend(paletteColors[4].rgb, paletteLayerA[4].rgb, paletteLayerB[4].rgb, paletteLayerC[4].rgb, paletteLayerD[4].rgb, paletteLayerE[4].rgb, paletteLayerF[4].rgb);", "    return themeBlend(paletteColors[5].rgb, paletteLayerA[5].rgb, paletteLayerB[5].rgb, paletteLayerC[5].rgb, paletteLayerD[5].rgb, paletteLayerE[5].rgb, paletteLayerF[5].rgb);", "}", "", "vec3 paletteRamp(float t) {", "    int count = int(paletteMeta.x + 0.5);", "    if (count <= 1) {", "        return paletteColorAt(0);", "    }", "    float f = clamp(t, 0.0, 1.0) * float(count - 1);", "    int i = clamp(int(floor(f)), 0, count - 1);", "    int j = min(i + 1, count - 1);", "    float frac = clamp(f - float(i), 0.0, 1.0);", "    frac = frac * frac * (3.0 - 2.0 * frac);", "    return mix(paletteColorAt(i), paletteColorAt(j), frac);", "}", "", "vec3 paletteLoop(float t) {", "    int count = int(paletteMeta.x + 0.5);", "    if (count <= 1) {", "        return paletteColorAt(0);", "    }", "    float f = fract(t) * float(count);", "    int i1 = clamp(int(floor(f)), 0, count - 1);", "    float u = clamp(f - float(i1), 0.0, 1.0);", "", "    int i0 = i1 - 1; if (i0 < 0) i0 += count;", "    int i2 = i1 + 1; if (i2 >= count) i2 -= count;", "    int i3 = i1 + 2; if (i3 >= count) i3 -= count;", "", "    vec3 c0 = paletteColorAt(i0);", "    vec3 c1 = paletteColorAt(i1);", "    vec3 c2 = paletteColorAt(i2);", "    vec3 c3 = paletteColorAt(i3);", "", "    float u2 = u * u;", "    float u3 = u2 * u;", "    vec3 cyclicCol = 0.5 * ((2.0 * c1)", "                      + (-c0 + c2) * u", "                      + (2.0 * c0 - 5.0 * c1 + 4.0 * c2 - c3) * u2", "                      + (-c0 + 3.0 * c1 - 3.0 * c2 + c3) * u3);", "", "    float tri = 0.5 - 0.5 * cos(6.2831853 * fract(t));", "    vec3 mirrorCol = paletteRamp(tri);", "", "    float closed = clamp(paletteMeta2.y, 0.0, 1.0);", "    return clamp(mix(mirrorCol, cyclicCol, closed), 0.0, 1.0);", "}", "", "vec3 boxGradient(vec2 uv, float phase) {", "    vec3 tl = paletteLoop(phase);", "    vec3 tr = paletteLoop(phase + 0.25);", "    vec3 br = paletteLoop(phase + 0.5);", "    vec3 bl = paletteLoop(phase + 0.75);", "    vec3 top = mix(tl, tr, uv.x);", "    vec3 bot = mix(bl, br, uv.x);", "    return mix(top, bot, uv.y);", "}", "", "vec3 meshGradient(vec2 uv, float phase, float aspect) {", "    float a = 6.2831853 * phase;", "    vec3 sum = vec3(0.0);", "    float wsum = 0.0;", "    for (int i = 0; i < 6; i++) {", "        float fi = float(i);", "        float kx = mod(fi, 2.0) < 0.5 ? 1.0 : 2.0;", "        float ky = mod(fi, 2.0) < 0.5 ? 2.0 : 1.0;", "        vec2 pos = vec2(0.5) + 0.34 * vec2(sin(a * kx + fi * 2.3999), cos(a * ky - fi * 1.618));", "        vec2 d = (uv - pos) * vec2(aspect, 1.0);", "        float w = 1.0 / (dot(d, d) * 5.0 + 0.06);", "        sum += paletteRamp(fi / 5.0) * w;", "        wsum += w;", "    }", "    return sum / wsum;", "}", "", "vec3 styleColor(float id, vec2 uv, float phase, float aspect, float tri) {", "    int s = int(id + 0.5);", "    if (s == 1) return meshGradient(uv, phase, aspect);", "    if (s == 2) return boxGradient(uv, phase * 20.0);", "    return paletteRamp(smoothstep(0.0, 1.0, tri)) * 0.86;", "}", "", "vec4 outlineColor(vec2 coord, vec4 topLeft, vec4 topRight, vec4 bottomRight, vec4 bottomLeft) {", "    vec4 top = mix(topLeft, topRight, coord.x);", "    vec4 bottom = mix(bottomLeft, bottomRight, coord.x);", "    return mix(top, bottom, coord.y);", "}", "", "float outlineAlpha(vec2 coord, vec2 size, vec4 radius, float thickness, float smoothness) {", "    float outer = ralpha(size, coord, radius, smoothness);", "    vec2 innerSize = size - vec2(thickness * 2.0);", "    if (innerSize.x <= 0.0 || innerSize.y <= 0.0) {", "        return outer;", "    }", "", "    vec2 local = coord * size;", "    vec2 innerCoord = (local - vec2(thickness)) / innerSize;", "    vec4 innerRadius = max(radius - vec4(thickness), vec4(0.0));", "    float inner = ralpha(innerSize, innerCoord, innerRadius, smoothness);", "    return clamp(outer - inner, 0.0, 1.0);", "}", "", "void main() {", "    int base = QuadIndex * 10;", "    vec4 radius = params[base];", "    vec4 sizeThicknessSmooth = params[base + 1];", "    vec4 topLeft = params[base + 2];", "    vec4 topRight = params[base + 3];", "    vec4 bottomRight = params[base + 4];", "    vec4 bottomLeft = params[base + 5];", "    vec4 scissorPosSize = params[base + 6];", "    vec4 scissorRadius = params[base + 7];", "    vec4 scissorRotation = params[base + 8];", "    vec4 scissorFade = params[base + 9];", "", "    vec2 size = max(sizeThicknessSmooth.xy, vec2(1.0));", "", "    const float AA_PAD = 1.5;", "    vec2 f = vec2(AA_PAD) / size;", "    vec2 shapeCoord = FragCoord * (vec2(1.0) + 2.0 * f) - f;", "    vec2 coord = clamp(shapeCoord, vec2(0.0), vec2(1.0));", "    float thickness = max(sizeThicknessSmooth.z, 0.0);", "    float alpha = outlineAlpha(shapeCoord, size, radius, thickness, sizeThicknessSmooth.w);", "", "    vec4 color = outlineColor(coord, topLeft, topRight, bottomRight, bottomLeft);", "", "    // Follow the fill's gradient when asked to, so an outline drawn around a", "    // client rect runs the same colours through the same style rather than", "    // fading between four corners of its own.", "    float paletteStrength = clamp(scissorRotation.z, 0.0, 1.0);", "    if (paletteStrength > 0.001 && int(paletteMeta.x + 0.5) >= 2) {", "        float colorOffset = scissorRotation.w;", "        vec2 gradCoord = coord;", "        float gradAspect = size.x / size.y;", "        themeWaveMix(gl_FragCoord.xy);", "", "        float tri = abs(fract(gradCoord.x + fract(colorOffset)) * 2.0 - 1.0);", "        vec3 targetCol = styleColor(paletteMeta.z, gradCoord, paletteMeta.y, gradAspect, tri);", "", "        float sweep = paletteMeta.w;", "        if (sweep >= 0.0) {", "            vec3 prevCol = styleColor(paletteMeta2.x, gradCoord, paletteMeta.y, gradAspect, tri);", "            float axis = gradCoord.x + (gradCoord.y - 0.5) * 0.30;", "            float head = sweep * 1.5 - 0.25;", "            float wipe = 1.0 - smoothstep(head - 0.04, head + 0.04, axis);", "            color.rgb = mix(color.rgb, mix(prevCol, targetCol, wipe), paletteStrength);", "        } else {", "            color.rgb = mix(color.rgb, targetCol, paletteStrength);", "        }", "    }", "", "    color.a *= alpha;", "", "    if (scissorPosSize.z > 0.0 && scissorPosSize.w > 0.0) {", "        vec2 clipPoint = ScreenPos;", "        if (abs(scissorRotation.y) > 0.0001) {", "            vec2 clipCenter = scissorPosSize.xy + scissorPosSize.zw * 0.5;", "            vec2 rel = ScreenPos - clipCenter;", "            clipPoint = clipCenter + vec2(", "                rel.x * scissorRotation.x + rel.y * scissorRotation.y,", "                -rel.x * scissorRotation.y + rel.y * scissorRotation.x", "            );", "        }", "        vec2 scissorCoord = (clipPoint - scissorPosSize.xy) / scissorPosSize.zw;", "        color.a *= ralpha(scissorPosSize.zw, scissorCoord, max(scissorRadius, vec4(0.0)), sizeThicknessSmooth.w);", "        if (scissorFade.x > 0.0) {", "            color.a *= smoothstep(0.0, scissorFade.x, clipPoint.y - scissorPosSize.y);", "        }", "    }", "", "    if (color.a <= 0.001) {", "        discard;", "    }", "", "    OutColor = color;", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }

    private final String s59() {
        Object[] objectArray = new String[]{"//!vertex", "#version 150", "", "#moj_import <minecraft:dynamictransforms.glsl>", "#moj_import <minecraft:projection.glsl>", "", "in vec3 Position;", "in vec4 Color;", "in float LineWidth;", "", "out vec2 FragCoord;", "flat out int QuadIndex;", "", "layout(std140) uniform GlassOutlineParamsArray {", "    vec4 params[3072];", "};", "", "void main() {", "    int index = max(int(LineWidth + 0.5) - 1, 0);", "    int base = index * 7;", "    vec4 flagsDistortZSquirt = params[base + 4];", "", "    gl_Position = ProjMat * ModelViewMat * vec4(Position.xy, flagsDistortZSquirt.z, 1.0);", "    FragCoord = Color.rg;", "    QuadIndex = index;", "}", "//!fragment", "#version 150", "", "#moj_import <kimiko:common.glsl>", "", "in vec2 FragCoord;", "flat in int QuadIndex;", "", "uniform sampler2D Sampler0;", "", "layout(std140) uniform GlassOutlineParamsArray {", "    vec4 params[3072];", "};", "", "out vec4 OutColor;", "", "float outlineAlpha(vec2 coord, vec2 size, vec4 radius, float thickness, float smoothness) {", "    float outer = ralpha(size, coord, radius, smoothness);", "    vec2 innerSize = size - vec2(thickness * 2.0);", "    if (innerSize.x <= 0.0 || innerSize.y <= 0.0) {", "        return outer;", "    }", "", "    vec2 local = coord * size;", "    vec2 innerCoord = (local - vec2(thickness)) / innerSize;", "    vec4 innerRadius = max(radius - vec4(thickness), vec4(0.0));", "    float inner = ralpha(innerSize, innerCoord, innerRadius, smoothness);", "    return clamp(outer - inner, 0.0, 1.0);", "}", "", "void main() {", "    int base = QuadIndex * 7;", "    vec4 radius = max(params[base], vec4(0.0));", "    vec4 sizeThicknessSmooth = params[base + 1];", "    vec4 alphaPowerMix = params[base + 2];", "    vec4 fresnelColor = params[base + 3];", "    vec4 flagsDistortZSquirt = params[base + 4];", "    vec4 tintColor = params[base + 5];", "    vec4 reg = params[base + 6];", "", "    vec2 coord = clamp(FragCoord, vec2(0.0), vec2(1.0));", "    vec2 size = max(sizeThicknessSmooth.xy, vec2(1.0));", "    float thickness = max(sizeThicknessSmooth.z, 0.0);", "    float smoothness = max(sizeThicknessSmooth.w, 0.001);", "    float globalAlpha = clamp(alphaPowerMix.x, 0.0, 1.0);", "    float fresnelPower = max(alphaPowerMix.y, 0.001);", "    float baseAlpha = clamp(alphaPowerMix.z, 0.0, 1.0);", "    float fresnelMix = clamp(alphaPowerMix.w, 0.0, 1.0);", "    float fresnelInvert = flagsDistortZSquirt.x;", "    float distortStrength = flagsDistortZSquirt.y;", "", "    float alpha = outlineAlpha(coord, size, radius, thickness, smoothness);", "", "    vec2 center = size * 0.5;", "    vec2 halfSize = max(center - 1.0, vec2(0.0));", "    vec2 pos = center - coord * size;", "    float distToEdge = abs(rdist(pos, halfSize, radius));", "    float maxDistNorm = max(min(halfSize.x, halfSize.y), 0.001);", "    float edgeGradient = 1.0 - clamp(distToEdge / maxDistNorm, 0.0, 1.0);", "    float fresnelBase = (fresnelInvert > 0.5) ? edgeGradient : (1.0 - edgeGradient);", "", "    float fresnel;", "    if (fresnelPower > 20.0) {", "        fresnel = exp(fresnelPower * log(clamp(fresnelBase, 0.001, 1.0)));", "    } else {", "        fresnel = pow(clamp(fresnelBase, 0.0, 1.0), fresnelPower);", "    }", "    fresnel = clamp(fresnel, 0.0, 1.0);", "", "    vec2 dir = (length(pos) > 0.001) ? normalize(-pos) : vec2(0.0);", "    vec2 texCoord = (gl_FragCoord.xy - reg.xy) / max(reg.zw, vec2(1.0));", "", "    float cornerRad = (pos.x > 0.0) ? ((pos.y > 0.0) ? radius.x : radius.w)", "                                    : ((pos.y > 0.0) ? radius.y : radius.z);", "    cornerRad = max(cornerRad, 1.0);", "    vec2 edgeXY = halfSize - abs(pos);", "    float cornerness = (1.0 - smoothstep(0.0, cornerRad, edgeXY.x)) * (1.0 - smoothstep(0.0, cornerRad, edgeXY.y));", "", "    vec2 ofs = dir * fresnel * distortStrength * (1.0 - 0.5 * cornerness);", "    vec2 sampleUv = clamp(texCoord + ofs, vec2(0.0), vec2(1.0));", "    vec4 baseCol = texture(Sampler0, sampleUv);", "", "    vec2 tap = vec2(2.2) / max(reg.zw, vec2(1.0));", "    vec4 smCol = baseCol * 0.20;", "    smCol += texture(Sampler0, clamp(sampleUv + vec2(tap.x, 0.0), 0.0, 1.0)) * 0.12;", "    smCol += texture(Sampler0, clamp(sampleUv - vec2(tap.x, 0.0), 0.0, 1.0)) * 0.12;", "    smCol += texture(Sampler0, clamp(sampleUv + vec2(0.0, tap.y), 0.0, 1.0)) * 0.12;", "    smCol += texture(Sampler0, clamp(sampleUv - vec2(0.0, tap.y), 0.0, 1.0)) * 0.12;", "    smCol += texture(Sampler0, clamp(sampleUv + tap, 0.0, 1.0)) * 0.08;", "    smCol += texture(Sampler0, clamp(sampleUv - tap, 0.0, 1.0)) * 0.08;", "    smCol += texture(Sampler0, clamp(sampleUv + vec2(tap.x, -tap.y), 0.0, 1.0)) * 0.08;", "    smCol += texture(Sampler0, clamp(sampleUv + vec2(-tap.x, tap.y), 0.0, 1.0)) * 0.08;", "    vec4 texColor = mix(baseCol, smCol, cornerness);", "", "    vec3 tintedColor = mix(texColor.rgb, tintColor.rgb, clamp(tintColor.a, 0.0, 1.0) * 0.65);", "    vec3 finalColor = mix(tintedColor, fresnelColor.rgb, fresnel * fresnelMix);", "    float finalAlpha = mix(baseAlpha, max(fresnelColor.a, tintColor.a), fresnel) * alpha * globalAlpha;", "", "    if (finalAlpha < 0.001) {", "        discard;", "    }", "", "    OutColor = vec4(finalColor, finalAlpha);", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }

    private final String s60() {
        Object[] objectArray = new String[]{"#version 150", "", "#moj_import <minecraft:dynamictransforms.glsl>", "#moj_import <minecraft:projection.glsl>", "", "in vec3 Position;", "in vec4 Color;", "in float LineWidth;", "", "out vec2 FragCoord;", "flat out int QuadIndex;", "", "layout(std140) uniform GlassOutlineParamsArray {", "    vec4 params[3072];", "};", "", "void main() {", "    int index = max(int(LineWidth + 0.5) - 1, 0);", "    int base = index * 7;", "    vec4 flagsDistortZSquirt = params[base + 4];", "", "    gl_Position = ProjMat * ModelViewMat * vec4(Position.xy, flagsDistortZSquirt.z, 1.0);", "    FragCoord = Color.rg;", "    QuadIndex = index;", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }

    private final String s61() {
        Object[] objectArray = new String[]{"#version 150", "", "#moj_import <kimiko:common.glsl>", "", "in vec2 FragCoord;", "flat in int QuadIndex;", "", "uniform sampler2D Sampler0;", "", "layout(std140) uniform GlassOutlineParamsArray {", "    vec4 params[3072];", "};", "", "out vec4 OutColor;", "", "float outlineAlpha(vec2 coord, vec2 size, vec4 radius, float thickness, float smoothness) {", "    float outer = ralpha(size, coord, radius, smoothness);", "    vec2 innerSize = size - vec2(thickness * 2.0);", "    if (innerSize.x <= 0.0 || innerSize.y <= 0.0) {", "        return outer;", "    }", "", "    vec2 local = coord * size;", "    vec2 innerCoord = (local - vec2(thickness)) / innerSize;", "    vec4 innerRadius = max(radius - vec4(thickness), vec4(0.0));", "    float inner = ralpha(innerSize, innerCoord, innerRadius, smoothness);", "    return clamp(outer - inner, 0.0, 1.0);", "}", "", "void main() {", "    int base = QuadIndex * 7;", "    vec4 radius = max(params[base], vec4(0.0));", "    vec4 sizeThicknessSmooth = params[base + 1];", "    vec4 alphaPowerMix = params[base + 2];", "    vec4 fresnelColor = params[base + 3];", "    vec4 flagsDistortZSquirt = params[base + 4];", "    vec4 tintColor = params[base + 5];", "    vec4 reg = params[base + 6];", "", "    vec2 coord = clamp(FragCoord, vec2(0.0), vec2(1.0));", "    vec2 size = max(sizeThicknessSmooth.xy, vec2(1.0));", "    float thickness = max(sizeThicknessSmooth.z, 0.0);", "    float smoothness = max(sizeThicknessSmooth.w, 0.001);", "    float globalAlpha = clamp(alphaPowerMix.x, 0.0, 1.0);", "    float fresnelPower = max(alphaPowerMix.y, 0.001);", "    float baseAlpha = clamp(alphaPowerMix.z, 0.0, 1.0);", "    float fresnelMix = clamp(alphaPowerMix.w, 0.0, 1.0);", "    float fresnelInvert = flagsDistortZSquirt.x;", "    float distortStrength = flagsDistortZSquirt.y;", "", "    float alpha = outlineAlpha(coord, size, radius, thickness, smoothness);", "", "    vec2 center = size * 0.5;", "    vec2 halfSize = max(center - 1.0, vec2(0.0));", "    vec2 pos = center - coord * size;", "    float distToEdge = abs(rdist(pos, halfSize, radius));", "    float maxDistNorm = max(min(halfSize.x, halfSize.y), 0.001);", "    float edgeGradient = 1.0 - clamp(distToEdge / maxDistNorm, 0.0, 1.0);", "    float fresnelBase = (fresnelInvert > 0.5) ? edgeGradient : (1.0 - edgeGradient);", "", "    float fresnel;", "    if (fresnelPower > 20.0) {", "        fresnel = exp(fresnelPower * log(clamp(fresnelBase, 0.001, 1.0)));", "    } else {", "        fresnel = pow(clamp(fresnelBase, 0.0, 1.0), fresnelPower);", "    }", "    fresnel = clamp(fresnel, 0.0, 1.0);", "", "    vec2 dir = (length(pos) > 0.001) ? normalize(-pos) : vec2(0.0);", "    vec2 texCoord = (gl_FragCoord.xy - reg.xy) / max(reg.zw, vec2(1.0));", "", "    float cornerRad = (pos.x > 0.0) ? ((pos.y > 0.0) ? radius.x : radius.w)", "                                    : ((pos.y > 0.0) ? radius.y : radius.z);", "    cornerRad = max(cornerRad, 1.0);", "    vec2 edgeXY = halfSize - abs(pos);", "    float cornerness = (1.0 - smoothstep(0.0, cornerRad, edgeXY.x)) * (1.0 - smoothstep(0.0, cornerRad, edgeXY.y));", "", "    vec2 ofs = dir * fresnel * distortStrength * (1.0 - 0.5 * cornerness);", "    vec2 sampleUv = clamp(texCoord + ofs, vec2(0.0), vec2(1.0));", "    vec4 baseCol = texture(Sampler0, sampleUv);", "", "    vec2 tap = vec2(2.2) / max(reg.zw, vec2(1.0));", "    vec4 smCol = baseCol * 0.20;", "    smCol += texture(Sampler0, clamp(sampleUv + vec2(tap.x, 0.0), 0.0, 1.0)) * 0.12;", "    smCol += texture(Sampler0, clamp(sampleUv - vec2(tap.x, 0.0), 0.0, 1.0)) * 0.12;", "    smCol += texture(Sampler0, clamp(sampleUv + vec2(0.0, tap.y), 0.0, 1.0)) * 0.12;", "    smCol += texture(Sampler0, clamp(sampleUv - vec2(0.0, tap.y), 0.0, 1.0)) * 0.12;", "    smCol += texture(Sampler0, clamp(sampleUv + tap, 0.0, 1.0)) * 0.08;", "    smCol += texture(Sampler0, clamp(sampleUv - tap, 0.0, 1.0)) * 0.08;", "    smCol += texture(Sampler0, clamp(sampleUv + vec2(tap.x, -tap.y), 0.0, 1.0)) * 0.08;", "    smCol += texture(Sampler0, clamp(sampleUv + vec2(-tap.x, tap.y), 0.0, 1.0)) * 0.08;", "    vec4 texColor = mix(baseCol, smCol, cornerness);", "", "    vec3 tintedColor = mix(texColor.rgb, tintColor.rgb, clamp(tintColor.a, 0.0, 1.0) * 0.65);", "    vec3 finalColor = mix(tintedColor, fresnelColor.rgb, fresnel * fresnelMix);", "    float finalAlpha = mix(baseAlpha, max(fresnelColor.a, tintColor.a), fresnel) * alpha * globalAlpha;", "", "    if (finalAlpha < 0.001) {", "        discard;", "    }", "", "    OutColor = vec4(finalColor, finalAlpha);", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }

    private final String s62() {
        Object[] objectArray = new String[]{"//!vertex", "#version 150", "", "#moj_import <minecraft:dynamictransforms.glsl>", "#moj_import <minecraft:projection.glsl>", "", "in vec3 Position;", "in vec4 Color;", "in float LineWidth;", "", "out vec2 FragCoord;", "flat out int QuadIndex;", "", "void main() {", "    gl_Position = ProjMat * ModelViewMat * vec4(Position, 1.0);", "    FragCoord = Color.rg;", "    QuadIndex = max(int(LineWidth + 0.5) - 1, 0);", "}", "//!fragment", "#version 150", "", "in vec2 FragCoord;", "flat in int QuadIndex;", "", "layout(std140) uniform PickerParamsArray {", "    vec4 params[768];", "};", "", "out vec4 OutColor;", "", "vec3 hueRamp(float h) {", "    vec3 k = mod(h * 6.0 + vec3(0.0, 4.0, 2.0), 6.0);", "    vec3 tri = clamp(abs(k - 3.0) - 1.0, 0.0, 1.0);", "    return tri * tri * (3.0 - 2.0 * tri);", "}", "", "float roundedAlpha(vec2 coord, vec2 size, float radius, float smoothness) {", "    vec2 p = coord * size;", "    vec2 center = size * 0.5;", "    float r = min(radius, min(center.x, center.y));", "    vec2 q = abs(p - center) - (center - vec2(r));", "    float dist = length(max(q, vec2(0.0))) + min(max(q.x, q.y), 0.0) - r;", "    return 1.0 - smoothstep(-smoothness, smoothness, dist);", "}", "", "void main() {", "    int base = QuadIndex * 3;", "    vec4 cfg = params[base];", "    vec4 sz = params[base + 1];", "    vec4 solid = params[base + 2];", "", "    int mode = int(cfg.x + 0.5);", "    vec2 coord = clamp(FragCoord, vec2(0.0), vec2(1.0));", "    vec2 size = max(sz.xy, vec2(1.0));", "", "    float shapeAlpha = roundedAlpha(coord, size, max(cfg.y, 0.0), max(cfg.z, 0.5));", "", "    vec3 rgb;", "    if (mode == 0) {", "        rgb = hueRamp(coord.x);", "    } else {", "", "        float cell = max(sz.z, 1.0);", "        vec2 c = floor((coord * size) / cell);", "        float square = mod(c.x + c.y, 2.0) < 0.5 ? 0.95 : 0.42;", "        rgb = mix(vec3(square), solid.rgb, coord.x);", "    }", "", "    float a = shapeAlpha * cfg.w;", "    if (a <= 0.001) {", "        discard;", "    }", "    OutColor = vec4(rgb, a);", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }

    private final String s63() {
        Object[] objectArray = new String[]{"#version 150", "", "#moj_import <minecraft:dynamictransforms.glsl>", "#moj_import <minecraft:projection.glsl>", "", "in vec3 Position;", "in vec4 Color;", "in float LineWidth;", "", "out vec2 FragCoord;", "flat out int QuadIndex;", "", "void main() {", "    gl_Position = ProjMat * ModelViewMat * vec4(Position, 1.0);", "    FragCoord = Color.rg;", "    QuadIndex = max(int(LineWidth + 0.5) - 1, 0);", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }

    private final String s64() {
        Object[] objectArray = new String[]{"#version 150", "", "in vec2 FragCoord;", "flat in int QuadIndex;", "", "layout(std140) uniform PickerParamsArray {", "    vec4 params[768];", "};", "", "out vec4 OutColor;", "", "vec3 hueRamp(float h) {", "    vec3 k = mod(h * 6.0 + vec3(0.0, 4.0, 2.0), 6.0);", "    vec3 tri = clamp(abs(k - 3.0) - 1.0, 0.0, 1.0);", "    return tri * tri * (3.0 - 2.0 * tri);", "}", "", "float roundedAlpha(vec2 coord, vec2 size, float radius, float smoothness) {", "    vec2 p = coord * size;", "    vec2 center = size * 0.5;", "    float r = min(radius, min(center.x, center.y));", "    vec2 q = abs(p - center) - (center - vec2(r));", "    float dist = length(max(q, vec2(0.0))) + min(max(q.x, q.y), 0.0) - r;", "    return 1.0 - smoothstep(-smoothness, smoothness, dist);", "}", "", "void main() {", "    int base = QuadIndex * 3;", "    vec4 cfg = params[base];", "    vec4 sz = params[base + 1];", "    vec4 solid = params[base + 2];", "", "    int mode = int(cfg.x + 0.5);", "    vec2 coord = clamp(FragCoord, vec2(0.0), vec2(1.0));", "    vec2 size = max(sz.xy, vec2(1.0));", "", "    float shapeAlpha = roundedAlpha(coord, size, max(cfg.y, 0.0), max(cfg.z, 0.5));", "", "    vec3 rgb;", "    if (mode == 0) {", "        rgb = hueRamp(coord.x);", "    } else {", "", "        float cell = max(sz.z, 1.0);", "        vec2 c = floor((coord * size) / cell);", "        float square = mod(c.x + c.y, 2.0) < 0.5 ? 0.95 : 0.42;", "        rgb = mix(vec3(square), solid.rgb, coord.x);", "    }", "", "    float a = shapeAlpha * cfg.w;", "    if (a <= 0.001) {", "        discard;", "    }", "    OutColor = vec4(rgb, a);", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }

    private final String s65() {
        Object[] objectArray = new String[]{"//!vertex", "#version 330", "", "#moj_import <minecraft:dynamictransforms.glsl>", "#moj_import <minecraft:projection.glsl>", "", "in vec3 Position;", "in vec2 UV0;", "in vec4 Color;", "", "out vec2 portalCoord;", "out vec4 accent;", "", "void main() {", "    gl_Position = ProjMat * ModelViewMat * vec4(Position + ModelOffset, 1.0);", "    portalCoord = UV0;", "    accent = Color;", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }

    private final String s66() {
        Object[] objectArray = new String[]{"#version 330", "", "#moj_import <minecraft:dynamictransforms.glsl>", "#moj_import <minecraft:projection.glsl>", "", "in vec3 Position;", "in vec2 UV0;", "in vec4 Color;", "", "out vec2 portalCoord;", "out vec4 accent;", "", "void main() {", "    gl_Position = ProjMat * ModelViewMat * vec4(Position + ModelOffset, 1.0);", "    portalCoord = UV0;", "    accent = Color;", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }

    private final String s67() {
        Object[] objectArray = new String[]{"//!fragment", "#version 330", "", "#moj_import <minecraft:dynamictransforms.glsl>", "", "in vec2 portalCoord;", "in vec4 accent;", "", "out vec4 fragColor;", "", "const float TAU = 6.28318530718;", "", "void main() {", "    vec2 p = portalCoord;", "    float r = length(p);", "    if (r > 1.35) {", "        discard;", "    }", "", "    float t = accent.a * TAU;", "    float ang = atan(p.y, p.x);", "", "    float d = r - 1.0;", "    float outerFade = smoothstep(1.35, 1.08, r);", "    float noise = 0.5 + 0.5 * sin(ang * 7.0 + 2.0 * t) * sin(ang * 11.0 - 3.0 * t);", "    float core = exp(-d * d * 1543.2);", "    float flameW = 0.045 + 0.16 * noise * noise;", "    float flame = (d > 0.0 ? exp(-d / flameW) * outerFade : exp(d * 20.0)) * (0.35 + 0.65 * noise);", "    float rim = core + 0.7 * flame;", "", "    vec3 whiteRef = vec3(max(accent.r, max(accent.g, accent.b)));", "    vec3 col = mix(accent.rgb, whiteRef, core * 0.5) * (rim + core * 0.9);", "    float dn = fract(sin(dot(gl_FragCoord.xy, vec2(12.9898, 78.233))) * 43758.5453) - 0.5;", "    col = max(col + dn / 255.0, 0.0);", "    if (max(col.r, max(col.g, col.b)) <= 0.004) {", "        discard;", "    }", "", "    fragColor = vec4(col, 1.0);", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }

    private final String s68() {
        Object[] objectArray = new String[]{"#version 330", "", "#moj_import <minecraft:dynamictransforms.glsl>", "", "in vec2 portalCoord;", "in vec4 accent;", "", "out vec4 fragColor;", "", "const float TAU = 6.28318530718;", "", "void main() {", "    vec2 p = portalCoord;", "    float r = length(p);", "    if (r > 1.35) {", "        discard;", "    }", "", "    float t = accent.a * TAU;", "    float ang = atan(p.y, p.x);", "", "    float d = r - 1.0;", "    float outerFade = smoothstep(1.35, 1.08, r);", "    float noise = 0.5 + 0.5 * sin(ang * 7.0 + 2.0 * t) * sin(ang * 11.0 - 3.0 * t);", "    float core = exp(-d * d * 1543.2);", "    float flameW = 0.045 + 0.16 * noise * noise;", "    float flame = (d > 0.0 ? exp(-d / flameW) * outerFade : exp(d * 20.0)) * (0.35 + 0.65 * noise);", "    float rim = core + 0.7 * flame;", "", "    vec3 whiteRef = vec3(max(accent.r, max(accent.g, accent.b)));", "    vec3 col = mix(accent.rgb, whiteRef, core * 0.5) * (rim + core * 0.9);", "    float dn = fract(sin(dot(gl_FragCoord.xy, vec2(12.9898, 78.233))) * 43758.5453) - 0.5;", "    col = max(col + dn / 255.0, 0.0);", "    if (max(col.r, max(col.g, col.b)) <= 0.004) {", "        discard;", "    }", "", "    fragColor = vec4(col, 1.0);", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }

    private final String s69() {
        Object[] objectArray = new String[]{"//!fragment", "#version 330", "", "#moj_import <minecraft:dynamictransforms.glsl>", "", "uniform sampler2D Sampler0;", "", "in vec2 portalCoord;", "in vec4 accent;", "", "out vec4 fragColor;", "", "const float CROP_X = 0.162829;", "", "void main() {", "    vec2 p = portalCoord;", "    float r = length(p);", "    if (r > 1.02) {", "        discard;", "    }", "", "    float aa = max(fwidth(r), 0.004);", "    float fade = accent.a;", "    float mask = 1.0 - smoothstep(1.0 - aa, 1.0 + aa, r);", "    if (mask <= 0.002 || fade <= 0.002) {", "        discard;", "    }", "", "    vec2 pd = p * (1.0 - 0.075 * r * r * r);", "    vec2 base = vec2(0.5 - pd.x * CROP_X, 0.5 - pd.y * 0.5);", "    float blur = 0.016 * accent.r;", "    vec3 video = texture(Sampler0, base).rgb * 0.28;", "    video += texture(Sampler0, base + vec2(blur, blur * 0.6)).rgb * 0.12;", "    video += texture(Sampler0, base + vec2(-blur, blur * 0.6)).rgb * 0.12;", "    video += texture(Sampler0, base + vec2(blur, -blur * 0.6)).rgb * 0.12;", "    video += texture(Sampler0, base + vec2(-blur, -blur * 0.6)).rgb * 0.12;", "    video += texture(Sampler0, base + vec2(blur * 1.4, 0.0)).rgb * 0.12;", "    video += texture(Sampler0, base + vec2(-blur * 1.4, 0.0)).rgb * 0.12;", "    video *= 0.60 + 0.40 * smoothstep(1.0, 0.45, r);", "", "    float dn = fract(sin(dot(gl_FragCoord.xy, vec2(12.9898, 78.233))) * 43758.5453) - 0.5;", "    video = max(video + dn / 255.0, 0.0);", "", "    fragColor = vec4(video, mask) * fade * ColorModulator;", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }

    private final String s70() {
        Object[] objectArray = new String[]{"#version 330", "", "#moj_import <minecraft:dynamictransforms.glsl>", "", "uniform sampler2D Sampler0;", "", "in vec2 portalCoord;", "in vec4 accent;", "", "out vec4 fragColor;", "", "const float CROP_X = 0.162829;", "", "void main() {", "    vec2 p = portalCoord;", "    float r = length(p);", "    if (r > 1.02) {", "        discard;", "    }", "", "    float aa = max(fwidth(r), 0.004);", "    float fade = accent.a;", "    float mask = 1.0 - smoothstep(1.0 - aa, 1.0 + aa, r);", "    if (mask <= 0.002 || fade <= 0.002) {", "        discard;", "    }", "", "    vec2 pd = p * (1.0 - 0.075 * r * r * r);", "    vec2 base = vec2(0.5 - pd.x * CROP_X, 0.5 - pd.y * 0.5);", "    float blur = 0.016 * accent.r;", "    vec3 video = texture(Sampler0, base).rgb * 0.28;", "    video += texture(Sampler0, base + vec2(blur, blur * 0.6)).rgb * 0.12;", "    video += texture(Sampler0, base + vec2(-blur, blur * 0.6)).rgb * 0.12;", "    video += texture(Sampler0, base + vec2(blur, -blur * 0.6)).rgb * 0.12;", "    video += texture(Sampler0, base + vec2(-blur, -blur * 0.6)).rgb * 0.12;", "    video += texture(Sampler0, base + vec2(blur * 1.4, 0.0)).rgb * 0.12;", "    video += texture(Sampler0, base + vec2(-blur * 1.4, 0.0)).rgb * 0.12;", "    video *= 0.60 + 0.40 * smoothstep(1.0, 0.45, r);", "", "    float dn = fract(sin(dot(gl_FragCoord.xy, vec2(12.9898, 78.233))) * 43758.5453) - 0.5;", "    video = max(video + dn / 255.0, 0.0);", "", "    fragColor = vec4(video, mask) * fade * ColorModulator;", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }

    private final String s71() {
        Object[] objectArray = new String[]{"//!fragment", "#version 330", "", "#moj_import <minecraft:dynamictransforms.glsl>", "", "in vec2 portalCoord;", "in vec4 accent;", "", "out vec4 fragColor;", "", "const float TAU = 6.28318530718;", "", "void main() {", "    vec2 p = portalCoord;", "    float r = length(p);", "    if (r > 1.35) {", "        discard;", "    }", "", "    float t = accent.a * TAU;", "    float ang = atan(p.y, p.x);", "    float d = r - 1.0;", "    float outerFade = smoothstep(1.35, 1.08, r);", "    float noise = 0.5 + 0.5 * sin(ang * 7.0 + 2.0 * t) * sin(ang * 11.0 - 3.0 * t);", "    float core = exp(-d * d * 1543.2);", "    float flameW = 0.045 + 0.16 * noise * noise;", "    float flame = (d > 0.0 ? exp(-d / flameW) * outerFade : exp(d * 20.0)) * (0.35 + 0.65 * noise);", "    float rim = core + 0.7 * flame;", "", "    vec3 whiteRef = vec3(max(accent.r, max(accent.g, accent.b)));", "    vec3 col = mix(accent.rgb, whiteRef, core * 0.5) * (rim + core * 0.9);", "    float dn = fract(sin(dot(gl_FragCoord.xy, vec2(12.9898, 78.233))) * 43758.5453) - 0.5;", "    col = max(col + dn / 255.0, 0.0);", "    if (max(col.r, max(col.g, col.b)) <= 0.004) {", "        discard;", "    }", "", "    fragColor = vec4(col, 1.0);", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }

    private final String s72() {
        Object[] objectArray = new String[]{"#version 330", "", "#moj_import <minecraft:dynamictransforms.glsl>", "", "in vec2 portalCoord;", "in vec4 accent;", "", "out vec4 fragColor;", "", "const float TAU = 6.28318530718;", "", "void main() {", "    vec2 p = portalCoord;", "    float r = length(p);", "    if (r > 1.35) {", "        discard;", "    }", "", "    float t = accent.a * TAU;", "    float ang = atan(p.y, p.x);", "    float d = r - 1.0;", "    float outerFade = smoothstep(1.35, 1.08, r);", "    float noise = 0.5 + 0.5 * sin(ang * 7.0 + 2.0 * t) * sin(ang * 11.0 - 3.0 * t);", "    float core = exp(-d * d * 1543.2);", "    float flameW = 0.045 + 0.16 * noise * noise;", "    float flame = (d > 0.0 ? exp(-d / flameW) * outerFade : exp(d * 20.0)) * (0.35 + 0.65 * noise);", "    float rim = core + 0.7 * flame;", "", "    vec3 whiteRef = vec3(max(accent.r, max(accent.g, accent.b)));", "    vec3 col = mix(accent.rgb, whiteRef, core * 0.5) * (rim + core * 0.9);", "    float dn = fract(sin(dot(gl_FragCoord.xy, vec2(12.9898, 78.233))) * 43758.5453) - 0.5;", "    col = max(col + dn / 255.0, 0.0);", "    if (max(col.r, max(col.g, col.b)) <= 0.004) {", "        discard;", "    }", "", "    fragColor = vec4(col, 1.0);", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }

    private final String s73() {
        Object[] objectArray = new String[]{"//!fragment", "#version 330", "", "#moj_import <minecraft:dynamictransforms.glsl>", "", "uniform sampler2D Sampler0;", "", "in vec2 portalCoord;", "in vec4 accent;", "", "out vec4 fragColor;", "", "const float CROP_X = 0.162829;", "", "void main() {", "    vec2 p = portalCoord;", "    float r = length(p);", "    if (r > 1.02) {", "        discard;", "    }", "", "    float aa = max(fwidth(r), 0.004);", "    float fade = accent.a;", "    float mask = 1.0 - smoothstep(1.0 - aa, 1.0 + aa, r);", "    if (mask <= 0.002 || fade <= 0.002) {", "        discard;", "    }", "", "    vec2 pd = p * (1.0 - 0.075 * r * r * r);", "    vec2 base = vec2(0.5 - pd.x * CROP_X, 0.5 - pd.y * 0.5);", "    vec2 caDir = pd * vec2(CROP_X, 0.5) * (0.010 * r * r);", "    vec3 video = vec3(", "            texture(Sampler0, base + caDir).r,", "            texture(Sampler0, base).g,", "            texture(Sampler0, base - caDir).b);", "", "    float blurAmount = clamp((1.0 - fade) * 1.4, 0.0, 1.0);", "    if (blurAmount > 0.02) {", "        float br = 0.016 * blurAmount;", "        vec3 blurred = texture(Sampler0, base + vec2(br, br * 0.6)).rgb;", "        blurred += texture(Sampler0, base + vec2(-br, br * 0.6)).rgb;", "        blurred += texture(Sampler0, base + vec2(br, -br * 0.6)).rgb;", "        blurred += texture(Sampler0, base + vec2(-br, -br * 0.6)).rgb;", "        blurred += texture(Sampler0, base + vec2(0.0, br)).rgb;", "        blurred += texture(Sampler0, base + vec2(0.0, -br)).rgb;", "        video = mix(video, blurred / 6.0, blurAmount);", "    }", "", "    video *= 0.60 + 0.40 * smoothstep(1.0, 0.45, r);", "", "    float ang = atan(p.y, p.x);", "    float streak = smoothstep(0.72, 1.0, r) * (0.5 + 0.5 * sin(ang * 9.0 + r * 14.0));", "    video += accent.rgb * (streak * streak * 0.10);", "", "    float dn = fract(sin(dot(gl_FragCoord.xy, vec2(12.9898, 78.233))) * 43758.5453) - 0.5;", "    video = max(video + dn / 255.0, 0.0);", "", "    fragColor = vec4(video, mask * fade) * ColorModulator;", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }

    private final String s74() {
        Object[] objectArray = new String[]{"#version 330", "", "#moj_import <minecraft:dynamictransforms.glsl>", "", "uniform sampler2D Sampler0;", "", "in vec2 portalCoord;", "in vec4 accent;", "", "out vec4 fragColor;", "", "const float CROP_X = 0.162829;", "", "void main() {", "    vec2 p = portalCoord;", "    float r = length(p);", "    if (r > 1.02) {", "        discard;", "    }", "", "    float aa = max(fwidth(r), 0.004);", "    float fade = accent.a;", "    float mask = 1.0 - smoothstep(1.0 - aa, 1.0 + aa, r);", "    if (mask <= 0.002 || fade <= 0.002) {", "        discard;", "    }", "", "    vec2 pd = p * (1.0 - 0.075 * r * r * r);", "    vec2 base = vec2(0.5 - pd.x * CROP_X, 0.5 - pd.y * 0.5);", "    vec2 caDir = pd * vec2(CROP_X, 0.5) * (0.010 * r * r);", "    vec3 video = vec3(", "            texture(Sampler0, base + caDir).r,", "            texture(Sampler0, base).g,", "            texture(Sampler0, base - caDir).b);", "", "    float blurAmount = clamp((1.0 - fade) * 1.4, 0.0, 1.0);", "    if (blurAmount > 0.02) {", "        float br = 0.016 * blurAmount;", "        vec3 blurred = texture(Sampler0, base + vec2(br, br * 0.6)).rgb;", "        blurred += texture(Sampler0, base + vec2(-br, br * 0.6)).rgb;", "        blurred += texture(Sampler0, base + vec2(br, -br * 0.6)).rgb;", "        blurred += texture(Sampler0, base + vec2(-br, -br * 0.6)).rgb;", "        blurred += texture(Sampler0, base + vec2(0.0, br)).rgb;", "        blurred += texture(Sampler0, base + vec2(0.0, -br)).rgb;", "        video = mix(video, blurred / 6.0, blurAmount);", "    }", "", "    video *= 0.60 + 0.40 * smoothstep(1.0, 0.45, r);", "", "    float ang = atan(p.y, p.x);", "    float streak = smoothstep(0.72, 1.0, r) * (0.5 + 0.5 * sin(ang * 9.0 + r * 14.0));", "    video += accent.rgb * (streak * streak * 0.10);", "", "    float dn = fract(sin(dot(gl_FragCoord.xy, vec2(12.9898, 78.233))) * 43758.5453) - 0.5;", "    video = max(video + dn / 255.0, 0.0);", "", "    fragColor = vec4(video, mask * fade) * ColorModulator;", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }

    private final String s75() {
        Object[] objectArray = new String[]{"//!fragment", "#version 330", "", "#moj_import <minecraft:dynamictransforms.glsl>", "", "in vec2 portalCoord;", "in vec4 accent;", "", "out vec4 fragColor;", "", "void main() {", "    vec2 p = portalCoord;", "    float r = length(p);", "    if (r > 1.02) {", "        discard;", "    }", "", "    float aa = max(fwidth(r), 0.004);", "    float fade = accent.a;", "    float mask = 1.0 - smoothstep(1.0 - aa, 1.0 + aa, r);", "    if (mask <= 0.002 || fade <= 0.002) {", "        discard;", "    }", "", "    float ang = atan(p.y, p.x);", "    float sw = 0.5 + 0.5 * sin(ang * 3.0 + r * 7.0) * sin(ang * 5.0 - r * 11.0);", "    float depth = smoothstep(1.0, 0.15, r);", "    vec3 voidCol = vec3(0.016, 0.020, 0.038) * (0.7 + 0.6 * depth);", "    voidCol += accent.rgb * (sw * depth * 0.16);", "", "    float dn = fract(sin(dot(gl_FragCoord.xy, vec2(12.9898, 78.233))) * 43758.5453) - 0.5;", "    voidCol = max(voidCol + dn / 255.0, 0.0);", "", "    fragColor = vec4(voidCol, 0.92 * mask * fade) * ColorModulator;", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }

    private final String s76() {
        Object[] objectArray = new String[]{"#version 330", "", "#moj_import <minecraft:dynamictransforms.glsl>", "", "in vec2 portalCoord;", "in vec4 accent;", "", "out vec4 fragColor;", "", "void main() {", "    vec2 p = portalCoord;", "    float r = length(p);", "    if (r > 1.02) {", "        discard;", "    }", "", "    float aa = max(fwidth(r), 0.004);", "    float fade = accent.a;", "    float mask = 1.0 - smoothstep(1.0 - aa, 1.0 + aa, r);", "    if (mask <= 0.002 || fade <= 0.002) {", "        discard;", "    }", "", "    float ang = atan(p.y, p.x);", "    float sw = 0.5 + 0.5 * sin(ang * 3.0 + r * 7.0) * sin(ang * 5.0 - r * 11.0);", "    float depth = smoothstep(1.0, 0.15, r);", "    vec3 voidCol = vec3(0.016, 0.020, 0.038) * (0.7 + 0.6 * depth);", "    voidCol += accent.rgb * (sw * depth * 0.16);", "", "    float dn = fract(sin(dot(gl_FragCoord.xy, vec2(12.9898, 78.233))) * 43758.5453) - 0.5;", "    voidCol = max(voidCol + dn / 255.0, 0.0);", "", "    fragColor = vec4(voidCol, 0.92 * mask * fade) * ColorModulator;", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }

    private final String s77() {
        Object[] objectArray = new String[]{"//!vertex", "#version 150", "", "#moj_import <minecraft:dynamictransforms.glsl>", "#moj_import <minecraft:projection.glsl>", "", "in vec3 Position;", "in vec4 Color;", "in float LineWidth;", "", "out vec2 FragCoord;", "out vec2 ScreenPos;", "flat out int QuadIndex;", "", "void main() {", "    gl_Position = ProjMat * ModelViewMat * vec4(Position, 1.0);", "    FragCoord = Color.rg;", "    ScreenPos = Position.xy;", "    QuadIndex = max(int(LineWidth + 0.5) - 1, 0);", "}", "//!fragment", "#version 150", "", "#moj_import <kimiko:common.glsl>", "", "in vec2 FragCoord;", "in vec2 ScreenPos;", "flat in int QuadIndex;", "", "layout(std140) uniform RectangleParamsArray {", "    vec4 params[4032];", "};", "", "layout(std140) uniform PaletteParams {", "    vec4 paletteMeta;", "    vec4 paletteColors[6];", "    vec4 paletteMeta2;", "    vec4 paletteLayerA[6];", "    vec4 paletteWaveA;", "    vec4 paletteWave2;", "    vec4 paletteLayerB[6];", "    vec4 paletteWaveB;", "    vec4 palettePadB;", "    vec4 paletteLayerC[6];", "    vec4 paletteWaveC;", "    vec4 palettePadC;", "    vec4 paletteLayerD[6];", "    vec4 paletteWaveD;", "    vec4 palettePadD;", "    vec4 paletteLayerE[6];", "    vec4 paletteWaveE;", "    vec4 palettePadE;", "    vec4 paletteLayerF[6];", "    vec4 paletteWaveF;", "    vec4 palettePadF;", "};", "", "out vec4 OutColor;", "", "float kLayerA = 0.0;", "float kLayerB = 0.0;", "float kLayerC = 0.0;", "float kLayerD = 0.0;", "float kLayerE = 0.0;", "float kLayerF = 0.0;", "", "float themeLayerCoverage(vec4 wave, vec2 fragXY) {", "    if (wave.w < 0.5) {", "        return 0.0;", "    }", "    if (wave.w > 1.5) {", "        return clamp(wave.z, 0.0, 1.0);", "    }", "    vec2 res = max(paletteWave2.xy, vec2(1.0));", "    float aspect = res.x / res.y;", "    vec2 d = (fragXY / res - wave.xy) * vec2(aspect, 1.0);", "    float wf = max(paletteWave2.z, 0.0005);", "    return 1.0 - smoothstep(wave.z - wf, wave.z + wf, length(d));", "}", "", "void themeWaveMix(vec2 fragXY) {", "    kLayerA = themeLayerCoverage(paletteWaveA, fragXY);", "    kLayerB = themeLayerCoverage(paletteWaveB, fragXY);", "    kLayerC = themeLayerCoverage(paletteWaveC, fragXY);", "    kLayerD = themeLayerCoverage(paletteWaveD, fragXY);", "    kLayerE = themeLayerCoverage(paletteWaveE, fragXY);", "    kLayerF = themeLayerCoverage(paletteWaveF, fragXY);", "}", "", "vec3 themeBlend(vec3 base, vec3 a, vec3 b, vec3 c, vec3 d, vec3 e, vec3 f) {", "    vec3 r = mix(base, a, kLayerA);", "    r = mix(r, b, kLayerB);", "    r = mix(r, c, kLayerC);", "    r = mix(r, d, kLayerD);", "    r = mix(r, e, kLayerE);", "    return mix(r, f, kLayerF);", "}", "", "vec3 paletteColorAt(int idx) {", "    if (idx <= 0) return themeBlend(paletteColors[0].rgb, paletteLayerA[0].rgb, paletteLayerB[0].rgb, paletteLayerC[0].rgb, paletteLayerD[0].rgb, paletteLayerE[0].rgb, paletteLayerF[0].rgb);", "    if (idx == 1) return themeBlend(paletteColors[1].rgb, paletteLayerA[1].rgb, paletteLayerB[1].rgb, paletteLayerC[1].rgb, paletteLayerD[1].rgb, paletteLayerE[1].rgb, paletteLayerF[1].rgb);", "    if (idx == 2) return themeBlend(paletteColors[2].rgb, paletteLayerA[2].rgb, paletteLayerB[2].rgb, paletteLayerC[2].rgb, paletteLayerD[2].rgb, paletteLayerE[2].rgb, paletteLayerF[2].rgb);", "    if (idx == 3) return themeBlend(paletteColors[3].rgb, paletteLayerA[3].rgb, paletteLayerB[3].rgb, paletteLayerC[3].rgb, paletteLayerD[3].rgb, paletteLayerE[3].rgb, paletteLayerF[3].rgb);", "    if (idx == 4) return themeBlend(paletteColors[4].rgb, paletteLayerA[4].rgb, paletteLayerB[4].rgb, paletteLayerC[4].rgb, paletteLayerD[4].rgb, paletteLayerE[4].rgb, paletteLayerF[4].rgb);", "    return themeBlend(paletteColors[5].rgb, paletteLayerA[5].rgb, paletteLayerB[5].rgb, paletteLayerC[5].rgb, paletteLayerD[5].rgb, paletteLayerE[5].rgb, paletteLayerF[5].rgb);", "}", "", "vec3 paletteRamp(float t) {", "    int count = int(paletteMeta.x + 0.5);", "    if (count <= 1) {", "        return paletteColorAt(0);", "    }", "    float f = clamp(t, 0.0, 1.0) * float(count - 1);", "    int i = clamp(int(floor(f)), 0, count - 1);", "    int j = min(i + 1, count - 1);", "    float frac = clamp(f - float(i), 0.0, 1.0);", "    frac = frac * frac * (3.0 - 2.0 * frac);", "    return mix(paletteColorAt(i), paletteColorAt(j), frac);", "}", "", "vec3 paletteLoop(float t) {", "    int count = int(paletteMeta.x + 0.5);", "    if (count <= 1) {", "        return paletteColorAt(0);", "    }", "    float f = fract(t) * float(count);", "    int i1 = clamp(int(floor(f)), 0, count - 1);", "    float u = clamp(f - float(i1), 0.0, 1.0);", "", "    int i0 = i1 - 1; if (i0 < 0) i0 += count;", "    int i2 = i1 + 1; if (i2 >= count) i2 -= count;", "    int i3 = i1 + 2; if (i3 >= count) i3 -= count;", "", "    vec3 c0 = paletteColorAt(i0);", "    vec3 c1 = paletteColorAt(i1);", "    vec3 c2 = paletteColorAt(i2);", "    vec3 c3 = paletteColorAt(i3);", "", "    float u2 = u * u;", "    float u3 = u2 * u;", "    vec3 cyclicCol = 0.5 * ((2.0 * c1)", "                      + (-c0 + c2) * u", "                      + (2.0 * c0 - 5.0 * c1 + 4.0 * c2 - c3) * u2", "                      + (-c0 + 3.0 * c1 - 3.0 * c2 + c3) * u3);", "", "    float tri = 0.5 - 0.5 * cos(6.2831853 * fract(t));", "    vec3 mirrorCol = paletteRamp(tri);", "", "    float closed = clamp(paletteMeta2.y, 0.0, 1.0);", "    return clamp(mix(mirrorCol, cyclicCol, closed), 0.0, 1.0);", "}", "", "float roundedAlpha(vec2 coord, vec2 size, vec4 radius, float smoothness) {", "    return ralpha(size, coord, radius, smoothness);", "}", "", "vec4 rectangleColor(vec2 coord, vec4 topLeft, vec4 topRight, vec4 bottomRight, vec4 bottomLeft) {", "    vec4 top = mix(topLeft, topRight, coord.x);", "    vec4 bottom = mix(bottomLeft, bottomRight, coord.x);", "    return mix(top, bottom, coord.y);", "}", "", "void main() {", "    int base = QuadIndex * 9;", "    vec4 radius = params[base];", "    vec4 sizeSmooth = params[base + 1];", "    vec4 topLeft = params[base + 2];", "    vec4 topRight = params[base + 3];", "    vec4 bottomRight = params[base + 4];", "    vec4 bottomLeft = params[base + 5];", "    vec4 scissorPosSize = params[base + 6];", "    vec4 scissorRadius = params[base + 7];", "    vec4 scissorRotation = params[base + 8];", "", "    vec2 size = max(sizeSmooth.xy, vec2(1.0));", "", "    const float AA_PAD = 1.5;", "    vec2 f = vec2(AA_PAD) / size;", "    vec2 shapeCoord = FragCoord * (vec2(1.0) + 2.0 * f) - f;", "    vec2 coord = clamp(shapeCoord, vec2(0.0), vec2(1.0));", "    float alpha;", "    if (sizeSmooth.z < 0.0) {", "", "        vec2 halfSize = size * 0.5;", "        vec2 pos = halfSize - shapeCoord * size;", "        float r = max(radius.x, 0.0);", "        vec2 q = abs(pos) - halfSize + vec2(r);", "        float dist = min(max(q.x, q.y), 0.0) + length(max(q, vec2(0.0))) - r;", "        float feather = max(fwidth(dist) * 0.5, -sizeSmooth.z);", "        alpha = 1.0 - smoothstep(-feather, feather, dist);", "    } else {", "        alpha = roundedAlpha(shapeCoord, size, max(radius, vec4(0.0)), sizeSmooth.z);", "    }", "", "    int paletteMode = int(sizeSmooth.w + 0.5);", "    vec4 color;", "    if (paletteMode > 0) {", "        themeWaveMix(gl_FragCoord.xy);", "", "        if (paletteMode == 3) {", "", "            color = vec4(paletteLoop(coord.x + paletteMeta2.z) * topLeft.x, topLeft.y);", "        } else {", "            float t = (paletteMode == 1) ? coord.y : coord.x;", "            color = vec4(paletteRamp(t) * topLeft.x, topLeft.y);", "        }", "    } else {", "        color = rectangleColor(coord, topLeft, topRight, bottomRight, bottomLeft);", "    }", "    color.a *= alpha;", "", "    if (scissorPosSize.z > 0.0 && scissorPosSize.w > 0.0) {", "        vec2 clipPoint = ScreenPos;", "        if (abs(scissorRotation.y) > 0.0001) {", "            vec2 clipCenter = scissorPosSize.xy + scissorPosSize.zw * 0.5;", "            vec2 rel = ScreenPos - clipCenter;", "            clipPoint = clipCenter + vec2(", "                rel.x * scissorRotation.x + rel.y * scissorRotation.y,", "                -rel.x * scissorRotation.y + rel.y * scissorRotation.x", "            );", "        }", "        vec2 scissorCoord = (clipPoint - scissorPosSize.xy) / scissorPosSize.zw;", "        float scissorAlpha = ralpha(scissorPosSize.zw, scissorCoord, max(scissorRadius, vec4(0.0)), sizeSmooth.z);", "        if (scissorRotation.z > 0.0) {", "            scissorAlpha *= smoothstep(0.0, scissorRotation.z, clipPoint.y - scissorPosSize.y);", "        }", "        color.a *= scissorAlpha;", "    }", "", "    if (color.a <= 0.001) {", "        discard;", "    }", "", "    OutColor = color;", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }

    private final String s78() {
        Object[] objectArray = new String[]{"#version 150", "", "#moj_import <minecraft:dynamictransforms.glsl>", "#moj_import <minecraft:projection.glsl>", "", "in vec3 Position;", "in vec4 Color;", "in float LineWidth;", "", "out vec2 FragCoord;", "out vec2 ScreenPos;", "flat out int QuadIndex;", "", "void main() {", "    gl_Position = ProjMat * ModelViewMat * vec4(Position, 1.0);", "    FragCoord = Color.rg;", "    ScreenPos = Position.xy;", "    QuadIndex = max(int(LineWidth + 0.5) - 1, 0);", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }

    private final String s79() {
        Object[] objectArray = new String[]{"#version 150", "", "#moj_import <kimiko:common.glsl>", "", "in vec2 FragCoord;", "in vec2 ScreenPos;", "flat in int QuadIndex;", "", "layout(std140) uniform RectangleParamsArray {", "    vec4 params[4032];", "};", "", "layout(std140) uniform PaletteParams {", "    vec4 paletteMeta;", "    vec4 paletteColors[6];", "    vec4 paletteMeta2;", "    vec4 paletteLayerA[6];", "    vec4 paletteWaveA;", "    vec4 paletteWave2;", "    vec4 paletteLayerB[6];", "    vec4 paletteWaveB;", "    vec4 palettePadB;", "    vec4 paletteLayerC[6];", "    vec4 paletteWaveC;", "    vec4 palettePadC;", "    vec4 paletteLayerD[6];", "    vec4 paletteWaveD;", "    vec4 palettePadD;", "    vec4 paletteLayerE[6];", "    vec4 paletteWaveE;", "    vec4 palettePadE;", "    vec4 paletteLayerF[6];", "    vec4 paletteWaveF;", "    vec4 palettePadF;", "};", "", "out vec4 OutColor;", "", "float kLayerA = 0.0;", "float kLayerB = 0.0;", "float kLayerC = 0.0;", "float kLayerD = 0.0;", "float kLayerE = 0.0;", "float kLayerF = 0.0;", "", "float themeLayerCoverage(vec4 wave, vec2 fragXY) {", "    if (wave.w < 0.5) {", "        return 0.0;", "    }", "    if (wave.w > 1.5) {", "        return clamp(wave.z, 0.0, 1.0);", "    }", "    vec2 res = max(paletteWave2.xy, vec2(1.0));", "    float aspect = res.x / res.y;", "    vec2 d = (fragXY / res - wave.xy) * vec2(aspect, 1.0);", "    float wf = max(paletteWave2.z, 0.0005);", "    return 1.0 - smoothstep(wave.z - wf, wave.z + wf, length(d));", "}", "", "void themeWaveMix(vec2 fragXY) {", "    kLayerA = themeLayerCoverage(paletteWaveA, fragXY);", "    kLayerB = themeLayerCoverage(paletteWaveB, fragXY);", "    kLayerC = themeLayerCoverage(paletteWaveC, fragXY);", "    kLayerD = themeLayerCoverage(paletteWaveD, fragXY);", "    kLayerE = themeLayerCoverage(paletteWaveE, fragXY);", "    kLayerF = themeLayerCoverage(paletteWaveF, fragXY);", "}", "", "vec3 themeBlend(vec3 base, vec3 a, vec3 b, vec3 c, vec3 d, vec3 e, vec3 f) {", "    vec3 r = mix(base, a, kLayerA);", "    r = mix(r, b, kLayerB);", "    r = mix(r, c, kLayerC);", "    r = mix(r, d, kLayerD);", "    r = mix(r, e, kLayerE);", "    return mix(r, f, kLayerF);", "}", "", "vec3 paletteColorAt(int idx) {", "    if (idx <= 0) return themeBlend(paletteColors[0].rgb, paletteLayerA[0].rgb, paletteLayerB[0].rgb, paletteLayerC[0].rgb, paletteLayerD[0].rgb, paletteLayerE[0].rgb, paletteLayerF[0].rgb);", "    if (idx == 1) return themeBlend(paletteColors[1].rgb, paletteLayerA[1].rgb, paletteLayerB[1].rgb, paletteLayerC[1].rgb, paletteLayerD[1].rgb, paletteLayerE[1].rgb, paletteLayerF[1].rgb);", "    if (idx == 2) return themeBlend(paletteColors[2].rgb, paletteLayerA[2].rgb, paletteLayerB[2].rgb, paletteLayerC[2].rgb, paletteLayerD[2].rgb, paletteLayerE[2].rgb, paletteLayerF[2].rgb);", "    if (idx == 3) return themeBlend(paletteColors[3].rgb, paletteLayerA[3].rgb, paletteLayerB[3].rgb, paletteLayerC[3].rgb, paletteLayerD[3].rgb, paletteLayerE[3].rgb, paletteLayerF[3].rgb);", "    if (idx == 4) return themeBlend(paletteColors[4].rgb, paletteLayerA[4].rgb, paletteLayerB[4].rgb, paletteLayerC[4].rgb, paletteLayerD[4].rgb, paletteLayerE[4].rgb, paletteLayerF[4].rgb);", "    return themeBlend(paletteColors[5].rgb, paletteLayerA[5].rgb, paletteLayerB[5].rgb, paletteLayerC[5].rgb, paletteLayerD[5].rgb, paletteLayerE[5].rgb, paletteLayerF[5].rgb);", "}", "", "vec3 paletteRamp(float t) {", "    int count = int(paletteMeta.x + 0.5);", "    if (count <= 1) {", "        return paletteColorAt(0);", "    }", "    float f = clamp(t, 0.0, 1.0) * float(count - 1);", "    int i = clamp(int(floor(f)), 0, count - 1);", "    int j = min(i + 1, count - 1);", "    float frac = clamp(f - float(i), 0.0, 1.0);", "    frac = frac * frac * (3.0 - 2.0 * frac);", "    return mix(paletteColorAt(i), paletteColorAt(j), frac);", "}", "", "vec3 paletteLoop(float t) {", "    int count = int(paletteMeta.x + 0.5);", "    if (count <= 1) {", "        return paletteColorAt(0);", "    }", "    float f = fract(t) * float(count);", "    int i1 = clamp(int(floor(f)), 0, count - 1);", "    float u = clamp(f - float(i1), 0.0, 1.0);", "", "    int i0 = i1 - 1; if (i0 < 0) i0 += count;", "    int i2 = i1 + 1; if (i2 >= count) i2 -= count;", "    int i3 = i1 + 2; if (i3 >= count) i3 -= count;", "", "    vec3 c0 = paletteColorAt(i0);", "    vec3 c1 = paletteColorAt(i1);", "    vec3 c2 = paletteColorAt(i2);", "    vec3 c3 = paletteColorAt(i3);", "", "    float u2 = u * u;", "    float u3 = u2 * u;", "    vec3 cyclicCol = 0.5 * ((2.0 * c1)", "                      + (-c0 + c2) * u", "                      + (2.0 * c0 - 5.0 * c1 + 4.0 * c2 - c3) * u2", "                      + (-c0 + 3.0 * c1 - 3.0 * c2 + c3) * u3);", "", "    float tri = 0.5 - 0.5 * cos(6.2831853 * fract(t));", "    vec3 mirrorCol = paletteRamp(tri);", "", "    float closed = clamp(paletteMeta2.y, 0.0, 1.0);", "    return clamp(mix(mirrorCol, cyclicCol, closed), 0.0, 1.0);", "}", "", "float roundedAlpha(vec2 coord, vec2 size, vec4 radius, float smoothness) {", "    return ralpha(size, coord, radius, smoothness);", "}", "", "vec4 rectangleColor(vec2 coord, vec4 topLeft, vec4 topRight, vec4 bottomRight, vec4 bottomLeft) {", "    vec4 top = mix(topLeft, topRight, coord.x);", "    vec4 bottom = mix(bottomLeft, bottomRight, coord.x);", "    return mix(top, bottom, coord.y);", "}", "", "void main() {", "    int base = QuadIndex * 9;", "    vec4 radius = params[base];", "    vec4 sizeSmooth = params[base + 1];", "    vec4 topLeft = params[base + 2];", "    vec4 topRight = params[base + 3];", "    vec4 bottomRight = params[base + 4];", "    vec4 bottomLeft = params[base + 5];", "    vec4 scissorPosSize = params[base + 6];", "    vec4 scissorRadius = params[base + 7];", "    vec4 scissorRotation = params[base + 8];", "", "    vec2 size = max(sizeSmooth.xy, vec2(1.0));", "", "    const float AA_PAD = 1.5;", "    vec2 f = vec2(AA_PAD) / size;", "    vec2 shapeCoord = FragCoord * (vec2(1.0) + 2.0 * f) - f;", "    vec2 coord = clamp(shapeCoord, vec2(0.0), vec2(1.0));", "    float alpha;", "    if (sizeSmooth.z < 0.0) {", "", "        vec2 halfSize = size * 0.5;", "        vec2 pos = halfSize - shapeCoord * size;", "        float r = max(radius.x, 0.0);", "        vec2 q = abs(pos) - halfSize + vec2(r);", "        float dist = min(max(q.x, q.y), 0.0) + length(max(q, vec2(0.0))) - r;", "        float feather = max(fwidth(dist) * 0.5, -sizeSmooth.z);", "        alpha = 1.0 - smoothstep(-feather, feather, dist);", "    } else {", "        alpha = roundedAlpha(shapeCoord, size, max(radius, vec4(0.0)), sizeSmooth.z);", "    }", "", "    int paletteMode = int(sizeSmooth.w + 0.5);", "    vec4 color;", "    if (paletteMode > 0) {", "        themeWaveMix(gl_FragCoord.xy);", "", "        if (paletteMode == 3) {", "", "            color = vec4(paletteLoop(coord.x + paletteMeta2.z) * topLeft.x, topLeft.y);", "        } else {", "            float t = (paletteMode == 1) ? coord.y : coord.x;", "            color = vec4(paletteRamp(t) * topLeft.x, topLeft.y);", "        }", "    } else {", "        color = rectangleColor(coord, topLeft, topRight, bottomRight, bottomLeft);", "    }", "    color.a *= alpha;", "", "    if (scissorPosSize.z > 0.0 && scissorPosSize.w > 0.0) {", "        vec2 clipPoint = ScreenPos;", "        if (abs(scissorRotation.y) > 0.0001) {", "            vec2 clipCenter = scissorPosSize.xy + scissorPosSize.zw * 0.5;", "            vec2 rel = ScreenPos - clipCenter;", "            clipPoint = clipCenter + vec2(", "                rel.x * scissorRotation.x + rel.y * scissorRotation.y,", "                -rel.x * scissorRotation.y + rel.y * scissorRotation.x", "            );", "        }", "        vec2 scissorCoord = (clipPoint - scissorPosSize.xy) / scissorPosSize.zw;", "        float scissorAlpha = ralpha(scissorPosSize.zw, scissorCoord, max(scissorRadius, vec4(0.0)), sizeSmooth.z);", "        if (scissorRotation.z > 0.0) {", "            scissorAlpha *= smoothstep(0.0, scissorRotation.z, clipPoint.y - scissorPosSize.y);", "        }", "        color.a *= scissorAlpha;", "    }", "", "    if (color.a <= 0.001) {", "        discard;", "    }", "", "    OutColor = color;", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }

    private final String s80() {
        Object[] objectArray = new String[]{"//!vertex", "#version 150", "", "#moj_import <minecraft:dynamictransforms.glsl>", "#moj_import <minecraft:projection.glsl>", "", "in vec3 Position;", "in vec2 UV0;", "in vec4 Color;", "in float LineWidth;", "", "out vec2 TexCoord;", "out vec4 VertexColor;", "out vec2 LocalPos;", "flat out int RectIndex;", "", "layout(std140) uniform RectHalfIconParamsArray {", "    vec4 params[768];", "};", "", "void main() {", "    RectIndex = max(int(LineWidth + 0.5) - 1, 0);", "    int base = RectIndex * 3;", "    vec4 rect = params[base];", "", "    gl_Position = ProjMat * ModelViewMat * vec4(Position, 1.0);", "    TexCoord = UV0;", "    VertexColor = Color;", "    LocalPos = Position.xy - rect.xy;", "}", "//!fragment", "#version 150", "", "#moj_import <kimiko:common.glsl>", "#moj_import <minecraft:dynamictransforms.glsl>", "", "uniform sampler2D Sampler0;", "", "in vec2 TexCoord;", "in vec4 VertexColor;", "in vec2 LocalPos;", "flat in int RectIndex;", "", "layout(std140) uniform RectHalfIconParamsArray {", "    vec4 params[768];", "};", "", "out vec4 OutColor;", "", "void main() {", "    int base = RectIndex * 3;", "    vec4 rect = params[base];", "    vec4 radius = params[base + 1];", "    vec4 smoothness = params[base + 2];", "    vec2 size = max(rect.zw, vec2(1.0));", "", "    if (LocalPos.x < 0.0 || LocalPos.y < 0.0 || LocalPos.x > size.x || LocalPos.y > size.y) {", "        discard;", "    }", "", "    vec2 coord = LocalPos / size;", "    float shapeAlpha = ralpha(size, coord, max(radius, vec4(0.0)), max(smoothness.x, 0.001));", "    vec4 sampleColor = texture(Sampler0, TexCoord);", "    float alpha = sampleColor.a * VertexColor.a * shapeAlpha;", "", "    if (alpha <= 0.001) {", "        discard;", "    }", "", "    OutColor = vec4(VertexColor.rgb, alpha) * ColorModulator;", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }

    private final String s81() {
        Object[] objectArray = new String[]{"#version 150", "", "#moj_import <minecraft:dynamictransforms.glsl>", "#moj_import <minecraft:projection.glsl>", "", "in vec3 Position;", "in vec2 UV0;", "in vec4 Color;", "in float LineWidth;", "", "out vec2 TexCoord;", "out vec4 VertexColor;", "out vec2 LocalPos;", "flat out int RectIndex;", "", "layout(std140) uniform RectHalfIconParamsArray {", "    vec4 params[768];", "};", "", "void main() {", "    RectIndex = max(int(LineWidth + 0.5) - 1, 0);", "    int base = RectIndex * 3;", "    vec4 rect = params[base];", "", "    gl_Position = ProjMat * ModelViewMat * vec4(Position, 1.0);", "    TexCoord = UV0;", "    VertexColor = Color;", "    LocalPos = Position.xy - rect.xy;", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }

    private final String s82() {
        Object[] objectArray = new String[]{"#version 150", "", "#moj_import <kimiko:common.glsl>", "#moj_import <minecraft:dynamictransforms.glsl>", "", "uniform sampler2D Sampler0;", "", "in vec2 TexCoord;", "in vec4 VertexColor;", "in vec2 LocalPos;", "flat in int RectIndex;", "", "layout(std140) uniform RectHalfIconParamsArray {", "    vec4 params[768];", "};", "", "out vec4 OutColor;", "", "void main() {", "    int base = RectIndex * 3;", "    vec4 rect = params[base];", "    vec4 radius = params[base + 1];", "    vec4 smoothness = params[base + 2];", "    vec2 size = max(rect.zw, vec2(1.0));", "", "    if (LocalPos.x < 0.0 || LocalPos.y < 0.0 || LocalPos.x > size.x || LocalPos.y > size.y) {", "        discard;", "    }", "", "    vec2 coord = LocalPos / size;", "    float shapeAlpha = ralpha(size, coord, max(radius, vec4(0.0)), max(smoothness.x, 0.001));", "    vec4 sampleColor = texture(Sampler0, TexCoord);", "    float alpha = sampleColor.a * VertexColor.a * shapeAlpha;", "", "    if (alpha <= 0.001) {", "        discard;", "    }", "", "    OutColor = vec4(VertexColor.rgb, alpha) * ColorModulator;", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }

    private final String s83() {
        Object[] objectArray = new String[]{"//!vertex", "#version 150", "", "#moj_import <minecraft:dynamictransforms.glsl>", "#moj_import <minecraft:projection.glsl>", "", "in vec3 Position;", "in vec4 Color;", "in float LineWidth;", "", "out vec2 FragCoord;", "flat out int QuadIndex;", "", "void main() {", "    gl_Position = ProjMat * ModelViewMat * vec4(Position, 1.0);", "    FragCoord = Color.rg;", "    QuadIndex = max(int(LineWidth + 0.5) - 1, 0);", "}", "//!fragment", "#version 150", "", "#moj_import <kimiko:common.glsl>", "", "in vec2 FragCoord;", "flat in int QuadIndex;", "", "layout(std140) uniform HalftoneRectangleParamsArray {", "    vec4 params[3072];", "};", "", "out vec4 OutColor;", "", "vec4 rectangleColor(vec2 coord, vec4 topLeft, vec4 topRight, vec4 bottomRight, vec4 bottomLeft) {", "    vec4 top = mix(topLeft, topRight, coord.x);", "    vec4 bottom = mix(bottomLeft, bottomRight, coord.x);", "    return mix(top, bottom, coord.y);", "}", "", "float hash21(vec2 p) {", "    return fract(sin(dot(floor(p), vec2(127.1, 311.7))) * 43758.5453123);", "}", "", "vec3 squareMosaic(vec2 cell, vec3 baseRgb) {", "    float h = hash21(cell);", "    float step = floor(h * 8.0) / 7.0;", "    float shade = mix(0.86, 1.05, step);", "    return baseRgb * shade;", "}", "", "void main() {", "    int base = QuadIndex * 8;", "    vec4 radius = params[base];", "    vec4 sizeSmoothDot = params[base + 1];", "    vec4 topLeft = params[base + 2];", "    vec4 topRight = params[base + 3];", "    vec4 bottomRight = params[base + 4];", "    vec4 bottomLeft = params[base + 5];", "", "    vec2 coord = clamp(FragCoord, vec2(0.0), vec2(1.0));", "    vec2 size = max(sizeSmoothDot.xy, vec2(1.0));", "    float shapeAlpha = ralpha(size, coord, radius, sizeSmoothDot.z);", "", "    vec4 baseColor = rectangleColor(coord, topLeft, topRight, bottomRight, bottomLeft);", "    baseColor.a *= shapeAlpha;", "", "    float pitch = max(sizeSmoothDot.w, 1.0);", "", "    vec2 cell = floor(gl_FragCoord.xy / pitch);", "", "    vec3 grainRgb = squareMosaic(cell, baseColor.rgb);", "", "    vec4 color = vec4(grainRgb, baseColor.a);", "    if (color.a <= 0.001) discard;", "", "    OutColor = color;", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }

    private final String s84() {
        Object[] objectArray = new String[]{"#version 150", "", "#moj_import <minecraft:dynamictransforms.glsl>", "#moj_import <minecraft:projection.glsl>", "", "in vec3 Position;", "in vec4 Color;", "in float LineWidth;", "", "out vec2 FragCoord;", "flat out int QuadIndex;", "", "void main() {", "    gl_Position = ProjMat * ModelViewMat * vec4(Position, 1.0);", "    FragCoord = Color.rg;", "    QuadIndex = max(int(LineWidth + 0.5) - 1, 0);", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }

    private final String s85() {
        Object[] objectArray = new String[]{"#version 150", "", "#moj_import <kimiko:common.glsl>", "", "in vec2 FragCoord;", "flat in int QuadIndex;", "", "layout(std140) uniform HalftoneRectangleParamsArray {", "    vec4 params[3072];", "};", "", "out vec4 OutColor;", "", "vec4 rectangleColor(vec2 coord, vec4 topLeft, vec4 topRight, vec4 bottomRight, vec4 bottomLeft) {", "    vec4 top = mix(topLeft, topRight, coord.x);", "    vec4 bottom = mix(bottomLeft, bottomRight, coord.x);", "    return mix(top, bottom, coord.y);", "}", "", "float hash21(vec2 p) {", "    return fract(sin(dot(floor(p), vec2(127.1, 311.7))) * 43758.5453123);", "}", "", "vec3 squareMosaic(vec2 cell, vec3 baseRgb) {", "    float h = hash21(cell);", "    float step = floor(h * 8.0) / 7.0;", "    float shade = mix(0.86, 1.05, step);", "    return baseRgb * shade;", "}", "", "void main() {", "    int base = QuadIndex * 8;", "    vec4 radius = params[base];", "    vec4 sizeSmoothDot = params[base + 1];", "    vec4 topLeft = params[base + 2];", "    vec4 topRight = params[base + 3];", "    vec4 bottomRight = params[base + 4];", "    vec4 bottomLeft = params[base + 5];", "", "    vec2 coord = clamp(FragCoord, vec2(0.0), vec2(1.0));", "    vec2 size = max(sizeSmoothDot.xy, vec2(1.0));", "    float shapeAlpha = ralpha(size, coord, radius, sizeSmoothDot.z);", "", "    vec4 baseColor = rectangleColor(coord, topLeft, topRight, bottomRight, bottomLeft);", "    baseColor.a *= shapeAlpha;", "", "    float pitch = max(sizeSmoothDot.w, 1.0);", "", "    vec2 cell = floor(gl_FragCoord.xy / pitch);", "", "    vec3 grainRgb = squareMosaic(cell, baseColor.rgb);", "", "    vec4 color = vec4(grainRgb, baseColor.a);", "    if (color.a <= 0.001) discard;", "", "    OutColor = color;", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }

    private final String s86() {
        Object[] objectArray = new String[]{"//!vertex", "#version 150", "", "#moj_import <minecraft:dynamictransforms.glsl>", "#moj_import <minecraft:projection.glsl>", "", "in vec3 Position;", "in vec2 UV0;", "in float LineWidth;", "", "out vec2 FragCoord;", "out vec2 TexCoord;", "flat out int QuadIndex;", "", "void main() {", "    gl_Position = ProjMat * ModelViewMat * vec4(Position, 1.0);", "    FragCoord = UV0;", "    TexCoord = UV0;", "    QuadIndex = max(int(LineWidth + 0.5) - 1, 0);", "}", "//!fragment", "#version 150", "", "#moj_import <kimiko:common.glsl>", "#moj_import <minecraft:dynamictransforms.glsl>", "", "in vec2 FragCoord;", "in vec2 TexCoord;", "flat in int QuadIndex;", "", "layout(std140) uniform RippleParamsArray {", "    vec4 params[2560];", "};", "", "uniform sampler2D Sampler0;", "", "out vec4 OutColor;", "", "void main() {", "    int base = QuadIndex * 5;", "", "    vec4 radius = params[base];", "    vec4 sizeSmooth = params[base + 1];", "    vec4 ripple = params[base + 2];", "    vec4 sourceColor = params[base + 3];", "    vec4 targetColor = params[base + 4];", "", "    vec2 coord = clamp(FragCoord, vec2(0.0), vec2(1.0));", "    vec2 size = max(sizeSmooth.xy, vec2(1.0));", "", "    float alpha = ralpha(size, coord, max(radius, vec4(0.0)), sizeSmooth.z);", "", "    vec2 delta = (coord - ripple.xy) * size;", "    float dist = length(delta);", "", "    float rippleRadius = ripple.z;", "    float rippleSmooth = max(ripple.w, 0.001);", "", "    float mask = smoothstep(rippleRadius - rippleSmooth, rippleRadius, dist);", "    vec4 finalColor = mix(targetColor, sourceColor, mask);", "", "    if (sizeSmooth.w > 0.5) {", "        vec4 texColor = texture(Sampler0, TexCoord);", "        finalColor *= texColor;", "    }", "", "    finalColor.a *= alpha;", "", "    if (finalColor.a <= 0.001) {", "        discard;", "    }", "", "    OutColor = finalColor * ColorModulator;", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }

    private final String s87() {
        Object[] objectArray = new String[]{"#version 150", "", "#moj_import <minecraft:dynamictransforms.glsl>", "#moj_import <minecraft:projection.glsl>", "", "in vec3 Position;", "in vec2 UV0;", "in float LineWidth;", "", "out vec2 FragCoord;", "out vec2 TexCoord;", "flat out int QuadIndex;", "", "void main() {", "    gl_Position = ProjMat * ModelViewMat * vec4(Position, 1.0);", "    FragCoord = UV0;", "    TexCoord = UV0;", "    QuadIndex = max(int(LineWidth + 0.5) - 1, 0);", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }

    private final String s88() {
        Object[] objectArray = new String[]{"#version 150", "", "#moj_import <kimiko:common.glsl>", "#moj_import <minecraft:dynamictransforms.glsl>", "", "in vec2 FragCoord;", "in vec2 TexCoord;", "flat in int QuadIndex;", "", "layout(std140) uniform RippleParamsArray {", "    vec4 params[2560];", "};", "", "uniform sampler2D Sampler0;", "", "out vec4 OutColor;", "", "void main() {", "    int base = QuadIndex * 5;", "", "    vec4 radius = params[base];", "    vec4 sizeSmooth = params[base + 1];", "    vec4 ripple = params[base + 2];", "    vec4 sourceColor = params[base + 3];", "    vec4 targetColor = params[base + 4];", "", "    vec2 coord = clamp(FragCoord, vec2(0.0), vec2(1.0));", "    vec2 size = max(sizeSmooth.xy, vec2(1.0));", "", "    float alpha = ralpha(size, coord, max(radius, vec4(0.0)), sizeSmooth.z);", "", "    vec2 delta = (coord - ripple.xy) * size;", "    float dist = length(delta);", "", "    float rippleRadius = ripple.z;", "    float rippleSmooth = max(ripple.w, 0.001);", "", "    float mask = smoothstep(rippleRadius - rippleSmooth, rippleRadius, dist);", "    vec4 finalColor = mix(targetColor, sourceColor, mask);", "", "    if (sizeSmooth.w > 0.5) {", "        vec4 texColor = texture(Sampler0, TexCoord);", "        finalColor *= texColor;", "    }", "", "    finalColor.a *= alpha;", "", "    if (finalColor.a <= 0.001) {", "        discard;", "    }", "", "    OutColor = finalColor * ColorModulator;", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }

    private final String s89() {
        Object[] objectArray = new String[]{"//!vertex", "#version 150", "", "#moj_import <minecraft:dynamictransforms.glsl>", "#moj_import <minecraft:projection.glsl>", "", "in vec3 Position;", "in vec2 UV0;", "in vec4 Color;", "in float LineWidth;", "", "out vec2  TexCoord;", "out vec4  VertexColor;", "flat out int BatchIndex;", "", "void main() {", "    gl_Position = ProjMat * ModelViewMat * vec4(Position, 1.0);", "    TexCoord    = UV0;", "    VertexColor = Color;", "    BatchIndex  = max(int(LineWidth + 0.5) - 1, 0);", "}", "//!fragment", "#version 150", "", "#moj_import <minecraft:dynamictransforms.glsl>", "", "in vec2  TexCoord;", "in vec4  VertexColor;", "flat in int BatchIndex;", "", "layout(std140) uniform ShimmerParamsArray {", "    vec4 params[256];", "};", "", "out vec4 OutColor;", "", "void main() {", "    int   base      = BatchIndex;", "    float center    = params[base].x;", "    float halfW     = params[base].y;", "    float intensity = params[base].z;", "", "    float dist    = abs(TexCoord.x - center);", "    float spot    = smoothstep(halfW, 0.0, dist);", "    spot          = spot * spot;", "", "    float alpha = spot * intensity * VertexColor.a;", "    if (alpha < 0.001) discard;", "", "    OutColor = vec4(VertexColor.rgb, alpha) * ColorModulator;", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }

    private final String s90() {
        Object[] objectArray = new String[]{"#version 150", "", "#moj_import <minecraft:dynamictransforms.glsl>", "#moj_import <minecraft:projection.glsl>", "", "in vec3 Position;", "in vec2 UV0;", "in vec4 Color;", "in float LineWidth;", "", "out vec2  TexCoord;", "out vec4  VertexColor;", "flat out int BatchIndex;", "", "void main() {", "    gl_Position = ProjMat * ModelViewMat * vec4(Position, 1.0);", "    TexCoord    = UV0;", "    VertexColor = Color;", "    BatchIndex  = max(int(LineWidth + 0.5) - 1, 0);", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }

    private final String s91() {
        Object[] objectArray = new String[]{"#version 150", "", "#moj_import <minecraft:dynamictransforms.glsl>", "", "in vec2  TexCoord;", "in vec4  VertexColor;", "flat in int BatchIndex;", "", "layout(std140) uniform ShimmerParamsArray {", "    vec4 params[256];", "};", "", "out vec4 OutColor;", "", "void main() {", "    int   base      = BatchIndex;", "    float center    = params[base].x;", "    float halfW     = params[base].y;", "    float intensity = params[base].z;", "", "    float dist    = abs(TexCoord.x - center);", "    float spot    = smoothstep(halfW, 0.0, dist);", "    spot          = spot * spot;", "", "    float alpha = spot * intensity * VertexColor.a;", "    if (alpha < 0.001) discard;", "", "    OutColor = vec4(VertexColor.rgb, alpha) * ColorModulator;", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }

    private final String s92() {
        Object[] objectArray = new String[]{"//!vertex", "#version 150", "", "#moj_import <minecraft:dynamictransforms.glsl>", "#moj_import <minecraft:projection.glsl>", "", "in vec3 Position;", "in vec4 Color;", "in float LineWidth;", "", "out vec2 FragCoord;", "flat out int QuadIndex;", "", "void main() {", "    gl_Position = ProjMat * ModelViewMat * vec4(Position, 1.0);", "    FragCoord = Color.rg;", "    QuadIndex = max(int(LineWidth + 0.5) - 1, 0);", "}", "//!fragment", "#version 150", "", "#moj_import <kimiko:common.glsl>", "", "in vec2 FragCoord;", "flat in int QuadIndex;", "", "layout(std140) uniform TablePanelParamsArray {", "    vec4 params[512];", "};", "", "out vec4 OutColor;", "", "float hash21(vec2 p) {", "    return fract(sin(dot(p, vec2(127.1, 311.7))) * 43758.5453123);", "}", "", "float vnoise(vec2 p) {", "    vec2 i = floor(p);", "    vec2 f = fract(p);", "    f = f * f * (3.0 - 2.0 * f);", "    float a = hash21(i);", "    float b = hash21(i + vec2(1.0, 0.0));", "    float c = hash21(i + vec2(0.0, 1.0));", "    float d = hash21(i + vec2(1.0, 1.0));", "    return mix(mix(a, b, f.x), mix(c, d, f.x), f.y);", "}", "", "float fbm(vec2 p) {", "    float v = 0.0;", "    float amp = 0.5;", "    for (int i = 0; i < 4; i++) {", "        v += amp * vnoise(p);", "        p *= 2.03;", "        amp *= 0.5;", "    }", "    return v;", "}", "", "vec3 feltSurface(vec2 px, vec2 coord, vec3 top, vec3 bottom, float cell, float strength, float seed) {", "    vec3 base = mix(top, bottom, coord.y);", "    base *= 1.0 + 0.045 * (0.5 - coord.x);", "", "    float size = max(cell, 2.0);", "    vec2 uv = px / size;", "    vec2 gid = floor(uv);", "    vec2 gf = fract(uv);", "", "    float checker = mod(gid.x + gid.y, 2.0);", "", "    float lw = 0.055;", "    float lineX = smoothstep(lw, 0.0, gf.x) + smoothstep(1.0 - lw, 1.0, gf.x);", "    float lineY = smoothstep(lw, 0.0, gf.y) + smoothstep(1.0 - lw, 1.0, gf.y);", "    float grid = clamp(lineX + lineY, 0.0, 1.0);", "", "    float weave = 0.5 + 0.5 * sin((px.x + px.y) * 0.55 + seed);", "    float darken = (checker * 0.34 + grid * 0.62 + weave * 0.06) * clamp(strength, 0.0, 1.0);", "    base *= 1.0 - darken;", "", "    float grain = hash21(floor(px * 1.35) + seed);", "    base *= 0.985 + 0.03 * grain;", "    return base;", "}", "", "vec3 woodSurface(vec2 pos, vec2 halfSize, float frame, float u, vec3 light, vec3 dark, float seed) {", "    vec2 pAbs = abs(pos);", "    float distX = halfSize.x - pAbs.x;", "    float distY = halfSize.y - pAbs.y;", "", "    float blend = max(frame * 1.6, 6.0);", "    float mitre = smoothstep(-blend, blend, distX - distY);", "", "    vec2 horizPlank = vec2(pos.x, distY);", "    vec2 vertPlank = vec2(pos.y, distX);", "    vec2 plank = mix(vertPlank, horizPlank, mitre);", "", "    float along = plank.x;", "    float across = plank.y;", "", "    float warp = fbm(vec2(along * 0.014, across * 0.10) + seed);", "    float rings = fract(across * 0.42 + warp * 3.4 + seed * 0.37);", "    float ring = smoothstep(0.0, 0.52, rings) * smoothstep(1.0, 0.48, rings);", "    vec3 wood = mix(dark, light, 0.30 + 0.70 * ring);", "", "    float fiber = hash21(vec2(floor(along * 0.85), floor(across * 2.6)) + seed);", "    wood *= 0.955 + 0.09 * fiber;", "", "    float knot = fbm(vec2(along * 0.05, across * 0.22) + seed * 1.7);", "    wood *= 0.92 + 0.16 * knot;", "", "    float bevel = 1.0 - abs(u * 2.0 - 1.0);", "    wood *= 0.84 + 0.30 * bevel;", "", "    float lightHoriz = pos.y > 0.0 ? 0.17 : -0.13;", "    float lightVert = pos.x > 0.0 ? 0.08 : -0.06;", "    wood *= 1.0 + mix(lightVert, lightHoriz, mitre);", "", "    float corner = 1.0 - abs(mitre * 2.0 - 1.0);", "    wood *= 1.0 - 0.045 * corner;", "", "    float outerDark = smoothstep(0.20, 0.0, u);", "    float innerDark = smoothstep(0.84, 1.0, u);", "    wood *= 1.0 - 0.34 * outerDark - 0.22 * innerDark;", "", "    float sheen = smoothstep(0.88, 0.955, u) * (1.0 - smoothstep(0.955, 1.0, u));", "    wood += vec3(0.09, 0.07, 0.05) * sheen;", "", "    return wood;", "}", "", "void main() {", "    int base = QuadIndex * 8;", "    vec4 radius = params[base];", "    vec4 sizeSmoothFrame = params[base + 1];", "    vec4 feltTop = params[base + 2];", "    vec4 feltBottom = params[base + 3];", "    vec4 woodLight = params[base + 4];", "    vec4 woodDark = params[base + 5];", "    vec4 cellParams = params[base + 6];", "    vec4 innerParams = params[base + 7];", "", "    vec2 size = max(sizeSmoothFrame.xy, vec2(1.0));", "    float smoothness = sizeSmoothFrame.z;", "    float frame = max(sizeSmoothFrame.w, 0.0);", "", "    vec2 coord = clamp(FragCoord, vec2(0.0), vec2(1.0));", "    vec2 halfSize = size * 0.5;", "    vec2 pos = halfSize - coord * size;", "    vec4 rad = max(radius, vec4(0.0));", "", "    float dist = rdist(pos, halfSize, rad);", "    float feather = max(fwidth(dist) * 0.5, smoothness);", "    float shapeAlpha = 1.0 - smoothstep(-feather, feather, dist);", "    if (shapeAlpha <= 0.001) {", "        discard;", "    }", "", "    vec2 px = coord * size;", "    float seed = cellParams.z;", "", "    vec3 felt = feltSurface(px, coord, feltTop.rgb, feltBottom.rgb, cellParams.x, cellParams.y, seed);", "", "    vec2 innerHalf = max(halfSize - vec2(frame), vec2(0.5));", "    float innerRadius = max(innerParams.x, 0.0);", "    float innerDist = rdist(pos, innerHalf, vec4(innerRadius));", "", "    float innerTint = clamp(cellParams.w, 0.0, 1.0);", "    float inset = smoothstep(-4.0, -22.0, innerDist);", "    felt *= 1.0 - innerTint * inset;", "", "    float shadowSpan = max(frame * 0.85, 6.0);", "    float innerShadow = smoothstep(-shadowSpan, 0.0, innerDist);", "    felt *= 1.0 - 0.30 * innerShadow;", "", "    vec3 rgb = felt;", "    float alpha = feltTop.a;", "", "    if (frame > 0.05) {", "        float aa = max(fwidth(innerDist), 0.5);", "        float woodMask = smoothstep(-aa, aa, innerDist);", "        if (woodMask > 0.001) {", "            float span = max(innerDist - dist, 0.001);", "            float u = clamp(-dist / span, 0.0, 1.0);", "            vec3 wood = woodSurface(pos, halfSize, frame, u, woodLight.rgb, woodDark.rgb, seed);", "            rgb = mix(rgb, wood, woodMask);", "            alpha = mix(alpha, woodLight.a, woodMask);", "        }", "    }", "", "    vec4 color = vec4(rgb, alpha * shapeAlpha);", "    if (color.a <= 0.001) {", "        discard;", "    }", "", "    OutColor = color;", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }

    private final String s93() {
        Object[] objectArray = new String[]{"#version 150", "", "#moj_import <minecraft:dynamictransforms.glsl>", "#moj_import <minecraft:projection.glsl>", "", "in vec3 Position;", "in vec4 Color;", "in float LineWidth;", "", "out vec2 FragCoord;", "flat out int QuadIndex;", "", "void main() {", "    gl_Position = ProjMat * ModelViewMat * vec4(Position, 1.0);", "    FragCoord = Color.rg;", "    QuadIndex = max(int(LineWidth + 0.5) - 1, 0);", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }

    private final String s94() {
        Object[] objectArray = new String[]{"#version 150", "", "#moj_import <kimiko:common.glsl>", "", "in vec2 FragCoord;", "flat in int QuadIndex;", "", "layout(std140) uniform TablePanelParamsArray {", "    vec4 params[512];", "};", "", "out vec4 OutColor;", "", "float hash21(vec2 p) {", "    return fract(sin(dot(p, vec2(127.1, 311.7))) * 43758.5453123);", "}", "", "float vnoise(vec2 p) {", "    vec2 i = floor(p);", "    vec2 f = fract(p);", "    f = f * f * (3.0 - 2.0 * f);", "    float a = hash21(i);", "    float b = hash21(i + vec2(1.0, 0.0));", "    float c = hash21(i + vec2(0.0, 1.0));", "    float d = hash21(i + vec2(1.0, 1.0));", "    return mix(mix(a, b, f.x), mix(c, d, f.x), f.y);", "}", "", "float fbm(vec2 p) {", "    float v = 0.0;", "    float amp = 0.5;", "    for (int i = 0; i < 4; i++) {", "        v += amp * vnoise(p);", "        p *= 2.03;", "        amp *= 0.5;", "    }", "    return v;", "}", "", "vec3 feltSurface(vec2 px, vec2 coord, vec3 top, vec3 bottom, float cell, float strength, float seed) {", "    vec3 base = mix(top, bottom, coord.y);", "    base *= 1.0 + 0.045 * (0.5 - coord.x);", "", "    float size = max(cell, 2.0);", "    vec2 uv = px / size;", "    vec2 gid = floor(uv);", "    vec2 gf = fract(uv);", "", "    float checker = mod(gid.x + gid.y, 2.0);", "", "    float lw = 0.055;", "    float lineX = smoothstep(lw, 0.0, gf.x) + smoothstep(1.0 - lw, 1.0, gf.x);", "    float lineY = smoothstep(lw, 0.0, gf.y) + smoothstep(1.0 - lw, 1.0, gf.y);", "    float grid = clamp(lineX + lineY, 0.0, 1.0);", "", "    float weave = 0.5 + 0.5 * sin((px.x + px.y) * 0.55 + seed);", "    float darken = (checker * 0.34 + grid * 0.62 + weave * 0.06) * clamp(strength, 0.0, 1.0);", "    base *= 1.0 - darken;", "", "    float grain = hash21(floor(px * 1.35) + seed);", "    base *= 0.985 + 0.03 * grain;", "    return base;", "}", "", "vec3 woodSurface(vec2 pos, vec2 halfSize, float frame, float u, vec3 light, vec3 dark, float seed) {", "    vec2 pAbs = abs(pos);", "    float distX = halfSize.x - pAbs.x;", "    float distY = halfSize.y - pAbs.y;", "", "    float blend = max(frame * 1.6, 6.0);", "    float mitre = smoothstep(-blend, blend, distX - distY);", "", "    vec2 horizPlank = vec2(pos.x, distY);", "    vec2 vertPlank = vec2(pos.y, distX);", "    vec2 plank = mix(vertPlank, horizPlank, mitre);", "", "    float along = plank.x;", "    float across = plank.y;", "", "    float warp = fbm(vec2(along * 0.014, across * 0.10) + seed);", "    float rings = fract(across * 0.42 + warp * 3.4 + seed * 0.37);", "    float ring = smoothstep(0.0, 0.52, rings) * smoothstep(1.0, 0.48, rings);", "    vec3 wood = mix(dark, light, 0.30 + 0.70 * ring);", "", "    float fiber = hash21(vec2(floor(along * 0.85), floor(across * 2.6)) + seed);", "    wood *= 0.955 + 0.09 * fiber;", "", "    float knot = fbm(vec2(along * 0.05, across * 0.22) + seed * 1.7);", "    wood *= 0.92 + 0.16 * knot;", "", "    float bevel = 1.0 - abs(u * 2.0 - 1.0);", "    wood *= 0.84 + 0.30 * bevel;", "", "    float lightHoriz = pos.y > 0.0 ? 0.17 : -0.13;", "    float lightVert = pos.x > 0.0 ? 0.08 : -0.06;", "    wood *= 1.0 + mix(lightVert, lightHoriz, mitre);", "", "    float corner = 1.0 - abs(mitre * 2.0 - 1.0);", "    wood *= 1.0 - 0.045 * corner;", "", "    float outerDark = smoothstep(0.20, 0.0, u);", "    float innerDark = smoothstep(0.84, 1.0, u);", "    wood *= 1.0 - 0.34 * outerDark - 0.22 * innerDark;", "", "    float sheen = smoothstep(0.88, 0.955, u) * (1.0 - smoothstep(0.955, 1.0, u));", "    wood += vec3(0.09, 0.07, 0.05) * sheen;", "", "    return wood;", "}", "", "void main() {", "    int base = QuadIndex * 8;", "    vec4 radius = params[base];", "    vec4 sizeSmoothFrame = params[base + 1];", "    vec4 feltTop = params[base + 2];", "    vec4 feltBottom = params[base + 3];", "    vec4 woodLight = params[base + 4];", "    vec4 woodDark = params[base + 5];", "    vec4 cellParams = params[base + 6];", "    vec4 innerParams = params[base + 7];", "", "    vec2 size = max(sizeSmoothFrame.xy, vec2(1.0));", "    float smoothness = sizeSmoothFrame.z;", "    float frame = max(sizeSmoothFrame.w, 0.0);", "", "    vec2 coord = clamp(FragCoord, vec2(0.0), vec2(1.0));", "    vec2 halfSize = size * 0.5;", "    vec2 pos = halfSize - coord * size;", "    vec4 rad = max(radius, vec4(0.0));", "", "    float dist = rdist(pos, halfSize, rad);", "    float feather = max(fwidth(dist) * 0.5, smoothness);", "    float shapeAlpha = 1.0 - smoothstep(-feather, feather, dist);", "    if (shapeAlpha <= 0.001) {", "        discard;", "    }", "", "    vec2 px = coord * size;", "    float seed = cellParams.z;", "", "    vec3 felt = feltSurface(px, coord, feltTop.rgb, feltBottom.rgb, cellParams.x, cellParams.y, seed);", "", "    vec2 innerHalf = max(halfSize - vec2(frame), vec2(0.5));", "    float innerRadius = max(innerParams.x, 0.0);", "    float innerDist = rdist(pos, innerHalf, vec4(innerRadius));", "", "    float innerTint = clamp(cellParams.w, 0.0, 1.0);", "    float inset = smoothstep(-4.0, -22.0, innerDist);", "    felt *= 1.0 - innerTint * inset;", "", "    float shadowSpan = max(frame * 0.85, 6.0);", "    float innerShadow = smoothstep(-shadowSpan, 0.0, innerDist);", "    felt *= 1.0 - 0.30 * innerShadow;", "", "    vec3 rgb = felt;", "    float alpha = feltTop.a;", "", "    if (frame > 0.05) {", "        float aa = max(fwidth(innerDist), 0.5);", "        float woodMask = smoothstep(-aa, aa, innerDist);", "        if (woodMask > 0.001) {", "            float span = max(innerDist - dist, 0.001);", "            float u = clamp(-dist / span, 0.0, 1.0);", "            vec3 wood = woodSurface(pos, halfSize, frame, u, woodLight.rgb, woodDark.rgb, seed);", "            rgb = mix(rgb, wood, woodMask);", "            alpha = mix(alpha, woodLight.a, woodMask);", "        }", "    }", "", "    vec4 color = vec4(rgb, alpha * shapeAlpha);", "    if (color.a <= 0.001) {", "        discard;", "    }", "", "    OutColor = color;", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }

    private final String s95() {
        Object[] objectArray = new String[]{"//!vertex", "#version 330", "", "#moj_import <minecraft:fog.glsl>", "#moj_import <minecraft:globals.glsl>", "#moj_import <minecraft:chunksection.glsl>", "#moj_import <minecraft:projection.glsl>", "", "in vec3 Position;", "in vec4 Color;", "in vec2 UV0;", "in ivec2 UV2;", "in vec3 Normal;", "", "uniform sampler2D Sampler2;", "", "layout(std140) uniform WaveParams {", "    vec4 WaveData;", "};", "", "out float sphericalVertexDistance;", "out float cylindricalVertexDistance;", "out vec4 vertexColor;", "out vec2 texCoord0;", "", "vec4 minecraft_sample_lightmap(sampler2D lightMap, ivec2 uv) {", "    return texture(lightMap, clamp((uv / 256.0) + 0.5 / 16.0, vec2(0.5 / 16.0), vec2(15.5 / 16.0)));", "}", "", "vec3 kimiko_raw_wave(vec3 p, float wind) {", "    float magnitude = sin(wind * 0.0027 + p.x + p.y) * WaveData.w + 0.04;", "    float d0 = sin(wind * 0.0127);", "    float d1 = sin(wind * 0.0089);", "    float d2 = sin(wind * 0.0114);", "    vec3 wave;", "    wave.x = magnitude * sin(wind * 0.0224 + d1 + d2 + p.x - p.z + p.y);", "    wave.y = magnitude * sin(wind * 0.0015 + d2 + d0 + p.x);", "    wave.z = magnitude * sin(wind * 0.0063 + d0 + d1 - p.x + p.z + p.y);", "    return wave;", "}", "", "void main() {", "    vec3 pos = Position + (ChunkPosition - CameraBlockPos) + CameraOffset;", "    float alpha = Color.a;", "    int tag = int(round(alpha * 255.0));", "", "    if (tag >= 252 && tag <= 254) {", "        vec3 worldPos = mod(Position + vec3(ChunkPosition), 50.26548245743669);", "        float wind = WaveData.x * 170.0;", "", "        vec3 wave;", "        if (tag == 254) {", "            float sky = clamp(float(UV2.y) / 240.0, 0.0, 1.0);", "            float gate = clamp(sky - 0.87, 0.0, 0.1);", "            vec3 p = vec3(worldPos.x, worldPos.y * 0.5, worldPos.z);", "            wave = kimiko_raw_wave(p, wind);", "            wave.z = wave.z * 8.0 + wave.y * 4.0;", "            wave.x *= 3.0;", "            wave.y = 0.0;", "            wave *= gate * WaveData.y;", "        } else {", "            vec3 p = worldPos * vec3(0.75, 0.375, 0.75);", "            wave = kimiko_raw_wave(p, wind) * vec3(4.0, 3.0, 8.0);", "            wave *= 0.1 * WaveData.z;", "            if (tag == 252) {", "                wave *= 0.75;", "            }", "        }", "", "        pos += wave;", "        alpha = 1.0;", "    }", "", "    gl_Position = ProjMat * ModelViewMat * vec4(pos, 1.0);", "", "    sphericalVertexDistance = fog_spherical_distance(pos);", "    cylindricalVertexDistance = fog_cylindrical_distance(pos);", "    vertexColor = vec4(Color.rgb, alpha) * minecraft_sample_lightmap(Sampler2, UV2);", "    texCoord0 = UV0;", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }

    private final String s96() {
        Object[] objectArray = new String[]{"#version 330", "", "#moj_import <minecraft:fog.glsl>", "#moj_import <minecraft:globals.glsl>", "#moj_import <minecraft:chunksection.glsl>", "#moj_import <minecraft:projection.glsl>", "", "in vec3 Position;", "in vec4 Color;", "in vec2 UV0;", "in ivec2 UV2;", "in vec3 Normal;", "", "uniform sampler2D Sampler2;", "", "layout(std140) uniform WaveParams {", "    vec4 WaveData;", "};", "", "out float sphericalVertexDistance;", "out float cylindricalVertexDistance;", "out vec4 vertexColor;", "out vec2 texCoord0;", "", "vec4 minecraft_sample_lightmap(sampler2D lightMap, ivec2 uv) {", "    return texture(lightMap, clamp((uv / 256.0) + 0.5 / 16.0, vec2(0.5 / 16.0), vec2(15.5 / 16.0)));", "}", "", "vec3 kimiko_raw_wave(vec3 p, float wind) {", "    float magnitude = sin(wind * 0.0027 + p.x + p.y) * WaveData.w + 0.04;", "    float d0 = sin(wind * 0.0127);", "    float d1 = sin(wind * 0.0089);", "    float d2 = sin(wind * 0.0114);", "    vec3 wave;", "    wave.x = magnitude * sin(wind * 0.0224 + d1 + d2 + p.x - p.z + p.y);", "    wave.y = magnitude * sin(wind * 0.0015 + d2 + d0 + p.x);", "    wave.z = magnitude * sin(wind * 0.0063 + d0 + d1 - p.x + p.z + p.y);", "    return wave;", "}", "", "void main() {", "    vec3 pos = Position + (ChunkPosition - CameraBlockPos) + CameraOffset;", "    float alpha = Color.a;", "    int tag = int(round(alpha * 255.0));", "", "    if (tag >= 252 && tag <= 254) {", "        vec3 worldPos = mod(Position + vec3(ChunkPosition), 50.26548245743669);", "        float wind = WaveData.x * 170.0;", "", "        vec3 wave;", "        if (tag == 254) {", "            float sky = clamp(float(UV2.y) / 240.0, 0.0, 1.0);", "            float gate = clamp(sky - 0.87, 0.0, 0.1);", "            vec3 p = vec3(worldPos.x, worldPos.y * 0.5, worldPos.z);", "            wave = kimiko_raw_wave(p, wind);", "            wave.z = wave.z * 8.0 + wave.y * 4.0;", "            wave.x *= 3.0;", "            wave.y = 0.0;", "            wave *= gate * WaveData.y;", "        } else {", "            vec3 p = worldPos * vec3(0.75, 0.375, 0.75);", "            wave = kimiko_raw_wave(p, wind) * vec3(4.0, 3.0, 8.0);", "            wave *= 0.1 * WaveData.z;", "            if (tag == 252) {", "                wave *= 0.75;", "            }", "        }", "", "        pos += wave;", "        alpha = 1.0;", "    }", "", "    gl_Position = ProjMat * ModelViewMat * vec4(pos, 1.0);", "", "    sphericalVertexDistance = fog_spherical_distance(pos);", "    cylindricalVertexDistance = fog_cylindrical_distance(pos);", "    vertexColor = vec4(Color.rgb, alpha) * minecraft_sample_lightmap(Sampler2, UV2);", "    texCoord0 = UV0;", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }

    private final String s97() {
        Object[] objectArray = new String[]{"//!vertex", "#version 150", "", "#moj_import <minecraft:dynamictransforms.glsl>", "#moj_import <minecraft:projection.glsl>", "", "in vec3 Position;", "in vec2 UV0;", "in vec4 Color;", "", "out vec2 TexCoord;", "out vec4 VertexColor;", "", "void main() {", "    gl_Position = ProjMat * ModelViewMat * vec4(Position, 1.0);", "    TexCoord = UV0;", "    VertexColor = Color;", "}", "//!fragment", "#version 150", "", "#moj_import <minecraft:dynamictransforms.glsl>", "", "uniform sampler2D Sampler0;", "", "in vec2 TexCoord;", "in vec4 VertexColor;", "", "out vec4 OutColor;", "", "float sampledCoverage(vec2 uv) {", "    vec2 pixelX = dFdx(uv);", "    vec2 pixelY = dFdy(uv);", "    vec2 atlasSize = vec2(textureSize(Sampler0, 0));", "    float footprint = max(length(pixelX * atlasSize), length(pixelY * atlasSize));", "", "    float center = texture(Sampler0, uv).a;", "    float axes =", "        texture(Sampler0, uv + pixelX * 0.42).a +", "        texture(Sampler0, uv - pixelX * 0.42).a +", "        texture(Sampler0, uv + pixelY * 0.42).a +", "        texture(Sampler0, uv - pixelY * 0.42).a;", "    float diagonals =", "        texture(Sampler0, uv + (pixelX + pixelY) * 0.34).a +", "        texture(Sampler0, uv - (pixelX + pixelY) * 0.34).a +", "        texture(Sampler0, uv + (pixelX - pixelY) * 0.34).a +", "        texture(Sampler0, uv + (-pixelX + pixelY) * 0.34).a;", "", "    float coverage = center * 0.36 + axes * 0.11 + diagonals * 0.05;", "    float smallText = smoothstep(3.0, 5.5, footprint);", "    coverage = mix(coverage, clamp(coverage * 1.14 + 0.026, 0.0, 1.0), smallText);", "", "    coverage = smoothstep(0.025, mix(0.88, 0.78, smallText), coverage);", "    return pow(clamp(coverage, 0.0, 1.0), mix(0.95, 0.86, smallText));", "}", "", "void main() {", "    float alpha = sampledCoverage(TexCoord) * VertexColor.a;", "    if (alpha <= 0.001) {", "        discard;", "    }", "    OutColor = vec4(VertexColor.rgb, alpha) * ColorModulator;", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }

    private final String s98() {
        Object[] objectArray = new String[]{"#version 150", "", "#moj_import <minecraft:dynamictransforms.glsl>", "#moj_import <minecraft:projection.glsl>", "", "in vec3 Position;", "in vec2 UV0;", "in vec4 Color;", "", "out vec2 TexCoord;", "out vec4 VertexColor;", "", "void main() {", "    gl_Position = ProjMat * ModelViewMat * vec4(Position, 1.0);", "    TexCoord = UV0;", "    VertexColor = Color;", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }

    private final String s99() {
        Object[] objectArray = new String[]{"#version 150", "", "#moj_import <minecraft:dynamictransforms.glsl>", "", "uniform sampler2D Sampler0;", "", "in vec2 TexCoord;", "in vec4 VertexColor;", "", "out vec4 OutColor;", "", "float sampledCoverage(vec2 uv) {", "    vec2 pixelX = dFdx(uv);", "    vec2 pixelY = dFdy(uv);", "    vec2 atlasSize = vec2(textureSize(Sampler0, 0));", "    float footprint = max(length(pixelX * atlasSize), length(pixelY * atlasSize));", "", "    float center = texture(Sampler0, uv).a;", "    float axes =", "        texture(Sampler0, uv + pixelX * 0.42).a +", "        texture(Sampler0, uv - pixelX * 0.42).a +", "        texture(Sampler0, uv + pixelY * 0.42).a +", "        texture(Sampler0, uv - pixelY * 0.42).a;", "    float diagonals =", "        texture(Sampler0, uv + (pixelX + pixelY) * 0.34).a +", "        texture(Sampler0, uv - (pixelX + pixelY) * 0.34).a +", "        texture(Sampler0, uv + (pixelX - pixelY) * 0.34).a +", "        texture(Sampler0, uv + (-pixelX + pixelY) * 0.34).a;", "", "    float coverage = center * 0.36 + axes * 0.11 + diagonals * 0.05;", "    float smallText = smoothstep(3.0, 5.5, footprint);", "    coverage = mix(coverage, clamp(coverage * 1.14 + 0.026, 0.0, 1.0), smallText);", "", "    coverage = smoothstep(0.025, mix(0.88, 0.78, smallText), coverage);", "    return pow(clamp(coverage, 0.0, 1.0), mix(0.95, 0.86, smallText));", "}", "", "void main() {", "    float alpha = sampledCoverage(TexCoord) * VertexColor.a;", "    if (alpha <= 0.001) {", "        discard;", "    }", "    OutColor = vec4(VertexColor.rgb, alpha) * ColorModulator;", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }

    private final String s100() {
        Object[] objectArray = new String[]{"//!vertex", "#version 150", "", "#moj_import <minecraft:dynamictransforms.glsl>", "#moj_import <minecraft:projection.glsl>", "", "in vec3 Position;", "in vec2 UV0;", "in vec4 Color;", "", "out vec2 TexCoord;", "out vec4 VertexColor;", "", "void main() {", "    gl_Position = ProjMat * ModelViewMat * vec4(Position, 1.0);", "    TexCoord = UV0;", "    VertexColor = Color;", "}", "//!fragment", "#version 150", "", "#moj_import <minecraft:dynamictransforms.glsl>", "", "uniform sampler2D Sampler0;", "", "in vec2 TexCoord;", "in vec4 VertexColor;", "", "out vec4 OutColor;", "", "float sampledCoverage(vec2 uv) {", "    vec2 pixelX = dFdx(uv);", "    vec2 pixelY = dFdy(uv);", "    vec2 atlasSize = vec2(textureSize(Sampler0, 0));", "    float footprint = max(length(pixelX * atlasSize), length(pixelY * atlasSize));", "", "    float center = texture(Sampler0, uv).a;", "    float axes =", "        texture(Sampler0, uv + pixelX * 0.42).a +", "        texture(Sampler0, uv - pixelX * 0.42).a +", "        texture(Sampler0, uv + pixelY * 0.42).a +", "        texture(Sampler0, uv - pixelY * 0.42).a;", "    float diagonals =", "        texture(Sampler0, uv + (pixelX + pixelY) * 0.34).a +", "        texture(Sampler0, uv - (pixelX + pixelY) * 0.34).a +", "        texture(Sampler0, uv + (pixelX - pixelY) * 0.34).a +", "        texture(Sampler0, uv + (-pixelX + pixelY) * 0.34).a;", "", "    float coverage = center * 0.36 + axes * 0.11 + diagonals * 0.05;", "    float smallText = smoothstep(3.0, 5.5, footprint);", "    coverage = mix(coverage, clamp(coverage * 1.14 + 0.026, 0.0, 1.0), smallText);", "", "    coverage = smoothstep(0.025, mix(0.88, 0.78, smallText), coverage);", "    return pow(clamp(coverage, 0.0, 1.0), mix(0.95, 0.86, smallText));", "}", "", "void main() {", "    float fadeAlpha = clamp(VertexColor.a, 0.0, 1.0);", "    float alpha = sampledCoverage(TexCoord) * fadeAlpha;", "    if (alpha <= 0.001) {", "        discard;", "    }", "    OutColor = vec4(VertexColor.rgb, alpha) * ColorModulator;", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }

    private final String s101() {
        Object[] objectArray = new String[]{"#version 150", "", "#moj_import <minecraft:dynamictransforms.glsl>", "#moj_import <minecraft:projection.glsl>", "", "in vec3 Position;", "in vec2 UV0;", "in vec4 Color;", "", "out vec2 TexCoord;", "out vec4 VertexColor;", "", "void main() {", "    gl_Position = ProjMat * ModelViewMat * vec4(Position, 1.0);", "    TexCoord = UV0;", "    VertexColor = Color;", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }

    private final String s102() {
        Object[] objectArray = new String[]{"#version 150", "", "#moj_import <minecraft:dynamictransforms.glsl>", "", "uniform sampler2D Sampler0;", "", "in vec2 TexCoord;", "in vec4 VertexColor;", "", "out vec4 OutColor;", "", "float sampledCoverage(vec2 uv) {", "    vec2 pixelX = dFdx(uv);", "    vec2 pixelY = dFdy(uv);", "    vec2 atlasSize = vec2(textureSize(Sampler0, 0));", "    float footprint = max(length(pixelX * atlasSize), length(pixelY * atlasSize));", "", "    float center = texture(Sampler0, uv).a;", "    float axes =", "        texture(Sampler0, uv + pixelX * 0.42).a +", "        texture(Sampler0, uv - pixelX * 0.42).a +", "        texture(Sampler0, uv + pixelY * 0.42).a +", "        texture(Sampler0, uv - pixelY * 0.42).a;", "    float diagonals =", "        texture(Sampler0, uv + (pixelX + pixelY) * 0.34).a +", "        texture(Sampler0, uv - (pixelX + pixelY) * 0.34).a +", "        texture(Sampler0, uv + (pixelX - pixelY) * 0.34).a +", "        texture(Sampler0, uv + (-pixelX + pixelY) * 0.34).a;", "", "    float coverage = center * 0.36 + axes * 0.11 + diagonals * 0.05;", "    float smallText = smoothstep(3.0, 5.5, footprint);", "    coverage = mix(coverage, clamp(coverage * 1.14 + 0.026, 0.0, 1.0), smallText);", "", "    coverage = smoothstep(0.025, mix(0.88, 0.78, smallText), coverage);", "    return pow(clamp(coverage, 0.0, 1.0), mix(0.95, 0.86, smallText));", "}", "", "void main() {", "    float fadeAlpha = clamp(VertexColor.a, 0.0, 1.0);", "    float alpha = sampledCoverage(TexCoord) * fadeAlpha;", "    if (alpha <= 0.001) {", "        discard;", "    }", "    OutColor = vec4(VertexColor.rgb, alpha) * ColorModulator;", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }

    private final String s103() {
        Object[] objectArray = new String[]{"//!vertex", "#version 330", "", "#moj_import <minecraft:dynamictransforms.glsl>", "#moj_import <minecraft:projection.glsl>", "", "in vec3 Position;", "in vec2 UV0;", "in vec4 Color;", "", "out vec3 viewPos;", "out vec2 params;", "out vec4 vColor;", "", "void main() {", "    vec4 vp = ModelViewMat * vec4(Position + ModelOffset, 1.0);", "    gl_Position = ProjMat * vp;", "    viewPos = vp.xyz;", "    params = UV0;", "    vColor = Color;", "}", "", "//!fragment", "#version 330", "", "#moj_import <minecraft:dynamictransforms.glsl>", "", "uniform sampler2D Sampler0;", "", "in vec3 viewPos;", "in vec2 params;", "in vec4 vColor;", "", "out vec4 fragColor;", "", "void main() {", "    vec3 n = normalize(cross(dFdx(viewPos), dFdy(viewPos)));", "    if (dot(n, viewPos) > 0.0) {", "        n = -n;", "    }", "    float reflectS = floor(params.x) / 20.0;", "    float distort = fract(params.x) * 3.0;", "    float tintS = floor(params.y) / 20.0;", "    float blur = fract(params.y) * 3.0;", "", "    vec2 size = vec2(textureSize(Sampler0, 0));", "    vec2 uv = gl_FragCoord.xy / size;", "    vec2 scaleUv = vec2(size.y / size.x, 1.0);", "    vec2 nxy = n.xy * scaleUv;", "", "    vec2 base = clamp(uv - nxy * distort * 0.014, 0.0, 1.0);", "    vec3 col = texture(Sampler0, base).rgb;", "", "    if (blur > 0.001) {", "        float r = blur * 0.0045;", "        vec3 acc = col;", "        for (int k = 0; k < 6; k++) {", "            float a = float(k) * 1.0471976;", "            vec2 d = vec2(cos(a), sin(a)) * scaleUv;", "            acc += texture(Sampler0, clamp(base + d * r, 0.0, 1.0)).rgb;", "            acc += texture(Sampler0, clamp(base + d * r * 0.5 + vec2(-d.y, d.x) * r * 0.25, 0.0, 1.0)).rgb;", "        }", "        col = acc / 13.0;", "    }", "", "    if (reflectS > 0.001) {", "        vec2 ruv = clamp(uv + nxy * 0.12, 0.0, 1.0);", "        col = mix(col, texture(Sampler0, ruv).rgb, reflectS * 0.6);", "    }", "", "    float facing = clamp(abs(n.z), 0.0, 1.0);", "    float fres = pow(1.0 - facing, 2.0);", "    col = mix(col, vColor.rgb, tintS);", "    col += vColor.rgb * fres * 0.3;", "", "    fragColor = vec4(min(col, vec3(1.0)), vColor.a) * ColorModulator;", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }

    private final String s104() {
        Object[] objectArray = new String[]{"#version 330", "", "#moj_import <minecraft:dynamictransforms.glsl>", "#moj_import <minecraft:projection.glsl>", "", "in vec3 Position;", "in vec2 UV0;", "in vec4 Color;", "", "out vec3 viewPos;", "out vec2 params;", "out vec4 vColor;", "", "void main() {", "    vec4 vp = ModelViewMat * vec4(Position + ModelOffset, 1.0);", "    gl_Position = ProjMat * vp;", "    viewPos = vp.xyz;", "    params = UV0;", "    vColor = Color;", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }

    private final String s105() {
        Object[] objectArray = new String[]{"#version 330", "", "#moj_import <minecraft:dynamictransforms.glsl>", "", "uniform sampler2D Sampler0;", "", "in vec3 viewPos;", "in vec2 params;", "in vec4 vColor;", "", "out vec4 fragColor;", "", "void main() {", "    vec3 n = normalize(cross(dFdx(viewPos), dFdy(viewPos)));", "    if (dot(n, viewPos) > 0.0) {", "        n = -n;", "    }", "    float reflectS = floor(params.x) / 20.0;", "    float distort = fract(params.x) * 3.0;", "    float tintS = floor(params.y) / 20.0;", "    float blur = fract(params.y) * 3.0;", "", "    vec2 size = vec2(textureSize(Sampler0, 0));", "    vec2 uv = gl_FragCoord.xy / size;", "    vec2 scaleUv = vec2(size.y / size.x, 1.0);", "    vec2 nxy = n.xy * scaleUv;", "", "    vec2 base = clamp(uv - nxy * distort * 0.014, 0.0, 1.0);", "    vec3 col = texture(Sampler0, base).rgb;", "", "    if (blur > 0.001) {", "        float r = blur * 0.0045;", "        vec3 acc = col;", "        for (int k = 0; k < 6; k++) {", "            float a = float(k) * 1.0471976;", "            vec2 d = vec2(cos(a), sin(a)) * scaleUv;", "            acc += texture(Sampler0, clamp(base + d * r, 0.0, 1.0)).rgb;", "            acc += texture(Sampler0, clamp(base + d * r * 0.5 + vec2(-d.y, d.x) * r * 0.25, 0.0, 1.0)).rgb;", "        }", "        col = acc / 13.0;", "    }", "", "    if (reflectS > 0.001) {", "        vec2 ruv = clamp(uv + nxy * 0.12, 0.0, 1.0);", "        col = mix(col, texture(Sampler0, ruv).rgb, reflectS * 0.6);", "    }", "", "    float facing = clamp(abs(n.z), 0.0, 1.0);", "    float fres = pow(1.0 - facing, 2.0);", "    col = mix(col, vColor.rgb, tintS);", "    col += vColor.rgb * fres * 0.3;", "", "    fragColor = vec4(min(col, vec3(1.0)), vColor.a) * ColorModulator;", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }

    private final String s106() {
        Object[] objectArray = new String[]{"//!vertex", "#version 150", "", "#moj_import <minecraft:dynamictransforms.glsl>", "#moj_import <minecraft:projection.glsl>", "", "in vec3 Position;", "in vec2 UV0;", "in ivec2 UV1;", "in vec4 Color;", "", "out vec2 LocalPx;", "flat out vec2 HalfSize;", "flat out float Radius;", "out vec4 FillColor;", "", "void main() {", "    gl_Position = ProjMat * ModelViewMat * vec4(Position.xy, 0.0, 1.0);", "    LocalPx = UV0;", "    HalfSize = vec2(UV1) / 16.0;", "    Radius = Position.z;", "    FillColor = Color;", "}", "//!fragment", "#version 150", "", "in vec2 LocalPx;", "flat in vec2 HalfSize;", "flat in float Radius;", "in vec4 FillColor;", "", "out vec4 fragColor;", "", "void main() {", "    vec2 p = LocalPx - HalfSize;", "    vec2 q = abs(p) - (HalfSize - vec2(Radius));", "    float d = length(max(q, vec2(0.0))) + min(max(q.x, q.y), 0.0) - Radius;", "    float alpha = clamp(0.5 - d, 0.0, 1.0);", "    if (alpha <= 0.003) {", "        discard;", "    }", "    fragColor = vec4(FillColor.rgb, FillColor.a * alpha);", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }

    private final String s107() {
        Object[] objectArray = new String[]{"#version 150", "", "#moj_import <minecraft:dynamictransforms.glsl>", "#moj_import <minecraft:projection.glsl>", "", "in vec3 Position;", "in vec2 UV0;", "in ivec2 UV1;", "in vec4 Color;", "", "out vec2 LocalPx;", "flat out vec2 HalfSize;", "flat out float Radius;", "out vec4 FillColor;", "", "void main() {", "    gl_Position = ProjMat * ModelViewMat * vec4(Position.xy, 0.0, 1.0);", "    LocalPx = UV0;", "    HalfSize = vec2(UV1) / 16.0;", "    Radius = Position.z;", "    FillColor = Color;", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }

    private final String s108() {
        Object[] objectArray = new String[]{"#version 150", "", "in vec2 LocalPx;", "flat in vec2 HalfSize;", "flat in float Radius;", "in vec4 FillColor;", "", "out vec4 fragColor;", "", "void main() {", "    vec2 p = LocalPx - HalfSize;", "    vec2 q = abs(p) - (HalfSize - vec2(Radius));", "    float d = length(max(q, vec2(0.0))) + min(max(q.x, q.y), 0.0) - Radius;", "    float alpha = clamp(0.5 - d, 0.0, 1.0);", "    if (alpha <= 0.003) {", "        discard;", "    }", "    fragColor = vec4(FillColor.rgb, FillColor.a * alpha);", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }

    private final String s109() {
        Object[] objectArray = new String[]{"//!vertex", "#version 150", "", "#moj_import <minecraft:dynamictransforms.glsl>", "#moj_import <minecraft:projection.glsl>", "", "in vec3 Position;", "in vec4 Color;", "in float LineWidth;", "", "out vec2 FragCoord;", "flat out int QuadIndex;", "", "void main() {", "    gl_Position = ProjMat * ModelViewMat * vec4(Position, 1.0);", "    FragCoord = Color.rg;", "    QuadIndex = max(int(LineWidth + 0.5) - 1, 0);", "}", "//!fragment", "#version 150", "", "#moj_import <kimiko:common.glsl>", "", "in vec2 FragCoord;", "flat in int QuadIndex;", "", "layout(std140) uniform VeilParamsArray {", "    vec4 params[336];", "};", "", "out vec4 OutColor;", "", "void main() {", "    int base = QuadIndex * 84;", "    vec4 quad = params[base];", "    vec4 color = params[base + 1];", "    vec4 panel = params[base + 2];", "    vec4 meta = params[base + 3];", "", "    vec2 p = quad.xy + FragCoord * quad.zw;", "", "    vec2 panelCenter = panel.xy + panel.zw * 0.5;", "    float sd = rdist(p - panelCenter, panel.zw * 0.5, vec4(meta.x));", "", "    int count = int(meta.y + 0.5);", "    for (int i = 0; i < 40; i++) {", "        if (i >= count) break;", "        vec4 r0 = params[base + 4 + 2 * i];", "        vec4 r1 = params[base + 5 + 2 * i];", "        vec2 lp = p - r0.xy;", "        lp = vec2(lp.x * r1.x + lp.y * r1.y, -lp.x * r1.y + lp.y * r1.x);", "        sd = min(sd, rdist(lp, r0.zw, vec4(r1.z)));", "    }", "", "    float aa = max(fwidth(sd) * 0.75, 0.5);", "    float coverage = 1.0 - smoothstep(-aa, aa, sd);", "    float alpha = color.a * coverage;", "    if (alpha <= 0.001) {", "        discard;", "    }", "", "    OutColor = vec4(color.rgb, alpha);", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }

    private final String s110() {
        Object[] objectArray = new String[]{"#version 150", "", "#moj_import <minecraft:dynamictransforms.glsl>", "#moj_import <minecraft:projection.glsl>", "", "in vec3 Position;", "in vec4 Color;", "in float LineWidth;", "", "out vec2 FragCoord;", "flat out int QuadIndex;", "", "void main() {", "    gl_Position = ProjMat * ModelViewMat * vec4(Position, 1.0);", "    FragCoord = Color.rg;", "    QuadIndex = max(int(LineWidth + 0.5) - 1, 0);", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }

    private final String s111() {
        Object[] objectArray = new String[]{"#version 150", "", "#moj_import <kimiko:common.glsl>", "", "in vec2 FragCoord;", "flat in int QuadIndex;", "", "layout(std140) uniform VeilParamsArray {", "    vec4 params[336];", "};", "", "out vec4 OutColor;", "", "void main() {", "    int base = QuadIndex * 84;", "    vec4 quad = params[base];", "    vec4 color = params[base + 1];", "    vec4 panel = params[base + 2];", "    vec4 meta = params[base + 3];", "", "    vec2 p = quad.xy + FragCoord * quad.zw;", "", "    vec2 panelCenter = panel.xy + panel.zw * 0.5;", "    float sd = rdist(p - panelCenter, panel.zw * 0.5, vec4(meta.x));", "", "    int count = int(meta.y + 0.5);", "    for (int i = 0; i < 40; i++) {", "        if (i >= count) break;", "        vec4 r0 = params[base + 4 + 2 * i];", "        vec4 r1 = params[base + 5 + 2 * i];", "        vec2 lp = p - r0.xy;", "        lp = vec2(lp.x * r1.x + lp.y * r1.y, -lp.x * r1.y + lp.y * r1.x);", "        sd = min(sd, rdist(lp, r0.zw, vec4(r1.z)));", "    }", "", "    float aa = max(fwidth(sd) * 0.75, 0.5);", "    float coverage = 1.0 - smoothstep(-aa, aa, sd);", "    float alpha = color.a * coverage;", "    if (alpha <= 0.001) {", "        discard;", "    }", "", "    OutColor = vec4(color.rgb, alpha);", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }

    private final String s112() {
        Object[] objectArray = new String[]{"//!vertex", "#version 330", "", "#moj_import <minecraft:dynamictransforms.glsl>", "#moj_import <minecraft:projection.glsl>", "", "in vec3 Position;", "in vec2 UV0;", "", "out vec2 texCoord0;", "out vec3 vPos;", "", "void main() {", "    vec3 pos = Position + ModelOffset;", "    vec4 viewPos = ModelViewMat * vec4(pos, 1.0);", "    gl_Position = ProjMat * viewPos;", "    vPos = viewPos.xyz;", "    texCoord0 = (TextureMat * vec4(UV0, 0.0, 1.0)).xy;", "}", "//!fragment", "#version 330", "", "#moj_import <minecraft:dynamictransforms.glsl>", "", "uniform sampler2D Sampler0;", "", "in vec2 texCoord0;", "in vec3 vPos;", "", "out vec4 fragColor;", "", "void main() {", "    vec4 color = texture(Sampler0, texCoord0);", "    if (color.a == 0.0) {", "        discard;", "    }", "    float fade = 1.0 - smoothstep(28.0, 96.0, length(vPos));", "    if (fade <= 0.003) {", "        discard;", "    }", "    fragColor = vec4(color.rgb, color.a * fade) * ColorModulator;", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }

    private final String s113() {
        Object[] objectArray = new String[]{"#version 330", "", "#moj_import <minecraft:dynamictransforms.glsl>", "#moj_import <minecraft:projection.glsl>", "", "in vec3 Position;", "in vec2 UV0;", "", "out vec2 texCoord0;", "out vec3 vPos;", "", "void main() {", "    vec3 pos = Position + ModelOffset;", "    vec4 viewPos = ModelViewMat * vec4(pos, 1.0);", "    gl_Position = ProjMat * viewPos;", "    vPos = viewPos.xyz;", "    texCoord0 = (TextureMat * vec4(UV0, 0.0, 1.0)).xy;", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }

    private final String s114() {
        Object[] objectArray = new String[]{"#version 330", "", "#moj_import <minecraft:dynamictransforms.glsl>", "", "uniform sampler2D Sampler0;", "", "in vec2 texCoord0;", "in vec3 vPos;", "", "out vec4 fragColor;", "", "void main() {", "    vec4 color = texture(Sampler0, texCoord0);", "    if (color.a == 0.0) {", "        discard;", "    }", "    float fade = 1.0 - smoothstep(28.0, 96.0, length(vPos));", "    if (fade <= 0.003) {", "        discard;", "    }", "    fragColor = vec4(color.rgb, color.a * fade) * ColorModulator;", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }

    private final String s115() {
        Object[] objectArray = new String[]{"//!vertex", "#version 150", "", "#moj_import <minecraft:dynamictransforms.glsl>", "#moj_import <minecraft:projection.glsl>", "", "in vec3 Position;", "in vec4 Color;", "in float LineWidth;", "", "out vec2 FragCoord;", "flat out int QuadIndex;", "", "void main() {", "    gl_Position = ProjMat * ModelViewMat * vec4(Position, 1.0);", "    FragCoord = Color.rg;", "    QuadIndex = max(int(LineWidth + 0.5) - 1, 0);", "}", "//!fragment", "#version 150", "", "#moj_import <kimiko:common.glsl>", "", "in vec2 FragCoord;", "flat in int QuadIndex;", "", "layout(std140) uniform ZippyParamsArray {", "    vec4 params[768];", "};", "", "out vec4 OutColor;", "", "vec2 stanh(vec2 value) {", "    return tanh(clamp(value, vec2(-40.0), vec2(40.0)));", "}", "", "void main() {", "    int base = QuadIndex * 3;", "    vec4 radius = params[base];", "    vec4 sizeSmoothTime = params[base + 1];", "    vec4 tint = params[base + 2];", "", "    vec2 coord = clamp(FragCoord, vec2(0.0), vec2(1.0));", "    vec2 size = max(sizeSmoothTime.xy, vec2(1.0));", "    float shapeAlpha = ralpha(size, coord, max(radius, vec4(0.0)), sizeSmoothTime.z);", "", "    if (shapeAlpha <= 0.001) {", "        discard;", "    }", "", "    vec2 pixelPos = coord * size;", "    vec2 v = size;", "    vec2 u = 0.12 * (2.0 * pixelPos - v) / max(v.y, 1.0);", "    vec4 o = vec4(1.0, 2.0, 3.0, 0.0);", "    vec4 z = o;", "    float t = sizeSmoothTime.w;", "", "    for (float a = 0.5, i = 0.0; i < 18.0; i += 1.0) {", "        a += 0.03;", "        t += 1.0;", "        v = cos(t - 7.0 * u * pow(a, i + 1.0)) - 5.0 * u;", "", "        float angle = i + 1.0 + 0.02 * t;", "        float s = sin(angle);", "        float c = cos(angle);", "        mat2 rotate = mat2(c, -s, s, c);", "", "        u *= rotate;", "        u += stanh(40.0 * dot(u, u) * cos(100.0 * u.yx + t)) / 200.0", "           + 0.2 * a * u", "           + cos(4.0 / exp(dot(o, o) / 100.0) + t) / 300.0;", "", "        float wave = length((1.0 + (i + 1.0) * dot(v, v)) * sin(1.5 * u / (0.5 - dot(u, u)) - 9.0 * u.yx + t));", "        o += (1.0 + cos(z + t)) / max(wave, 0.001);", "    }", "", "    o = 25.6 / (min(o, vec4(13.0)) + 164.0 / max(o, vec4(0.001))) - dot(u, u) / 250.0;", "", "    vec3 color = clamp(o.rgb * tint.rgb, vec3(0.0), vec3(1.0));", "    float alpha = tint.a * shapeAlpha;", "", "    if (alpha <= 0.001) {", "        discard;", "    }", "", "    OutColor = vec4(color, alpha);", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }

    private final String s116() {
        Object[] objectArray = new String[]{"#version 150", "", "#moj_import <minecraft:dynamictransforms.glsl>", "#moj_import <minecraft:projection.glsl>", "", "in vec3 Position;", "in vec4 Color;", "in float LineWidth;", "", "out vec2 FragCoord;", "flat out int QuadIndex;", "", "void main() {", "    gl_Position = ProjMat * ModelViewMat * vec4(Position, 1.0);", "    FragCoord = Color.rg;", "    QuadIndex = max(int(LineWidth + 0.5) - 1, 0);", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }

    private final String s117() {
        Object[] objectArray = new String[]{"#version 150", "", "#moj_import <kimiko:common.glsl>", "", "in vec2 FragCoord;", "flat in int QuadIndex;", "", "layout(std140) uniform ZippyParamsArray {", "    vec4 params[768];", "};", "", "out vec4 OutColor;", "", "vec2 stanh(vec2 value) {", "    return tanh(clamp(value, vec2(-40.0), vec2(40.0)));", "}", "", "void main() {", "    int base = QuadIndex * 3;", "    vec4 radius = params[base];", "    vec4 sizeSmoothTime = params[base + 1];", "    vec4 tint = params[base + 2];", "", "    vec2 coord = clamp(FragCoord, vec2(0.0), vec2(1.0));", "    vec2 size = max(sizeSmoothTime.xy, vec2(1.0));", "    float shapeAlpha = ralpha(size, coord, max(radius, vec4(0.0)), sizeSmoothTime.z);", "", "    if (shapeAlpha <= 0.001) {", "        discard;", "    }", "", "    vec2 pixelPos = coord * size;", "    vec2 v = size;", "    vec2 u = 0.12 * (2.0 * pixelPos - v) / max(v.y, 1.0);", "    vec4 o = vec4(1.0, 2.0, 3.0, 0.0);", "    vec4 z = o;", "    float t = sizeSmoothTime.w;", "", "    for (float a = 0.5, i = 0.0; i < 18.0; i += 1.0) {", "        a += 0.03;", "        t += 1.0;", "        v = cos(t - 7.0 * u * pow(a, i + 1.0)) - 5.0 * u;", "", "        float angle = i + 1.0 + 0.02 * t;", "        float s = sin(angle);", "        float c = cos(angle);", "        mat2 rotate = mat2(c, -s, s, c);", "", "        u *= rotate;", "        u += stanh(40.0 * dot(u, u) * cos(100.0 * u.yx + t)) / 200.0", "           + 0.2 * a * u", "           + cos(4.0 / exp(dot(o, o) / 100.0) + t) / 300.0;", "", "        float wave = length((1.0 + (i + 1.0) * dot(v, v)) * sin(1.5 * u / (0.5 - dot(u, u)) - 9.0 * u.yx + t));", "        o += (1.0 + cos(z + t)) / max(wave, 0.001);", "    }", "", "    o = 25.6 / (min(o, vec4(13.0)) + 164.0 / max(o, vec4(0.001))) - dot(u, u) / 250.0;", "", "    vec3 color = clamp(o.rgb * tint.rgb, vec3(0.0), vec3(1.0));", "    float alpha = tint.a * shapeAlpha;", "", "    if (alpha <= 0.001) {", "        discard;", "    }", "", "    OutColor = vec4(color, alpha);", "}"};
        return String.join("\n", (CharSequence[]) objectArray);
    }
}

